<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>区块链初涉</title>
    <url>/2018/04/15/Block-Chain/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="简述"><a href="#简述" class="headerlink" title="简述"></a>简述</h2><hr>
<ul>
<li><p>区块链在当今可以说是非常的火了，但也有人批判区块链被炒得过于浮夸了。这里我们姑且不论，毕竟这只是一门技术，关键在于我们怎么利用它。</p>
</li>
<li><p>区块链是一种特殊的 <strong>分布式数据库</strong>，任何人都可以架设服务器，加入区块链网络，成为一个节点。区块链的世界里面，没有中心节点，每个节点都是平等的，都保存着整个数据库。你可以向任何一个节点，写入&#x2F;读取数据，因为所有节点最后都会同步，保证区块链一致。</p>
</li>
</ul>
<h2 id="区块链的数据安全性问题"><a href="#区块链的数据安全性问题" class="headerlink" title="区块链的数据安全性问题"></a>区块链的数据安全性问题</h2><hr>
<ul>
<li><p>区块链由一个个区块(block)构成，<em>区块很像数据库的记录，每次写入数据，就是创建一个区块。</em></p>
</li>
<li><p>每个区块包含两个部分：区块头(head)——记录当前区块的元信息，区块体(body)——实际信息</p>
</li>
<li><p>区块头的元信息包括：生成时间，实际数据（即区块体）的 Hash，上一个区块的 Hash等等。区块链的 Hash 长度是256位，这就是说，不管原始内容是什么，最后都会计算出一个256位的二进制数字。而且可以保证，只要原始内容不同，对应的 Hash 一定是不同的。<strong>因此，每个区块的hash值一定是不同的，可以根据hash值区分不同的区块。而如果区块的内容变了，那么他的hash值也一定会变</strong></p>
</li>
<li><p>一个区块的hash是对这个区块的区块头计算的。区块头包含了区块体的hash(注意不是整个区块的hash)和上个区块的hash(这个是整个区块的hash)，只要这个区块的内容或者上个区块的hash(内容)变了，那么一定会引起这个区块内容的改变。<strong>因此，要想修改某个区块，那么就会引起一系列连锁反应，被修改区块后面的所有的区块的内容都必须修改，否则这个被改掉的区块就脱离区块链了，而这是不可能的，因为计算hash值是非常耗费时间和资源的，除非他一个人控制了全网一半以上的计算能力。</strong> 因此，区块一旦被写入便不可能被修改了，就像发生过的历史永远刻在时间上了一样，因此区块链的数据是十分安全可靠的。</p>
</li>
</ul>
<h2 id="区块链的分叉"><a href="#区块链的分叉" class="headerlink" title="区块链的分叉"></a>区块链的分叉</h2><hr>
<ul>
<li><p>在上述区块链的添加过程中，有一个问题，就是：如果有一个节点同时有两个区块加入，他们都连向前一个区块，那么这时候就形成了 <strong>分叉</strong>。</p>
</li>
<li><p>现在的规则是，新节点总是采用最长的那条区块链。如果区块链有分叉，将看哪个分支在分叉点后面，先达到6个新区块（称为”六次确认”）。按照10分钟一个区块计算，一小时就可以确认。(区块链的发明者中本聪（这是假名，真实身份至今未知）故意让添加新区块，变得很困难。他的设计是，平均每10分钟，全网才能生成一个新区块，一小时也就六个)。因此新的区块链的生成规则就是，拥有计算能力更强的那条链，就是正宗的区块链。</p>
</li>
</ul>
<h2 id="回顾"><a href="#回顾" class="headerlink" title="回顾"></a>回顾</h2><hr>
<ul>
<li><p>区块链作为无人管理的分布式数据库，从2009年运行至今无明显问题，体现了其设计的合理性。</p>
</li>
<li><p>但是，有利必有弊。区块链的安全可靠是建立在效率和能耗的牺牲上的。生成一个区块最少要等待十分钟，而等待所有节点同步就更久了。同时，生成一个新的区块，需要矿工做许多无意义的挖矿，极其地耗费能源。</p>
</li>
</ul>
<h2 id="展望"><a href="#展望" class="headerlink" title="展望"></a>展望</h2><hr>
<ul>
<li>区块链虽然有许多争议，但是其在某些领域确实有其独特的作用。其最大的特点就是数据的追踪和溯源。对于管理那些流通的物品，比如虚拟货币，一些农产品，枪支等等，区块链可以十分有效地保证这些领域的物资安全可靠性。</li>
</ul>
<blockquote>
<p>最后更新于2018.4.28</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑BlockChain</tag>
      </tags>
  </entry>
  <entry>
    <title>区块链一些相关概念的记录</title>
    <url>/2018/05/17/BlockChain-concepts/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="一些主要的平台"><a href="#一些主要的平台" class="headerlink" title="一些主要的平台"></a>一些主要的平台</h2><hr>
<ul>
<li><p>比特币</p>
</li>
<li><p><a href="https://ethereum.org/">以太坊</a> (<strong>需要翻墙</strong>)</p>
</li>
<li><p>fabric</p>
</li>
<li><p><a href="https://eos.io/">EOS</a></p>
</li>
</ul>
<h2 id="一些概念"><a href="#一些概念" class="headerlink" title="一些概念"></a>一些概念</h2><hr>
<ul>
<li><p>智能合约：智能合约是1994年由密码学家尼克萨博（Nick Szabo）首次提出的理念，几乎与互联网同龄。<strong>智能合约是指能够自动执行合约条款的计算机程序</strong>，在比特币出现以前，因为不存在安全可靠的执行环境，智能合约一直不能够应用到现实中。区块链由于其去中心化、公开透明等特性，天生就可以为智能合约提供可信的执行环境。所以，新型的区块链框架几乎都会内置智能合约的功能。</p>
</li>
<li><p>图灵完备：图灵完备性(Turing Compeleteness)是指针对一套数据操作规则而言的概念。数据操作规则可以是一门编程语言，也可以是计算机里具体实现了的指令集。当这套规则可以实现图灵机模型里的全部功能时，就称他具有图灵完备性。——<a href="https://www.zhihu.com/question/20115374">参考</a></p>
</li>
</ul>
<blockquote>
<p>简单来说，对于任何一个可以由计算解决的问题都能解决的编程语言或者是虚拟机称为是图灵完备的</p>
</blockquote>
<h3 id="共识算法"><a href="#共识算法" class="headerlink" title="共识算法"></a>共识算法</h3><ul>
<li><p>POW(proof-of-work)：</p>
<blockquote>
<p>工作量证明机制。工作量证明同时也是一种代币分发机制，它通过经济激励的方式来鼓励节点参与区块的构造过程，节点在构造区块的时候需要穷举一个随机数以使得区块符合规定的难度要求，一旦区块链出现分叉，诚实的节点将选择工作量较大的链条，而抛弃工作量较小的。由于假设所有节点都是逐利的，而选择工作量较小的链条就会使自己获得的激励无效，所以最终所有的节点都会是诚实的，从而使每个节点的区块链数据都保持一致。</p>
</blockquote>
</li>
<li><p>BFT(Byzantine Fault Tolerance):</p>
<blockquote>
<p>拜占庭容错模型的共识机制。节点被分为普通节点和记账节点（Validating Peer），只有记账节点才会参与到区块的构造过程，这种角色的分离使得算法的设计者有机会将运行共识算法的节点数量限定在一个可控的规模内。</p>
</blockquote>
</li>
<li><p>PBFT(Practical Byzantine Fault Tolerance)：</p>
<blockquote>
<p>实用拜占庭容错算法。PBFT是一种状态机副本复制算法，即服务作为状态机进行建模，状态机在分布式系统的不同节点进行副本复制。每个状态机的副本都保存了服务的状态，同时也实现了服务的操作。将所有的副本组成的集合使用大写字母R表示，使用0到<code>|R|-1</code>的整数表示每一个副本。为了描述方便，假设<code>|R|=3f+1</code>，这里f是有可能失效的副本的最大个数。尽管可以存在多于3f+1个副本，但是额外的副本除了降低性能之外不能提高可靠性。</p>
</blockquote>
</li>
<li><p>POS(Proof of Stake)：</p>
<blockquote>
<p>股权证明。类似于财产储存在银行，这种模式会根据你持有数字货币的量和时间，分配给你相应的利息。 简单来说，就是一个根据你持有货币的量和时间，给你发利息的一个制度，在股权证明POS模式下，有一个名词叫币龄，每个币每天产生1币龄，比如你持有100个币，总共持有了30天，那么，此时你的币龄就为3000，这个时候，如果你发现了一个POS区块，你的币龄就会被清空为0。你每被清空365币龄，你将会从区块中获得0.05个币的利息(假定利息可理解为年利率5%)，那么在这个案例中，利息 &#x3D; 3000 * 5% &#x2F; 365 &#x3D; 0.41个币，这下就很有意思了，持币有利息。</p>
</blockquote>
</li>
<li><p>DPOS(Delegated Proof of Stake)：</p>
<blockquote>
<p>委任权益证明。股份授权证明机制（又称受托人机制），它的原理是让每一个持有比特股的人进行投票，由此产生101位代表 , 我们可以将其理解为101个超级节点或者矿池，而这101个超级节点彼此的权利是完全相等的。从某种角度来看，DPOS有点像是议会制度或人民代表大会制度。如果代表不能履行他们的职责（当轮到他们时，没能生成区块），他们会被除名，网络会选出新的超级节点来取代他们。DPOS的出现最主要还是因为矿机的产生，大量的算力在不了解也不关心比特币的人身上，类似演唱会的黄牛，大量囤票而丝毫不关心演唱会的内容。</p>
</blockquote>
</li>
<li><p>石墨烯技术：</p>
<blockquote>
<p>石墨烯技术是新一代的区块链技术，基于DPOS共识算法。目前市场上流行的区块链阵营有三种，一种是第一代以比特币为主的生态体系，他们是基于POW共识，纯粹的去中心化，基于p2p的加密数字货币技术；第二种就是以以太坊构成的生态体系，主要以基于智能合约的ERC20的代币体系，他们是基于POW共识，目前以太坊正准备切换到POW+POS的多共识体系；第三种就是进化到目前最强劲的石墨烯技术生态体系，它是基于DPOS（股份授权证明共识），支持高并发，高性能等大规模工业级商业场景的基础设施，诞生了BTS（BitShare）开源商业系统，Steem去中心化社交网络平台以及EOS。特点：转账速度特别快，吞吐量tps极高，安全性很高，没有原生bug出现，功能强大，应用性极高。</p>
</blockquote>
</li>
</ul>
<h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><ul>
<li><p>WebAssembly：</p>
<blockquote>
<p>WebAssembly(缩写Wasm)是一种基于堆栈的虚拟机的二进制指令格式。Wasm设计作为一个便携式的针对高级语言的编译器，例如C&#x2F;C++&#x2F;Rust，使各种客户端或服务端应用程序都能够在web中部署。</p>
</blockquote>
</li>
<li><p>TPS(transaction per second)：</p>
<blockquote>
<p>服务器每秒处理的事务数。TPS包括一条消息入和一条消息出，加上一次用户数据库访问。（业务TPS &#x3D; CAPS × 每个呼叫平均TPS）TPS是软件测试结果的测量单位。一个事务是指一个客户机向服务器发送请求然后服务器做出反应的过程。客户机在发送请求时开始计时，收到服务器响应后结束计时，以此来计算使用的时间和完成的事务个数。一般的，评价系统性能均以每秒钟完成的技术交易的数量来衡量。系统整体处理能力取决于处理能力最低模块的TPS值。</p>
</blockquote>
</li>
<li><p>QPS(quary per second)：</p>
<blockquote>
<p>每秒查询率QPS是对一个特定的查询服务器在规定时间内所处理流量多少的衡量标准，在因特网上，作为域名系统服务器的机器的性能经常用每秒查询率来衡量。<br>对应fetches&#x2F;sec，即每秒的响应请求数，也即是最大吞吐能力。</p>
</blockquote>
</li>
</ul>
<br>
- 一些参考文章：[比特币、以太坊、Fabric…你知道它们的优缺点吗？](https://blog.csdn.net/Blockchain_lemon/article/details/79236199)、
[区块链3.0：拥抱EOS](https://www.cnblogs.com/Evsward/p/eos-intro.html)

<br>
> 最后更新于2018.5.17
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑BlockChain</tag>
      </tags>
  </entry>
  <entry>
    <title>BootStrap4-响应式建站</title>
    <url>/2019/04/09/BootStrap4/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><hr>
<ul>
<li><p>最近学习需要,为了搭建网站方便,决定学习一下使用BootStrap.</p>
</li>
<li><p>目前最新的即为BootStrap4</p>
</li>
<li><p>参考教程-<a href="http://www.runoob.com/bootstrap4/bootstrap4-tutorial.html">菜鸟教程BootStrap4</a></p>
</li>
</ul>
<blockquote>
<p>本文仅为跟着教程自学梳理一遍,无他用.</p>
</blockquote>
<h2 id="BootStrap4"><a href="#BootStrap4" class="headerlink" title="BootStrap4"></a>BootStrap4</h2><hr>
<ul>
<li><p>使用CDN的库,也可以去官网下载文件到本地然后引入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!-- 新 Bootstrap4 核心 CSS 文件 --&gt;</span><br><span class="line">&lt;link rel=&quot;stylesheet&quot; href=&quot;https://cdn.staticfile.org/twitter-bootstrap/4.1.0/css/bootstrap.min.css&quot;&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- jQuery文件。务必在bootstrap.min.js 之前引入 --&gt;</span><br><span class="line">&lt;script src=&quot;https://cdn.staticfile.org/jquery/3.2.1/jquery.min.js&quot;&gt;&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- popper.min.js 用于弹窗、提示、下拉菜单 --&gt;</span><br><span class="line">&lt;script src=&quot;https://cdn.staticfile.org/popper.js/1.12.5/umd/popper.min.js&quot;&gt;&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- 最新的 Bootstrap4 核心 JavaScript 文件 --&gt;</span><br><span class="line">&lt;script src=&quot;https://cdn.staticfile.org/twitter-bootstrap/4.1.0/js/bootstrap.min.js&quot;&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>
</li>
<li><p>一个简单的模板,注释在代码中:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">    &lt;head&gt;</span><br><span class="line">        &lt;meta charset=&quot;utf-8&quot;&gt;</span><br><span class="line">        &lt;!-- 适配移动设备 --&gt;</span><br><span class="line">        &lt;!-- width=device-width 表示宽度是设备屏幕的宽度。 --&gt;</span><br><span class="line">        &lt;!-- initial-scale=1 表示初始的缩放比例。 --&gt;</span><br><span class="line">        &lt;!-- shrink-to-fit=no 自动适应手机屏幕的宽度。 --&gt;</span><br><span class="line">        &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, shrink-to-fit=no&quot;&gt;</span><br><span class="line">        &lt;!-- Bootstrap4 核心 CSS 文件 --&gt;</span><br><span class="line">        &lt;link rel=&quot;stylesheet&quot; href=&quot;https://cdn.staticfile.org/twitter-bootstrap/4.1.0/css/bootstrap.min.css&quot;&gt;      </span><br><span class="line">        &lt;!-- jQuery文件。务必在bootstrap.min.js 之前引入 --&gt;</span><br><span class="line">        &lt;script src=&quot;https://cdn.staticfile.org/jquery/3.2.1/jquery.min.js&quot;&gt;&lt;/script&gt;  </span><br><span class="line">        &lt;!-- popper.min.js 用于弹窗、提示、下拉菜单 --&gt;</span><br><span class="line">        &lt;script src=&quot;https://cdn.staticfile.org/popper.js/1.12.5/umd/popper.min.js&quot;&gt;&lt;/script&gt;</span><br><span class="line">        &lt;!-- 最新的 Bootstrap4 核心 JavaScript 文件 --&gt;</span><br><span class="line">        &lt;script src=&quot;https://cdn.staticfile.org/twitter-bootstrap/4.1.0/js/bootstrap.min.js&quot;&gt;&lt;/script&gt;</span><br><span class="line">    &lt;/head&gt;</span><br><span class="line">    &lt;body&gt;</span><br><span class="line">    &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="两个容器类"><a href="#两个容器类" class="headerlink" title="两个容器类"></a>两个容器类</h3><ol>
<li><code>.container</code>,使得被修饰的容器为固定宽度</li>
<li><code>.container-fluid</code>,使得显示为100%宽度,占据全部视口(viewport)<blockquote>
<p><em>例如可用在<code>&lt;div&gt;</code>标签</em></p>
</blockquote>
</li>
</ol>
<h3 id="网格系统"><a href="#网格系统" class="headerlink" title="网格系统"></a>网格系统</h3><ul>
<li>BootStrap提供的网格系统,会随着屏幕或视口(viewport)尺寸的增加,系统会自动分为最多十二列.也可以根据自己的需要重新定义列数.</li>
</ul>
<h4 id="网格类"><a href="#网格类" class="headerlink" title="网格类"></a>网格类</h4><ul>
<li><p><code>.col-</code>: 针对所有设备</p>
</li>
<li><p><code>.col-sm-</code>: 平板-屏幕宽度大于等于576px</p>
</li>
<li><p><code>.col-md-</code>: 桌面-屏幕宽大于等于768px</p>
</li>
<li><p><code>col-lg-</code>: &gt;&#x3D;992px</p>
</li>
<li><p><code>col-xl-</code>: &gt;&#x3D;1200px</p>
</li>
<li><p>以上的宽度要求均为最小宽度,最大宽度没有限制</p>
</li>
</ul>
<h5 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h5><ul>
<li><p>网格的每一行需要放在<code>.container</code>或<code>.container-fluid</code>容器中,这样就可以自动设置一些外边距与内边距。</p>
</li>
<li><p>使用行来创建水平的列组。内容需要放置在列中，并且只有列可以是行的直接子节点。</p>
</li>
<li><p>预定义的类如 <code>.row</code> 和 <code>.col-sm-4</code> 可用于快速制作网格布局。</p>
</li>
<li><p><strong>网格列是通过跨越指定的 12 个列来创建。</strong> 例如，设置三个相等的列，需要使用用三个<code>.col-sm-4</code> 来设置。而设置4个相等的列,则使用四个<code>.col-sm-3</code>来创建.</p>
</li>
<li><p>Bootstrap 3 和 Bootstrap 4 最大的区别在于 Bootstrap 4 现在使用 flexbox（弹性盒子） 而不是浮动。 Flexbox 的一大优势是，<strong>没有指定宽度的网格列将自动设置为等宽与等高列.</strong></p>
</li>
</ul>
<h5 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h5><ul>
<li><p>指定列的显示形式</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;div class=&quot;row&quot;&gt;</span><br><span class="line">  &lt;div class=&quot;col-*-*&quot;&gt;&lt;/div&gt;</span><br><span class="line">&lt;/div&gt;</span><br><span class="line">&lt;div class=&quot;row&quot;&gt;</span><br><span class="line">  &lt;div class=&quot;col-*-*&quot;&gt;&lt;/div&gt;</span><br><span class="line">  &lt;div class=&quot;col-*-*&quot;&gt;&lt;/div&gt;</span><br><span class="line">  &lt;div class=&quot;col-*-*&quot;&gt;&lt;/div&gt;</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure>
</li>
<li><p>其中第一个<code>*</code>表示需要响应的设备(见上文的5种设备),第二个<code>*</code>表示一个数字,显示在同一行的列数字之和需要为12.</p>
</li>
<li><p>让BootStrap自动处理</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;div class=&quot;row&quot;&gt;</span><br><span class="line">  &lt;div class=&quot;col&quot;&gt;&lt;/div&gt;</span><br><span class="line">  &lt;div class=&quot;col&quot;&gt;&lt;/div&gt;</span><br><span class="line">  &lt;div class=&quot;col&quot;&gt;&lt;/div&gt;</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure></li>
<li><p>不在每个 col 上添加数字，让 bootstrap 自动处理布局，同一行的每个列宽度相等： 两个 “col” ，每个就为 50% 的宽度。三个 “col”每个就为 33.33% 的宽度，四个 “col”每个就为 25% 的宽度，以此类推。同样，你可以使用 <code>.col-sm|md|lg|xl</code>来设置列的响应规则。</p>
</li>
</ul>
<h5 id="不等宽的例子"><a href="#不等宽的例子" class="headerlink" title="不等宽的例子"></a>不等宽的例子</h5><ul>
<li><pre><code>
</code></pre>
</li>
</ul>
<div class="row">
  <div class="col-sm-4">.col-sm-4</div>
  <div class="col-sm-8">.col-sm-8</div>
</div>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">并且,由于设置为`sm`,**则宽度不足576px时,两个列会自动上下堆叠显示**.其他的类推.</span><br><span class="line"></span><br><span class="line">- 并且可以叠加使用:</span><br></pre></td></tr></table></figure>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
      <p>RUNOOB</p>
    </div>
    <div class="col-sm-9 col-md-6 col-lg-8 col-xl-10">
      <p>菜鸟教程</p>
    </div>
  </div>
</div>
```
- 实例在平板、桌面、大桌面显示器、超大桌面显示器的宽度比例为分别为：`25%/75%、50%/50%、33.33%/66.67%、16.67/83.33%`, 在移动手机等小型设备上会堆叠显示。

<ul>
<li>总而言之,很灵活,并且不同的设备之间的类会一一对应,不会相互影响.</li>
</ul>
<h2 id="结尾"><a href="#结尾" class="headerlink" title="结尾"></a>结尾</h2><hr>
<ul>
<li>总之,BootStrap在建立网站的过程中非常好用,一些模板可以直接去网站上拷贝到本地修改即可使用.非常方便.</li>
</ul>
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑BootStrap</tag>
      </tags>
  </entry>
  <entry>
    <title>C++学习笔记——一些报错信息归因</title>
    <url>/2018/10/08/C++-learning-error-collection/</url>
    <content><![CDATA[<h2 id="Expression-public-stream-nullptr"><a href="#Expression-public-stream-nullptr" class="headerlink" title="Expression: public_stream !&#x3D; nullptr"></a>Expression: public_stream !&#x3D; nullptr</h2><hr>
<ul>
<li><p>出现这个原因的问题很简单，就是用fopen打开文件失败了，创建的文件指针为空引起的。</p>
</li>
<li><p>解决方法就是查看文件路径是否正确。</p>
</li>
<li><p>在最后<code>fclose</code>的时候，先判断一下文件指针是否为空。</p>
</li>
</ul>
<h2 id="abort-has-been-called"><a href="#abort-has-been-called" class="headerlink" title="abort() has been called"></a>abort() has been called</h2><hr>
<ul>
<li><p>出现这个错误的原因基本上是越界或者内存泄漏。</p>
</li>
<li><p>这里的越界包括指针越界和数组越界</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++学习笔记——数值类型,char和string的相互转化问题</title>
    <url>/2018/10/08/C++-Int-Char-String-translation/</url>
    <content><![CDATA[<h2 id="C-提供了一些功能强大的函数来高效地完成这些转化"><a href="#C-提供了一些功能强大的函数来高效地完成这些转化" class="headerlink" title="C++提供了一些功能强大的函数来高效地完成这些转化"></a>C++提供了一些功能强大的函数来高效地完成这些转化</h2><hr>
<h3 id="string转char"><a href="#string转char" class="headerlink" title="string转char"></a>string转char</h3><ol>
<li>使用<code>c_str()</code>方法</li>
</ol>
<ul>
<li><p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string s = &quot;abcd&quot;;</span><br><span class="line">char p[10];</span><br><span class="line">p = (char*)s.c_str();</span><br></pre></td></tr></table></figure>
</li>
<li><p>上面的强制类型转换在 <strong>不修改字符串内容的情况下</strong> 是可以省略的。因为<code>c_str()</code>方法的返回值类型为<code>const char*</code>，返回的是一个不可修改的指向该字符串的指针。</p>
</li>
</ul>
<ol start="2">
<li>使用<code>data()</code>方法</li>
</ol>
<ul>
<li><code>data()</code>方法与<code>c_str()</code>方法用法几乎一致，这里不再赀述。</li>
</ul>
<ol start="3">
<li>使用<code>copy()</code>方法</li>
</ol>
<ul>
<li><p><code>copy()</code>方法与上面两种有所不同，它是拷贝函数，这只是他的一种用法。</p>
</li>
<li><p>实例:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string s = &quot;abcd&quot;;</span><br><span class="line">char p[5];</span><br><span class="line">s.copy(p, 4, 0);  //4代表拷贝四个字符，0代表写入的位置</span><br><span class="line">*(p + 4) = &#x27;\0&#x27;;  //拷贝的字符串需要手动加上结束标记</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="char转string"><a href="#char转string" class="headerlink" title="char转string"></a>char转string</h3><ul>
<li>对于已经存在的<code>char*</code>，可以直接赋值给string类型的数据</li>
</ul>
<h3 id="数值类型转string"><a href="#数值类型转string" class="headerlink" title="数值类型转string"></a>数值类型转string</h3><ul>
<li><p>C++库有直接的转换函数<code>to_string()</code></p>
</li>
<li><p><code>to_string()</code>函数有多个重载函数，支持目前所有的数据类型转换为string类型。</p>
</li>
<li><p>需要引入头文件<code>#include&lt;string&gt;</code></p>
</li>
<li><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;string&gt;</span><br><span class="line">...</span><br><span class="line">int a = 10;</span><br><span class="line">string s = to_string(a);</span><br><span class="line">...</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="数值类型转char"><a href="#数值类型转char" class="headerlink" title="数值类型转char"></a>数值类型转char</h3><ul>
<li><p>这里就需要用到强大的转换函数<code>itoa()</code>。</p>
</li>
<li><p>还有<code>ltoa</code>,<code>ultoa</code>等函数可以将 <strong>任意类型(整型、长整型、浮点型等)的数字</strong> 转换为字符串。</p>
</li>
<li><p>需要引入头文件<code>stdlib.h</code></p>
</li>
<li><p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;iostream&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line">using namespace std;</span><br><span class="line">int main()&#123;</span><br><span class="line">  int i = 100;</span><br><span class="line">  char p[10];</span><br><span class="line">  itoa(i, p, 10);   //说明：第一个参数为待转换的数字，第二个参数为待存储数值的char数组指针，第三个参数为待转换数字的进制，这里为十进制。</span><br><span class="line">  cout &lt;&lt; p &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>itoa并不是一个标准的C函数，它是Windows特有的，如果要写跨平台的程序，请用sprintf。</p>
</blockquote>
</li>
</ul>
<h3 id="字符串转数值类型"><a href="#字符串转数值类型" class="headerlink" title="字符串转数值类型"></a>字符串转数值类型</h3><ul>
<li><p>函数原型为 <code>int atoi (const char * str);</code>    &#x2F;&#x2F;参数为指针类型，如果为string需要在变量名前加取址符。</p>
</li>
<li><p><code>atoi()</code> 函数会扫描参数<code>str</code>字符串，跳过前面的空白字符（例如空格，tab缩进等，可以通过<code>isspace()</code>函数来检测），直到遇上数字或正负符号才开始做转换，而再遇到非数字或字符串结束时(<code>\0</code>)才结束转换，并将结果返回。</p>
</li>
<li><p>ANSI C 规范定义了 <code>stof()、atoi()、atol()、strtod()、strtol()、strtoul()</code>共6个可以将字符串转换为数字的函数。另外在 C99 &#x2F; C++11 规范中又新增了5个函数，分别是 <code>atoll()、strtof()、strtold()、strtoll()、strtoull()</code>。这里根据名称就可以对应看出各个函数的转换目标。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++学习笔记——强大的sprintf</title>
    <url>/2018/10/08/C++-sprintf-printf/</url>
    <content><![CDATA[<h2 id="sprintf-函数与printf-函数"><a href="#sprintf-函数与printf-函数" class="headerlink" title="sprintf()函数与printf()函数"></a>sprintf()函数与printf()函数</h2><hr>
<ul>
<li><p><code>sprintf()</code>函数与<code>printf()</code>函数的用法几乎一样。只是前者是将内容打印到字符串中，后者直接在命令行打印出来。</p>
</li>
<li><p><code>sprintf()</code>函数原型： <code>int sprintf( char *buffer, const char *format [, argument] ... );</code> 除了前两个参数固定，后面可以接任意多个参数。</p>
</li>
<li><p>第二个参数为格式化字符串，<code>printf</code> 和<code>sprintf</code> 都使用格式化字符串来指定串的格式，以<code>%</code>开头，然后在后边的变参列表中提供相应的变量，最终相应的位置的变量就会被替换进来。</p>
</li>
<li><p><code>sprintf</code>最常见的应用之一莫过于把整数打印到字符串中，所以，<code>spritnf</code> 在大多数场合可以替代<code>itoa</code>。</p>
</li>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sprintf(s, &quot;%d&quot;, 123);</span><br><span class="line">sprintf(s, &quot;%8d%8d&quot;, 123, 5678);  //指定宽度，不足的左边补空格，结果为&quot; 123 5678&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>浮点数用<code>%f</code>控制，默认打印6位小数。</p>
</li>
<li><p>使用<code>%d</code>或者<code>%x</code>打印一个字符，便能得出它的10进制或16进制的ASCII码；反过来，使用<code>%c</code>打印一个整数，便可以看到它所对应的ASCII字符。<strong>因为char类型本质上也是存储数值类型，只是习惯上用char来表示字符，实际上char就是byte，一个字节的数值类型。</strong></p>
</li>
<li><p><a href="http://lib.csdn.net/article/cplusplus/29221">实现各种各样格式化字符串</a></p>
</li>
<li><p><code>sprinf</code>就是将格式化字符串里的所有内容打印到第一个参数(字符数组)中，因此也可以用来连接字符串，非常灵活。</p>
</li>
<li><p><code>spritnf</code>和<code>printf</code>都最终返回本次函数调用最终打印到字符缓冲区中的字符数目。</p>
</li>
</ul>
<h2 id="转换说明符"><a href="#转换说明符" class="headerlink" title="转换说明符"></a>转换说明符</h2><hr>
<h3 id="格式符"><a href="#格式符" class="headerlink" title="格式符"></a>格式符</h3><table>
<thead>
<tr>
<th>格式符</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>%a(%A)</td>
<td>浮点数、十六进制数字和p-(P-)记数法(C99)</td>
</tr>
<tr>
<td>%c</td>
<td>字符</td>
</tr>
<tr>
<td>%d</td>
<td>有符号十进制整数</td>
</tr>
<tr>
<td>%f</td>
<td>浮点数(包括float和doulbe)</td>
</tr>
<tr>
<td>%e(%E)</td>
<td>浮点数指数输出[e-(E-)记数法]</td>
</tr>
<tr>
<td>%g(%G)</td>
<td>浮点数不显无意义的零”0”</td>
</tr>
<tr>
<td>%i</td>
<td>有符号十进制整数(与%d相同)</td>
</tr>
<tr>
<td>%u</td>
<td>无符号十进制整数</td>
</tr>
<tr>
<td>%o</td>
<td>八进制整数</td>
</tr>
<tr>
<td>%x(%X)</td>
<td>十六进制整数0f(0F)</td>
</tr>
<tr>
<td>%p</td>
<td>指针</td>
</tr>
<tr>
<td>%s</td>
<td>字符串</td>
</tr>
<tr>
<td>%%</td>
<td>输出字符%</td>
</tr>
</tbody></table>
<h3 id="标志"><a href="#标志" class="headerlink" title="标志"></a>标志</h3><table>
<thead>
<tr>
<th>标志</th>
<th>例子(说明)</th>
</tr>
</thead>
<tbody><tr>
<td>左对齐：”-“</td>
<td>%-20s</td>
</tr>
<tr>
<td>右对齐：”+”</td>
<td>%+20s</td>
</tr>
<tr>
<td>空格</td>
<td>若符号为正，则显示空格，负则显示”-“  比如：”% 6.2f”</td>
</tr>
<tr>
<td>#</td>
<td>对c,s,d,u类无影响；对o类，在输出时加前缀o；对x类，在输出时加前缀0x；对e,g,f 类当结果有小数时才给出小数点。</td>
</tr>
</tbody></table>
<h2 id="sprintf使用过程中遇到的一些问题记录"><a href="#sprintf使用过程中遇到的一些问题记录" class="headerlink" title="sprintf使用过程中遇到的一些问题记录"></a>sprintf使用过程中遇到的一些问题记录</h2><hr>
<h3 id="编译器报错：Buffer-is-too-small"><a href="#编译器报错：Buffer-is-too-small" class="headerlink" title="编译器报错：Buffer is too small"></a>编译器报错：<code>Buffer is too small</code></h3><ul>
<li><p>出现这个情况是因为被赋值的字符数组太小了。</p>
</li>
<li><p>实例：(矩阵乘积)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//计算矩阵的乘积</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">	char tmpc[24];		//存储转换的值,因为存储的是双精度浮点数，这里数据的整数部分为10位，小数部分要显示11位则至少开10+1+11=22位的数组才够</span><br><span class="line">	for (int i = 0; i &lt; M; i++) &#123;</span><br><span class="line">		for (int j = 0; j &lt; N; j++) &#123;</span><br><span class="line">			C[i][j] = 0;			//initialize the result martrix element</span><br><span class="line">			for (int k = 0; k &lt; K; k++) &#123;</span><br><span class="line">				C[i][j] += A[i][k] * B[k][j];</span><br><span class="line">			&#125;</span><br><span class="line">			//---------------------------------------------------------------------------------------------</span><br><span class="line">			//itoa不如sprintf灵活，这里采用sprintf来完成double到char的转换</span><br><span class="line">			////将数字转化为char数组写入, 采用itoa函数。函数原型：char *itoa(int value, char* string, int radix);</span><br><span class="line">			//_itoa(C[i][j], tmpc, 10);</span><br><span class="line">			//fputs(tmpc, fout);		//fputs的第一个参数为char*</span><br><span class="line">			//fputc(32, fout);			//写入空格，fputc的第一个参数为int*(ASCII码)</span><br><span class="line">			//---------------------------------------------------------------------------------------------</span><br><span class="line">			//使用强大的sprintf来完成转换，打印11位小数</span><br><span class="line">			sprintf_s(tmpc, &quot;%.11f &quot;, C[i][j]);				//这里的数值是整数部分10位，小数点一位，小数部分11位，末尾空格1位，结束符&#x27;\0&#x27;一位所以一共要开24位的数组才够</span><br><span class="line">			fputs(tmpc, fout);		//fputs的第一个参数为char*</span><br><span class="line">		&#125;</span><br><span class="line">		fputc(10, fout);		//写入换行符</span><br><span class="line">	&#125;</span><br><span class="line">  ...</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ auto关键字使用</title>
    <url>/2019/03/06/C++_auto_usage/</url>
    <content><![CDATA[<h2 id="简述"><a href="#简述" class="headerlink" title="简述"></a>简述</h2><hr>
<blockquote>
<p>auto的使用在C++11之后得以推广和支持</p>
</blockquote>
<ul>
<li><p>对于C++这门静态编程语言而言,所有的变量数据类型是需要明确声明的,在编译时即需要检查和确定.而诸如python这样的语言是动态判断变量类型的,在运行时才会确定数据类型.</p>
</li>
<li><p><code>auto</code>关键字赋予了C++强大的类型自动判断的能力,并且会在很长的类型声明的地方极大地简化我们的代码.</p>
</li>
</ul>
<h2 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h2><hr>
<ul>
<li><p>例如:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//遍历vector,使用迭代器(原始版本)</span><br><span class="line">for(std::vector&lt;int&gt;::iterator it = vec.begin(); it != it.end(); it++)&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//简化版</span><br><span class="line">for(auto it = vec.begin(); it != it.end(); it++)&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>还有一种遍历vector的简化写法(适合需要访问每个元素一遍的情况):</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main()&#123;</span><br><span class="line">  vector&lt;int&gt; vec;</span><br><span class="line">  ....</span><br><span class="line">  for(auto n: vec)&#123;   //这里n即为vec中的每个元素</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><hr>
<ul>
<li><p>简洁</p>
</li>
<li><p>在代码维护方面有一些”泛型”的好处,比如某个函数的返回值类型改变了,但是赋值的语句使用了<code>auto</code>关键字,那么便不再需要修改除函数以外的地方,减轻了代码的维护量.非常契合模板类等编程.</p>
</li>
<li><p>可以保存lambda表达式的类型的变量声明:<br><code>auto ptr = [](double x)&#123;return 2*x;&#125;; //类型为std::function&lt;double(double))&gt;函数对象</code></p>
</li>
</ul>
<h2 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h2><hr>
<ul>
<li><p><strong>使用<code>auto</code>声明的变量必须初始化</strong></p>
</li>
<li><p><code>auto</code>不能与其他类型组合使用,例如<code>auto int a;</code>是错误的.</p>
</li>
<li><p>函数和模板参数不能被声明为<code>auto</code>:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;auto T&gt;    //错误</span><br></pre></td></tr></table></figure>
<blockquote>
<p>因为auto本质上只是一个占位符,并不是一个数据类型,因此也不能用于类型转换和sizeof以及typeid等操作.</p>
</blockquote>
</li>
<li><p><code>auto</code>会自动退化为指向数组的指针,除非被声明为引用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int arr[5];</span><br><span class="line">auto x = a;</span><br><span class="line">cout &lt;&lt; typeid(x).name() &lt;&lt; endl;   // result: int*</span><br><span class="line">auto &amp;y = a;</span><br><span class="line">cout &lt;&lt; typeid(y).name() &lt;&lt; endl;   // result: int[5]</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++学习笔记——指针</title>
    <url>/2018/12/19/C++_ptr/</url>
    <content><![CDATA[<h2 id="典型错误"><a href="#典型错误" class="headerlink" title="典型错误"></a>典型错误</h2><hr>
<ul>
<li><pre><code>
</code></pre>
</li>
</ul>
<p>long * fellow;<br>*fellow &#x3D; 23333;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- fellow的确是一个指针,但是它指向的地址还没确定,那么23333会被存放到哪里呢?我们也不知道,这就会引发一个编译错误.</span><br><span class="line"></span><br><span class="line">- **指针使用的金科玉律: 一定要在对指针解除引用运算符(*)之前,将指针初始化为一个确定的,适当的地址.**</span><br><span class="line"></span><br><span class="line">&gt; --------from C++ Primer Plus</span><br><span class="line"></span><br><span class="line">## 指针和数字</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- 例子:</span><br></pre></td></tr></table></figure>
<p>int * pt;<br>pt &#x3D; 0xB8000000;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在这里左边是一个int类型的指针,而右边是一个 **整数**, 0xB8000000是老式计算机系统中组合段的偏移地址,但是在这条语句中编译器并不知道这是个地址,因此C++编译器会报错.</span><br><span class="line"></span><br><span class="line">- 正确的写法应该是:</span><br></pre></td></tr></table></figure>
<p>int * pt;<br>pt &#x3D; (int *) 0xB8000000;    </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">## 使用new来分配内存</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- 在C语言中,只能用库函数malloc()来分配内存,C++也支持这样做,但是有更好的选择---new运算符.</span><br><span class="line"></span><br><span class="line">- new运算符同时要配合delete来使用</span><br><span class="line"></span><br><span class="line">- 例子:</span><br></pre></td></tr></table></figure>
<p>int * pt &#x3D; new int;<br>…<br>delete pt;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">这将释放pt指向的内存块,但不会删除指针pt本身,它的值也没有改变.</span><br><span class="line"></span><br><span class="line">- 一点要配对地使用new 和 delete,否则会发生内存泄漏(memory leak).</span><br><span class="line"></span><br><span class="line">- 不要尝试释放已经释放的内存块,这会导致很严重的不确定性.</span><br><span class="line"></span><br><span class="line">- 不能使用delete来释放声明变量所获得的内存:(**也就是delete只能用来删除new分配的内存**)</span><br></pre></td></tr></table></figure>
<p>int * ps &#x3D; new int;   &#x2F;&#x2F;ok<br>delete ps;      &#x2F;&#x2F;ok<br>delte ps;     &#x2F;&#x2F;not ok now<br>int jugs &#x3D; 5;       &#x2F;&#x2F;ok<br>int * pi &#x3D; &jugs;   &#x2F;&#x2F;ok<br>delete pi;      &#x2F;&#x2F;not allowed now, memory not allocated by ‘new’</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">## 使用new创建动态数组</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- ```</span><br><span class="line">int num = 100;</span><br><span class="line">int * psome = new int[num];</span><br><span class="line">//可以使用psome[0]这样的形式来访问数组元素,与普通的数组无异</span><br><span class="line">...</span><br><span class="line">delete []psome;</span><br></pre></td></tr></table></figure>
<p><strong>delete的方括号告诉程序,应该释放整个数组,而不是仅仅是指针指向的元素</strong></p>
<ul>
<li><p>C++将数组名解释为地址(一般情况)</p>
</li>
<li><p>数组名被解释为其第一个元素的地址,而对数组名应用取地址符时,得到的是整个数组的地址.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">short tell[10];</span><br><span class="line">cout &lt;&lt; tell &lt;&lt; endl;   //display &amp;tell[0]</span><br><span class="line">cout &lt;&lt; &amp;tell &lt;&lt; endl;  //display address of whole array</span><br></pre></td></tr></table></figure>
<p>上面这个例子中,从数值上来说,这两个地址相同;但从概念上来说,<code>&amp;tell[0]</code>(即tell)是一个2字节内存块地址,<strong>而&amp;tell是一个20字节内存块的地址</strong>.因此,表达式tell+1将地址的值加2,而表达式&amp;tell+1将地址的值加20.</p>
</li>
<li><p>换句话说:*<em>tell是一个short指针(<em>short),而&amp;tell是一个这样的指针,即指向包含20个元素的short数组(short(</em>)[20]).</em>* 这种指针可以这样初始化:<code>short (*pas)[20] = &amp;tell</code>.这里pas的类型为<code>short (*)[20]</code></p>
</li>
</ul>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><hr>
<ul>
<li><p>应将内存地址赋值给指针.</p>
</li>
<li><p>可以是对已经赋值的变量名使用<code>&amp;</code>来获得 <em>被命名的内存的地址</em>, 或者使用new运算符返回未命名的内存的地址,只有这两种情况可以赋值给指针.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++学习笔记——类</title>
    <url>/2018/04/06/C++learning-class/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="类"><a href="#类" class="headerlink" title="类"></a>类</h2><hr>
<h3 id="模板类"><a href="#模板类" class="headerlink" title="模板类"></a>模板类</h3><ul>
<li><p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template &lt;class T&gt; //声明一个模板，虚拟类型名为T。注意：这里没有分号。  </span><br><span class="line">class T_Class //类模板名为Compare  </span><br><span class="line">&#123;  </span><br><span class="line">public :  </span><br><span class="line">   Compare(T a,T b)&#123;  </span><br><span class="line">      x=a;y=b;  </span><br><span class="line">   &#125;  </span><br><span class="line">   T max( )&#123;  </span><br><span class="line">      return (x&gt;y)?x:y;  </span><br><span class="line">   &#125;  </span><br><span class="line">   T min( )&#123;  </span><br><span class="line">      return (x&lt;y)?x:y;  </span><br><span class="line">   &#125;  </span><br><span class="line">private :  </span><br><span class="line">   T x,y;  </span><br><span class="line">&#125;;  </span><br></pre></td></tr></table></figure>
</li>
<li><p>模板类所有的函数，如果在类外面实现的话，需要写成如下形式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template &lt;class T&gt;</span><br><span class="line">T_Class&lt;T&gt;::max( )&#123;  </span><br><span class="line">     return (x &gt; y)? x : y;  </span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>模板类不能将类的声明写在头文件而定义写在源文件中</strong>，这样会出现无法辨析的编译错误。</p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.4.6</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
      </tags>
  </entry>
  <entry>
    <title>以太坊(ethereum)开发</title>
    <url>/2018/05/23/Ethereum/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<ul>
<li><strong><a href="https://ethfans.org/">以太坊社区</a></strong></li>
</ul>
<h2 id="安装Truffle"><a href="#安装Truffle" class="headerlink" title="安装Truffle"></a>安装Truffle</h2><hr>
<h3 id="Node-js安装"><a href="#Node-js安装" class="headerlink" title="Node.js安装"></a>Node.js安装</h3><ul>
<li>先要进行node.js环境的安装，见上一篇博客。</li>
</ul>
<h3 id="命令行安装truffle"><a href="#命令行安装truffle" class="headerlink" title="命令行安装truffle"></a>命令行安装truffle</h3><ul>
<li>执行命令 <code> npm install -g truffle</code></li>
</ul>
<h2 id="创建工程"><a href="#创建工程" class="headerlink" title="创建工程"></a>创建工程</h2><hr>
<ul>
<li><p>执行命令 <code>truffle init</code></p>
</li>
<li><p>truffle init会默认创建一个构建在以太坊内的代币demo应用。我们可以使用这个工程来进行快速的学习。</p>
</li>
<li><p><code>init</code> 之后目录下就会有若干文件和文件夹。结构如下：<br><img src="/images/block_chain/structure.png"></p>
</li>
<li><p>contracts目录中包含Solidity合约代码，其中Migrations.sol是必须有的，其他就是你自己写的合约代码了。</p>
</li>
<li><p>migrations目录中包含合约部署脚本，其中1_initial_migration.js就是用来部署Migrations.sol的，其他的脚本会按照顺序依次执行。</p>
</li>
<li><p>test目录保存用来测试应用和合约的测试文件</p>
</li>
<li><p>truffle.js - Truffle的配置文件</p>
</li>
<li><p>官方文档(命令行指令，前面要加truffle)：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Commands:</span><br><span class="line">  init      //Initialize new and empty Ethereum project</span><br><span class="line">  compile   //Compile contract source files</span><br><span class="line">  migrate   //Run migrations to deploy contracts</span><br><span class="line">  deploy    //(alias for migrate)</span><br><span class="line">  build     //Execute build pipeline (if configuration present)</span><br><span class="line">  test      //Run JavaScript and Solidity tests</span><br><span class="line">  debug     //Interactively debug any transaction on the blockchain (experimental)</span><br><span class="line">  opcode    //Print the compiled opcodes for a given contract</span><br><span class="line">  console   //Run a console with contract abstractions and commands available</span><br><span class="line">  develop   //Open a console with a local development blockchain</span><br><span class="line">  create    //Helper to create new contracts, migrations and tests</span><br><span class="line">  install   //Install a package from the Ethereum Package Registry</span><br><span class="line">  publish   //Publish a package to the Ethereum Package Registry</span><br><span class="line">  networks  //Show addresses for deployed contracts on each network</span><br><span class="line">  watch     //Watch filesystem for changes and rebuild the project automatically</span><br><span class="line">  serve     //Serve the build directory on localhost and watch for changes</span><br><span class="line">  exec      //Execute a JS module within this Truffle environment</span><br><span class="line">  unbox     //Download a Truffle Box, a pre-built Truffle project</span><br><span class="line">  version   //Show version number and exit</span><br></pre></td></tr></table></figure>
<p>终端输入<code>truffle</code>即可查看上述命令</p>
<h2 id="安装以太坊客户端"><a href="#安装以太坊客户端" class="headerlink" title="安装以太坊客户端"></a>安装以太坊客户端</h2><hr>
<ul>
<li><p>这里我们选择了Ganache——<a href="https://github.com/trufflesuite/ganache-cli">github项目</a></p>
</li>
<li><p><a href="https://github.com/trufflesuite/ganache/releases">图形版本下载地址</a>(linux下载 XX.AppImage文件格式的)</p>
</li>
<li><p>命令行版本（ganache-cli）下载 <code>npm install -g ganache-cli</code></p>
</li>
<li><p>Ganache的前身是testRPC</p>
</li>
</ul>
<h2 id="编译和部署合约"><a href="#编译和部署合约" class="headerlink" title="编译和部署合约"></a>编译和部署合约</h2><hr>
<ul>
<li><p>Ganache默认运行在7545端口，可以在界面右上方的“设置”里进行更改。运行后默认创建10个账号，每个账号里有100ETH的余额。<br>要部署到链上，需要把IP、端口、网络ID告诉truffle。</p>
</li>
<li><p>若要修改，则修改truffle.js：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">module.exports = &#123;  </span><br><span class="line">    networks: &#123;  </span><br><span class="line">        development: &#123;  </span><br><span class="line">            host: &#x27;localhost&#x27;,  </span><br><span class="line">            port: &#x27;7545&#x27;,  </span><br><span class="line">            network_id: &#x27;*&#x27; // Match any network id  </span><br><span class="line">        &#125;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;;  </span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="编译合约"><a href="#编译合约" class="headerlink" title="编译合约"></a>编译合约</h3><h4 id="合约位置"><a href="#合约位置" class="headerlink" title="合约位置"></a>合约位置</h4><ul>
<li><p>所有你的合约应该位于<code>./contracts</code>目录。默认我们提供了一个合约文件，一个库文件，均以.sol结尾作为示例。尽管库文件有一定的特殊性，但为简单起见，当前均称之为合约。</p>
</li>
<li><p><code>truffle cmpile</code> 仅默认编译自上次编译后被修改过的文件，来减少不必要的编译。如果你想编译全部文件，可以使用–compile-all选项。</p>
</li>
</ul>
<h4 id="约定"><a href="#约定" class="headerlink" title="约定"></a>约定</h4><ul>
<li><p>Truffle需要定义的合约名称和文件名准确匹配。例如，如果文件名为 <code>MyContract.sol</code>，那么合约文件须为如下两者之一(并且匹配 <strong>区分大小写</strong>)：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract MyContract &#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br><span class="line">// or</span><br><span class="line">library MyContract &#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编译目录:编译的输出位于<code>./build/contracts</code> 目录。如果目录不存在会自动创建。这些编译文件对于Truffle框架能否正常工作至关重要。你不应该在正常的编译或发布以外手动修改这些文件。</p>
</li>
</ul>
<h3 id="移植"><a href="#移植" class="headerlink" title="移植"></a>移植</h3><ul>
<li><p>命令 <code>truffle migrate</code></p>
</li>
<li><p>这个命令会执行所有的位于 <code>migrations</code> 目录内的移植脚本。如果你之前的移植是成功执行的。<code>truffle migrate</code> 仅会执行新创建的移植。如果没有新的移植脚本，这个命令不同执行任何操作。可以使用选项 <code>--reset</code>来从头执行移植脚本。</p>
</li>
</ul>
<h3 id="修改配置"><a href="#修改配置" class="headerlink" title="修改配置"></a>修改配置</h3><ul>
<li><p>Ganache默认运行在7545端口，可以在界面右上方的“设置”里进行更改。运行后默认创建10个账号，每个账号里有100ETH的余额。<br>要部署到链上，需要把IP、端口、网络ID告诉truffle。修改truffle.js：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">module.exports = &#123;</span><br><span class="line">  networks: &#123;</span><br><span class="line">    development: &#123;</span><br><span class="line">      host: &quot;127.0.0.1&quot;,</span><br><span class="line">      port: 8545,</span><br><span class="line">      network_id: &quot;*&quot; // Match any network id</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>详见<a href="http://truffleframework.com/docs/advanced/configuration">官方文档</a></p>
</li>
</ul>
<h2 id="测试合约"><a href="#测试合约" class="headerlink" title="测试合约"></a>测试合约</h2><hr>
<ul>
<li><p>直接执行命令：<code>truffle unbox metacoin </code>,创建这个项目。</p>
</li>
<li><p>由于metacoin的示例代码里已经把测试代码写好了，按照上面的修改完truffle.js之后，<strong>我们就可以编译部署测试我们的代码了：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">truffle compile</span><br><span class="line">truffle migrate</span><br><span class="line">truffle test</span><br></pre></td></tr></table></figure>
</li>
<li><p>结果如下:<br><img src="/images/block_chain/metacion_test_result.png"></p>
</li>
<li><p>然后打开 ganache查看区块链的变化。 Accounts标签：第一个账户里ETH略有减少，因为交易消耗了gas；Blocks标签：Ganache是自动挖矿，生成了6个新区块，每个区块里有一个交易；Transactions标签：有6笔新交易，可以点开看交易详情；Logs标签：显示交易和挖矿日志</p>
</li>
</ul>
<br>
> 最后更新于2018.5.23
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑BlockChain</tag>
      </tags>
  </entry>
  <entry>
    <title>一些文件类型记录</title>
    <url>/2018/04/15/File-types/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="一些特殊的符号收集"><a href="#一些特殊的符号收集" class="headerlink" title="一些特殊的符号收集"></a>一些特殊的符号收集</h2><hr>
<ul>
<li><p><a href="https://blog.csdn.net/rhinemetal/article/details/6887172">符号收集</a></p>
</li>
<li><p><a href="http://www.fuhao123.com/fuhao/1.shtml">符号大全</a></p>
</li>
</ul>
<h2 id="CSV文件（逗号分隔值文件格式）-XX-csv"><a href="#CSV文件（逗号分隔值文件格式）-XX-csv" class="headerlink" title="CSV文件（逗号分隔值文件格式）(XX.csv)"></a>CSV文件（逗号分隔值文件格式）(XX.csv)</h2><hr>
<ul>
<li>.csv文件实际上就是一种文本文件，其<strong>以纯文本形式存储表格数据</strong>，一般以逗号或者制表符作为分隔符</li>
</ul>
<h3 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h3><ul>
<li><p>开头是不留空，以行为单位。（每行末尾只有换行符，没有别的分隔符）</p>
</li>
<li><p>可含或不含列名，含列名则居文件第一行。</p>
</li>
<li><p>一行数据不跨行，无空行。</p>
</li>
<li><p>以半角逗号（即,）作分隔符，列为空也要表达其存在。</p>
</li>
<li><p><strong>不支持数字，不支持特殊字符</strong></p>
</li>
</ul>
<blockquote>
<p>实际上csv文件就类似于一个没有线的表格</p>
</blockquote>
<h2 id="Markdown文件-XX-markdown-XX-md"><a href="#Markdown文件-XX-markdown-XX-md" class="headerlink" title="Markdown文件(XX.markdown &#x2F; XX.md)"></a>Markdown文件(XX.markdown &#x2F; XX.md)</h2><hr>
<ul>
<li><p>Markdown 的目标是实现「易读易写」。可读性，无论如何，都是最重要的。一份使用 Markdown 格式撰写的文件应该可以直接以纯文本发布，并且看起来不会像是由许多标签或是格式指令所构成。Markdown 语法的目标是：成为一种适用于网络的书写语言。Markdown 不是想要取代 HTML，甚至也没有要和它相近，它的语法种类很少，只对应 HTML 标记的一小部分。Markdown 的构想不是要使得 HTML 文档更容易书写。在我看来， HTML 已经很容易写了。Markdown 的理念是，能让文档更容易读、写和随意改。HTML 是一种发布的格式，Markdown 是一种书写的格式。就这样，Markdown 的格式语法只涵盖纯文本可以涵盖的范围</p>
</li>
<li><p>markdown可以直接在Atom中编辑和预览(ctrl + shift + m)</p>
</li>
<li><p>markdown可以转换为pdf文件。你可以选择下载一些markdown编辑器来完成这个转化，但是，我在这里要推荐一个大佬的web应用(完美支持UTF-8中文编码，不会像其他的一些外国的网站转化时中文全部变成乱码的问题)，知乎上看到的。<a href="http://www.mdtr2pdf.com/index_en.html">在线markdown转化为PDF</a></p>
</li>
</ul>
<h2 id="Appimage文件-XX-AppImage"><a href="#Appimage文件-XX-AppImage" class="headerlink" title="Appimage文件(XX.AppImage)"></a>Appimage文件(XX.AppImage)</h2><hr>
<ul>
<li>AppImage是一种在Linux系统中用于分发便携式软件而不需要超级用户权限来安装它们的格式。[1] 它还试图让允许Linux的上游开发者来分发他们的程序而不用考虑不同Linux发行版间的区别。</li>
</ul>
<h3 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h3><ul>
<li><p>AppImage不把Linux应用程序安装在文件系统相应的目录中。相反,它没有进行实际的安装。AppImage文件只是个压缩文件，在它运行时候挂载。</p>
</li>
<li><p>用AppImage打包的程序，一个程序就是一个文件。每一个文件都包含了该程序在其所要运行的目标平台上所需的运行库。AppImage文件是基于ISO 9660并经过zisofs压缩的包含有一个最小化的AppDir目录和一个极小的运行环境的文件。只要把这个文件添加到live CD中，这个程序便可被轻而易举地添加进live CD中。</p>
</li>
<li><p>用AppImage文件比安装一个应用程序更加简单。它不需要解压也不需要为系统环境做调整。使用主流Linux发行版的用户可以下载它，使其可执行，并且运行即可。</p>
</li>
</ul>
<h3 id="运行方法"><a href="#运行方法" class="headerlink" title="运行方法"></a>运行方法</h3><ul>
<li><p>添加可执行权限 <code>chmod a+x *.AppImage</code></p>
</li>
<li><p>执行 <code>./*.AppImage</code></p>
</li>
<li><p>或者 <code>右键 &gt; 属性(properties) &gt; 权限(Permissions) &gt; 勾选Allow executing file as program</code></p>
</li>
<li><p>之后直接运行即可</p>
</li>
</ul>
<h2 id="DLL文件"><a href="#DLL文件" class="headerlink" title="DLL文件"></a>DLL文件</h2><hr>
<ul>
<li>DLL是<code>Dynamic Link Library</code>的缩写，意为动态链接库。</li>
</ul>
<blockquote>
<p>A DLL file, short for Dynamic Link Library, is a type of file that contains instructions that other programs can call upon to do certain things. This way, multiple programs can share the abilities programmed into a single file, and even do so simultaneously.</p>
</blockquote>
<blockquote>
<p>Unlike executable programs, like those with the EXE file extension, DLL files can’t be run directly but instead must be called upon by other code that is already running. However, DLLs are in the same format as EXEs and some may even use the .EXE file extension. While most Dynamic Link Libraries end in the file extension .DLL, others may use .OCX, .CPL, or .DRV.</p>
</blockquote>
<ul>
<li><p>DLL文件存储了别的应用程序可以调用的做某项特定的事情的一系列操作，这样，多个程序就可以并发地执行一些操作，仅仅通过将这些操作写入一个文件。</p>
</li>
<li><p>操作系统有时候会因为中了流氓软件或者病毒而导致杀毒软件删除被感染的捆绑时清除dll文件，但是不到万不得已不要去第三方网页上下载缺失的dll文件——<a href="https://www.lifewire.com/important-reasons-not-to-download-dll-files-2624455">Important Reasons NOT to Download DLL Files</a>。这篇文章详细地描述了直接下载缺失的DLL文件的可能的潜在危害。</p>
</li>
<li><p>如果电脑的DLL文件缺失导致某个软件不能运行之类的错误，尽量尝试重新安装这个软件而不是直接下载DLL文件来解决问题。</p>
</li>
<li><p>参考文档——<a href="https://www.lifewire.com/what-is-a-dll-file-2625852">What Is a DLL File</a></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑FileTypes</tag>
      </tags>
  </entry>
  <entry>
    <title>前端学习——CSS</title>
    <url>/2018/06/17/FrontEnd-CSS/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<ul>
<li><p>CSS全称为“层叠样式表 (Cascading Style Sheets)”，它主要是用于定义HTML内容在浏览器内的显示样式，如文字大小、颜色、字体加粗等。</p>
</li>
<li><p><strong>在CSS代码中不要随便加空格</strong></p>
</li>
<li><p><a href="https://www.jianshu.com/p/57ba5da7f9f6">CSS3动画效果</a></p>
</li>
</ul>
<h2 id="CSS代码语法"><a href="#CSS代码语法" class="headerlink" title="CSS代码语法"></a>CSS代码语法</h2><hr>
<h3 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h3><blockquote>
<p>注意各个语句之间以分号间隔</p>
</blockquote>
<ul>
<li><p>css 样式由选择符和声明组成，而声明又由属性和值组成，例如：<code>p&#123;color:blue&#125;</code></p>
</li>
<li><p>其中，<code>p</code>为 <strong>选择符</strong>，<code>&#123;..&#125;</code>为 <strong>声明</strong>，<code>color</code>为 <strong>属性</strong>，<code>blue</code>为 <strong>值</strong>。</p>
</li>
<li><p>选择符：又称选择器，<strong>指明网页中要应用样式规则的元素</strong>，如本例中是网页中所有的段（p）的文字将变成蓝色，而其他的元素（如ol）不会受到影响。</p>
</li>
<li><p>声明：在英文大括号<code>｛ ｝</code>中的的就是声明，属性和值之间用英文冒号<code>：</code>分隔。当有多条声明时，中间可以英文分号<code>；</code>分隔，如：<code>p&#123;font-size:12px;color:red;&#125;</code></p>
</li>
<li><p>最后一条声明可以没有分号，但是为了以后修改方便，一般也加上分号</p>
</li>
<li><p>为了使用样式更加容易阅读，可以将每条代码写在一个新行内，所以上面的应该写为：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">p&#123;</span><br><span class="line">  font-size:12px;</span><br><span class="line">  color:red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="CSS中的注释"><a href="#CSS中的注释" class="headerlink" title="CSS中的注释"></a>CSS中的注释</h3><ul>
<li>格式：<code>/*注释语句*/</code></li>
</ul>
<h2 id="三种CSS样式"><a href="#三种CSS样式" class="headerlink" title="三种CSS样式"></a>三种CSS样式</h2><hr>
<ul>
<li>从CSS 样式代码插入的形式来看基本可以分为以下3种：内联式、嵌入式和外部式三种。</li>
</ul>
<h3 id="内联式css样式，直接写在现有的HTML标签中"><a href="#内联式css样式，直接写在现有的HTML标签中" class="headerlink" title="内联式css样式，直接写在现有的HTML标签中"></a>内联式css样式，直接写在现有的HTML标签中</h3><ul>
<li>内联式css样式表就是把css代码直接写在现有的HTML标签中，如下面代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;p style=&quot;color:red&quot;&gt;这里文字是红色&lt;/p&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意要写在元素的开始标签里</p>
</blockquote>
<ul>
<li>并且css样式代码要写在<code>style=&quot;&quot;</code>双引号中，如果有多条css样式代码设置可以写在一起，中间用分号隔开。如下代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;p style=&quot;color:red;font-size:12px&quot;&gt;这里文字是红色&lt;/p&gt;</span><br></pre></td></tr></table></figure>

<h3 id="嵌入式css样式，写在当前的文件中"><a href="#嵌入式css样式，写在当前的文件中" class="headerlink" title="嵌入式css样式，写在当前的文件中"></a>嵌入式css样式，写在当前的文件中</h3><ul>
<li>嵌入式css样式，就是可以把css样式代码写在<code>&lt;style type=&quot;text/css&quot;&gt; &lt;/style&gt;</code>标签之间。如下面代码实现把三个<code>&lt;span&gt;</code>标签中的文字设置为红色：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;style type=&quot;text/css&quot;&gt;</span><br><span class="line">span&#123;</span><br><span class="line">color:red;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li>嵌入式css样式必须写在<code>&lt;style&gt; &lt;/style&gt;</code>之间，并且一般情况下嵌入式css样式写在<code>&lt;head&gt; &lt;/head&gt;</code>之间</li>
</ul>
<h3 id="外部式css样式，写在单独的一个文件中"><a href="#外部式css样式，写在单独的一个文件中" class="headerlink" title="外部式css样式，写在单独的一个文件中"></a>外部式css样式，写在单独的一个文件中</h3><ul>
<li>外部式css样式(也可称为外联式)就是把css代码写一个单独的外部文件中，这个css样式文件以<code>.css</code>为扩展名，在<code>&lt;head&gt;</code>内（不是在<code>&lt;style&gt;</code>标签内）使用<code>&lt;link&gt;</code>标签将css样式文件链接到HTML文件内，如下面代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;link href=&quot;base.css&quot; rel=&quot;stylesheet&quot; type=&quot;text/css&quot; /&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>css样式文件名称以有意义的英文字母命名，如 main.css。</p>
</li>
<li><p><code>rel=&quot;stylesheet&quot; type=&quot;text/css&quot;</code> 是 <strong>固定写法</strong> 不可修改。</p>
</li>
<li><p><code>&lt;link&gt;</code>标签位置一般写在<code>&lt;head&gt;</code>标签之内。</p>
</li>
</ul>
<h3 id="三种方法的优先级"><a href="#三种方法的优先级" class="headerlink" title="三种方法的优先级"></a>三种方法的优先级</h3><ul>
<li><p>前提：在相同的权值的情况下。</p>
</li>
<li><p>优先级：<code>内联式 &gt; 嵌入式 &gt; 外部式</code></p>
</li>
<li><p>总而言之，就是就近原则（离被设置元素越近优先级别越高）。</p>
</li>
</ul>
<h2 id="选择器"><a href="#选择器" class="headerlink" title="选择器"></a>选择器</h2><hr>
<ul>
<li>每一条css样式声明（定义）由两部分组成，形式如下：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">选择器&#123;</span><br><span class="line">    样式;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在<code>&#123;&#125;</code>之前的部分就是“选择器”，“选择器”指明了<code>&#123;&#125;</code>中的“样式”的作用对象，也就是“样式”作用于网页中的哪些元素。</p>
<ul>
<li>例：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE HTML&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;meta http-equiv=&quot;Content-Type&quot; content=&quot;text/html; charset=utf-8&quot;&gt;</span><br><span class="line">&lt;title&gt;选择器&lt;/title&gt;</span><br><span class="line">&lt;style type=&quot;text/css&quot;&gt;</span><br><span class="line">body&#123;</span><br><span class="line">    font-size:12px;</span><br><span class="line">    color:red;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;p&gt;</span><br><span class="line">XXXXXXXX</span><br><span class="line">&lt;/p&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<p>其中<code>body&#123;..&#125;</code>就是一个选择器（标签选择器），他作用于<code>&lt;body&gt;</code>部分</p>
<h3 id="标签选择器"><a href="#标签选择器" class="headerlink" title="标签选择器"></a>标签选择器</h3><ul>
<li><p>标签选择器其实就是html代码中的标签。</p>
</li>
<li><p>例：<code>p&#123;font-size:12px;line-height:1.6em;&#125;</code>这个就为p标签（<code>&lt;p&gt;</code>标签是段落部分）设置12px字号，行间距设置1.6em的样式。</p>
</li>
</ul>
<h3 id="类选择器"><a href="#类选择器" class="headerlink" title="类选择器"></a><strong>类选择器</strong></h3><ul>
<li><p>语法：<code>.类选器名称&#123;css样式代码;&#125;</code></p>
</li>
<li><p>注意：以英文圆点开头。类选择器可以任意起名字。</p>
</li>
<li><p>用法：</p>
</li>
</ul>
<ol>
<li><p>使用合适的标签把要修饰的内容标记起来，如：<code>&lt;span&gt;胆小如鼠&lt;/span&gt;</code></p>
</li>
<li><p>使用<code>class=&quot;类选择器名称&quot;</code>为标签设置一个类，如：<code>&lt;span class=&quot;stress&quot;&gt;胆小如鼠&lt;/span&gt;</code></p>
</li>
<li><p>设置类选器css样式，如：<code>.stress&#123;color:red;&#125; /*类前面要加入一个英文圆点*/</code></p>
</li>
</ol>
<ul>
<li>类选择器使得我们可以灵活地自定义不同的部分的格式。</li>
</ul>
<h3 id="ID选择器"><a href="#ID选择器" class="headerlink" title="ID选择器"></a><strong>ID选择器</strong></h3><ul>
<li>在很多方面，ID选择器都类似于类选择符，但也有一些重要的区别：</li>
</ul>
<ol>
<li><p><strong>为标签设置<code>id=&quot;ID名称&quot;</code>，而不是<code>class=&quot;类名称&quot;</code>。</strong></p>
</li>
<li><p>ID选择符的前面是井号（#），而不是英文圆点（.）</p>
</li>
</ol>
<ul>
<li>例如：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#test&#123;</span><br><span class="line">  color:blue;</span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line">XXX&lt;span id=&quot;test&quot;&gt;...&lt;/span&gt;</span><br></pre></td></tr></table></figure>

<h3 id="id选择器和类选择器的区别与联系"><a href="#id选择器和类选择器的区别与联系" class="headerlink" title="id选择器和类选择器的区别与联系"></a>id选择器和类选择器的区别与联系</h3><h4 id="相同点"><a href="#相同点" class="headerlink" title="相同点"></a>相同点</h4><ul>
<li>可以应用于任何元素</li>
</ul>
<h4 id="不同点"><a href="#不同点" class="headerlink" title="不同点"></a>不同点</h4><ul>
<li><p><strong>ID选择器只能在文档中使用一次</strong>。与类选择器不同，在一个HTML文档中，ID选择器只能使用一次，而且仅一次。而<strong>类选择器可以使用多次</strong>。</p>
</li>
<li><p><strong>这里只能使用一次是指同一个id选择器只能修饰一个地方，而类选择器可以同时修饰多个地方。当然我们可以定义多个id选择器来修饰不同的地方</strong></p>
</li>
<li><p>可以使用类选择器词列表方法为一个元素<strong>同时设置多个样式</strong>。我们可以为一个元素同时设多个样式，但只可以用类选择器的方法实现，ID选择器是不可以的（不能使用 ID 词列表）。</p>
</li>
<li><p><strong>正确示例</strong>：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.stress&#123;</span><br><span class="line">    color:red;</span><br><span class="line">&#125;</span><br><span class="line">.bigsize&#123;</span><br><span class="line">    font-size:25px;</span><br><span class="line">&#125;</span><br><span class="line">&lt;p&gt;</span><br><span class="line">到了&lt;span class=&quot;stress bigsize&quot;&gt;三年级&lt;/span&gt;下学期时，我们班上了一节公开课...</span><br><span class="line">&lt;/p&gt;</span><br></pre></td></tr></table></figure>
<p>以上代码在<code>&lt;span&gt;</code>里定义两个类来修饰，这样的语法是可以的。上面代码的作用是为“三年级”三个文字设置文本颜色为红色并且字号为25px。</p>
<ul>
<li>错误的例子：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#stressid&#123;</span><br><span class="line">    color:red;</span><br><span class="line">&#125;</span><br><span class="line">#bigsizeid&#123;</span><br><span class="line">    font-size:25px;</span><br><span class="line">&#125;</span><br><span class="line">&lt;p&gt;</span><br><span class="line">到了&lt;span id=&quot;stressid bigsizeid&quot;&gt;三年级&lt;/span&gt;下学期时，我们班上了一节公开课...</span><br><span class="line">&lt;/p&gt;</span><br></pre></td></tr></table></figure>
<p>以上代码是错误的，一个地方只能用一个id选择器来修饰。以上代码不能起到修饰的作用。</p>
<h3 id="子选择器"><a href="#子选择器" class="headerlink" title="子选择器"></a>子选择器</h3><ul>
<li><p>还有一个比较有用的选择器子选择器，即大于符号(&gt;),用于选择指定标签元素的<strong>第一代子元素</strong>。</p>
</li>
<li><p>例如：(以下代码会使class名为food下的子元素 li 加入红色实线边框。)</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">.food&gt;li&#123;border:1px solid red;&#125;</span><br><span class="line">...</span><br><span class="line">&lt;h1&gt;食物&lt;/h1&gt;</span><br><span class="line">&lt;ul class=&quot;food&quot;&gt;</span><br><span class="line">    &lt;li&gt;水果</span><br><span class="line">        &lt;ul&gt;</span><br><span class="line">        	&lt;li&gt;香蕉&lt;/li&gt;</span><br><span class="line">            &lt;li&gt;苹果&lt;/li&gt;</span><br><span class="line">            &lt;li&gt;梨&lt;/li&gt;</span><br><span class="line">        &lt;/ul&gt;</span><br><span class="line">    &lt;/li&gt;</span><br><span class="line">    &lt;li&gt;蔬菜</span><br><span class="line">    	&lt;ul&gt;</span><br><span class="line">        	&lt;li&gt;白菜&lt;/li&gt;</span><br><span class="line">            &lt;li&gt;油菜&lt;/li&gt;</span><br><span class="line">            &lt;li&gt;卷心菜&lt;/li&gt;</span><br><span class="line">        &lt;/ul&gt;</span><br><span class="line">    &lt;/li&gt;</span><br><span class="line">&lt;/ul&gt;</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<h3 id="包含-后代-选择器"><a href="#包含-后代-选择器" class="headerlink" title="包含(后代)选择器"></a>包含(后代)选择器</h3><ul>
<li><p>包含选择器，即加入空格,用于选择指定标签元素下的后辈元素。如：<code>.first  span&#123;color:red;&#125;</code></p>
</li>
<li><p>请注意这个选择器与子选择器的区别，子选择器（child selector）仅是指它的<strong>直接后代</strong>，或者你可以理解为作用于子元素的<strong>第一代后代</strong>。而后代选择器是作用于<strong>所有子后代元素</strong>。后代选择器通过<strong>空格</strong>来进行选择，而子选择器是通过<code>&gt;</code>进行选择。</p>
</li>
<li><p>总而言之就是，空格选择所有后代元素，<code>&gt;</code>只选择第一代后代。</p>
</li>
</ul>
<h3 id="通用选择器"><a href="#通用选择器" class="headerlink" title="通用选择器"></a><strong>通用选择器</strong></h3><ul>
<li>通用选择器是功能最强大的选择器，它使用一个<code>*</code>号指定，它的作用是匹配html中所有标签元素，如下使用下面代码使用html中任意标签元素字体颜色全部设置为红色：<code>* &#123;color:red;&#125;</code></li>
</ul>
<h3 id="伪类选择符"><a href="#伪类选择符" class="headerlink" title="伪类选择符"></a>伪类选择符</h3><ul>
<li><p>它允许给html不存在的标签（标签的某种状态）设置样式，比如说我们给html中一个标签元素的鼠标滑过的状态来设置字体颜色：<code>a:hover&#123;color:red;&#125;</code><br>这句代码就实现了<code>&lt;a&gt;</code>(超链接)鼠标划过的特效，当鼠标划过超链接时，超链接的字体颜色会变成红色。</p>
</li>
<li><p>一般<code>:hover</code>都是用来控制鼠标滑过的效果</p>
</li>
<li><p>其实 <code>:hover</code> 可以放在任意的标签上，比如说 <code>p:hover</code>，但是它们的兼容性也是很不好的，所以一般用在<code>a:hover</code>的组合。(<strong>现在也可以用在其他的地方</strong>)</p>
</li>
</ul>
<h3 id="分组选择符"><a href="#分组选择符" class="headerlink" title="分组选择符"></a><strong>分组选择符</strong></h3><ul>
<li><p>当你想为html中多个标签元素设置同一个样式时，可以使用分组选择符（，），如下代码将<code>&lt;h1&gt;</code>、<code>&lt;span&gt;</code>标签同时设置字体颜色为红色：<code>h1,span&#123;color:red;&#125;</code></p>
</li>
<li><p>以上代码相当于下面两行代码：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h1&#123;color:red;&#125;</span><br><span class="line">span&#123;color:red;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><ul>
<li>CSS的某些样式是具有继承性的，那么什么是继承呢？继承是一种规则，它允许样式不仅应用于某个特定html标签元素，而且应用于其后代。就是对某一些标签的样式的修改会对其子代也产生影响。</li>
</ul>
<h3 id="权值"><a href="#权值" class="headerlink" title="权值"></a>权值</h3><ul>
<li><p>如果对同一个标签设置了不同的样式，浏览器是根据权值来判断使用哪种css样式的，权值高的就使用哪种css样式。</p>
</li>
<li><p>权值的规则：<strong>标签的权值为1，类选择符的权值为10，ID选择符的权值最高为100。</strong></p>
</li>
<li><p>例如：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">p&#123;color:red;&#125; /*权值为1*/</span><br><span class="line">p span&#123;color:green;&#125; /*权值为1+1=2*/</span><br><span class="line">.warning&#123;color:white;&#125; /*权值为10*/</span><br><span class="line">p span.warning&#123;color:purple;&#125; /*权值为1+1+10=12*/</span><br><span class="line">#footer .note p&#123;color:yellow;&#125; /*权值为100+10+1=111*/</span><br></pre></td></tr></table></figure>
<p>注意：还有一个权值比较特殊–继承也有权值但很低，有的文献提出它只有0.1，所以可以理解为继承的权值最低。</p>
<ul>
<li>使用<code>!important</code>来使样式获得最高权值：<code>p&#123;color:red!important;&#125;</code><blockquote>
<p>注意：!important要写在分号的前面,中间不需要空格。</p>
</blockquote>
</li>
</ul>
<h3 id="层叠"><a href="#层叠" class="headerlink" title="层叠"></a>层叠</h3><ul>
<li><p>如果在html文件中对于同一个元素可以有多个css样式存在并且这多个css样式具有相同权重值怎么办？这时候我们就会用到层叠。</p>
</li>
<li><p>层叠就是在html文件中对于同一个元素可以有多个css样式存在，当有相同权重的样式存在时，会根据这些css样式的前后顺序来决定，处于<strong>最后面的css样式</strong>会被应用。</p>
</li>
<li><p>简单来说，就是，就是后面的样式会覆盖前面的样式。</p>
</li>
<li><p>所以之前的内外优先级关系：<code>内联样式表（标签内部）&gt; 嵌入样式表（当前文件中）&gt; 外部样式表（外部文件中）。</code> 也就很好理解了。</p>
</li>
</ul>
<h2 id="文字排版"><a href="#文字排版" class="headerlink" title="文字排版"></a>文字排版</h2><hr>
<h3 id="字体"><a href="#字体" class="headerlink" title="字体"></a>字体</h3><ul>
<li><p>例：为网页设置字体 <code>body&#123;font-family:&quot;Microsoft Yahei&quot;;&#125;</code> &#x2F;&#x2F;字体为微软雅黑</p>
</li>
<li><p>注意：这里设置的字体能否显示，取决于用户本地有没有安装这个字体。</p>
</li>
</ul>
<h3 id="字号颜色"><a href="#字号颜色" class="headerlink" title="字号颜色"></a>字号颜色</h3><ul>
<li><p>设置网页中文字的字号为12像素，并把字体颜色设置为#666(灰色)：<code>body&#123;font-size:12px;color:#666&#125;</code></p>
</li>
<li><p><code>font-size</code>:字体大小</p>
</li>
<li><p><code>color</code>：颜色(一般以<code>#</code>加数字来表示颜色，以增加颜色的多样化)</p>
</li>
</ul>
<h3 id="一些字体样式：粗体、斜体等"><a href="#一些字体样式：粗体、斜体等" class="headerlink" title="一些字体样式：粗体、斜体等"></a>一些字体样式：粗体、斜体等</h3><ul>
<li><p><code>font-weight</code>:字体粗细</p>
</li>
<li><p>例：<code>span&#123;font-weight:bold;&#125;</code>  &#x2F;&#x2F;粗体</p>
</li>
<li><p><code>font-style</code>:字体特点</p>
</li>
<li><p>例：<code>span&#123;font-style:italic;&#125;</code>  &#x2F;&#x2F;斜体(意大利体)</p>
</li>
<li><p><code>text-decoration</code>:文字修饰</p>
</li>
<li><p>例：<code>a&#123;text-decoration:underline;&#125;</code>  &#x2F;&#x2F;下划线</p>
</li>
<li><p>例：<code>.oldPrice&#123;text-decoration:line-through;&#125;</code>    &#x2F;&#x2F;删除线(将文字划去)</p>
</li>
</ul>
<h3 id="段落排版"><a href="#段落排版" class="headerlink" title="段落排版"></a>段落排版</h3><h4 id="首行缩进"><a href="#首行缩进" class="headerlink" title="首行缩进"></a>首行缩进</h4><ul>
<li><p>设置段落缩进两个字宽：<code>p&#123;text-indent:2em;&#125;</code></p>
</li>
<li><p><code>2em</code>的意思就是文字的2倍大小。</p>
</li>
</ul>
<h4 id="行间距"><a href="#行间距" class="headerlink" title="行间距"></a>行间距</h4><ul>
<li>例：<code>p&#123;line-height:1.5em;&#125;</code></li>
</ul>
<h4 id="中文字间距、字母间距"><a href="#中文字间距、字母间距" class="headerlink" title="中文字间距、字母间距"></a>中文字间距、字母间距</h4><ul>
<li><p>如果想在网页排版中设置<strong>文字间隔</strong>或者<strong>字母间隔</strong>就可以使用<code>letter-spacing</code>来实现。</p>
</li>
<li><p>例：<code>h1&#123;letter-spacing:10px;&#125;</code></p>
</li>
<li><p>注意：这个样式使用在英文单词时，是设置<strong>字母与字母之间</strong>的间距。</p>
</li>
<li><p>如果我想设置英文单词之间的间距，可以使用 <code>word-spacing</code> 来实现。</p>
</li>
<li><p>例：<code>h1&#123;word-spacing:20px;&#125;</code></p>
</li>
</ul>
<h4 id="对齐"><a href="#对齐" class="headerlink" title="对齐"></a>对齐</h4><ul>
<li><p>为块状元素中的文本、图片设置居中样式，可以使用<code>text-align</code>样式代码。</p>
</li>
<li><p>例：<code>h1&#123;text-align:center/left/right;&#125;</code>  &#x2F;&#x2F;居中&#x2F;左对齐&#x2F;右对齐</p>
</li>
</ul>
<h2 id="元素分类"><a href="#元素分类" class="headerlink" title="元素分类"></a>元素分类</h2><hr>
<ul>
<li><p>在CSS中，html中的标签元素大体被分为三种不同的类型：块状元素、内联元素(又叫行内元素)和内联块状元素。</p>
</li>
<li><p>常用的块状元素有：<code>&lt;div&gt;、&lt;p&gt;、&lt;h1&gt;...&lt;h6&gt;、&lt;ol&gt;、&lt;ul&gt;、&lt;dl&gt;、&lt;table&gt;、&lt;address&gt;、&lt;blockquote&gt; 、&lt;form&gt;</code></p>
</li>
<li><p>常用的内联元素有：<code>&lt;a&gt;、&lt;span&gt;、&lt;br&gt;、&lt;i&gt;、&lt;em&gt;、&lt;strong&gt;、&lt;label&gt;、&lt;q&gt;、&lt;var&gt;、&lt;cite&gt;、&lt;code&gt;</code></p>
</li>
<li><p>常用的内联块状元素有：<code>&lt;img&gt;、&lt;input&gt;</code></p>
</li>
</ul>
<h3 id="块级元素"><a href="#块级元素" class="headerlink" title="块级元素"></a>块级元素</h3><ul>
<li><p>设置<code>display:block</code>就是将元素显示为块级元素。如下代码就是将内联元素a转换为块状元素，从而使a元素具有块状元素特点：<code>a&#123;display:block;&#125;</code></p>
</li>
<li><p>块级元素特点：</p>
</li>
</ul>
<ol>
<li><p>每个块级元素都从新的一行开始，并且其后的元素也另起一行。</p>
</li>
<li><p>元素的高度、宽度、行高以及顶和底边距都可设置。</p>
</li>
<li><p>元素宽度在不设置的情况下，是它本身父容器的100%（和父元素的宽度一致），除非设定一个宽度。</p>
</li>
</ol>
<h3 id="内联元素"><a href="#内联元素" class="headerlink" title="内联元素"></a>内联元素</h3><ul>
<li><p>块状元素也可以通过代码<code>display:inline</code>将元素设置为内联元素。</p>
</li>
<li><p>如下代码就是将块状元素div转换为内联元素，从而使 div 元素具有内联元素特点：<code>div&#123;display:inline;&#125;</code></p>
</li>
<li><p>内联元素特点：</p>
</li>
</ul>
<ol>
<li><p>和其他元素都在一行上</p>
</li>
<li><p>元素的高度、宽度及顶部和底部边距不可设置</p>
</li>
<li><p>元素的宽度就是它包含的文字或图片的宽度，不可改变</p>
</li>
</ol>
<h3 id="内联块状元素"><a href="#内联块状元素" class="headerlink" title="内联块状元素"></a>内联块状元素</h3><ul>
<li><p>内联块状元素（inline-block）就是同时具备内联元素、块状元素的特点，代码<code>display:inline-block</code>就是将元素设置为内联块状元素。</p>
</li>
<li><p>inline-block 元素特点：</p>
</li>
</ul>
<ol>
<li><p>和其他元素都在一行上</p>
</li>
<li><p>元素的高度、宽度、行高以及顶和底边距都可设置</p>
</li>
</ol>
<h2 id="颜色值"><a href="#颜色值" class="headerlink" title="颜色值"></a>颜色值</h2><hr>
<ul>
<li><p>英文命令颜色：<code>p&#123;color:red;&#125;</code></p>
</li>
<li><p>RGB颜色：这个与 photoshop 中的 RGB 颜色是一致的，由 R(red)、G(green)、B(blue) 三种颜色的比例来配色。</p>
</li>
<li><p>例：<code>p&#123;color:rgb(133,45,200);&#125;</code> 每一项的值可以是 0<del>255 之间的整数，也可以是 0%</del>100% 的百分数，例如：<code>p&#123;color:rgb(20%,33%,25%);&#125;</code></p>
</li>
<li><p><strong>十六进制颜色</strong>：这种颜色设置方法是现在比较普遍使用的方法，其原理其实也是 RGB 设置，但是其每一项的值由 0-255 变成了十六进制 00-ff。</p>
</li>
<li><p>例：<code>p&#123;color:#00ffff;&#125;</code>  &#x2F;&#x2F;以<code>#</code>开头</p>
</li>
</ul>
<p><img src="/images/CSS/color.jpg"></p>
<p><img src="/images/CSS/color_list.jpg"></p>
<h2 id="长度值"><a href="#长度值" class="headerlink" title="长度值"></a>长度值</h2><hr>
<ul>
<li>目前比较常用到px（像素）、em、% 百分比。(但是这三种单位都是相对值)</li>
</ul>
<h3 id="像素"><a href="#像素" class="headerlink" title="像素"></a>像素</h3><ul>
<li>像素为什么是相对单位呢？因为像素指的是显示器上的小点（CSS规范中假设“90像素&#x3D;1英寸”）。实际情况是浏览器会使用显示器的实际像素值有关，在目前大多数的设计者都倾向于使用像素（px）作为单位。</li>
</ul>
<h3 id="em"><a href="#em" class="headerlink" title="em"></a>em</h3><ul>
<li>就是本元素给定字体的 font-size 值，如果元素的 font-size 为 14px ，那么 1em &#x3D; 14px；如果 font-size 为 18px，那么 1em &#x3D; 18px。如下代码：<code>p&#123;font-size:12px;text-indent:2em;&#125;</code>就是可以实现段落首行缩进 24px（也就是两个字体大小的距离）。</li>
</ul>
<h3 id="百分比"><a href="#百分比" class="headerlink" title="百分比"></a>百分比</h3><ul>
<li><code>p&#123;font-size:12px;line-height:130%&#125;</code> 设置行高（行间距）为字体的130%（12 * 1.3 &#x3D; 15.6px）。</li>
</ul>
<h2 id="一些补充"><a href="#一些补充" class="headerlink" title="一些补充"></a>一些补充</h2><hr>
<h3 id="元素宽度相关"><a href="#元素宽度相关" class="headerlink" title="元素宽度相关"></a>元素宽度相关</h3><ul>
<li><p><code>calc()</code>用法：<a href="https://www.w3cplus.com/css3/how-to-use-css3-calc-function.html">详解</a></p>
</li>
<li><p>如果你的元素宽度是100%时，只要你在元素中添加了border,padding,margin任何一值，都将会把元素盒子撑破（标准模式下，除IE怪异模式）。</p>
</li>
</ul>
<h4 id="opacity属性"><a href="#opacity属性" class="headerlink" title="opacity属性"></a>opacity属性</h4><ul>
<li><p>设置div元素的不透明级别</p>
</li>
<li><p>语法：<code>opacity: value|inherit;</code></p>
</li>
<li><p>值： value: 规定不透明度。从 0.0 （完全透明）到 1.0（完全不透明）; inherit: 应该从父元素继承 opacity 属性的值。</p>
</li>
</ul>
<h4 id="data-toggle属性"><a href="#data-toggle属性" class="headerlink" title="data-toggle属性"></a>data-toggle属性</h4><ul>
<li>data-toggle一般是a元素(即链接)的属性。data-toggle指以什么事件触发。<a href="https://blog.csdn.net/d295968572/article/details/56667863">详细的介绍</a></li>
</ul>
<h4 id="webkit-tap-highlight-color属性"><a href="#webkit-tap-highlight-color属性" class="headerlink" title="-webkit-tap-highlight-color属性"></a>-webkit-tap-highlight-color属性</h4><ul>
<li><code>-webkit-tap-highlight-color</code> 是一个没有标准化的属性，能够设置点击链接的时候出现的高亮颜色。显示给用户的高光是他们成功点击的标识，以及暗示了他们点击的元素。</li>
</ul>
<h3 id="media-用法"><a href="#media-用法" class="headerlink" title="@media 用法"></a><code>@media</code> 用法</h3><ul>
<li><p><code>@media</code>可以让你根据不同的屏幕大小而使用不同的样式，这可以使得不需要js代码就能实现响应式布局。(即根据不同大小的页面重新排版)</p>
</li>
<li><p>语法：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@media mediatype and|not|only (media feature) &#123;</span><br><span class="line">    CSS-Code;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>其中mediatype就是媒体类型，可以包括好多种，最常用的就是screen，其他多数已经作废。</p>
</li>
<li><p>media feature就定义了这个media设备的一些特征，简单的说就是媒体满足什么条件，常用的就是max-width，min-width。</p>
</li>
<li><p><a href="http://blog.51cto.com/cnn237111/1602797">具体的例子</a></p>
</li>
</ul>
<h3 id="margin属性"><a href="#margin属性" class="headerlink" title="margin属性"></a>margin属性</h3><ul>
<li><p>定义和用法：margin 简写属性在一个声明中设置所有外边距属性。该属性可以有 1 到 4 个值。</p>
</li>
<li><p>块级元素的垂直相邻外边距会合并，而行内元素实际上不占上下外边距。行内元素的的左右外边距不会合并。同样地，浮动元素的外边距也不会合并。允许指定负的外边距值，不过使用时要小心。</p>
</li>
</ul>
<blockquote>
<p>注释：允许使用负值。</p>
</blockquote>
<ul>
<li><p>例子 1：<code>margin:10px 5px 15px 20px;</code><br>上外边距是 10px;<br>右外边距是 5px;<br>下外边距是 15px;<br>左外边距是 20px;</p>
</li>
<li><p>例子 2: <code>margin:10px 5px 15px;</code><br>上外边距是 10px;<br>右外边距和左外边距是 5px;<br>下外边距是 15px;</p>
</li>
<li><p>例子 3:<code>margin:10px 5px;</code><br>上外边距和下外边距是 10px;<br>右外边距和左外边距是 5px;</p>
</li>
<li><p>例子 4:<code>margin:10px;</code><br>所有 4 个外边距都是 10px</p>
</li>
</ul>
<h3 id="media用法"><a href="#media用法" class="headerlink" title="@media用法"></a><code>@media</code>用法</h3><ul>
<li><a href="http://www.runoob.com/cssref/css3-pr-mediaquery.html">用法详解</a></li>
</ul>
<br>
> 最后更新与2018.7.9
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>git 与 github 随笔</title>
    <url>/2018/04/11/Git-and-Github/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的随笔~</p>
</blockquote>
<h2 id="本地初始化仓库并添加远程仓库"><a href="#本地初始化仓库并添加远程仓库" class="headerlink" title="本地初始化仓库并添加远程仓库"></a>本地初始化仓库并添加远程仓库</h2><hr>
<ul>
<li>示例：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git init //初始化本地仓库</span><br><span class="line">git add .  //将本地所有文件加到仓库里</span><br><span class="line">git commit -m &quot;...&quot; //设置提交信息</span><br><span class="line">git remote add origin git@github.com: ...  //添加远程仓库</span><br><span class="line">git push -u origin master</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="关于-多用fetch，merge少用pull"><a href="#关于-多用fetch，merge少用pull" class="headerlink" title="关于 多用fetch，merge少用pull"></a>关于 多用fetch，merge少用pull</h2><hr>
<ul>
<li>有一篇<a href="https://www.oschina.net/translate/git-fetch-and-merge">文章</a>提到了pull的一些弊端，主要是数据会被直接覆盖掉，这样回溯会比较麻烦。</li>
</ul>
<h2 id="关于windows和Ubuntu下的可视化git管理工具"><a href="#关于windows和Ubuntu下的可视化git管理工具" class="headerlink" title="关于windows和Ubuntu下的可视化git管理工具"></a>关于windows和Ubuntu下的可视化git管理工具</h2><hr>
<h3 id="windows"><a href="#windows" class="headerlink" title="windows"></a>windows</h3><ul>
<li><p>windows下有一个非常好用的git管理工具，与github有很好的连接——<a href="https://desktop.github.com/">github desktop</a>。界面美观，方便好用。</p>
</li>
<li><p>同时要搭配Windows下的<a href="https://git-scm.com/download/win">git</a>来使用。</p>
</li>
<li><p>登录你的github账号之后，便可以非常轻松地clone，add，commit，push等操作。</p>
</li>
<li><p>左上角点开选择clone到本地的repo，右键选择open in explorer 即可进入存放本地文件的位置。对文件进行过修改之后，按照下面的同步步骤即可。</p>
</li>
<li><p>推荐使用命令行来实现同步。左上角点开后选择要修改的repo，右键选择 open in command 即可进入命令行。</p>
</li>
<li><p>一些简单的命令操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git add .  //把本地的所有修改add</span><br><span class="line">git commit -m &quot;...&quot;  //commit本地的修改，“...”中写本次修改的备注（这个备注必须有）</span><br><span class="line">git push   //将本地的修改同步到github上</span><br></pre></td></tr></table></figure>
<p>这样修改同步就完成了，一共只需要三步即可。</p>
</li>
</ul>
<h3 id="Ubuntu"><a href="#Ubuntu" class="headerlink" title="Ubuntu"></a>Ubuntu</h3><ul>
<li><p>Ubuntu下找到一个甚至比Windows下的 github desktop更加好用的软件——<a href="https://www.gitkraken.com/">GitKraken</a></p>
</li>
<li><p>在上面不仅可以进行查看同步等操作，甚至可以看到之前在这个repo当中所有的人做出的任何修改，并且可以查看修改了哪些内容！<em>但是唯一美中不足的是有些更加NB的功能要开vip…</em></p>
</li>
<li><p>比Windows更加便捷的是，这里想要进入对一个repo进行同步的命令行（终端）时，只需要在界面按下<code>Alt+T</code>的组合键就可以打开相应的终端了。</p>
</li>
</ul>
<h3 id="atom"><a href="#atom" class="headerlink" title="atom"></a>atom</h3><hr>
<ul>
<li><p>讲道理第一次使用atom时我还是很震惊的，居然有这么好用的软件。atom是一款编辑器，Linux和Windows平台都有，但是这个小小的编辑器整合了许多功能于一身，诸如代码编辑，github仓库文件的编辑以及直接add-commit-push一条线操作等等，还有许许多多的插件等待你去发现。</p>
</li>
<li><p>Atom自带支持markdown文件的编辑和预览，组合键 <code>ctrl + shift + m</code> 弹出预览。</p>
</li>
<li><p>atom的<a href="https://atom.io/">下载地址</a></p>
</li>
</ul>
<br>
> 最后更新于2018.4.19
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑Git</tag>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>关于国产安卓机如何安装Google Play相关套件</title>
    <url>/2018/05/03/Google-play-Android/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><hr>
<ul>
<li><p>自从有了长城防火墙以来，国人的“翻墙”运动一直未曾间断</p>
</li>
<li><p>本教程无需root权限即可完成</p>
</li>
<li><p>但是！本教程需要你已经有科学上网的工具才行~ 而且是手机端的~ (因为无论是登录还是下载软件都要翻墙)</p>
</li>
</ul>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><hr>
<ul>
<li><p>作为一个经常用到各式各样软件的用户，显然国产的软件市场只是能满足一般的国人用户，像我们这样的高端玩家看来那点国内的软件显然是不够的~</p>
</li>
<li><p>就比如要下一个手机端的steam令牌，我用的是小米手机，之前还能在商店里找到下载，但是换了新手机之后莫名发现下架了，而且在商店里搜索所有有关谷歌的关键字都被自动屏蔽了…… 作为专业人士这怎么能忍？因此需要从google play store下载更全面的软件。</p>
</li>
<li><p>谷歌服务框架： Google服务框架全称Google Mobile Service，其中包含了应用包，就是一些google应用。Android虽然是开源的，但是google的应用却不包含在其中。厂家如果要预装google应用，都要与google达成付费协议以及其他一些原因，因此许多国内厂商不会自带google套件。</p>
</li>
<li><p>要体验完整的google服务，需要几个软件的支持：<strong>google服务框架(Google Services Framework)，google账户管理程序(google login Service)，google play服务(google play services) 以及 google play 商店(google play store)</strong></p>
</li>
<li><p>至于有个一键安装的软件叫 go谷歌安装器，自己百度即可找到。如果可以一键安装成功，那么恭喜你，你可以直接开始愉快的体验了。</p>
</li>
<li><p>如果你也像我一样，安装时提示网络连接错误，挂了VPN也一样的话，那就只能自己去网上一个一个找上面提到的几款软件 <strong>尤其要注意软件的英文名称，下载时以英文名称为准，防止下载到一些流氓软件或者是木马文件，下载最好去比较正规的网站</strong>。</p>
<blockquote>
<p>注意只有google play store 是安装后显示在手机桌面上的，其他的都是系统级的服务软件，装好之后不会显示。</p>
</blockquote>
</li>
<li><p>全部安装完成之后，你可以打开 go谷歌安装器 主界面检测这几个软件是否正常安装，如果全是绿勾，则说明安装完成，这时候重启一下手机。</p>
</li>
<li><p>最后，挂上VPN，打开google play store，登录或者注册google账号即可开始使用~</p>
</li>
</ul>
<blockquote>
<p>备注：如果找不到科学上网工具的朋友可以email我，如果我看到了可以给一些推荐。</p>
</blockquote>
<br>
> 最后更新于2018.5.3
]]></content>
      <categories>
        <category>ScientificSurfing</category>
      </categories>
      <tags>
        <tag>๑ScientificSurfing</tag>
      </tags>
  </entry>
  <entry>
    <title>HDFS相关笔记</title>
    <url>/2018/05/30/HDFS/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><ul>
<li><p>马上就六一了，赶在月底结束之前再记录一些东西吧～</p>
</li>
<li><p>以下HDFS的操作是基于Ubuntu18的</p>
</li>
</ul>
<h2 id="HDFS简介"><a href="#HDFS简介" class="headerlink" title="HDFS简介"></a>HDFS简介</h2><hr>
<ul>
<li><a href="https://hadoop.apache.org/docs/r1.0.4/cn/hdfs_design.html">Hadoop分布式文件系统(HDFS)官方中文文档</a></li>
</ul>
<h3 id="Namenode-和-Datanode"><a href="#Namenode-和-Datanode" class="headerlink" title="Namenode 和 Datanode"></a>Namenode 和 Datanode</h3><ul>
<li><p>HDFS采用master&#x2F;slave架构。一个HDFS集群是由一个Namenode和一定数目的Datanodes组成。</p>
</li>
<li><p>HDFS采用Java语言开发，因此任何支持Java的机器都可以部署Namenode或Datanode。</p>
</li>
<li><p>Namenode是一个中心服务器，负责管理文件系统的名字空间(namespace)以及客户端对文件的访问。集群中的Datanode一般是一个节点一个，负责管理它所在节点上的存储。HDFS暴露了文件系统的名字空间，用户能够以文件的形式在上面存储数据。</p>
</li>
<li><p>从内部看，一个文件其实被分成一个或多个数据块，这些块存储在一组Datanode上。Namenode执行文件系统的名字空间操作，比如打开、关闭、重命名文件或目录。它也负责确定数据块到具体Datanode节点的映射。Datanode负责处理文件系统客户端的读写请求。在Namenode的统一调度下进行数据块的创建、删除和复制。</p>
</li>
<li><p>Namenode负责维护文件系统的名字空间，任何对文件系统名字空间或属性的修改都将被Namenode记录下来。应用程序可以设置HDFS保存的文件的副本数目。文件副本的数目称为文件的副本系数，这个信息也是由Namenode保存的。</p>
</li>
<li><p>Namenode全权管理数据块的复制，它周期性地从集群中的每个Datanode接收心跳信号和块状态报告(Blockreport)。接收到心跳信号意味着该Datanode节点工作正常。块状态报告包含了一个该Datanode上所有数据块的列表。</p>
</li>
<li><p>HDFS中的文件都是一次性写入的，并且 <strong>严格要求在任何时候只能有一个写入者</strong>。</p>
</li>
</ul>
<h2 id="HDFS文件系统"><a href="#HDFS文件系统" class="headerlink" title="HDFS文件系统"></a>HDFS文件系统</h2><hr>
<h3 id="DFSShell"><a href="#DFSShell" class="headerlink" title="DFSShell"></a>DFSShell</h3><ul>
<li><p>HDFS以文件和目录的形式组织用户数据。它提供了一个命令行的接口(DFSShell)让用户与HDFS中的数据进行交互。命令的语法和用户熟悉的其他shell(例如 bash, csh)工具类似。下面是一些动作&#x2F;命令的示例：</p>
</li>
<li><p>创建一个名为 <code>/foodir</code> 的目录:	<code>bin/hadoop dfs -mkdir /foodir</code></p>
</li>
<li><p>创建一个名为 <code>/foodir</code> 的目录:	<code>bin/hadoop dfs -mkdir /foodir</code></p>
</li>
<li><p>查看名为 <code>/foodir/myfile.txt</code> 的文件内容:	<code>bin/hadoop dfs -cat /foodir/myfile.txt</code></p>
</li>
</ul>
<h4 id="常用"><a href="#常用" class="headerlink" title="常用"></a>常用</h4><ul>
<li><p><strong>注意，分布式文件系统中没有切换到某个目录下的概念，因为其文件全是分散在各个节点上的</strong></p>
</li>
<li><p>分布式文件系统的文件路径完整格式为 <code>hdfs://namenode|master:编号/root/...</code></p>
</li>
<li><p>例如：<code>hdfs://master:8020/dataset/evaluation/bots_10m_10.csv</code></p>
</li>
<li><p><code>hdfs dfs -ls  /...</code>显示分布式文件系统中某个目录（实际并不存在，只是逻辑上的）下的文件</p>
</li>
<li><p><code>hdfs dfs -rm -r file_name|directory_name</code> 删除某个文件或者目录</p>
</li>
</ul>
<h3 id="通讯协议"><a href="#通讯协议" class="headerlink" title="通讯协议"></a>通讯协议</h3><ul>
<li><p>所有的HDFS通讯协议都是建立在TCP&#x2F;IP协议之上。客户端通过一个可配置的TCP端口连接到Namenode，通过ClientProtocol协议与Namenode交互。而Datanode使用DatanodeProtocol协议与Namenode交互。</p>
</li>
<li><p>一个远程过程调用(RPC)模型被抽象出来封装ClientProtocol和Datanodeprotocol协议。在设计上，Namenode不会主动发起RPC，而是响应来自客户端或 Datanode 的RPC请求。</p>
</li>
</ul>
<h3 id="数据块"><a href="#数据块" class="headerlink" title="数据块"></a>数据块</h3><ul>
<li>HDFS被设计成支持大文件，适用HDFS的是那些需要处理大规模的数据集的应用。这些应用都是只写入数据一次，但却读取一次或多次，并且读取速度应能满足流式读取的需要。HDFS支持文件的“一次写入多次读取”语义。一个典型的数据块大小是64MB。因而，HDFS中的文件总是按照64M被切分成不同的块，每个块尽可能地存储于不同的Datanode中。</li>
</ul>
<br>
> 最后更新于2018.5.31
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Hadoop</tag>
        <tag>๑BigData</tag>
        <tag>๑HDFS</tag>
        <tag>๑Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>前端学习——HTML</title>
    <url>/2018/06/17/FrontEnd-HTML/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="HTML基础"><a href="#HTML基础" class="headerlink" title="HTML基础"></a>HTML基础</h2><hr>
<ul>
<li>学习web前端开发基础技术需要掌握：HTML、CSS、JavaScript语言。(网页三件套大礼包～)</li>
</ul>
<h3 id="概念解析"><a href="#概念解析" class="headerlink" title="概念解析"></a>概念解析</h3><ul>
<li><p>HTML是网页内容的载体。内容就是网页制作者放在页面上想要让用户浏览的信息，可以包含文字、图片、视频等。</p>
</li>
<li><p>CSS样式是表现。就像网页的外衣。比如，标题字体、颜色变化，或为标题加入背景图片、边框等。所有这些用来改变内容外观的东西称之为表现。</p>
</li>
<li><p>JavaScript是用来实现网页上的特效效果。如：鼠标滑过弹出下拉菜单。或鼠标滑过表格的背景颜色改变。还有焦点新闻（新闻图片）的轮换。可以这么理解，有动画的，有交互的一般都是用JavaScript来实现的。</p>
</li>
</ul>
<h3 id="一个html网页的基本结构"><a href="#一个html网页的基本结构" class="headerlink" title="一个html网页的基本结构"></a>一个html网页的基本结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">...</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;h1&gt;</span><br><span class="line">...</span><br><span class="line">&lt;/h1&gt;</span><br><span class="line">&lt;p&gt;</span><br><span class="line">...</span><br><span class="line">&lt;/p&gt;</span><br><span class="line">&lt;img src=&quot;...&quot;&gt;</span><br><span class="line">...</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<h4 id="标签语法"><a href="#标签语法" class="headerlink" title="标签语法"></a>标签语法</h4><ul>
<li><p>标签由英文尖括号<code>&lt;</code>和<code>&gt;</code>括起来，如<code>&lt;html&gt;</code>就是一个标签。</p>
</li>
<li><p>每组标签都是一一对应的，且后一个标签以 <code>/</code>(正斜杠)开头，标志着这一个部分的结束。</p>
</li>
<li><p>标签与标签之间是可以嵌套的，但先后顺序必须保持一致，如：<div>里嵌套<p>，那么</p>必须放在</div>的前面。<code>&lt;div&gt; &lt;p&gt; ... &lt;/p&gt; &lt;/div&gt;</code></p>
</li>
<li><p>HTML标签不区分大小写，<code>&lt;h1&gt;</code>和<code>&lt;H1&gt;</code>是一样的，但建议小写，因为大部分程序员都以小写为准</p>
</li>
</ul>
<h3 id="代码注释"><a href="#代码注释" class="headerlink" title="代码注释"></a>代码注释</h3><ul>
<li>html代码注释的语法如： <code>&lt;!--注释文字--&gt;</code></li>
</ul>
<h2 id="标签详解"><a href="#标签详解" class="headerlink" title="标签详解"></a>标签详解</h2><ul>
<li><p><code>&lt;html&gt;&lt;/html&gt;</code>称为根标签，所有的网页标签都在<code>&lt;html&gt;&lt;/html&gt;</code>中。</p>
</li>
<li><p><code>&lt;head&gt;</code> 标签用于定义文档的头部，它是所有头部元素的容器。头部元素有<code>&lt;title&gt;、&lt;script&gt;、 &lt;style&gt;、&lt;link&gt;、 &lt;meta&gt;</code>等标签</p>
</li>
<li><p>在<code>&lt;body&gt;</code>和<code>&lt;/body&gt;</code>标签之间的内容是网页的主要内容，如<code>&lt;h1&gt;、&lt;p&gt;、&lt;a&gt;、&lt;img&gt;</code>等网页内容标签，在这里的标签中的内容会在浏览器中显示出来。</p>
</li>
<li><p>其中，<code>&lt;h1&gt;</code>就是标题标签</p>
</li>
<li><p><code>&lt;p&gt;</code>就是段落标签，段落内容就写在<code>&lt;p&gt;</code>与<code>&lt;\p&gt;</code>之间</p>
</li>
<li><p><code>&lt;img src= &quot;..&quot;&gt;</code>表示图片，src中填写图片保存的路径</p>
</li>
</ul>
<h3 id="标签"><a href="#标签" class="headerlink" title="&lt;head&gt;标签"></a><code>&lt;head&gt;</code>标签</h3><ul>
<li><p>文档的头部描述了文档的各种属性和信息，包括文档的标题等。绝大多数文档头部包含的数据都不会真正作为内容显示给读者。</p>
</li>
<li><p>下面这些标签可以用在<code>&lt;head&gt;部分</code>：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;title&gt;...&lt;/title&gt;</span><br><span class="line">    &lt;meta&gt;</span><br><span class="line">    &lt;link&gt;</span><br><span class="line">    &lt;style&gt;...&lt;/style&gt;</span><br><span class="line">    &lt;script&gt;...&lt;/script&gt;</span><br><span class="line">&lt;/head&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>&lt;title&gt;</code>标签：在<code>&lt;title&gt;</code>和<code>&lt;/title&gt;</code>标签之间的文字内容是网页的标题信息，<strong>它会出现在浏览器的标题栏中</strong>。网页的title标签用于告诉用户和搜索引擎这个网页的主要内容是什么，搜索引擎可以通过网页标题，迅速的判断出网页的主题。每个网页的内容都是不同的，每个网页都应该有一个独一无二的title。</li>
</ul>
<h3 id="标签-1"><a href="#标签-1" class="headerlink" title="&lt;p&gt;标签"></a><code>&lt;p&gt;</code>标签</h3><ul>
<li>每一对<code>&lt;p&gt;&lt;/p&gt;</code>标签表示一个段落，所以网页上每一段文字都要放在一对单独的<code>&lt;p&gt;&lt;/p&gt;</code>中。</li>
</ul>
<h3 id="标签-2"><a href="#标签-2" class="headerlink" title="&lt;hx&gt;标签"></a><code>&lt;hx&gt;</code>标签</h3><ul>
<li>上面的<code>&lt;p&gt;</code>标签是用来表示文章的段落的，那么文章的标题可以用<code>&lt;hx&gt;</code>来表示，<strong>其中<code>x</code>为1-6的整数</strong>，数字越小，代表越重要，字体也就越大。(这一点与markdown的用#号个数来表示几级标题是一致的)</li>
</ul>
<h3 id="文本修饰：-和标签"><a href="#文本修饰：-和标签" class="headerlink" title="文本修饰：&lt;strong&gt;,&lt;em&gt;和&lt;span&gt;标签"></a>文本修饰：<code>&lt;strong&gt;</code>,<code>&lt;em&gt;</code>和<code>&lt;span&gt;</code>标签</h3><ul>
<li><p><code>&lt;em&gt;</code>表示强调，<code>&lt;strong&gt;</code>表示更强烈的强调。并且在浏览器中<code>&lt;em&gt;</code>默认用 <em>斜体</em> 表示，<code>&lt;strong&gt;</code> 用 <strong>粗体</strong> 表示。</p>
</li>
<li><p><code>&lt;span&gt;</code>标签是没有语义的，它的作用就是为了设置单独的样式用的。</p>
</li>
<li><p>例子：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE HTML&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;meta http-equiv=&quot;Content-Type&quot; content=&quot;text/html; charset=utf-8&quot;&gt;</span><br><span class="line">&lt;title&gt;了不起的盖茨比&lt;/title&gt;</span><br><span class="line">&lt;style&gt;</span><br><span class="line">span&#123;</span><br><span class="line">    color:blue;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;p&gt;</span><br><span class="line">    1922年的春天，一个想要成名名叫尼克•卡拉威（托比•马奎尔Tobey Maguire 饰）的作家，离开了美国中西部，来到了纽约。那是一个道德感渐失，爵士乐流行，走私为王，股票飞涨的时代。为了追寻他的&lt;span&gt;美国梦&lt;/span&gt;，他搬入纽约附近一海湾居住。</span><br><span class="line">    &lt;/p&gt;</span><br><span class="line">    &lt;p&gt;</span><br><span class="line">    菲茨杰拉德，二十世纪美国文学巨擘之一，兼具作家和编剧双重身份。他以诗人的敏感和戏剧家的想象为&quot;爵士乐时代&quot;吟唱华丽挽歌，其诗人和梦想家的气质亦为那个奢靡年代的不二注解。</span><br><span class="line">    &lt;/p&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<p>其中第一段中的“美国梦”被<code>&lt;span&gt;</code>标签标记，在<code>&lt;style&gt;</code>标签中，我们定义了<code>&lt;span&gt;</code>标签修饰的文本的格式为蓝色。</p>
<h3 id="标签-3"><a href="#标签-3" class="headerlink" title="&lt;q&gt;标签"></a><code>&lt;q&gt;标签</code></h3><ul>
<li><p><code>&lt;q&gt;&lt;/q&gt;</code>标签表示引用，注意要引用的文本不用加双引号，浏览器会对q标签自动添加双引号。</p>
</li>
<li><p><code>&lt;q&gt;&lt;/q&gt;</code>适合一句话的引用。</p>
</li>
</ul>
<h3 id="标签-4"><a href="#标签-4" class="headerlink" title="&lt;blockquote&gt;标签"></a><code>&lt;blockquote&gt;</code>标签</h3><ul>
<li>相对于<code>&lt;q&gt;&lt;/q&gt;</code>，<code>&lt;blockquote&gt;...&lt;/blockquote&gt;</code>适合用来引用一段长文字。</li>
</ul>
<h3 id="换行与空格"><a href="#换行与空格" class="headerlink" title="换行与空格"></a>换行与空格</h3><ul>
<li><p>在 html 中是<strong>忽略回车和空格</strong>的，你输入的再多回车和空格也是显示不出来的</p>
</li>
<li><p>换行标签语法：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">xhtml1.0写法： &lt;br /&gt;</span><br><span class="line">html4.01写法： &lt;br&gt;</span><br></pre></td></tr></table></figure>
<p>现在一般用html4的写法</p>
<ul>
<li><p>与以前我们学过的标签不一样，<code>&lt;br /&gt;</code>标签是一个 <strong>空标签</strong> 。没有HTML内容的标签就是空标签，<strong>空标签只需要写一个开始标签</strong>，这样的标签有<code>&lt;br /&gt;</code>、<code>&lt;hr /&gt;</code>和<code>&lt;img /&gt;</code>。</p>
</li>
<li><p>空格表示方法：<code>&amp;nbsp;</code>。</p>
</li>
</ul>
<h3 id="水平分割线"><a href="#水平分割线" class="headerlink" title="水平分割线"></a>水平分割线</h3><ul>
<li><p>在信息展示时，有时会需要加一些用于分隔的横线，这样会使文章看起来整齐些。效果如下</p>
<hr>
</li>
<li><p>语法：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">html4.01版本 &lt;hr&gt;</span><br><span class="line">xhtml1.0版本 &lt;hr /&gt;</span><br></pre></td></tr></table></figure>
<p>现在一般用html4的写法</p>
<ul>
<li><p><code>&lt;hr /&gt;</code>标签和<code>&lt;br /&gt;</code>标签一样也是一个空标签，所以只有一个开始标签，没有结束标签。</p>
</li>
<li><p><code>&lt;hr /&gt;</code>标签的在浏览器中的默认样式线条比较粗，颜色为灰色，可能有些人觉得这种样式不美观，没有关系，这些外在样式在我们以后学习了css样式表之后，都可以对其修改。</p>
</li>
</ul>
<h3 id="标签：为网页加入地址信息"><a href="#标签：为网页加入地址信息" class="headerlink" title="&lt;address&gt;标签：为网页加入地址信息"></a><code>&lt;address&gt;</code>标签：为网页加入地址信息</h3><ul>
<li><code>&lt;address&gt;&lt;/address&gt;</code>标签默认为斜体。这个也可以通过CSS修改。</li>
</ul>
<h3 id="和标签"><a href="#和标签" class="headerlink" title="&lt;code&gt;和&lt;pre&gt;标签"></a><code>&lt;code&gt;</code>和<code>&lt;pre&gt;</code>标签</h3><ul>
<li><p>在网页上<code>&lt;code&gt;&lt;/code&gt;</code>标签用来标注一行代码。</p>
</li>
<li><p>如果是多行代码，可以使用<code>&lt;pre&gt;</code>标签。语法：<code>&lt;pre&gt;语言代码段&lt;/pre&gt;</code></p>
</li>
<li><p><code>&lt;pre&gt;</code> 标签的主要作用:预格式化的文本。被包围在 pre 元素中的文本通常会保留空格和换行符。</p>
</li>
<li><p>注意：<code>&lt;pre&gt;</code> 标签不只是为显示计算机的源代码时用的，在你需要在网页中预显示格式时都可以使用它，只是<code>&lt;pre&gt;</code>标签的一个常见应用就是用来展示计算机的源代码。</p>
</li>
</ul>
<h3 id="ul-li标签"><a href="#ul-li标签" class="headerlink" title="ul-li标签"></a><code>ul-li</code>标签</h3><ul>
<li>实例：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;ul&gt;</span><br><span class="line">  &lt;li&gt;精彩少年&lt;/li&gt;</span><br><span class="line">  &lt;li&gt;美丽突然出现&lt;/li&gt;</span><br><span class="line">  &lt;li&gt;触动心灵的旋律&lt;/li&gt;</span><br><span class="line">&lt;/ul&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>ul-li</code>在网页中显示的默认样式一般为：每项<code>li</code>前都自带一个圆点：<br><img src="/images/HTML/ul-li.png"></li>
</ul>
<h3 id="ol-li标签"><a href="#ol-li标签" class="headerlink" title="ol-li标签"></a><code>ol-li</code>标签</h3><ul>
<li>与<code>ul-li</code>标签类似，只是前面的圆点变成了从1开始的序号</li>
</ul>
<h3 id="标签-5"><a href="#标签-5" class="headerlink" title="&lt;div&gt;标签"></a><code>&lt;div&gt;</code>标签</h3><ul>
<li><p>在网页制作过程过中，可以把一些独立的逻辑部分划分出来，放在一个<code>&lt;div&gt;</code>标签中，这个<code>&lt;div&gt;</code>标签的作用就相当于一个容器。</p>
</li>
<li><p>逻辑部分是页面上相互关联的一组元素。如网页中的独立的栏目版块，就是一个典型的逻辑部分。</p>
</li>
<li><p>一般一个网页都要用很多个<code>&lt;div&gt;&lt;/div&gt;</code>来划分成不同的逻辑部分。</p>
</li>
</ul>
<h3 id="标签的命名"><a href="#标签的命名" class="headerlink" title="&lt;div&gt;标签的命名"></a><code>&lt;div&gt;</code>标签的命名</h3><ul>
<li><p>为了使逻辑更加清晰，我们可以为这一个独立的逻辑部分设置一个名称，用<code>id</code>属性来为<code>&lt;div&gt;</code>提供唯一的名称，这个就像我们每个人都有一个身份证号，这个身份证号是唯一标识我们的身份的，也是必须唯一的。</p>
</li>
<li><p>语法：<code>&lt;div  id=&quot;版块名称&quot;&gt;...&lt;/div&gt;</code>(只需要在开始的部分添加，结尾不变)</p>
</li>
</ul>
<h3 id="标签-6"><a href="#标签-6" class="headerlink" title="&lt;table&gt;标签"></a><code>&lt;table&gt;</code>标签</h3><ul>
<li><p>创建表格的四个元素：table、tbody、tr、th、td</p>
</li>
<li><p>整个表格以<code>&lt;table&gt;</code>标记开始、<code>&lt;/table&gt;</code>标记结束。</p>
</li>
<li><p>如果不加<code>&lt;thead&gt;&lt;tbody&gt;&lt;tfooter&gt;</code> , table表格加载完后才显示。加上这些表格结构， <code>tbody</code>包含行的内容下载完优先显示，不必等待表格结束后在显示，同时如果表格很长，用<code>tbody</code>分段，可以一部分一部分地显示。（通俗理解table 可以按结构一块块的显示，不在等整个表格加载完后显示。）</p>
</li>
<li><p><code>&lt;tr&gt;...&lt;/tr&gt;</code>：表格的一行，所以有几对tr 表格就有几行。</p>
</li>
<li><p><code>&lt;td&gt;...&lt;/td&gt;</code>：表格的一个单元格，一行中包含几对<code>&lt;td&gt;...&lt;/td&gt;</code>，说明一行中就有几列。</p>
</li>
<li><p><code>&lt;th&gt;…&lt;/th&gt;</code>：表格的头部的一个单元格，表格表头。</p>
</li>
<li><p>表格中列的个数，取决于一行中数据单元格的个数。</p>
</li>
<li><p>table表格在没有添加css样式之前，在浏览器中显示是没有表格线的</p>
</li>
</ul>
<h3 id="标签-7"><a href="#标签-7" class="headerlink" title="&lt;a&gt;标签"></a><code>&lt;a&gt;</code>标签</h3><ul>
<li><p><code>&lt;a&gt;</code>标签实现了超链接。</p>
</li>
<li><p>语法：<code>&lt;a  href=&quot;http://...(网址)&quot;  title=&quot;鼠标滑过显示的文本&quot;&gt;链接显示的文本&lt;/a&gt;</code></p>
</li>
<li><p>设置链接在新建窗口中打开：<code>&lt;a href=&quot;目标网址&quot; target=&quot;_blank&quot;&gt;链接显示的文本&lt;/a&gt;</code></p>
</li>
</ul>
<h3 id="标签-8"><a href="#标签-8" class="headerlink" title="&lt;img&gt;标签"></a><code>&lt;img&gt;</code>标签</h3><ul>
<li><p>语法：<code>&lt;img src=&quot;图片地址&quot; [alt=&quot;下载失败时的替换文本&quot;](一般可以不写) title = &quot;提示文本&quot;&gt;</code></p>
</li>
<li><p>src：标识图像的位置</p>
</li>
<li><p>alt：指定图像的描述性文本，当图像不可见时（下载不成功时），可看到该属性指定的文本</p>
</li>
<li><p>title：提供在图像可见时对图像的描述(鼠标滑过图片时显示的文本)</p>
</li>
</ul>
<h3 id="表单标签"><a href="#表单标签" class="headerlink" title="表单标签"></a>表单标签</h3><ul>
<li><p>表单是可以把浏览者输入的数据传送到服务器端，这样服务器端程序就可以处理表单传过来的数据。</p>
</li>
<li><p>语法：<code>&lt;form   method=&quot;传送方式&quot;   action=&quot;服务器文件&quot;&gt;</code></p>
</li>
<li><form> ：<form>标签是成对出现的，以<form>开始，以</form>结束。
</li>
<li><p>action ：浏览者输入的数据被传送到的地方,比如一个PHP页面(save.php)。</p>
</li>
<li><p>method ： 数据传送的方式（get&#x2F;post）。</p>
</li>
<li><p>例：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;form    method=&quot;post&quot;   action=&quot;save.php&quot;&gt;</span><br><span class="line">        &lt;label for=&quot;username&quot;&gt;用户名:&lt;/label&gt;</span><br><span class="line">        &lt;input type=&quot;text&quot; name=&quot;username&quot; /&gt;</span><br><span class="line">        &lt;label for=&quot;pass&quot;&gt;密码:&lt;/label&gt;</span><br><span class="line">        &lt;input type=&quot;password&quot; name=&quot;pass&quot; /&gt;</span><br><span class="line">&lt;/form&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>所有表单控件（文本框、文本域、按钮、单选框、复选框等）都必须放在 <form></form> 标签之间，否则用户的数据可能提交不上。</p>
</li>
<li><p>method : post&#x2F;get 的区别这一部分内容属于后端程序员考虑的问题。</p>
</li>
</ul>
<h3 id="文本输入框、密码输入框"><a href="#文本输入框、密码输入框" class="headerlink" title="文本输入框、密码输入框"></a>文本输入框、密码输入框</h3><ul>
<li><p>文本输入框可以转化为密码输入框。</p>
</li>
<li><p>语法：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;form&gt;</span><br><span class="line">   &lt;input type=&quot;text/password&quot; name=&quot;名称&quot; value=&quot;文本&quot; /&gt;</span><br><span class="line">&lt;/form&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>type：当type&#x3D;”text”时，输入框为文本输入框; 当type&#x3D;”password”时, 输入框为密码输入框。</p>
</li>
<li><p>name：为文本框命名，以备后台程序ASP 、PHP使用。</p>
</li>
<li><p>value：为文本输入框设置默认值。(一般起到提示作用)</p>
</li>
</ul>
<h3 id="文本域"><a href="#文本域" class="headerlink" title="文本域"></a>文本域</h3><ul>
<li><p>语法：<code>&lt;textarea  rows=&quot;行数&quot; cols=&quot;列数&quot;&gt;文本&lt;/textarea&gt;</code></p>
</li>
<li><p>cols ：多行输入域的列数。</p>
</li>
<li><p>rows ：多行输入域的行数。</p>
</li>
<li><p>注意这两个属性可用css样式的width和height来代替：col用width、row用height来代替。</p>
</li>
<li><p>在<code>&lt;textarea&gt;&lt;/textarea&gt;</code>标签之间可以输入默认值。</p>
</li>
</ul>
<h2 id="Html5新元素一览"><a href="#Html5新元素一览" class="headerlink" title="Html5新元素一览"></a>Html5新元素一览</h2><hr>
<ul>
<li><a href="http://www.runoob.com/html/html5-new-element.html">Html5新元素</a></li>
</ul>
<br>
> 最后更新于2018.6.17
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑Html</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo重新构建博客</title>
    <url>/2018/09/03/Hexo-rebuild/</url>
    <content><![CDATA[<h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><hr>
<ul>
<li><p>在使用hexo搭建博客之前，需要搭建好环境。需要git、node.js和hexo</p>
</li>
<li><p>详细搭建见——<a href="https://neveryu.github.io/2016/09/03/hexo-next-one/">链接</a></p>
</li>
</ul>
<h2 id="hexo"><a href="#hexo" class="headerlink" title="hexo"></a>hexo</h2><hr>
<ul>
<li><p>hexo有大量的主题可供选择，而且支持一键部署</p>
</li>
<li><p>但是hexo每次预览要重新生成页面，而jekyll是即时体现更改</p>
</li>
</ul>
<h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><ul>
<li><code>npm install -g hexo-cli</code></li>
</ul>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><ul>
<li><p>初始化： <code>hexo init  //在一个空文件夹下初始化</code></p>
</li>
<li><p>hexo一些命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ hexo new &quot;postName&quot;  #新建文章</span><br><span class="line">$ hexo new page &quot;pageName&quot; # 新建页面</span><br><span class="line">$ hexo generate # 生成静态页面至public目录 =hexo g</span><br><span class="line">$ hexo server # 开启预览访问端口(默认端口4000，&#x27;ctrl+c&#x27;关闭server) = hexo s</span><br><span class="line">$ hexo deploy # 项目部署 = hexo d</span><br><span class="line">$ hexo help # 查看帮助</span><br><span class="line">$ hexo version # 查看Hexo的版本</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="一些问题记录"><a href="#一些问题记录" class="headerlink" title="一些问题记录"></a>一些问题记录</h2><hr>
<h3 id="无法’hexo-d’-缺少插件"><a href="#无法’hexo-d’-缺少插件" class="headerlink" title="无法’hexo d’:缺少插件"></a>无法’hexo d’:缺少插件</h3><ul>
<li>使用命令：<code>npm install hexo-deployer-git --save</code> (在博客的根目录下执行)</li>
</ul>
<h3 id="侧边栏文章索引错乱"><a href="#侧边栏文章索引错乱" class="headerlink" title="侧边栏文章索引错乱"></a>侧边栏文章索引错乱</h3><ul>
<li><p>尽量不要使用跳跃式的标题，比如使用了一个一号标题，然后使用了一个三号标题，接着又使用了一个一号标题，这样很容易就会出现错乱的现象。</p>
</li>
<li><p>标题应当逐级递增逐级递减，这样规范之后才会有清晰美观的显示效果。</p>
</li>
</ul>
<h4 id="部署到git"><a href="#部署到git" class="headerlink" title="部署到git"></a>部署到git</h4><ul>
<li><p>部署到Github前需要配置<code>_config.yml</code>文件:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">	type: git</span><br><span class="line">	repository: git@github.com:Fleschier/Fleschier.github.io.git</span><br><span class="line">	branch: master</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后输入:<code>hexo d</code></p>
</li>
</ul>
<h2 id="三部曲"><a href="#三部曲" class="headerlink" title="三部曲"></a>三部曲</h2><hr>
<ul>
<li><p>修改然后预览</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s --debug //运行预览同时实时生成错误报告</span><br></pre></td></tr></table></figure>
</li>
<li><p>修改然后部署</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="添加页面"><a href="#添加页面" class="headerlink" title="添加页面"></a>添加页面</h2><hr>
<ul>
<li><p>刚使用的主题是默认只有两个页面的，home和archive。要添加页面，需要在source中添加文件夹并创建index.md文件。</p>
</li>
<li><p>例如，添加tags页面：</p>
</li>
</ul>
<ol>
<li>新建tags文件夹，并创建index.md</li>
<li>在index.md中写入如下内容：</li>
</ol>
<blockquote>
<hr>
<p>title: tags<br>date: 2016-09-05 23:41:32<br>type: “tags”<br>comments: false</p>
</blockquote>
<hr>
<ol start="3">
<li>到_config.yml文件中将对应的路径添加进去(next主题默认是将这几个页面注释掉的，取消注释即可)</li>
<li>重新建站即可看到效果</li>
</ol>
<h2 id="添加搜索引擎支持"><a href="#添加搜索引擎支持" class="headerlink" title="添加搜索引擎支持"></a>添加搜索引擎支持</h2><hr>
<ul>
<li><p>首先登录google的<a href="https://www.google.com/webmasters/tools/home?hl=zh-CN">Search Console</a></p>
</li>
<li><p>因为hexo每次clean都会将验证的html删除，所以我们采用其他的验证方法。将Search Console给的meta标签的信息添加到主题的目录下<code>head.swig</code>文件中，这里使用的是Next主题,其他主题也是类似的，在<code>Hexo/themes/next/layout/_partials/head/head.swig</code>文件中原有meta标签后面添加刚才复制的meta标签。</p>
</li>
<li><p><a href="https://ziyuan.baidu.com/site/siteadd?siteurl=">百度搜索引擎</a></p>
</li>
<li><p>百度验证与google验证方法一致</p>
</li>
<li><p>重新deploy博客之后可以验证通过</p>
</li>
<li><p>一些细节补充见<a href="https://jactor-sue.github.io/how-githubio-blog-can-be-searched-by-google/">博客</a></p>
</li>
</ul>
<h3 id="安装插件"><a href="#安装插件" class="headerlink" title="安装插件"></a>安装插件</h3><ul>
<li>为Hexo安装<code>hexo-generator-sitemap</code>和<code>hexo-generator-baidu-sitemap</code>插件，在Hexo博客目录下运行：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-sitemap --save</span><br><span class="line">npm install hexo-generator-baidu-sitemap --save</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="重新建站"><a href="#重新建站" class="headerlink" title="重新建站"></a>重新建站</h3><ul>
<li><p>配置Hexo的<code>_config.yml</code>文件，添加如下字段:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sitemap:</span><br><span class="line">		path: sitemap.xml</span><br><span class="line">baidusitemap:</span><br><span class="line">		path: baidusitemap.xml</span><br></pre></td></tr></table></figure>
</li>
<li><p>在博客的<code>_config.yml</code>文件中添加博客的url,<strong>否则生成的站点地图在提交的时候会报错</strong>。</p>
</li>
<li><p>例如我的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># URL</span><br><span class="line">## If your site is put in a subdirectory, set url as &#x27;http://yoursite.com/child&#x27; and root as &#x27;/child/&#x27;</span><br><span class="line">url: https://fleschier.github.io/</span><br><span class="line">root: /</span><br><span class="line">permalink: :year/:month/:day/:title/</span><br><span class="line">permalink_defaults:</span><br></pre></td></tr></table></figure>
</li>
<li><p>重新生成博客页面即可看到sitemap.xml文件和baidusitemap.xml文件</p>
</li>
<li><p>最后分别在google和百度搜索引擎界面提交各自的站点地图即可。</p>
</li>
</ul>
<h3 id="添加robots文件"><a href="#添加robots文件" class="headerlink" title="添加robots文件"></a>添加robots文件</h3><ul>
<li><p>可以在source文件夹下添加<code>robots.txt</code>文件来限制爬虫抓取网页的内容范围</p>
</li>
<li><p>robots例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># hexo robots.txt</span><br><span class="line">User-agent: *</span><br><span class="line">Allow: /</span><br><span class="line">Allow: /archives/</span><br><span class="line">Allow: /tags/</span><br><span class="line"></span><br><span class="line">Disallow: /vendors/</span><br><span class="line">Disallow: /js/</span><br><span class="line">Disallow: /css/</span><br><span class="line">Disallow: /fonts/</span><br><span class="line">Disallow: /vendors/</span><br><span class="line">Disallow: /fancybox/</span><br><span class="line"></span><br><span class="line">Sitemap: https://Fleschier.github.io/sitemap.xml</span><br><span class="line">Sitemap: https://Fleschier.github.io/baidusitemap.xml</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="文章中的图片"><a href="#文章中的图片" class="headerlink" title="文章中的图片"></a>文章中的图片</h2><hr>
<ul>
<li>由于换到hexo,图片的文件夹在themes文件夹下的source文件夹中,我们在博客中写图片路径时还是<code>/images/...</code>这样即可.</li>
</ul>
<h2 id="md文章命名"><a href="#md文章命名" class="headerlink" title="md文章命名"></a>md文章命名</h2><hr>
<ul>
<li>hexo文章的命名不需要日期。日期在文章的开头的date属性里写。</li>
</ul>
<blockquote>
<p>11.4日更</p>
</blockquote>
<h2 id="hexo文章统计阅读量"><a href="#hexo文章统计阅读量" class="headerlink" title="hexo文章统计阅读量"></a>hexo文章统计阅读量</h2><hr>
<ul>
<li><p>使用leanCloud来完成访问统计</p>
</li>
<li><p>需要设置安全链接,参考<a href="https://leaferx.online/2018/02/11/lc-security/">博客</a></p>
</li>
</ul>
<h3 id="问题错误码429"><a href="#问题错误码429" class="headerlink" title="问题错误码429"></a>问题错误码429</h3><ul>
<li><p>出现这个问题的报错信息是<code>too many requests</code>,原因是我们使用leanCloud的免费版本线程数是十分受限的,在我们deploy的时候发送了太多的同步请求导致线程栈溢出而报错.</p>
</li>
<li><p>解决方案 —<a href="https://blog.csdn.net/weixin_42591190/article/details/80958675">hexo-leancloud-counter-security 插件 Too many requests 错误</a></p>
</li>
</ul>
<h3 id="问题依旧存在"><a href="#问题依旧存在" class="headerlink" title="问题依旧存在"></a>问题依旧存在</h3><hr>
<ul>
<li><em>使用leanCloud deploy之后博客修改的内容迟迟没有生效不知为何,用git的方法deploy之后就能很快生效,难道只能两个方法各deploy一次?待续…</em></li>
</ul>
<h3 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h3><ul>
<li><p>原来是我还缺少了一个步骤,就是需要在leanCloud的<code>云引擎&gt;设置&gt;deploy</code>中将自己的github仓库地址填入,并且将其deploy key添加到我们github的仓库deploy key 一栏中.如下</p>
</li>
<li><p><img src="/images/hexo_blog/blog_deploy.png"></p>
</li>
<li><p><img src="/images/hexo_blog/res.png"></p>
</li>
<li><p>最后不要忘了在leanCloud上点保存,并且生成生产环境:</p>
</li>
<li><p><img src="/images/hexo_blog/res1.png"></p>
</li>
<li><p>但是好像还是不能同步到github…待更新</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Personal Blog</category>
      </categories>
      <tags>
        <tag>๑Blog</tag>
      </tags>
  </entry>
  <entry>
    <title>Hadoop单机伪分布式环境搭建</title>
    <url>/2018/05/30/Hadoop-usage/</url>
    <content><![CDATA[<h2 id="Hadoop-单机伪分布式环境搭建"><a href="#Hadoop-单机伪分布式环境搭建" class="headerlink" title="Hadoop 单机伪分布式环境搭建"></a>Hadoop 单机伪分布式环境搭建</h2><hr>
<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><ul>
<li><p>这是对之前spark环境搭建的一点补充。</p>
</li>
<li><p>首先要了解一下Hadoop的运行模式：</p>
</li>
<li><p>单机模式（standalone）:单机模式是Hadoop的默认模式。当首次解压Hadoop的源码包时，Hadoop无法了解硬件安装环境，便保守地选择了最小配置。在这种默认模式下所有3个XML文件均为空。当配置文件为空时，Hadoop会完全运行在本地。因为不需要与其他节点交互，单机模式就不使用HDFS，也不加载任何Hadoop的守护进程。该模式主要用于开发调试MapReduce程序的应用逻辑。</p>
</li>
<li><p>伪分布模式（Pseudo-Distributed Mode）: 伪分布模式在“单节点集群”上运行Hadoop，其中所有的守护进程都运行在同一台机器上。该模式在单机模式之上增加了代码调试功能，允许你检查内存使用情况，HDFS输入输出，以及其他的守护进程交互。</p>
</li>
<li><p>全分布模式（Fully Distributed Mode）:Hadoop守护进程运行在一个集群上。</p>
</li>
</ul>
<h3 id="新建用户"><a href="#新建用户" class="headerlink" title="新建用户"></a>新建用户</h3><ul>
<li><p>添加一个名为hadoop到系统用户，专门用来做Hadoop测试:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo addgroup hadoop</span><br><span class="line">sudo adduser --ingroup hadoop hadoop</span><br></pre></td></tr></table></figure>
</li>
<li><p>再赋予hadoop用户 sudo的权限：<code>sudo usermod -aG sudo hadoop </code></p>
</li>
<li><p>这里<code>sudo</code>是我的电脑上有sudo权限的用户组的名称，你可以<code>cat /etc/group</code>看看有哪些用户组以及这些用户组下的用户。</p>
</li>
<li><p>实在不行就吧你原来用户在的组都加一个hadoop用户</p>
</li>
<li><p><a href="https://blog.csdn.net/lymemoryzz/article/details/50627767">创建新用户并增加管理员详解</a></p>
</li>
</ul>
<h3 id="安装ssh"><a href="#安装ssh" class="headerlink" title="安装ssh"></a>安装ssh</h3><ul>
<li><p><strong>注意先切换到hadoop用户</strong></p>
</li>
<li><p>命令：<code>sudo apt-get install openssh-server </code></p>
</li>
<li><p>启动服务：<code>sudo /etc/init.d/ssh start </code></p>
</li>
<li><p>检查服务是否正确启动：<code>ps -e | grep ssh  </code></p>
</li>
</ul>
<p><img src="/images/Hadoop/ssh_log.png"></p>
<h4 id="配置ssh无密码登录"><a href="#配置ssh无密码登录" class="headerlink" title="配置ssh无密码登录"></a>配置ssh无密码登录</h4><ul>
<li><p>这个也要在hadoop用户下完成(因为这会在你这个用户的文件夹下的.ssh文件夹生成key)</p>
</li>
<li><p>我们要设置成免密码登录，生成私钥和公钥：<code>ssh-keygen -t rsa -P &quot;&quot;</code></p>
</li>
</ul>
<p><img src="/images/Hadoop/ssh_key.png"></p>
<ul>
<li><p>第一次操作时会提示输入密码，按Enter直接过，这时会在～&#x2F;home&#x2F;{username}&#x2F;.ssh下生成两个文件：id_rsa和id_rsa.pub，前者为私钥，后者为公钥，现在我们将公钥追加到到authorized_keys中（authorized_keys用于保存所有允许以当前用户身份登录到ssh客户端用户的公钥内容）：</p>
</li>
<li><p><code>cat ~/.ssh/id_rsa.pub &gt;&gt; ~/.ssh/authorized_keys  </code></p>
</li>
<li><p>多台实体机进行通信（发送或者读取数据，namenode和datanode之间）就是借助ssh，在通信过程中如果需要操作人员频繁输入密码是不切实际的，所以需要ssh的无密码登录。</p>
</li>
<li><p>现在登录就不需要密码了：<code>ssh localhost </code></p>
</li>
<li><p><code>exit</code>命令退出登录localhost</p>
</li>
</ul>
<h4 id="安装hadoop"><a href="#安装hadoop" class="headerlink" title="安装hadoop"></a>安装hadoop</h4><ul>
<li><p>我下载的是hadoop-2.7.6版本</p>
</li>
<li><p>存放的路径是<code>/uer/local/Apache/hadoop-2.7.6</code></p>
</li>
<li><p>然后修改path环境变量，路径对应自己的实际存放路径：<br><img src="/images/Hadoop/path.png"></p>
</li>
<li><p>修改hadoop-env.sh: <strong>(这个不能用默认的，否则会报错)</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export JAVA_HOME=$&#123;JAVA_HOME&#125; #将这个改成JDK路径，如下  </span><br><span class="line">export JAVA_HOME=/opt/jdk1.8.0_172</span><br></pre></td></tr></table></figure>
</li>
<li><p>确保下面的操作在hadoop用户下完成：<code>sudo chown -R  hadoop:hadoop /usr/local/Apache/hadoop-2.7.6 (自己hadoop的安装路径)</code></p>
</li>
<li><p>最后输入 <code>hadoop version</code> 确认安装</p>
</li>
<li><p>至此单机模式配置完成(在hadoop用户下的单机模式)</p>
</li>
</ul>
<h3 id="伪分布式环境搭建"><a href="#伪分布式环境搭建" class="headerlink" title="伪分布式环境搭建"></a>伪分布式环境搭建</h3><p>-设定*-site.xml:这里需要设定三个文件。我的三个文件的路径是<code>/usr/local/Apache/hadoop-2.7.6/etc/hadoop/</code></p>
<ul>
<li>core-site.xml:  Hadoop Core的配置项，例如HDFS和MapReduce常用的I&#x2F;O设置等。</li>
<li>hdfs-site.xml:  Hadoop 守护进程的配置项，包括namenode，辅助namenode和datanode等。</li>
<li>mapred-site.xml： MapReduce 守护进程的配置项，包括jobtracker和tasktracker。</li>
</ul>
<h4 id="首先在hadoop根目录下新建几个文件夹："><a href="#首先在hadoop根目录下新建几个文件夹：" class="headerlink" title="首先在hadoop根目录下新建几个文件夹："></a>首先在hadoop根目录下新建几个文件夹：</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mkdir hdfs</span><br><span class="line">mkdir hdfs/name</span><br><span class="line">mkdir hdfs/data</span><br><span class="line">mkdir tmp</span><br></pre></td></tr></table></figure>

<ul>
<li>接下来编辑这三个文件：</li>
</ul>
<p>core-site.xml:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;  </span><br><span class="line">    &lt;property&gt;  </span><br><span class="line">        &lt;name&gt;fs.default.name&lt;/name&gt;  </span><br><span class="line">        &lt;value&gt;hdfs://localhost:9000&lt;/value&gt;  </span><br><span class="line">    &lt;/property&gt;  </span><br><span class="line">    &lt;property&gt;  </span><br><span class="line">        &lt;name&gt;hadoop.tmp.dir&lt;/name&gt;  </span><br><span class="line">        &lt;value&gt;/usr/local/Apache/hadoop-2.7.1/tmp&lt;/value&gt;  </span><br><span class="line">    &lt;/property&gt;  </span><br><span class="line">&lt;/configuration&gt;  </span><br></pre></td></tr></table></figure>

<p>hdfs-site.xml:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;  </span><br><span class="line">    &lt;property&gt;  &lt;!--设置副本数1，不写默认是3份--&gt;</span><br><span class="line">        &lt;name&gt;dfs.replication&lt;/name&gt;  </span><br><span class="line">        &lt;value&gt;1&lt;/value&gt;  </span><br><span class="line">    &lt;/property&gt;  </span><br><span class="line">    &lt;property&gt;  </span><br><span class="line">        &lt;name&gt;dfs.name.dir&lt;/name&gt;  </span><br><span class="line">        &lt;value&gt;/usr/local/Apache/hadoop-2.7.1/hdfs/name&lt;/value&gt;  </span><br><span class="line">    &lt;/property&gt;  </span><br><span class="line">    &lt;property&gt;  </span><br><span class="line">        &lt;name&gt;dfs.data.dir&lt;/name&gt;  </span><br><span class="line">        &lt;value&gt;/usr/local/Apache/hadoop-2.7.1/hdfs/data&lt;/value&gt;  </span><br><span class="line">    &lt;/property&gt;  </span><br><span class="line">&lt;/configuration&gt;  </span><br></pre></td></tr></table></figure>

<p>mapred-site.xml:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;  </span><br><span class="line">    &lt;property&gt;  </span><br><span class="line">        &lt;name&gt;mapred.job.tracker&lt;/name&gt;  </span><br><span class="line">        &lt;value&gt;localhost:9001&lt;/value&gt;  </span><br><span class="line">    &lt;/property&gt;  </span><br><span class="line">&lt;/configuration&gt;  </span><br></pre></td></tr></table></figure>
<h4 id="格式化hdfs"><a href="#格式化hdfs" class="headerlink" title="格式化hdfs"></a>格式化hdfs</h4><ul>
<li>启动Hadoop到相关服务，格式化namenode,secondarynamenode,tasktracker的文件系统目录:<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">source /usr/local/Apache/hadoop-2.7.1/etc/hadoop/hadoop-env.sh</span><br><span class="line">hadoop namenode -format</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="启动hadoop"><a href="#启动hadoop" class="headerlink" title="启动hadoop"></a>启动hadoop</h4><ul>
<li><p><strong>首先要先连接到localhost!!</strong>(否则无法在网页上查看节点)。<br><code>ssh localhost</code></p>
</li>
<li><p>我的<code>start-all.sh</code>脚本在sbin目录下，各个版本可能不太一样，如果这个文件夹没有就去别的文件夹找找。</p>
</li>
<li><p>执行start-all.sh来启动所有服务:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd sbin</span><br><span class="line">./start-all.sh</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用命令<code>jps</code>查看是否启动成功：<br><img src="/images/Hadoop/launch_info.png"><br>如果出现NodeManager DataNode Jps SecondaryNameNode ResourceManager NameNode则表示启动成功(缺一不可)</p>
</li>
<li><p>检查运行状态:<br>所有的设置已完成，Hadoop也启动了，现在可以通过下面的操作来查看服务是否正常，在Hadoop中用于监控集群健康状态的Web界面：</p>
</li>
</ul>
<p><code>http://localhost:50030/     - Hadoop 管理介面</code></p>
<p><code>http://localhost:50060/     - Hadoop Task Tracker 状态</code></p>
<p><code>http://localhost:50070/     - Hadoop DFS 状态</code></p>
<ul>
<li><p>目前我只有50070是正常访问的，其余的还不能访问，原因有待进一步探究<br><img src="/images/Hadoop/datanode_info.png"></p>
</li>
<li><p>停止服务：<br><code>./stop-all.sh</code></p>
</li>
</ul>
<p> <img src="/images/Hadoop/stop_all.png"></p>
<ul>
<li><p>由于之前已经将hadoop的<code>bin</code>和<code>sbin</code>两个目录添加到系统环境路径里去了，这样下次启动直接就可以是</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ssh localhost</span><br><span class="line">start-all.sh</span><br><span class="line">...</span><br><span class="line">stop-all.sh</span><br></pre></td></tr></table></figure>
<p>这样就方便很多了。</p>
</li>
<li><p>参考博客——<a href="https://blog.csdn.net/hitwengqi/article/details/8008203">hadoop伪分布式搭建</a>，<a href="https://blog.csdn.net/Dr_Guo/article/details/50886667">很详细的搭建步骤(Hadoop-2.7.2版本)</a></p>
</li>
</ul>
<br>
> 最后更新于2018.5.31
]]></content>
      <categories>
        <category>Hadoop CLuster</category>
      </categories>
      <tags>
        <tag>๑Hadoop</tag>
        <tag>๑BigData</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 基础 </title>
    <url>/2019/01/13/JS-learning-02/</url>
    <content><![CDATA[<h2 id="浏览器窗口中的DOM"><a href="#浏览器窗口中的DOM" class="headerlink" title="浏览器窗口中的DOM"></a>浏览器窗口中的DOM</h2><hr>
<ul>
<li>所有现代浏览器的基本对象模型:<br><img src="/images/JavaScript/model.png"></li>
</ul>
<ol>
<li><p>window对象: 在层次结构顶部的是window对象.这个对象代表显示HTML文档的浏览器窗口的内容区域.在多框架环境下,每个框架也是一个窗口.所有的文档动作都是在窗口内发生的,所以窗口是对象层次结构中最外部的元素,它包含文档.</p>
</li>
<li><p>navigator对象: 脚本从这个对象开始访问浏览器程序,主要是读取浏览器的品牌和版本.此对象为只读,以禁止流氓脚本对浏览器的不当操作.但如后面所述,不能依赖navigator对象获得当前浏览器的实际品牌,型号和版本.</p>
</li>
<li><p>screen对象: 这是另一个只读对象,它给脚本提供了运行浏览器的物理环境信息.例如,此对象显示了监视器的高度和宽度(像素值).</p>
</li>
<li><p>history对象: 尽管浏览器保留了最近浏览历史的内部细节(如单击后退显示的内容列表),但是脚本却无权访问这些细节.这一对象可以帮助脚本模拟后退或前进按钮的单击.</p>
</li>
<li><p>document对象: 每个载入窗口的HTML文档都会变成一个document对象.document对象包含脚本中的内容.除了每个HTML文档中都有的html,head和body元素对象外,文档中元素对象层次的具体标记和结构取决于文档的内容.</p>
</li>
</ol>
<h2 id="文档的载入"><a href="#文档的载入" class="headerlink" title="文档的载入"></a>文档的载入</h2><hr>
<h3 id="简单文档"><a href="#简单文档" class="headerlink" title="简单文档"></a>简单文档</h3><ul>
<li>空文档的对象映射:<br><img src="/images/JavaScript/document.png"><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line"> &lt;head&gt;</span><br><span class="line">  &lt;meta ...&gt;</span><br><span class="line">  &lt;title&gt;&lt;/title&gt;</span><br><span class="line"> &lt;/head&gt;</span><br><span class="line"> &lt;body&gt;</span><br><span class="line">  ...</span><br><span class="line"> &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="添加段落"><a href="#添加段落" class="headerlink" title="添加段落"></a>添加段落</h3><ul>
<li>添加段落标签, 在段落元素的开始和结束标记之间插入段落文本.在标记间插入的连续文本是DOM中的一种特殊的对象,称为文本结点.文本节点总是有一个容器元素,也就是说,文本结点是其父元素p的子元素.</li>
</ul>
<p><img src="/images/JavaScript/document1.png"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line"> &lt;head&gt;</span><br><span class="line">  &lt;meta ...&gt;</span><br><span class="line">  &lt;title&gt;&lt;/title&gt;</span><br><span class="line"> &lt;/head&gt;</span><br><span class="line"> &lt;body&gt;</span><br><span class="line">  &lt;p&gt;This is the one and only Paragraph&lt;/p&gt;         //添加了段落标签和段落内容</span><br><span class="line"> &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<h3 id="生成新元素"><a href="#生成新元素" class="headerlink" title="生成新元素"></a>生成新元素</h3><ul>
<li>将部分段落文本包含在一个<em>标签中,表示要强调这些文本.这个插入操作对p元素对象的层次结构有很大的影响.<strong>p元素从只有一个(文本结点)子元素变成有三个子元素:两个文本节点,和一个它们之间的元素</strong>. 在W3C DOM中, <strong>文本结点不能有任何子元素</strong>,因此不能包含元素对象.em元素中的文本结点不再是p元素的子元素,而是em元素的子元素.</li>
</ul>
<p><img src="/images/JavaScript/document2.png"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line"> &lt;head&gt;</span><br><span class="line">  &lt;meta ...&gt;</span><br><span class="line">  &lt;title&gt;&lt;/title&gt;</span><br><span class="line"> &lt;/head&gt;</span><br><span class="line"> &lt;body&gt;</span><br><span class="line">  &lt;p&gt;This is the one and only Paragraph&lt;/p&gt;         //添加了段落标签和段落内容</span><br><span class="line"> &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<h2 id="对象"><a href="#对象" class="headerlink" title="对象"></a>对象</h2><hr>
<h3 id="获取对象的属性"><a href="#获取对象的属性" class="headerlink" title="获取对象的属性"></a>获取对象的属性</h3><ul>
<li><p>例如,下面的HTML标记定义了一个input元素对象,它指定了四个属性和属性值:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;input type=&quot;button&quot; id=&quot;clicker&quot; name=&quot;clicker&quot; value=&quot;...&quot;&gt;</span><br></pre></td></tr></table></figure>
<p>不必为每个对象设置每个属性,多数属性都有默认值.假如HTML或脚本没有特殊要求,他们就自动设置为默认值.</p>
</li>
<li><p>为了访问对象的属性,需要使用和前面对象相同的圆点定位方式.属性是从属于对象的资源,所以属性的引用就是对象引用后加属性名.</p>
</li>
<li><p>对于刚才的例子,属性的引用如下:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">document.getElementById(&quot;clicker&quot;).name</span><br><span class="line">document.getElementById(&quot;clicker&quot;).value</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<blockquote>
<p>如果JavaScript尝试引用不存在的元素的属性,就会失败或终止.因此引用之前最好先测试一下.</p>
</blockquote>
</li>
<li><p>测试引用是否安全:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val oClicker = document.getElementById(&quot;clikcker&quot;);</span><br><span class="line">  if(!oClicker) return;</span><br><span class="line">var sName = oClicker.name;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在引用的过程中我们省略<code>window</code>部分(<em>完整为window.document.XXX</em>).因为一个窗口只能包含一个文档,所以文档中对象的引用可以省略window部分,但是不能省略document对象.</p>
</li>
</ul>
<h2 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h2><hr>
<ul>
<li><p>方法的调用与大多数语言一样,需要在方法名后加一对圆括号.</p>
</li>
<li><p>参数之间以逗号间隔</p>
</li>
</ul>
<h2 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h2><hr>
<ul>
<li><p>事件是文档执行中的动作,通常是用户活动的结果.</p>
</li>
<li><p>文档中的每个DOM对象几乎都接收某种类型的事件.我们只需要编写代码,告诉元素对象,只要接收到某种事件,就执行一个动作.这个动作就是执行其他的一些JavaScript代码.</p>
</li>
<li><p>具有事件处理能力的简单按钮:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//HTML: 1.html</span><br><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">  &lt;head&gt;</span><br><span class="line">    &lt;meta http-equiv=&quot;content-type&quot; content=&quot;text/html;charset=utf-8&quot;</span><br><span class="line">    &lt;title&gt;A Simple Button with an Event Handler&lt;/title&gt;</span><br><span class="line">    &lt;script type=&quot;text/javascript&quot; src=&quot;jab.js&quot;&gt;&lt;/script&gt;</span><br><span class="line">  &lt;/head&gt;</span><br><span class="line">  &lt;body&gt;</span><br><span class="line">    &lt;form action=&quot;&quot;&gt;</span><br><span class="line">      &lt;div&gt;</span><br><span class="line">        &lt;button id=&quot;clicker&quot;&gt;Click here&lt;/button&gt;</span><br><span class="line">      &lt;/div&gt;</span><br><span class="line">    &lt;/form&gt;</span><br><span class="line">  &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//JAVASCRIPT:jsb.js</span><br><span class="line">//tell the browser to run this script when the page has finished loading</span><br><span class="line">window.onload = applyBehavior;</span><br><span class="line"></span><br><span class="line">//apply behavior to the button</span><br><span class="line">function applyBehavior()&#123;</span><br><span class="line">  //ensure a DOM-aware user agent</span><br><span class="line">  if(document.getElementById)&#123;</span><br><span class="line">    //point to the button</span><br><span class="line">    var oButton = document.getElementById(&#x27;clicker&#x27;);</span><br><span class="line"></span><br><span class="line">    //if it exists, apply behavior</span><br><span class="line">    if(oButton)&#123;</span><br><span class="line">      oButton.onclick = behave;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//what to do when the button is clicked</span><br><span class="line">function behave(evt)&#123;</span><br><span class="line">  alert(&#x27;Ouch! &#x27;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>事件的名称一般为事件的类型(例如click)以及前缀on构成,表示一接收到XXX事件就XXX.</p>
</li>
</ul>
<h2 id="把脚本链接到文档上"><a href="#把脚本链接到文档上" class="headerlink" title="把脚本链接到文档上"></a>把脚本链接到文档上</h2><hr>
<ol>
<li><p>放在外部文件中:<code>&lt;script type=&quot;text/javascript&quot; src=&quot;[/..../]example.js&quot;&gt;&lt;/script&gt;</code></p>
</li>
<li><p>内嵌在html中:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script type=&quot;text/javascript&quot;&gt;</span><br><span class="line">  JavaScript code here</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="script标记的位置"><a href="#script标记的位置" class="headerlink" title="script标记的位置"></a>script标记的位置</h3><ul>
<li><p>大多数情况下script标记放在<code>&lt;head&gt;</code>标签中,有时候会放在<code>&lt;body&gt;</code>标签.</p>
</li>
<li><p>放在<code>&lt;head&gt;</code>中的标记一般会影响页面中的非内容设置—所谓的html指令元素,例如<code>&lt;meta&gt;</code>标记和文档题目,这里也适合放置响应页面载入或用户动作的脚本.</p>
</li>
<li><p>如果需要在页面载入时运行脚本,以便生成页面内容,则脚本应放在文档的<code>&lt;body&gt;</code>部分</p>
</li>
</ul>
<h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><ul>
<li>单行注释用<code>//</code>,多行注释用<code>/**/</code></li>
</ul>
<h2 id="脚本语句的执行时间"><a href="#脚本语句的执行时间" class="headerlink" title="脚本语句的执行时间"></a>脚本语句的执行时间</h2><hr>
<ul>
<li>根据脚本要完成的功能,脚本运行的时间有四种选择:</li>
</ul>
<ol>
<li>文档载入时</li>
<li>文档载入后</li>
<li>响应用户动作时</li>
<li>其他脚本语句调用时</li>
</ol>
<ul>
<li><p>决定性因素是脚本语句在文档中的位置.</p>
</li>
<li><p>除了第一个,其他三种统称为延时脚本</p>
</li>
</ul>
<h3 id="页面载入后执行"><a href="#页面载入后执行" class="headerlink" title="页面载入后执行"></a>页面载入后执行</h3><ul>
<li>假设定义了函数<code>function done()&#123;alert(&quot;....&quot;)&#125;</code>,则如果想要在页面加载完成是调用它,可以用<code>window.onload = done;</code>来实现在页面加载完成时调用<code>done</code>函数. <strong>注意此语句右侧只有函数名,没有圆括号.</strong></li>
</ul>
<h2 id="JavaScript值-数据-类型"><a href="#JavaScript值-数据-类型" class="headerlink" title="JavaScript值(数据)类型"></a>JavaScript值(数据)类型</h2><hr>
<table>
<thead>
<tr>
<th>类型</th>
<th>示例</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>String</td>
<td>“Test”</td>
<td>引号内的一系列字符</td>
</tr>
<tr>
<td>Number</td>
<td>4.5</td>
<td>不在引号内的数字</td>
</tr>
<tr>
<td>Boolean</td>
<td>false</td>
<td>逻辑真或假</td>
</tr>
<tr>
<td>Null</td>
<td>null</td>
<td>不包含任何内容,但仍然是一个值</td>
</tr>
<tr>
<td>Object</td>
<td></td>
<td>通过属性和方法定义的软件对象(数组也是对象)</td>
</tr>
<tr>
<td>Function</td>
<td></td>
<td>函数定义</td>
</tr>
</tbody></table>
<ul>
<li>假如用户在表单的文本的输入域中输入数字,浏览器就把该数字存储为字符串类型.假如脚本要对这个数字执行算数操作,就需要将字符串转化为数字再进行操作.</li>
</ul>
<h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><hr>
<ul>
<li><p>创建变量:<code>var valueName;</code>  <code>var</code>关键字用于声明和初始化变量,文档中任何变量都只需要使用一次var关键字.</p>
</li>
<li><p>变量赋值:<code>valueName = 45;</code></p>
</li>
<li><p><strong>JavaScript变量可以存储任何类型的值</strong>.在声明变量的时候,不必定义变量包含的值的类型.</p>
</li>
</ul>
<h3 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h3><ul>
<li><p>两个数值相加:<code>3 + 3</code>,结果为6没有问题.</p>
</li>
<li><p>但如果其中一个数字是字符串,JavaScript就会将另一个值转换为字符串,<strong>将这个加号操作从算数加变成字符串连接</strong>:<code>3 + &quot;3&quot;</code>,结果为<code>&quot;33&quot;</code>. <code>3 + 3 + &quot;3&quot;</code>,结果为<code>&quot;63&quot;</code>.</p>
</li>
</ul>
<h4 id="将字符串转化为数值"><a href="#将字符串转化为数值" class="headerlink" title="将字符串转化为数值"></a>将字符串转化为数值</h4><ul>
<li><p>JavaScript提供了两个内置函数:<code>parseInt()</code>和<code>parseFloat()</code></p>
</li>
<li><pre><code>
</code></pre>
</li>
</ul>
<p>parseInt(“43”)    &#x2F;&#x2F;结果为42<br>parseInt(“43.33”) &#x2F;&#x2F;结果为43,这里做截断处理</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- ```</span><br><span class="line">parseFloat(&quot;43&quot;)  //结果为43</span><br><span class="line">parseFloat(&quot;43.33&quot;)  //结果为43.33</span><br></pre></td></tr></table></figure>

<h4 id="将数字转化为字符串"><a href="#将数字转化为字符串" class="headerlink" title="将数字转化为字符串"></a>将数字转化为字符串</h4><ul>
<li>利用JavaScript的特性:<code>&quot;&quot;+2500</code>,结果即为<code>&quot;2500&quot;</code></li>
</ul>
<h2 id="控制结构"><a href="#控制结构" class="headerlink" title="控制结构"></a>控制结构</h2><hr>
<ul>
<li><p>if else与C++等用法一致</p>
</li>
<li><p>for循环(i用var声明,其余与C++一致):</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for(var i = startValue; i &lt;= MaxValue; i++)&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><hr>
<ul>
<li>函数的正式语法结构:<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">functoin functionName([Parameter1]...[,ParameterN])&#123;</span><br><span class="line">  statement[s]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="局部变量和全局变量"><a href="#局部变量和全局变量" class="headerlink" title="局部变量和全局变量"></a>局部变量和全局变量</h3><ul>
<li><p>在函数外声明的变量为全局变量,在函数体中定义的变量称为局部变量.</p>
</li>
<li><p>局部变量的作用域只局限于函数内部.</p>
</li>
<li><p>全局变量的作用域是 <strong>当前载入浏览器窗口或框架的文档</strong>.因此初始全局变量后,页面中所有的脚本语句可通过全局变量名直接访问其值.但是,一旦卸载页面,在页面中定义的所有全局变量都会从内存中清除.假如某个值需要在页面卸载后继续使用,则需要使用其他技术来保存它,比如将它存储为框架集文档中的全局变量,或将它存储在cookie中.</p>
</li>
<li><p>如果用<code>var</code>关键字在函数体中初始化了一个与全局变量同名的局部变量,则在函数体中该局部变量比全局变量优先级要高.</p>
</li>
</ul>
<h2 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h2><hr>
<ul>
<li><p>创建:<code>var Arr = new Array(100);</code></p>
</li>
<li><p>访问:<code>Array[0];</code></p>
</li>
</ul>
<h3 id="数组中的document对象"><a href="#数组中的document对象" class="headerlink" title="数组中的document对象"></a>数组中的document对象</h3><ul>
<li><p>例如,如果页面包含两个<code>&lt;form&gt;</code>标记对时,在文档中就会出现两组<code>&lt;form&gt;</code>标记.浏览器为文档使用一个from对象数组,对这些表单的引用如下:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">document.forms[0]</span><br><span class="line">document.forms[1]</span><br></pre></td></tr></table></figure>
</li>
<li><p>也可以使用DOM方法<code>getElementByTagName()</code>访问这个数组:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var aForms = document.getElementByTagName(&#x27;form&#x27;);</span><br><span class="line">aForms[0]</span><br><span class="line">aForms[1]</span><br></pre></td></tr></table></figure>
</li>
<li><p>document对象的下标是由对象的载入顺序决定的,form对象的顺序由文档中<code>&lt;form&gt;</code>标记出现的顺序确定.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Software</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑JS</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 入门</title>
    <url>/2019/01/12/JS-learning-01/</url>
    <content><![CDATA[<h2 id="引例"><a href="#引例" class="headerlink" title="引例"></a>引例</h2><hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//tell the browser to run this script when the page has finished loading</span><br><span class="line">window.onload = insertDateTime;</span><br><span class="line"></span><br><span class="line">//insert the data &amp; time</span><br><span class="line">function insertDateTime()&#123;</span><br><span class="line">  //ensure a DOM-aware user agent</span><br><span class="line">  if(!document.getElementById) return;</span><br><span class="line">  if(!document.createTextNode) return;</span><br><span class="line"></span><br><span class="line">  //create a date-time object</span><br><span class="line">  var oNow = new Data();</span><br><span class="line"></span><br><span class="line">  //get the current date &amp; time as a string</span><br><span class="line">  var sDateTime = oNow.toLocaleString();</span><br><span class="line"></span><br><span class="line">  //point to the target element where we want to insert the date &amp; time</span><br><span class="line">  var oTarget = document.getElementById(&#x27;output&#x27;);</span><br><span class="line">  //make sure the target is found</span><br><span class="line">  if(!oTarget) return;</span><br><span class="line"></span><br><span class="line">  //delete everything inside the target</span><br><span class="line">  while(oTarget.firstChild)&#123;</span><br><span class="line">    oTarget.removeChild(oTarget.firstChild);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  //use the date-time string to create a new text node for the page</span><br><span class="line">  var oNewText = document.createTextNode(sDateTime);</span><br><span class="line"></span><br><span class="line">  //insert the mew text into the span</span><br><span class="line">  oTarget.appendChild(oNewText);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="解析"><a href="#解析" class="headerlink" title="解析"></a>解析</h3><h4 id="触发事件"><a href="#触发事件" class="headerlink" title="触发事件"></a>触发事件</h4><ul>
<li><p>脚本要把日期和事件插入到页面的段落中,但浏览器在读取和运行JavaScript代码之前,并没有读取HTML体.在JavaScript开始运行时,要插入新内容的段落甚至在浏览器的内存中不存在.</p>
</li>
<li><p>解决办法就是告诉JavaScript,在文档完全读入内存之前,不要执行插入操作.为此使用了<code>onload</code>事件.页面的元素(<em>在DOM中称为对象</em>)可以感知很多事件,例如 <em>鼠标单击或按下一个键.</em> 文档加载到浏览器中后,就会触发window对象的<code>onload</code>事件.</p>
</li>
<li><p><code>window.onload = insertDateTime;</code>告诉JavaScript,文档加载完毕后调用<code>insertDateTime()</code>函数.</p>
</li>
</ul>
<h4 id="确保一个安全的环境"><a href="#确保一个安全的环境" class="headerlink" title="确保一个安全的环境"></a>确保一个安全的环境</h4><ul>
<li><pre><code>
</code></pre>
</li>
</ul>
<p>&#x2F;&#x2F;ensure a DOM-aware user agent<br>if(!document.getElementById) return;<br>if(!document.createTextNode) return;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">这里调用的`getElementById()`和`createTextNode()`是两个某些旧的JavaScript解释器不能理解的DOM方法.感叹号`!`表示&quot;not&quot;,`return`告诉JavaScript停止运行当前的函数. 如果不包含这些保护措施,某些老式的浏览器或其JavaScript解释器就会在运行其后代码的时候崩溃.</span><br><span class="line"></span><br><span class="line">#### 生成日期和时间</span><br><span class="line"></span><br><span class="line">- 要插入这个文档中的文本是当前的日期和时间.JavaScript很容易完成这个插入操作:创建一个Date对象,并使用它的一个方法生成字符串:</span><br></pre></td></tr></table></figure>
<p>&#x2F;&#x2F;create a date-time object<br>var oNow &#x3D; new Date()<br>&#x2F;&#x2F;get the current date &amp; time as a string<br>var sDateTime &#x3D; oNow.toLocaleString();</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">这些代码把oNow变量设置为一个新的Date对象.之后使用其toLocaleString()方法输出一个文本字符串,它包含:`Thrusday, August 20, 2009 4:16:05 PM`</span><br><span class="line"></span><br><span class="line">#### 确定目标</span><br><span class="line"></span><br><span class="line">- 下一步就是把日期和事件插入文档中需要的位置.执行了以下操作:</span><br><span class="line">1. 指向目标`span`元素</span><br><span class="line">2. 删除该文本中已有的文本</span><br><span class="line">3. 在元素中插入新的文本</span><br><span class="line"></span><br><span class="line">- 为了指向`output span`,使用ID的方式来定位:</span><br><span class="line">`var oTarget = document.getElementById(&#x27;output&#x27;);`</span><br><span class="line">该语句通过ID(ouput)lai定位目标元素,并把该对象的`引用`存储在oTarget变量中.</span><br><span class="line"></span><br><span class="line">- HTML规范指出,每个元素的ID在当前页面必须是唯一的,即使不小心在页面上使用了相同的ID,`getElementById()`方法也仅返回第一个ID实例.所以,这个方法仅定位一个元素.(本例中就是span元素)</span><br><span class="line"></span><br><span class="line">- 如果`getElementById()`没有找到对应的ID,则将返回空值.为了避免由于疏忽导致的这类错误,采用`if(!oTarget) return;`这个测试语句.</span><br><span class="line"></span><br><span class="line">- 也可以将这个错误变成警告提示:`if(!oTarget) return alert(&quot;Warning: output element not found!&quot;);`</span><br><span class="line"></span><br><span class="line">#### 删除原来的内容</span><br><span class="line"></span><br><span class="line">- 我们要删除span的文本,再插入新内容.在表示页面内容的DOM中,span是其所有子元素的父元素.span现在只有一个子元素:单词&quot;now&quot;.</span><br><span class="line"></span><br><span class="line">- 为了删除span的内容,执行一个简单的循环.只要还有要删除的元素,该循环就一次删除一个子元素:</span><br></pre></td></tr></table></figure>
<p>&#x2F;&#x2F;delete everything inside the target<br>while(oTarget.firstChild){<br>  oTarget.removeChild(oTarget.firstChild);<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在本例中这个循环只会执行一次.</span><br><span class="line"></span><br><span class="line">#### 插入日期和时间</span><br><span class="line"></span><br><span class="line">- 最后在DOM中创建一个包含所有所需文本的新节点,并插入span:</span><br></pre></td></tr></table></figure>
<p>&#x2F;&#x2F;use the date-time string to create a new text node for the page<br>var oNewText &#x3D; document.createTextNode(sDateTime);</p>
<p>&#x2F;&#x2F;insert the new text into the span<br>oTarget.appendChild(oNewText);</p>
<pre><code>sDateTime是从Date对象中生成的字符串.

- 这里调用document对象关联的一个方法,该方法会创建一个新的文本节点,并把日期和事件字符串作为其值.此时该文本结点还不是可见文档内容的一部分,使用目标元素的appendChild()方法就可以插入它.
</code></pre>
]]></content>
      <categories>
        <category>Computer Software</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑JS</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机网络-自底向上</title>
    <url>/2018/12/24/Internet_protocol/</url>
    <content><![CDATA[<blockquote>
<p>一些概念性的名词用 <em>斜体</em> 标出了</p>
</blockquote>
<h2 id="互联网概述"><a href="#互联网概述" class="headerlink" title="互联网概述"></a>互联网概述</h2><ul>
<li><p>计算机网络由若干的结点(node)和连接这些结点的链路(link)组成.结点可以是计算机,集线器,交换机或路由器.</p>
</li>
<li><p>网络之间可以通过路由器互连起来,因此互联网是 <em>网络的网络</em>.</p>
</li>
<li><p>网络把许多计算机连接在一起,而互联网把很多网络通过路由器连接在一起.与网络相连的计算机通常称为 <em>主机</em>.</p>
</li>
<li><p>以小写字母<code>i</code>开始的<code>internet(互连网)</code>是一个通用名词,泛指由多个计算机网络互连而成的计算机网络.</p>
</li>
<li><p>以大写字母<code>I</code>开始到<code>Internet(互联网)</code>是一个专有名词,特指采用TCP&#x2F;IP协议族作为通信规则的互联网.</p>
</li>
<li><p>RFC(Request For Comments)就是 “请求评论”</p>
</li>
<li><p>主机A和主机B进行通信,更为全面的说法是 <strong>主机A的某个进程和主机B的某个进程进行通信</strong></p>
</li>
</ul>
<h3 id="互联网的组成"><a href="#互联网的组成" class="headerlink" title="互联网的组成"></a>互联网的组成</h3><ul>
<li>互联网的拓扑结构非常复杂,从其工作方式上来看分为以下两大块:</li>
</ul>
<ol>
<li>边缘部分: 这部分是<code>用户直接使用的</code>,由所有连接在互联网上的主机构成</li>
<li>核心部分: 这部分是<code>为边缘部分提供服务的</code>,由大量网络和链接这些网络的路由器组成</li>
</ol>
<ul>
<li>在客户机和服务器之间通信的过程中,服务请求方和服务提供方 <strong>都要使用网络核心部分所提供的服务</strong></li>
</ul>
<h3 id="路由器"><a href="#路由器" class="headerlink" title="路由器"></a>路由器</h3><ul>
<li>路由器是一种专用计算机,实现 <em>分组交换</em> 的关键部件.其任务就是转发收到的分组.</li>
</ul>
<h4 id="电路交换"><a href="#电路交换" class="headerlink" title="电路交换"></a>电路交换</h4><ul>
<li>必须经过<code>建立连接(占用通信资源) -&gt; 通话(一直占用通信资源) -&gt; 释放连接(归还通信资源)</code><br>这三个步骤的交换方式就叫 <em>电路交换</em>.</li>
</ul>
<h4 id="分组交换"><a href="#分组交换" class="headerlink" title="分组交换"></a>分组交换</h4><ul>
<li><p>分组交换采用 <em>存储转发</em> 技术.</p>
</li>
<li><p>通常把要发送的一整块数据叫做 <em>报文</em>.</p>
</li>
<li><p>把较长的报文划分成一个个更小等长的数据段,在每一个数据段的前面,加上一些必要的控制信息组成 <em>首部</em> 后,就构成了一个 <em>分组</em>.(分组又称为<code>包</code>)</p>
</li>
<li><p>路由器收到一个分组,先暂时存储一下,检查其首部,查找转发表,按照首部中的目的地址找到合适的端口发送出去,把分组交给下一个路由器.</p>
</li>
</ul>
<h4 id="三种交换方式的比较"><a href="#三种交换方式的比较" class="headerlink" title="三种交换方式的比较"></a>三种交换方式的比较</h4><ol>
<li><p>电路交换: 整个报文的比特流连续地从源点直达终点,好像在一个管道中传送.</p>
</li>
<li><p>报文交换: 整个报文先传送到相邻结点,<strong>全部存储下来后查找转发表</strong>,转发到下一个结点.</p>
</li>
<li><p>分组交换: 单个分组(<strong>整个报文的一部分</strong>)传送到相邻的结点,存储下来后查找转发表,转发到下一个结点.</p>
</li>
</ol>
<h3 id="计算机网络的类别"><a href="#计算机网络的类别" class="headerlink" title="计算机网络的类别"></a>计算机网络的类别</h3><ul>
<li><p>计算机网络的定义: 计算机网络是有一些通用的,可编程的硬件互联而成的,而这些硬件并非专门用来实现某一特定的目的.</p>
</li>
<li><p><code>WAN</code>(Wide Area Network):广域网 <code>MAN</code>(Metropolitan Area Network):城域网 <code>LAN</code>(Local Area Network):局域网  <code>PAN</code>(Personal Area Network):个人局域网</p>
</li>
</ul>
<h3 id="计算机网络性能"><a href="#计算机网络性能" class="headerlink" title="计算机网络性能"></a>计算机网络性能</h3><h4 id="带宽"><a href="#带宽" class="headerlink" title="带宽"></a>带宽</h4><ul>
<li><p>带宽本来是指某个信号具有的<code>频带宽度</code>.信号的带宽是指该信号所包含的各种不同的频率成分所占据的频率 <strong>范围</strong>.例如,传统通信线路上的电话信号的标准带宽是3.1kHz(从300Hz到3.4kHz,这个值代表的是一个 <strong>范围的大小</strong>),这时带宽的单位是<code>Hz</code></p>
</li>
<li><p>带宽的另一个表示是网络中某通道传送数据的能力,即传输数据的速度,此时单位是<code>bit/s</code></p>
</li>
</ul>
<h4 id="时延"><a href="#时延" class="headerlink" title="时延"></a>时延</h4><ul>
<li>总时延 &#x3D; 发送时延 + 传播时延 + 处理时延 + 排队时延</li>
</ul>
<h4 id="利用率"><a href="#利用率" class="headerlink" title="利用率"></a>利用率</h4><ul>
<li><p>信道利用率是指某信道有百分之几的时间是被利用的(有数据通过)</p>
</li>
<li><p>令<code>D0</code>表示网络空闲时的时延(也就是网络所能达到的最低时延),<code>D</code>表示网络当前的时延,<code>U</code>为网络的利用率,则有:<code>D = D0/(1-U)</code></p>
</li>
<li><p>可以看出如果<code>U</code>(利用率)太高则时延会成指数级增长,一般尽量保证网络的利用率不超过50%.</p>
</li>
</ul>
<h3 id="计算机网络体系结构"><a href="#计算机网络体系结构" class="headerlink" title="计算机网络体系结构"></a>计算机网络体系结构</h3><h4 id="OSI的七层协议"><a href="#OSI的七层协议" class="headerlink" title="OSI的七层协议"></a>OSI的七层协议</h4><ul>
<li>物理层 - 数据链路层 - 网络层- 运输层 - 会话层 - 表示层 - 应用层</li>
</ul>
<h4 id="TCP-IP的四层协议"><a href="#TCP-IP的四层协议" class="headerlink" title="TCP&#x2F;IP的四层协议"></a>TCP&#x2F;IP的四层协议</h4><ul>
<li>网络接口层 - 网际层IP - 运输层(TCP或UDP) - 应用层(各种应用层协议如TELNET,FTP,SMTP等)</li>
</ul>
<h4 id="五层协议"><a href="#五层协议" class="headerlink" title="五层协议"></a>五层协议</h4><ul>
<li>物理层 - 数据链路层 - 网络层 - 运输层 - 应用层</li>
</ul>
<h2 id="物理层"><a href="#物理层" class="headerlink" title="物理层"></a>物理层</h2><hr>
<h3 id="波特率和比特率"><a href="#波特率和比特率" class="headerlink" title="波特率和比特率"></a>波特率和比特率</h3><ul>
<li><p>比特率(bit rate)又称传信率、信息传输速率(简称信息速率，information rate)。<code>其定义是：通信线路(或系统)单位时间(每秒)内传输的信息量，即每秒能传输的二进制位数，通常用Rb表示，其单位是比特/秒(bit/s或b/s，英文缩略语为bps)。</code></p>
</li>
<li><p>在二进制系统中，信息速率(比特率)与信号速率(波特率)相等，例如，当系统以每秒50个二进制符号传输时，信息速率为50bit&#x2F;s，信号速率也为50Bd(波特)。在无调制的情况下，比特率等于波特率；采用调相技术时，比特率不等于波特率。通信系统的发送设备和接收设备必须在相同的波特率下工作，否则会出现帧同步错误。</p>
</li>
<li><p>波特率(Baud rate)又称传码率、码元传输速率(简称码元速率)、信号传输速率(简称信号速率，signaling rate)或调制速率。<code>其定义是：通信线路(或系统)单位时间(每秒)内传输的码元(脉冲)个数；或者表示信号调制过程中，单位时间内调制信号波形的变换次数，通常用RB表示，单位是波特(Bd或Baud，前者规范)。</code>如果每秒传输1个码元就称为1Bd；如果1码元的时间长短为200ms，则每秒可传输5个码元，那么码元速率(波特率)就是5Bd。</p>
</li>
<li><p>波特率(码元速率)并没有限定是何种进制的码元，所以给出波特率时必须说明这个码元的进制。对于M进制码元，比特率(信息速率)Rb与波特率(码元速率)RB的关系式为:<code>Rb=RB·lbM</code>.式中：lbM&#x3D;log2M，表示M的以2为底的对数。显然，对于二进制码元，由于lb2&#x3D;1，所以Rb&#x3D;RB，即波特率与比特率在数值上相等，但单位不同，也即二者代表的意义不同。</p>
</li>
<li><p>例如，波特率为600Bd，则在二进制时，比特率也为600bit&#x2F;s；<strong>在四进制时，由于lb4&#x3D;2，所以比特率为1200bit&#x2F;s。可见，在一个码元中可以传送多个比特。</strong></p>
</li>
<li><p><strong>可以理解为,比特率在数值上等于二进制编码的波特率</strong>.</p>
</li>
</ul>
<h3 id="信道极限容量-香农公式"><a href="#信道极限容量-香农公式" class="headerlink" title="信道极限容量&amp;香农公式"></a>信道极限容量&amp;香农公式</h3><ul>
<li><p>码元的传输速率越高,或者信号传输距离越远,或噪声干扰越大,或传输媒体质量越差,在接收端的波形失真越严重.</p>
</li>
<li><p>限制码元在信道上传输速率的因素有两个:</p>
</li>
</ul>
<ol>
<li>信道能通过的频率范围:在任何信道中,码元传输的速率是有上限的,传输速率超过此上限,就会产生严重的码间串扰(码元之间的界限变得不再明确,前后都拖了”尾巴”).</li>
<li>信噪比: (突然的噪声会导致误判,0变成1,1变成0).信噪比就是信号的平均功率和噪声的平均功率之比.</li>
</ol>
<ul>
<li>码元:时间轴上的一个信号的编码单位</li>
</ul>
<h4 id="香农公式"><a href="#香农公式" class="headerlink" title="香农公式"></a>香农公式</h4><ul>
<li>信道的极限传输速率C是: <code>C = W log2(1+S/N) (bit/s)</code></li>
</ul>
<h3 id="数据编码"><a href="#数据编码" class="headerlink" title="数据编码"></a>数据编码</h3><h4 id="常用编码方式"><a href="#常用编码方式" class="headerlink" title="常用编码方式"></a>常用编码方式</h4><ul>
<li>不归零制,归零制,曼彻斯特编码,差分曼彻斯特编码</li>
</ul>
<h4 id="基本的带通调制方法-将数字信号转化为模拟信号"><a href="#基本的带通调制方法-将数字信号转化为模拟信号" class="headerlink" title="基本的带通调制方法(将数字信号转化为模拟信号)"></a>基本的带通调制方法(将数字信号转化为模拟信号)</h4><ul>
<li><p>调幅(AM):载波振幅随基带数字信号而变化.例如0或1对应于无载波输出和有载波输出</p>
</li>
<li><p>调频(FM):载波频率随基带数字信号而变化.0或10或1对应频率f1和f2</p>
</li>
<li><p>调相(PM):载波的初始相位随基带数字信号而变化.例0或1分别对应相位0度或180度</p>
</li>
</ul>
<h3 id="常用的通信介质"><a href="#常用的通信介质" class="headerlink" title="常用的通信介质"></a>常用的通信介质</h3><h4 id="导引型传输媒体"><a href="#导引型传输媒体" class="headerlink" title="导引型传输媒体"></a>导引型传输媒体</h4><ul>
<li><p>双绞线:绞合可减少对相邻导线的电磁干扰.绞合度越高最大传输速率就越高,但是价格也越高.</p>
</li>
<li><p>同轴电缆:主要用在有线电视网的居民小区.</p>
</li>
<li><p>光缆:多模光纤(只适合近距离传输),单模光纤.</p>
</li>
</ul>
<h4 id="非导引型传输媒体"><a href="#非导引型传输媒体" class="headerlink" title="非导引型传输媒体"></a>非导引型传输媒体</h4><ul>
<li><p>短波通信</p>
</li>
<li><p>微波接力</p>
</li>
<li><p>微信通信</p>
</li>
</ul>
<h3 id="多路复用技术"><a href="#多路复用技术" class="headerlink" title="多路复用技术"></a>多路复用技术</h3><ul>
<li><p>频分复用(FDM):同一时间不同用户使用不同的频带宽度(就是信号的频率)进行通信.</p>
</li>
<li><p>时分复用(TDM):不同用户在不同的时间点占用过同样的频带宽度(表现为周期性出现用户的时隙)</p>
</li>
<li><p>波分复用(WDM) &#x3D;&gt; 就是光的频分复用</p>
</li>
<li><p>码分复用(CDM, Code Division Multiplexing):更常用的是码分多址CDMA.码分复用即将0和1对应到一串特殊的二进制,每个用户不重复(更准确地说,要求是正交的).比如将1表示为001100这一串二进制,则对应的0就是将其每一位都取反,即110011.</p>
</li>
</ul>
<h2 id="数据链路层"><a href="#数据链路层" class="headerlink" title="数据链路层"></a>数据链路层</h2><hr>
<ul>
<li>数据链路层的基本传输单元叫做 <em>帧</em></li>
</ul>
<h3 id="帧同步问题"><a href="#帧同步问题" class="headerlink" title="帧同步问题"></a>帧同步问题</h3><blockquote>
<p>帧的开始和结束的确认</p>
</blockquote>
<ul>
<li>数据链路层的三个功能: 透明传输, 流量控制, 差错检测(只检错不纠错)</li>
</ul>
<h4 id="封装成帧"><a href="#封装成帧" class="headerlink" title="封装成帧"></a>封装成帧</h4><ul>
<li><p>所有在互联网上传送的数据都以分组(IP数据报)为传送单位,网络层的IP数据报送到数据链路层就成为帧的<code>数据部分</code>.对帧的数据部分加上首部和尾部就构成了一个完整的帧.</p>
</li>
<li><p>首部和尾部的一个重要作用就是帧定界(确认帧的开始和结束)</p>
</li>
<li><p>最大传输单元<code>MTU(Maximum Transfer Unit)</code>: MTU是的是帧的<code>数据部分</code>的最大长度.</p>
</li>
</ul>
<h5 id="帧定界"><a href="#帧定界" class="headerlink" title="帧定界"></a>帧定界</h5><ul>
<li><p><code>SOH(Start Of Header)</code>:帧开始符(由一串不与数据部分重合的二进制位组成)</p>
</li>
<li><p><code>EOT(End Of Transmission)</code>:帧结束符</p>
</li>
<li><p>帧定界符可以检测数据是否有差错,例如一个帧是否完整</p>
</li>
</ul>
<h5 id="透明传输"><a href="#透明传输" class="headerlink" title="透明传输"></a>透明传输</h5><ul>
<li><p>即用作帧定界的控制字符的比特编码不能出现在数据部分中</p>
</li>
<li><p>透明: 某一个实际存在的事物看起来却好像不存在一样</p>
</li>
<li><p>在数据链路层透明传输数据表示:<strong>无论什么样的比特组合的数据,都能够按照原样没有差错地通过这个数据链路层</strong></p>
</li>
<li><p>为了解决透明传输的问题,对于数据中出现的控制字符’EOT’和’SOH’的前面插入一个转义字符’ESC’,接收端的数据链路层会在将数据交付网络层之前删除这个转义字符.这种方法称为<code>字节填充</code>或者<code>字符填充</code>.(如出现转义字符则再加转义字符)</p>
</li>
</ul>
<h3 id="差错检测"><a href="#差错检测" class="headerlink" title="差错检测"></a>差错检测</h3><ul>
<li>目前在数据链路层广泛地使用了CRC(Cyclic Redundant Check)的检错技术.</li>
</ul>
<h3 id="点对点协议ppp"><a href="#点对点协议ppp" class="headerlink" title="点对点协议ppp"></a>点对点协议ppp</h3><blockquote>
<p>point to point protocol</p>
</blockquote>
<ul>
<li><p>互联网用户必须连接到某个ISP才能接入互联网,ppp协议就是用户计算机和ISP进行通信时使用的 <strong>数据链路层协议</strong></p>
</li>
<li><p>ppp协议支持多种网络层协议(如IP和IPX)和多种类型链路(例如串行(一次发送一个比特)和并行(一次并行地发送多个比特))</p>
</li>
<li><p>差错检测: ppp协议对接收端收到的帧进行检测,并 <strong>立即丢弃有差错的帧</strong></p>
</li>
<li><p>最大传送单元: ppp协议对每一种类型的点对点链路设置最大传送单元MTU的标准默认值,如果高层协议发送的分组过长并超过MTU的数组,则 <strong>丢弃这样的帧</strong></p>
</li>
</ul>
<h4 id="ppp协议的帧格式"><a href="#ppp协议的帧格式" class="headerlink" title="ppp协议的帧格式"></a>ppp协议的帧格式</h4><ul>
<li><p>首部为四个字段, 尾部为两个字段.</p>
</li>
<li><p>首部的第一个字段和尾部的第二个字段(最后一个字段)都是 <strong>标志字段</strong>,规定为<code>0x7E</code>.标志字段表示一个帧的开始或结束.因此标志字段就是ppp帧的定界符. <strong>连续两个帧之间只需要一个标志字段,连续两个标志字段表示这是一个空帧</strong></p>
</li>
<li><p>首部中的第二个字段(地址字段A)和第三个字段(控制字段C)没有实际的含义</p>
</li>
<li><p>首部的第四个字段是协议字段,占2字节.</p>
</li>
<li><p>数据部分(信息字段)长度是可变的,最长不超过1500字节</p>
</li>
<li><p>尾部的第一个字段(2字节)是使用CRC的检验序列FCS</p>
</li>
<li><p>字节填充: ppp使用异步传输时,转义符定义为<code>0x7D</code>,填充方法:将信息字段中每一个0x7E字符转化为(0x7D, 0x5E),将出现的0x7D转化为(0x7D, 0x5D),如果出现ASCII码的控制符(即数值小于0x20),则在该字符前增加一个0x7D,例如0x03转化为(0x7D, 0x03)</p>
</li>
</ul>
<h3 id="局域网的数据链路层"><a href="#局域网的数据链路层" class="headerlink" title="局域网的数据链路层"></a>局域网的数据链路层</h3><ul>
<li><p>局域网的主要特点:网络为一个单位所拥有,且地理范围和站点数目均有限.</p>
</li>
<li><p>局域网使用的是数据链路层的协议</p>
</li>
<li><p>局域网的优点:</p>
</li>
</ul>
<ol>
<li>具有广播功能,从一个站点可以很方便地访问全网.</li>
<li>便于系统的扩展和逐渐演变,各设备的位置可以灵活调整和改变.</li>
<li>提高了系统的可靠性,可用性和生存性.</li>
</ol>
<ul>
<li><p>局域网的拓扑结构: 星型网,环形网和总线网.</p>
</li>
<li><p>局域网工作的层次跨越了数据链路层和物理层.</p>
</li>
<li><p>数据链路层在局域网中分为两个子层:</p>
</li>
</ul>
<ol>
<li>逻辑链路控制层(LLC) (现在使用的局域网协议–<em>以太2型</em> 取消了LLC层,只留下MAC层)</li>
<li>介质访问控制层(MAC) (MAC层与物理层使用的介质密切相关)</li>
</ol>
<h4 id="共享信道"><a href="#共享信道" class="headerlink" title="共享信道"></a>共享信道</h4><ul>
<li>由于局域网的广播信道是一人发送,所有人都可以接收,所以要制定发送规则.</li>
</ul>
<h5 id="静态信道划分"><a href="#静态信道划分" class="headerlink" title="静态信道划分"></a>静态信道划分</h5><ul>
<li>使用诸如频分复用,时分复用等技术,用户只要分配到了信道就不会和其他用户发生冲突,但代价太高,不适合局域网使用.</li>
</ul>
<h5 id="动态媒体接入控制"><a href="#动态媒体接入控制" class="headerlink" title="动态媒体接入控制"></a>动态媒体接入控制</h5><ul>
<li><p>又称为 <em>多点接入(multiple access)</em></p>
</li>
<li><p>又分为两类:</p>
</li>
</ul>
<ol>
<li>随机接入: 所有用户都可以随机地发送信息,但可能发生碰撞,需要解决碰撞的网络协议.</li>
<li>受控接入: 用户发送信息要服从一定的控制,例如集中控制的多点线路 <em>探寻(polling)</em><blockquote>
<p>受控接入在目前的局域网中使用的较少</p>
</blockquote>
</li>
</ol>
<h4 id="适配器"><a href="#适配器" class="headerlink" title="适配器"></a>适配器</h4><ul>
<li><p>计算机与外界局域网的连接是通过适配器(adapter)进行的.</p>
</li>
<li><p>适配器实现的功能包括了数据链路层和物理层.</p>
</li>
<li><p>适配器在接收和发送各种帧的时候,不使用计算机的CPU.当适配器收到有差错的帧的时候,就直接丢弃而不通知计算机.<strong>当接收到正确的帧的时候,就使用中断来通知计算机,并交付协议栈中的网络层.当计算机要发送IP数据报时,就由协议栈把IP数据报向下交给适配器,组装成帧后发送到局域网</strong></p>
</li>
<li><p><strong>注意: 计算机的硬件地址(MAC地址)就在适配器的ROM中</strong></p>
</li>
</ul>
<h3 id="CSMA"><a href="#CSMA" class="headerlink" title="CSMA"></a>CSMA</h3><ul>
<li>为了通信简便,以太网使用以下两个措施:</li>
</ul>
<ol>
<li><p>采用 <strong>无连接</strong> 的工作方式,即尽最大努力交付(不可靠的交付).接收站对收到的差错帧直接丢弃(采用CRC检错),至于是否需要重传由高层协议决定(例如TCP协议)</p>
</li>
<li><p>以太网发送的数据都是使用曼彻斯特编码的信号</p>
</li>
</ol>
<h4 id="CSMA-CD"><a href="#CSMA-CD" class="headerlink" title="CSMA&#x2F;CD"></a>CSMA&#x2F;CD</h4><ul>
<li><p>CSMA&#x2F;CD: Carrier Sense Multiple Access Protocl with Collision Detection.意思是载波监听多点接入&#x2F;碰撞检测</p>
</li>
<li><p>要点:</p>
</li>
</ul>
<ol>
<li>“多点接入”: 许多计算机是以多点接入的方式连接在一根总线上.</li>
<li>“载波监听”: 就是用电子技术检测总线上有没有其他计算机也在发送.<strong>不管在发送前,发送中,每个站都必须不停地检测信道</strong>.在发送中检测信道,是为了及时发现有没有其它站的发送和本站发送的碰撞,这称为 <em>碰撞检测</em>.</li>
<li>“碰撞检测”: 也就是边发送边监听,也称为 “冲突检测”.一旦发生冲突,总线上传输的信号产生了严重的失真,无法恢复出有用的信息来,因此任何一个正在发送的站一旦检测到冲突其适配器立即停止发送.</li>
</ol>
<ul>
<li><p>检测时信道空闲不代表没有站在通信,因为存在传播时延.</p>
</li>
<li><p><strong>电磁波在1km电缆的传播时延约为5us</strong></p>
</li>
<li><p>若总线上单程端到端的传播时延记为<code>t</code>,则最多经过<code>2t</code>就能确定有没有发生冲突.(发送数据后监听<code>2t</code>时间,没有冲突即发送成功)</p>
</li>
<li><p>使用CSMA&#x2F;CD协议时,一个站不能边发送边接收,因此使用CSMA&#x2F;CD的以太网只能进行 <em>半双工通信</em></p>
</li>
<li><p>以太网规定最短帧长度为64字节(512bit).因为 <strong>对于10Mbit&#x2F;s的以太网,争用期为51.2us,而发送512bit需要恰好51.2us,如果数据短于64字节,则可能无法检测出发生冲突的帧</strong>. 因此规定<code>凡是长度小于64字节的帧都是由于冲突而异常终止的无效帧</code>.</p>
</li>
<li><p>名词概念: <code>10BASE-T</code>这里<code>10</code>表示10Mbit&#x2F;s的数据率, <code>BASE</code>表示连接线上的信号是基带信号, <code>T</code>代表双绞线.</p>
</li>
<li><p>星型网使用 <em>集线器(hub)</em> 作为中心.使用集线器的以太网在逻辑上仍然是一个总线网,各站共享逻辑上的总线,使用的还是CSMA&#x2F;CD协议.</p>
</li>
<li><p>集线器工作在物理层,它的每个接口仅仅简单地转发比特,<strong>不进行碰撞检测</strong>(碰撞检测由各站的适配器完成)</p>
</li>
</ul>
<h3 id="以太网的MAC层"><a href="#以太网的MAC层" class="headerlink" title="以太网的MAC层"></a>以太网的MAC层</h3><h4 id="以太网V2的MAC帧"><a href="#以太网V2的MAC帧" class="headerlink" title="以太网V2的MAC帧"></a>以太网V2的MAC帧</h4><ul>
<li>以太网V2的MAC帧由五个字段组成.</li>
</ul>
<ol>
<li>第一个字段为6个字节的目的地址</li>
<li>第二个字段为6个字节的源地址</li>
<li>第三个字段为2个字节的类型字段(用来标志上一层使用了什么协议,以便把收到的MAC帧交给上一层的这个协议)</li>
<li>第四个字段数据字段,<strong>长度在46-1500字节之间</strong>(由于之前提到帧的最小长度是64字节,所以46字节就是用64减去首部尾部得到的).数据字段就是网络层传下来的IP数据报.</li>
<li>最后一个字段是4字节的帧检验序列FCS(使用CRC检验)</li>
</ol>
<ul>
<li><p>从MAC子层向下传到物理层还要添加8个字节(包括7字节的前同步码(1和0的交替码)和1字节的帧开始定界符(定义为10101011)),它的作用是使接收端的适配器在接收MAC帧时能够迅速调整其时钟频率,使它和发送端同步.</p>
</li>
<li><p><strong>设置MAC帧长度的限制的原因</strong>:最短64字节的原因在之前以太网帧长度处规定了.最长不能超过1518字节是因为如果数据帧太长,则单一站点占用信道的时间太长影响其他站点分享带宽,同时太长的帧在传输时容易出错.</p>
</li>
</ul>
<h3 id="网桥-bridge"><a href="#网桥-bridge" class="headerlink" title="网桥(bridge)"></a>网桥(bridge)</h3><ul>
<li><p>中继器只能识别出位(bit),网桥能够识别数据帧.</p>
</li>
<li><p>网桥的升级版为交换机,两者都是工作在数据链路层.</p>
</li>
</ul>
<h4 id="交换机-switch"><a href="#交换机-switch" class="headerlink" title="交换机(switch)"></a>交换机(switch)</h4><ul>
<li><p>1990年问世的 <em>交换式集线器(switching hub)</em> 很快就淘汰了网桥.交换式集线器称为 <code>以太网交换机</code>或<code>第二层交换机</code></p>
</li>
<li><p>以太网交换机实际上就是一个 <strong>多接口的网桥</strong>, 和工作在物理层的转发器,集线器有很大的区别,以太网交换机的每个接口都直接与一个单台主机或另一个以太网交换机相连,并且一般都工作在 <strong>全双工方式</strong>.其内部的交换表(又称为地址表)是通过 <em>自学习</em> 算法自动建立起来的.</p>
</li>
</ul>
<h4 id="虚拟局域网"><a href="#虚拟局域网" class="headerlink" title="虚拟局域网"></a>虚拟局域网</h4><ul>
<li>使用了特殊的交换机,第三层交换机,即搭载了路由表的交换机,可以提供网络间通讯的功能.</li>
</ul>
<h2 id="网络层"><a href="#网络层" class="headerlink" title="网络层"></a>网络层</h2><hr>
<ul>
<li><p>网络层向上只提供简单灵活的,无连接的,尽最大努力交付的数据报服务.</p>
</li>
<li><p>传输单元为分组(即IP数据报)</p>
</li>
</ul>
<h3 id="虚电路和数据报的比较"><a href="#虚电路和数据报的比较" class="headerlink" title="虚电路和数据报的比较"></a>虚电路和数据报的比较</h3><table>
<thead>
<tr>
<th align="center">对比方面</th>
<th align="center">虚电路服务</th>
<th align="center">数据报服务</th>
</tr>
</thead>
<tbody><tr>
<td align="center">思路</td>
<td align="center">可靠通信应当由网络来保证</td>
<td align="center">可靠通信应当由用户主机来保证</td>
</tr>
<tr>
<td align="center">连接的建立</td>
<td align="center">必须有</td>
<td align="center">不需要</td>
</tr>
<tr>
<td align="center">终点地址</td>
<td align="center">尽在连接建立阶段使用,每个分组使用短的虚电路号</td>
<td align="center">每个分组都有完整地址</td>
</tr>
<tr>
<td align="center">分组的转发</td>
<td align="center">属于同一条虚电路的分组均按照同一路由进行转发</td>
<td align="center">每个分组独立选择路由进行转发</td>
</tr>
<tr>
<td align="center">当结点出现故障时</td>
<td align="center">所有通过出故障结点的虚电路均不能工作</td>
<td align="center">出故障的结点可能会丢失分组,一些路由可能会发生变化</td>
</tr>
<tr>
<td align="center">分组的顺序</td>
<td align="center">总是按发送顺序到达终点</td>
<td align="center">到达终点的时间不一定按发送顺序</td>
</tr>
<tr>
<td align="center">端到端的差错处理和流量控制</td>
<td align="center">可由网络负责,也可以由用户主机负责</td>
<td align="center">由用户主机负责</td>
</tr>
</tbody></table>
<h3 id="网际协议IP"><a href="#网际协议IP" class="headerlink" title="网际协议IP"></a>网际协议IP</h3><h4 id="网络互联"><a href="#网络互联" class="headerlink" title="网络互联"></a>网络互联</h4><ul>
<li>将互联网连接起来需要使用一些中间设备,不同的层次使用的中间设备有不同的名称:</li>
</ul>
<ol>
<li>物理层: 转发器(repeater)</li>
<li>数据链路层: 网桥或者桥接器(bridge)</li>
<li>网络层: 路由器(router)</li>
<li>网络层以上各层: 网关(gateway)</li>
</ol>
<h4 id="IP地址的分类"><a href="#IP地址的分类" class="headerlink" title="IP地址的分类"></a>IP地址的分类</h4><ul>
<li><p>A类,B类,C类地址都是由两个固定长度的字段组成: 第一个字段是网络号(net-id), 一个网络号在整个互联网范围内必须是唯一的. 第二个字段是主机号(host-id), 一台主机号在它前面的网络号所指明的网络范围内必须是唯一的.</p>
</li>
<li><p>IPV4地址长度是32位,A类,B类和C类的网络号分别是1个字节(8位),2个字节(16位)和3个字节(24位)长,在网络号最前面有1-3位类别位,A,B,C类网络分别为0,10和110.</p>
</li>
</ul>
<h5 id="A类地址"><a href="#A类地址" class="headerlink" title="A类地址"></a>A类地址</h5><ul>
<li><p>A类地址网络号占一个字节,只有7位可以使用(第一位固定为0),但可指派的网络号是126个(2^7 - 2),<strong>减2的原因是:</strong> (1)网络号全为0的IP地址是个保留地址,意思是”本网络”.(2) 网络号为127的网络地址保留作为本地软件 <em>环回测试(loopback test)</em> 本机的进程之间的通信之用.</p>
</li>
<li><p>A类地址主机号占3字节,可以指派的主机数为2^24-2, 这里 <strong>减2的原因是:</strong> (1)全0的主机号字段表示该IP地址是”本主机”所连接到的 <em>单个网络地址</em> (例如: 一台主机的IP地址是<code>5.6.7.8</code>,则<code>5.0.0.0</code>表示这个主机所在的 <em>网络地址</em>) (2)全1的主机号表示 <strong>所有的(all)</strong>, 因此全1的主机号表示该网络上的所有主机.</p>
</li>
</ul>
<blockquote>
<p>例如: B类地址128.7.255.255表示”在网络128.7.0.0上的所有主机”,A类地址0.0.0.35表示”在这个网络上主机号为35的主机”</p>
</blockquote>
<ul>
<li><strong>A类地址第一个可指派的网络号为1.0.0.0,最后一个可指派的网络号为126.0.0.0</strong></li>
</ul>
<h5 id="B类地址"><a href="#B类地址" class="headerlink" title="B类地址"></a>B类地址</h5><ul>
<li><p>B类地址网络号有2个字节,最前面两位已经固定(为10),值剩下14位.<strong>由于B类地址128.0.0.0是不指派的</strong>,所以可以指派的 <strong>B类最小网络地址是128.1.0.0</strong>.因此B类可指派的网络数为2^14-1.</p>
</li>
<li><p>B类地址主机号占2字节, 由于全0和全1的主机号不能指派(理由同A类地址),所以可以指派的主机数为2^16-2</p>
</li>
<li><p><strong>B类地址第一个可指派的网络号为128.1.0.0,最后一个可指派的网络号为191.255.0.0</strong></p>
</li>
</ul>
<h5 id="C类地址"><a href="#C类地址" class="headerlink" title="C类地址"></a>C类地址</h5><ul>
<li><p>C类地址有3个字节的网络号,前3个字节固定(为110),C类地址192.0.0.0也是不指派的,指派的 <strong>最小的C类网络地址是192.0.1.0</strong>,因此C类地址可指派的网络总数是2^21-1.</p>
</li>
<li><p>每个C类地址的最大主机数为2^8-2</p>
</li>
<li><p><strong>C类地址第一个可指派的网络号为192.0.1.0, 最后一个可指派的网络号为223.255.255.0</strong></p>
</li>
<li><p>一般不使用的特殊IP地址</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>网路号</th>
<th>主机号</th>
<th>源地址使用</th>
<th>目的地址使用</th>
<th>代表意思</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>0</td>
<td>可以</td>
<td>不可以</td>
<td>在本网络上的本主机</td>
</tr>
<tr>
<td>0</td>
<td>host-id</td>
<td>可以</td>
<td>不可以</td>
<td>在本网络上的某台主机(host-id)</td>
</tr>
<tr>
<td>全1</td>
<td>全1</td>
<td>不可以</td>
<td>可以</td>
<td>在本网络上进行广播</td>
</tr>
<tr>
<td>net-id</td>
<td>全1</td>
<td>不可以</td>
<td>可以</td>
<td>对net-id上的所有主机进行广播</td>
</tr>
<tr>
<td>127</td>
<td>非全0或全1的 任何数</td>
<td>可以</td>
<td>可以</td>
<td>用于本地软件环回测试</td>
</tr>
</tbody></table>
<h5 id="IP地址特点"><a href="#IP地址特点" class="headerlink" title="IP地址特点"></a>IP地址特点</h5><ul>
<li><p>实际上,IP地址标志一台主机(或路由器)和一条链路的 <em>接口</em>.当一台主机同时连接到两个网络上时,该主机就必须同时具有两个相应的IP地址,其网络号必须是不同的.</p>
</li>
<li><p>按照互联网的观点, 一个网络就是具有相同网路号net-id的主机的集合.</p>
</li>
</ul>
<h3 id="地址解析协议ARP"><a href="#地址解析协议ARP" class="headerlink" title="地址解析协议ARP"></a>地址解析协议ARP</h3><ul>
<li><p>ARP协议根据IP地址获取物理地址.</p>
</li>
<li><p>由于网络层使用的是IP地址,但是在实际网络的链路上传送数据帧时,最终还是必须使用该网络的硬件地址.但IP地址和下面的网络的硬件地址之间由于格式的不同而不存在简单的映射关系(IP地址32位而局域网的硬件地址是48位).此外,在一个网络上可能经常会有新的主机加进来或者撤走旧的主机.更换网络适配器也会使主机的硬件地址改变.</p>
</li>
<li><p>地址解析协议ARP解决这个问题的方法是,在主机ARP高速缓存中存放一个从IP地址到硬件地址的映射表,并且这个映射表还经常动态更新.</p>
</li>
</ul>
<h3 id="即IP数据报"><a href="#即IP数据报" class="headerlink" title="即IP数据报"></a>即IP数据报</h3><h4 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h4><ul>
<li><p>一个IP数据报由 <em>首部</em> 和 <em>数据</em> 两部分组成.</p>
</li>
<li><p>首部的前一部分是固定长度,共20字节,是所有IP数据报必须具有的.在首部固定部分后面是一些可选字段,其长度是可变的.</p>
</li>
</ul>
<h5 id="首部固定部分中的各字段"><a href="#首部固定部分中的各字段" class="headerlink" title="首部固定部分中的各字段"></a>首部固定部分中的各字段</h5><blockquote>
<p>一共12个字段</p>
</blockquote>
<ul>
<li><p>版本: 占4位,指IP协议的版本,通信双方使用的IP协议的版本必须一致.</p>
</li>
<li><p>首部长度: 占4位,可表示的最大十进制数值是15.<strong>注意,这个长度的单位是32位字(1个32位字是4字节),即当值为1111(15)时,表示首部长度为60字节</strong>.当IP分组的首部长度不是4字节的整数倍时,必须利用最后的填充字段加以填充.</p>
</li>
<li><p>区分服务: 占8位, 用来获得更好的服务.(一般情况下都不使用这个字段)</p>
</li>
<li><p>总长度: 占16为,因此最大值为2^16-1&#x3D;65535.总长度指首部和数据之和的长度,单位为字节.一般下面的数据链路层都规定了MTU(Maximum Transfer Unit),即最大传送单元.一个IP数据包封装成帧之后长度一定不能超过这个值,否则就要进行分片.</p>
</li>
<li><p>标识(identification): 占16位.IP软件在存储器中维持一个计数器,每产生一个数据报,计数器就加1,并将此值赋给标识字段.但这个不是序号,因为数据报不存在顺序接收.</p>
</li>
<li><p>标志(flag): 占3位,目前只有前两位有意义.</p>
</li>
</ul>
<ol>
<li>标志字段中的最低位记为<code>MF(More Fragment)</code>. MF&#x3D;1表示后面”还有分片”的数据报.MF&#x3D;0表示这已是若干数据报片的最后一个.</li>
<li>标志字段中间一位记为<code>DF(Don&#39;t Fragment)</code>,意思是”不能分片”.只有当DF&#x3D;0时才允许分片.</li>
</ol>
<ul>
<li><p>片偏移: 占13位.片偏移指出:较长的分组在分片后,某片在原分组中的相对位置.也就是说,相对于用户数据字段的起点,该片从何处开始.<strong>片偏移的单位是8字节</strong>,<em>所以说每个分片的长一定是8字节(64位)的整数倍.</em></p>
</li>
<li><p>生存时间: 占8位.英文缩写是TTL(Time To Live),表明这是数据包在网络中的寿命.其目的是防止无法交付的数据包无限在网络中兜圈子.路由器每转发一次就将数据报的TTL的值减1,如果某个数据报的TTL&#x3D;0,则路由器接收之后直接丢弃而不转发.</p>
</li>
<li><p>协议: 占8位.协议字段指出此数据包携带的数据使用的是何种协议, 以便使目的主机的IP层知道将数据部分上交给哪个协议进行处理.</p>
</li>
<li><p>首部校验和: 占16位.<strong>这个字段只检验数据报的首部,但不包括数据部分</strong>.每经过一个路由器,都要重新计算一遍校验和(因为生存时间,标志,片偏移等可能会变化).</p>
</li>
<li><p>源地址:占32位</p>
</li>
<li><p>目的地址: 占32位</p>
</li>
</ul>
<h4 id="IP转发分组的流程"><a href="#IP转发分组的流程" class="headerlink" title="IP转发分组的流程"></a>IP转发分组的流程</h4><ul>
<li><p>在路由表中,对每一条路由最主要的是以下两个信息(还会包括其他的一些信息):(目的网络地址, 下一跳地址)</p>
</li>
<li><p>只有到达最后一个路由器时,才试图向目的主机进行交付.</p>
</li>
</ul>
<h3 id="划分子网和构造超网"><a href="#划分子网和构造超网" class="headerlink" title="划分子网和构造超网"></a>划分子网和构造超网</h3><h4 id="子网"><a href="#子网" class="headerlink" title="子网"></a>子网</h4><ul>
<li><p>在IP地址中增加了一个”子网号字段”,使得两级IP地址变为三级IP地址.这种做法叫做 <em>划分子网(subnetting)</em>.</p>
</li>
<li><p><code>IP地址 ::= &#123;&lt;网络号&gt;,&lt;子网号&gt;,&lt;主机号&gt;&#125;</code></p>
</li>
<li><p>子网号字段占用了主机号字段的长度,因此划分子网会使主机数目减少,<strong>但是不改变原来的网络号</strong>,因此对外这个网络还是原来的网络.</p>
</li>
</ul>
<h5 id="子网掩码"><a href="#子网掩码" class="headerlink" title="子网掩码"></a>子网掩码</h5><ul>
<li><p>从IP数据报的首部无法看出源主机或者目的主机所连接的网络是否进行了子网划分,就必须使用 <em>子网掩码</em></p>
</li>
<li><p>例如将IP地址145.13.3.10与子网掩码255.255.255.0做与操作,得到的网络地址是145.13.3.0.</p>
</li>
<li><p>使用子网划分后,路由表必须包含一下三项内容:目的网络地址,子网掩码和下一跳地址.</p>
</li>
</ul>
<h4 id="超网"><a href="#超网" class="headerlink" title="超网"></a>超网</h4><h5 id="无分类编址CIDR"><a href="#无分类编址CIDR" class="headerlink" title="无分类编址CIDR"></a>无分类编址CIDR</h5><ul>
<li><p>使用 <em>变长子网掩码VLSM(Variable Length Subnet Mask)</em> 可以进一步提高IP地址资源的利用率.在VLSM的基础上又进一步研究出无分类编址方法,正式名称是 <em>无分类域间路由选择CIDR(Classless Inter-Domain Routing)</em></p>
</li>
<li><p>CIDR的主要特点有两个:</p>
</li>
</ul>
<ol>
<li>CIDR消除了传统的A类, B类和C类的地址以及划分子网的概念.CIDR把32位的IP地址分成前后两个部分,前面部分是<code>网络前缀(network-prefix)</code>,用来指明网络,后面部分用来指明主机.因而CIDR是无分类的两级编址,记法为:<br><code>IP地址:=&#123;&lt;网络前缀&gt;,&lt;主机号&gt;&#125;</code>.CIDR还使用”斜线记法”,即在IP地址后面加上斜线<code>/</code>,然后写上网络前缀所占的位数.</li>
<li>CIDR把网络前缀都相同的连续的IP地址组成一个”CIDR地址块”,我们只要知道CIDR地址块中的任何一个地址,就可以知道这个地址块的起始地址和最大地址,以及地址块中的地址数.例如:<br>128.14.35.78&#x2F;20 &#x3D; <strong>10000000 00001110 0010</strong> 0011 00000111 (20位网络前缀已加粗)<br>所以最小地址可以很方便地写出: 128.14.32.0(<strong>10000000 00001110 0010</strong>0000 00000000),最大地址也可以很方便写出:128.14.47.255(<strong>10000000 00001110 0010</strong> 1111 11111111)</li>
</ol>
<ul>
<li><p>为了更方便进行路由选择,CIDR使用32位 <em>地址掩码</em>(address mask), 地址掩码由一串1和一串0组成,1的个数就是网络前缀的长度.(原理与子网掩码一致,都是通过与操作获取网络地址)</p>
</li>
<li><p>CIDR仍然可以划分子网,但子网的网络前缀比整个单位的网络前缀长一些.</p>
</li>
<li><p>CIDR记法有很多种,例如地址块<code>10.0.0.0/10</code>可以简写为<code>10/10</code>,也就是把点分十进制中低位连续的0省略.</p>
</li>
<li><p>另一种简化表示法是在网络前缀后加一个<code>*</code>,如<code>00001010 00*</code>,表示<code>*</code>之前是网络前缀,而<code>*</code>表示IP地址中的主机号,可以是任意值.</p>
</li>
<li><p>使用CIDR时, 由于采用了网络前缀这种记法,IP地址由网络前缀和主机号这两个部分组成,因此路由表中的项目也要相应地改变.这时每个项目由”网络前缀”和”下一跳地址”组成.</p>
</li>
<li><p>但是查找路由表 <strong>可能会得到不止一个匹配结果</strong>, 因此 <strong>应当从匹配结果中选择具有最长网络前缀的路由</strong>,这叫做 <em>最长前缀匹配(longest-prefix matching)</em>. 这因为网络前缀越长,其地址块就越小,因而路由也就越具体.因此在转发的时候是选择匹配到的地址中更具体的.</p>
</li>
<li><p>最长前缀匹配又称为 <em>最长匹配</em> 或 <em>最佳匹配</em></p>
</li>
</ul>
<h3 id="路由选择算法"><a href="#路由选择算法" class="headerlink" title="路由选择算法"></a>路由选择算法</h3><ul>
<li><p>从路由算法能否随网络的通信量或拓扑自适应地进行调整变化来划分, 则只有两大类: <em>静态路由选择策略</em> 和 <em>动态路由选择策略</em></p>
</li>
<li><p>静态路由选择也叫 <em>非自适应路由选择</em>, 适用于简单的小网络.</p>
</li>
<li><p>动态路由选择也叫 <em>自适应路由选择</em>, 适用于复杂的大网络.(因为实现较复杂,开销也比较大)</p>
</li>
</ul>
<h4 id="分层次的路由选择协议"><a href="#分层次的路由选择协议" class="headerlink" title="分层次的路由选择协议"></a>分层次的路由选择协议</h4><ul>
<li><p>内部网关协议IGP(Interior Gateway Protocol): 在一个自治系统内部使用的路由选择协议.(如RIP和OSPF协议)</p>
</li>
<li><p>外部网关协议EGP(External Gateway Protocol): 若源主机和目的主机处在不同的自治系统中(这两个自治系统可能使用不同的内部网关协议),当数据包传到自治系统的边界时,就要使用外部网关协议.</p>
</li>
</ul>
<h3 id="IP多播"><a href="#IP多播" class="headerlink" title="IP多播"></a>IP多播</h3><ul>
<li>多播和单播的区别: 例如要向100台主机发送同一份数据, 单播则要发送90份相同的副本给每个主机,而多播只要发送一个副本就可以了(不需要复制分组).</li>
</ul>
<h3 id="虚拟专用网VPN和网络地址转换NAT"><a href="#虚拟专用网VPN和网络地址转换NAT" class="headerlink" title="虚拟专用网VPN和网络地址转换NAT"></a>虚拟专用网VPN和网络地址转换NAT</h3><ul>
<li><p>RFC1918指明了一些 <em>专用地址</em>(private address),这些地址只能用于一个机构内部通信,即只能用作本地地址不能用作全球地址.</p>
</li>
<li><p>例如:</p>
</li>
</ul>
<ol>
<li><code>10.0.0.0</code>到<code>10.255.255.255</code></li>
<li><code>172.16.0.0</code>到<code>172.31.255.255</code></li>
<li><code>192.168.0.0</code>到<code>192.168.255.255</code><br>这三个地址相当于一个A类网络,16个连续的B类网络和256个连续的C类网络.</li>
</ol>
<h4 id="VPN-Virtual-Private-Network"><a href="#VPN-Virtual-Private-Network" class="headerlink" title="VPN(Virtual Private Network)"></a>VPN(Virtual Private Network)</h4><ul>
<li><p>使用IP隧道技术实现虚拟专用网.</p>
</li>
<li><p>例子: 某个机构两个场所建立了专用网A和B,其网络地址分别为专用地址10.1.0.0和10.2.0.0.现在这两个场所需要 <strong>通过公用的互联网构成一个VPN</strong>.显然 <strong>每个场所至少有一个路由器具有合法的全球IP地址</strong>,这个具有全球IP地址的路由器在专用网内部的接口地址则是专用网的本地地址.即两个场所内主机的相互通信借由连到公网的路由器来转发和接收实现.</p>
</li>
</ul>
<h4 id="NAT-Network-Address-Translation"><a href="#NAT-Network-Address-Translation" class="headerlink" title="NAT(Network Address Translation)"></a>NAT(Network Address Translation)</h4><ul>
<li><p>需要在专用网连接到互联网的路由器上安装NAT软件.装有NAT软件的路由器叫做 <em>NAT路由器</em>, 它至少有一个有效的全球IP地址.<strong>所有使用本地地址的主机和外界通信的时候,都要在NAT路由器上将其本地地址转换成全球IP地址</strong></p>
</li>
<li><p>当NAT路由器具有N个全球IP地址的时候,专用网内部最多 <strong>同时</strong> 有N台主机可以接入互联网.</p>
</li>
<li><p>现在常用的NAT转换表把运输层的 <em>端口号</em> 也用上, 这样就可以使多个拥有本地地址的主机公用一个NAT路由器上的全球IP地址.</p>
</li>
</ul>
<h2 id="运输层"><a href="#运输层" class="headerlink" title="运输层"></a>运输层</h2><hr>
<ul>
<li>网络层只是将分组从一台主机传输到另一台主机,而运输层则再将这一通信过程精确到主机中的进程.</li>
</ul>
<blockquote>
<p>IP协议虽然能把分组送到目的主机, 但是这个分组还停留在主机的网络层而没有交付主机在的应用程序.从运输层角度来看,通信真正的端点并不是主机而是主机中的进程.也就是说, 端到端的通信是应用进程之间的通信.</p>
</blockquote>
<ul>
<li><p>TCP&#x2F;IP运输层的两个主要协议: <strong>用户数据报协议UDP(User Datagram Protocol)</strong> 和 <strong>传输控制协议TCP(Transmission Control Protocol)</strong></p>
</li>
<li><p>传送的数据单元根据协议的不同,分别称为 <strong>TCP报文段</strong> 和 <strong>UDP用户数据报</strong></p>
</li>
<li><p>使用UDP和TCP协议的各种应用和应用层协议</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>应用</th>
<th>应用层协议</th>
<th>运输层协议</th>
</tr>
</thead>
<tbody><tr>
<td>名字转换</td>
<td>DNS(域名系统)</td>
<td>UDP</td>
</tr>
<tr>
<td>文件传送</td>
<td>TFTP(简单文件传送协议)</td>
<td>UDP</td>
</tr>
<tr>
<td>路由选择协议</td>
<td>RIP(路由信息协议)</td>
<td>UDP</td>
</tr>
<tr>
<td>IP地址配置</td>
<td>DHCP(动态主机配置)</td>
<td>UDP</td>
</tr>
<tr>
<td>网络管理</td>
<td>SNMP(简单网络管理协议)</td>
<td>UDP</td>
</tr>
<tr>
<td>远程文件服务器</td>
<td>NFS(简单网络管理协议)</td>
<td>UDP</td>
</tr>
<tr>
<td>IP电话</td>
<td>专用协议</td>
<td>UDP</td>
</tr>
<tr>
<td>流式多媒体通信</td>
<td>专用协议</td>
<td>UDP</td>
</tr>
<tr>
<td>多播</td>
<td>IGMP(网际组管理协议)</td>
<td>UDP</td>
</tr>
<tr>
<td>电子邮件</td>
<td>SMTP(简单邮件传送协议)</td>
<td>TCP</td>
</tr>
<tr>
<td>远程终端接入</td>
<td>TELNET(远程终端协议)</td>
<td>TCP</td>
</tr>
<tr>
<td>万维网</td>
<td>HTTP(超文本传送协议)</td>
<td>TCP</td>
</tr>
<tr>
<td>文件传送</td>
<td>FTP(文件传送协议)</td>
<td>TCP</td>
</tr>
</tbody></table>
<h3 id="运输层的端口"><a href="#运输层的端口" class="headerlink" title="运输层的端口"></a>运输层的端口</h3><ul>
<li><p>由于进程的创建和撤销是动态的,所以通信的一方几乎无法识别对方机器上的进程.解决这个问题的方法就是在运输层使用 <em>端口协议号(protocol port number)</em>,通常简称为 <em>端口</em>.</p>
</li>
<li><p>不同的端口用来标志不同的应用进程.虽然通信的终点是应用进程,但是只要把所传送的报文交到目的主机的某个合适的目的端口,剩下的工作(交付目的进程)就由TCP和UDP来完成.</p>
</li>
<li><p>这种在协议栈层间的抽象的协议端口是 <strong>软件端口</strong>,和路由器或交换机的硬件端口是完全不同的概念.硬件端口是不同硬件设备进行交互的接口, 而软件端口是应用层的各种协议进程与运输实体进行层间交互的一种地址.</p>
</li>
<li><p>TCP&#x2F;IP的运输层用一个16位的端口号来标志一个端口. <strong>注意,端口号只有本地意义,只是为了标记本计算机应用层中各个进程在和运输层交互时的层间接口</strong></p>
</li>
<li><p>互联网上的计算机通信采用客户-服务器方式(C&#x2F;S方式).客户在发起通信请求的时候,必须知道对方服务器的IP地址和端口号.</p>
</li>
<li><p>运输层端口号分为两大类:</p>
</li>
</ul>
<h4 id="一-服务器端使用的端口号"><a href="#一-服务器端使用的端口号" class="headerlink" title="一. 服务器端使用的端口号"></a>一. 服务器端使用的端口号</h4><ul>
<li><p>这里又可以分为两类: <strong>熟知端口号</strong> 和 <strong>登记端口号</strong>.</p>
</li>
<li><p>熟知端口号是最重要的一类, 又叫做 <em>系统端口号</em> ,数值为0-1023.</p>
</li>
<li><p><strong>需要记住的常用端口号</strong>:</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>应用程序</th>
<th>熟知端口号</th>
<th>协议类型</th>
</tr>
</thead>
<tbody><tr>
<td>FTP</td>
<td>21</td>
<td>TCP</td>
</tr>
<tr>
<td>TELNET</td>
<td>23</td>
<td>TCP</td>
</tr>
<tr>
<td>SMTP</td>
<td>25</td>
<td>TCP</td>
</tr>
<tr>
<td>DNS</td>
<td>53</td>
<td>UDP</td>
</tr>
<tr>
<td>TFTP</td>
<td>69</td>
<td>UDP</td>
</tr>
<tr>
<td>HTTP</td>
<td>80</td>
<td>TCP</td>
</tr>
<tr>
<td>HTTPS</td>
<td>443</td>
<td>TCP</td>
</tr>
<tr>
<td>SNMP</td>
<td>161</td>
<td>UDP</td>
</tr>
<tr>
<td>SNMP(trap)</td>
<td>162</td>
<td>UDP</td>
</tr>
</tbody></table>
<h4 id="二-客户端使用的端口号"><a href="#二-客户端使用的端口号" class="headerlink" title="二. 客户端使用的端口号"></a>二. 客户端使用的端口号</h4><ul>
<li><p>数值为49152-65535</p>
</li>
<li><p>由于这类端口号仅在客户进程运行时才动态选择,因此又叫做 <em>短暂端口号</em></p>
</li>
<li><p>当服务器进程收到客户进程的报文时,就知道了客户进程现在所使用的端口号,因而可以把数据发送给客户进程.当通信结束后,刚才使用的客户端口号就不复存在了,这个端口号就可供其他客户进程使用.</p>
</li>
</ul>
<h3 id="连续ARQ协议"><a href="#连续ARQ协议" class="headerlink" title="连续ARQ协议"></a>连续ARQ协议</h3><ul>
<li><p>将流量控制和差错控制结合了起来.</p>
</li>
<li><p>ARQ协议规定,发送方每收到一个确认,就把发送窗口向前滑动一个分组的位置.于是就可以接着发送下一个分组进入窗口的分组.</p>
</li>
<li><p>接收方一般采用累积却认的方式,也就是说接收方不必对收到的分组逐个发送确认,<strong>只要对按序到达的最后一个分组发送确认</strong>,这表示:到这个分组为止的所有分组都已经正确收到了.</p>
</li>
<li><p>累积确认的优点是:容易实现,即使确认丢失也不必重传.缺点是:不能向发送方反映出接收方已经正确收到的所有分组的信息.(因为可能存在可能没有按序到达的分组,比如收到了1,2,3,5四个分组,只能对前三个进行确认,而5号分组到达的消息发送方没有办法知道,而且很有可能会重传5号分组)</p>
</li>
</ul>
<h3 id="TCP可靠传输的实现"><a href="#TCP可靠传输的实现" class="headerlink" title="TCP可靠传输的实现"></a>TCP可靠传输的实现</h3><h4 id="以字节为单位的滑动窗口"><a href="#以字节为单位的滑动窗口" class="headerlink" title="以字节为单位的滑动窗口"></a>以字节为单位的滑动窗口</h4><ul>
<li><p>发送窗口表示: 在没有收到B的确认的情况下,A可以连续地把窗口内的数据都发送出去.<strong>凡是已经发送过的数据,在未收到确认之前都必须暂时保留以便在超时重传时使用.</strong></p>
</li>
<li><p>接收方会把自己的接收窗口的数值放在窗口字段中发送给对方,这相当于告诉发送方,自己的剩余缓冲区域的大小.<strong>因此A的发送窗口一定不能超过B的接收窗口的值.</strong></p>
</li>
<li><p>发送窗口的位置由窗口的前后沿共同决定.发送窗口的后沿不动有两种可能:<strong>(1)没有收到新的确认. (2)收到了新的确认但是对方通知窗口缩小了,使得前沿刚好不动.</strong></p>
</li>
<li><p>发送缓存用来暂时存放:</p>
</li>
</ul>
<ol>
<li>发送应用程序发送给发送方TCP准备发送的数据</li>
<li>TCP已发送出但尚未收到确认的数据</li>
</ol>
<ul>
<li>接收缓存用来暂时存放:</li>
</ul>
<ol>
<li>按序到达的,但尚未被接收应用程序读取的数据</li>
<li>未按序到达的数据</li>
</ol>
<h3 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h3><ul>
<li><p>拥塞控制与流量控制的区别: 流量控制管理的是两个点,而拥塞控制管理的是整个网络.</p>
</li>
<li><p>计算机网络中的链路容量(即带宽),交换结点中的缓存和处理机等,都是网络的资源.在某段时间,对网络中某一资源的需求超过了该资源所能提供的可用部分,网络性能就要变坏,这种情况就叫 <em>拥塞(congestion)</em></p>
</li>
<li><p>问题的实质是,整个系统的各个部分不匹配,只有所有部分平衡了,问题才会得到解决.</p>
</li>
<li><p><em>拥塞控制</em> : 防止过多的数据注入到网络中,这样可以使网络中的路由器或链路不致过载.拥塞控制是一个全局性的过程,涉及到所有的主机,所有的路由器,以及降低网络传输性能的所有因素.</p>
</li>
<li><p>拥塞控制可以分为 <em>开环控制</em> 和 <em>闭环控制</em>.</p>
</li>
<li><p>开环控制就是事先考虑所有的因素,一旦开始运作就不再更改.</p>
</li>
<li><p>闭环控制是基于反馈回路的概念.</p>
</li>
</ul>
<h4 id="TCP的拥塞控制方法"><a href="#TCP的拥塞控制方法" class="headerlink" title="TCP的拥塞控制方法"></a>TCP的拥塞控制方法</h4><h5 id="慢开始和拥塞避免"><a href="#慢开始和拥塞避免" class="headerlink" title="慢开始和拥塞避免"></a>慢开始和拥塞避免</h5><ul>
<li><p>发送方维护一个叫做 <em>拥塞窗口cwnd(congestion window)</em> 的状态变量. <strong>发送方让自己的发送窗口等于拥塞窗口</strong>.</p>
</li>
<li><p>判断网络拥塞的依据是 <strong>出现了超时</strong>.</p>
</li>
<li><p>慢开始: 由小到大逐渐增加拥塞窗口(发送窗口).</p>
</li>
<li><p>从慢开始开始指数增长,直到一个阈值(慢开始门限ssthresh),开始加法增长,最后出现超时,再从慢开始从头再来.</p>
</li>
<li><p>当cwnd小于ssthresh时,使用慢开始算法(指数增长).当cwnd&gt;ssthresh时,停止使用慢开始算法 <strong>改用拥塞避免算法</strong>.(cwnd &#x3D; ssthresh时两个算法任意选取)</p>
</li>
<li><p><em>拥塞避免算法</em> 的思路是让拥塞窗口cwnd缓慢地增大,即每过一个RTT就把发送方的拥塞窗口加1.注意拥塞避免并不是完全避免了拥塞,而是是吧拥塞窗口控制为按线性增长,使网络比较不容易出现拥塞.</p>
</li>
</ul>
<h4 id="主动队列管理AQM"><a href="#主动队列管理AQM" class="headerlink" title="主动队列管理AQM"></a>主动队列管理AQM</h4><ul>
<li><em>随机早期检测RED(Random Early Detection)</em></li>
</ul>
<h3 id="TCP连接的建立和断开-重点"><a href="#TCP连接的建立和断开-重点" class="headerlink" title="TCP连接的建立和断开(重点)"></a>TCP连接的建立和断开(重点)</h3><blockquote>
<p>三次握手, 四次挥手</p>
</blockquote>
<ul>
<li><p><img src="/images/computer_network/connection.png"></p>
</li>
<li><p>ACK&#x3D;1 表示这是一个确认报文, 对之前收到的报文进行确认.</p>
</li>
<li><p>SYN&#x3D;1 表示这是一个同步报文段,用于请求同步.</p>
</li>
<li><p>SYN报文段 <strong>不能携带数据, 并且要消耗一个序号</strong></p>
</li>
<li><p>ACK报文段可以携带数据,如果不携带数据 <strong>则不消耗序列号</strong></p>
</li>
<li><p>FIN报文段可以携带数据, <strong>但是不管携带与否,都要消耗一个序列号</strong></p>
</li>
</ul>
<h4 id="三报文握手-TCP连接建立"><a href="#三报文握手-TCP连接建立" class="headerlink" title="三报文握手(TCP连接建立)"></a>三报文握手(TCP连接建立)</h4><ul>
<li><p>一开始,B的TCP服务器进程先创建一个 <em>传输控制块TCB(Transmission Control Block)</em>, 准备接收客户进程的连接请求. <em>然后服务器就处于LISTEN(收听)状态</em></p>
</li>
<li><p>A的TCP客户进程也是首先创建传输控制模块TCB,然后在打算建立TCP连接时,向B发出连接请求报文段,<strong>这时首部中的同步位SYN&#x3D;1</strong>,同时 <strong>选择一个初始序号seq&#x3D;x</strong>.(SYN报文不能携带数据,但要消耗掉一个序号).<em>TCP客户进程进入SYN-SENT(同步已发送)状态</em></p>
</li>
<li><p>B收到连接请求报文段后,如同意连接,则向A发送确认.这个确认报文段中SYN位和ACK位都置为1.<strong>确认号ack&#x3D;x+1(由A发送的确认号为x的请求报文而得到的)</strong>,同时也为自己选择一个初始序号seq&#x3D;y(<strong>注意B的确认报文的seq字段与A的同步报文的seq无关</strong>),同时,<strong>这个报文也不能携带数据,而且同样要消耗掉一个序列号.</strong> <em>这时TCP服务器进程进入SYN-RCVD(同步收到)状态</em></p>
</li>
</ul>
<blockquote>
<p>确认报文的确认号ack总是等于被确认的报文的序号seq的值加1</p>
</blockquote>
<ul>
<li><p>TCP客户进程收到B的确认后,还要向B给出确认.(<strong>防止A发出的连接请求报文在网络中长时间逗留而没有丢失,在A重新发送请求并且完成连接,结束通信后,B又收到了A之前误以为被丢失而实际只是滞留了的连接请求报文,这时B会以为这是A的一个新的连接请求,从而发送确认给A.而A没有请求,因此会忽略B的这个确认.设想如果没有A的第三次确认,则B以为连接已经建立而无限地等待A发送数据,从而造成资源的浪费.</strong>) 确认报文的ACK置为1,确认号为ack&#x3D;y+1(确认号为y+1是因为这是对B发送过来的报文的确认报文),自己的序号seq&#x3D;x+1.(<em>如果这个报文没有携带数据,则下一个报文的序号仍然是x+1</em>)这时,TCP连接已经建立,A进入ESTABLISHED(已建立连接)状态.</p>
</li>
<li><p>当B收到A的确认后,也进入ESTABLISHED状态.</p>
</li>
</ul>
<h4 id="四报文挥手-TCP连接释放"><a href="#四报文挥手-TCP连接释放" class="headerlink" title="四报文挥手(TCP连接释放)"></a>四报文挥手(TCP连接释放)</h4><ul>
<li><p>数据传输结束后,通信双方都可以释放连接(释放连接是单向的,可以一方停止向另一方发送数据,但另一方还可以向这一方发送数据).(<strong>同时也说明,两个方向都要释放一遍连接</strong>)</p>
</li>
<li><p>A的应用进程先向其TCP发出连接释放报文段,并停止再发送数据,主动关闭TCP连接.A把连接释放报文段首部的终止控制位FIN置1,其序号seq&#x3D;u(<strong>u等于前面A已经传送过的数据的最后一个字节的序号加1</strong>).<em>这时A进入FIN-WAIT-1(终止等待1)状态,等待B的确认.</em></p>
</li>
<li><p>B收到连接释放报文段后立即发出确认,确认号ack&#x3D;u+1.这个报文段自己的序号为v(v等于B前面已经传送过的数据的最后一个字节的序号加1).<em>然后B就进入CLOSE-WAIT(关闭等待)状态.</em> <strong>这时TCP服务器进程会通知高层应用进程,因而从A到B这个方向上的连接就释放了,这时TCP处于半关闭(half-close)状态,即A已经没有数据要向B发送了,但是如果B要发送数据,A仍然要接收.也就是说从B到A这个方向的连接还未关闭,这个状态可能会持续一段时间.</strong></p>
</li>
<li><p>A收到来自B的确认后,<em>就进入FIN-WAIT2(终止等待2)状态</em>,等待B发出的连接释放报文段.</p>
</li>
<li><p>若B也没有要向A发送的数据,其应用进程就通知TCP释放连接.这时B发出的连接释放报文段FIN&#x3D;1.假定B的序号为w(因为在半关闭状态B可能又发送了一些数据),此时B还必须重复上次发送过的确认号ack&#x3D;u+1.<em>这时B就进入了LAST-ACK(最后确认)状态</em>,等待A的确认.</p>
</li>
<li><p>A在收到B的连接释放报文段后,在确认报文段中把ACK置1,确认号ack&#x3D;w+1,序号seq&#x3D;u+1.<em>进入TIME-WAIT(时间等待)状态</em>. <strong>注意!此时TCP连接没有释放掉,必须经过时间等待计时器(TIME-WAIT timer)设置的时间2MSL后,A才进入CKISED状态</strong>.时间 <em>MSL</em> 叫做 <em>最长报文段寿命(Maximum Segment Lifetime)</em>.</p>
</li>
<li><p>这里A需要等待2MSL(<strong>B在收到确认后就立即进入CLOSED状态无需等待</strong>)有两个理由:</p>
</li>
</ul>
<ol>
<li>为了保证A发送的最后一个ACK报文段能够到达B(A发送的ACK确认报文段可能丢失,B会超时重传这个FIN+ACK报文段,而A就能在2MSL时间段内收到重传的FIN+ACK报文段,并重新发送一次确认,重启2MSL计时器).</li>
<li>防止”已失效的连接请求报文段”出现在本连接中.再经过2MSL,就可以使本连接持续时间内所产生的所有报文段从网络中消失,确保下一个连接不会收到旧的无效报文段而引发错误.</li>
</ol>
<ul>
<li>最后,TCP还设置 <em>保活计时器(keepalive timer)</em>,在服务器长时间(一般为2小时)没有收到客服的数据(服务器每收到一次客户数据就重置这个等待时间),服务器就发送一个探测请求报文,之后每隔75秒发送一次,若连续10次没有收到客户响应,则关闭这个连接.(为了防止客户机出现故障)</li>
</ul>
<h2 id="应用层"><a href="#应用层" class="headerlink" title="应用层"></a>应用层</h2><hr>
<h3 id="域名系统DNS"><a href="#域名系统DNS" class="headerlink" title="域名系统DNS"></a>域名系统DNS</h3><ul>
<li><p>域名系统DNS(Domain Name System)能够把互联网上的主机名字转换为IP地址.</p>
</li>
<li><p>每一个域名都由 <em>标号(label)</em> 序列组成,各标号之间用点隔开.例如<code>mail.cctv.com</code>,其中<code>mail</code>为三级域名,<code>cctv</code>为二级域名,<code>com</code>为一级域名.(级别最低的域名写在最左边)</p>
</li>
<li><p>每一个标号不超过63字符,也不区分大小写.</p>
</li>
</ul>
<h4 id="域名解析"><a href="#域名解析" class="headerlink" title="域名解析"></a>域名解析</h4><ul>
<li>过程如下:当某一个应用进程需要把主机名解析为IP地址时,该应用进程就调用 <em>解析程序(resolver)</em>, 并称为DNS的一个客户,把待解析的域名放在DNS请求报文中,以UDP用户数据报方式发给本地域名服务器.(使用UDP是为了减小开销) 如果本地域名服务器不能回答该请求,则此域名服务器就暂时成为DNS中的另一个客户,向其他域名服务器发出查询请求.</li>
</ul>
<h3 id="文件传送协议FTP"><a href="#文件传送协议FTP" class="headerlink" title="文件传送协议FTP"></a>文件传送协议FTP</h3><ul>
<li>FTP的工作流程:</li>
</ul>
<ol>
<li>打开熟知端口(21),使客户进程能够连接上.</li>
<li>等待客户进程发出连接请求</li>
<li>启动从属进程处理客户进程发来的请求.</li>
<li>回到等待状态</li>
</ol>
<h3 id="万维网WWW"><a href="#万维网WWW" class="headerlink" title="万维网WWW"></a>万维网WWW</h3><ul>
<li><p><em>万维网WWW(Worl Wide Web)</em> 是一个大规模的,联机式的信息储藏所.</p>
</li>
<li><p><strong>超文本</strong>: 所谓超文本是指 包含指向其他文档的链接的文本(text).也就是说,一个超文本由多个信息源链组成, 而这些信息源可以分布在世界各地,并且数目不受限制.</p>
</li>
<li><p><strong>超媒体</strong>: 超媒体与超文本的区别是文档内容不同.超文本文档仅包含文本信息, 而超媒体文档还包含其他表示方式的信息,如图形,图像,声音,动画以及视频图像等.</p>
</li>
<li><p>万维网以客户服务器方式工作,浏览器就是在用户主机上的万维网客户程序. <strong>客户程序向服务器程序发出请求,服务器程序就向客户程序送回客户所要的万维网文档</strong>.在一个客户程序主窗口上显示出的万维网文档称为 <em>页面(page)</em>.</p>
</li>
</ul>
<h3 id="三要素URL-HTML和HTTP"><a href="#三要素URL-HTML和HTTP" class="headerlink" title="三要素URL,HTML和HTTP"></a>三要素URL,HTML和HTTP</h3><ul>
<li><p>万维网使用 <em>统一资源定位符URL(Uniform Resource Locator)</em> 来标志万维网上的各种文档,并使每一个文档在整个互联网的范围内具有唯一的标识符URL.(解决怎样标志分布在整个互联网上的万维网文档)</p>
</li>
<li><p><em>超文本传送协议HTTP(HyperText  Transfer Protocol)</em>: HTTP是一个应用层协议,使用TCP连接进行可靠的传送.HTTP协议使得万维网客户程序与万维网服务器程序之间的交互严格遵守这一标准.(解决用什么样的协议来实现万维网上的各种链接)</p>
</li>
<li><p>万维网使用 <em>超文本标记语言HTML(HyperText Markup Language)</em>,使得万维网页面的设计者可以很方便地用链接从本页面的某处链接到互联网上的任何一个万维网页面.(解决各种不同风格的文档都能在各种不同的主机上显示出来的问题)</p>
</li>
</ul>
<h3 id="电子邮件"><a href="#电子邮件" class="headerlink" title="电子邮件"></a>电子邮件</h3><ul>
<li><p>TCP&#x2F;IP协议规定的电子邮件地址的格式如下:<code>用户名@邮件服务器域名</code></p>
</li>
<li><p>电子邮件最重要的两个标准就是: <em>简单邮件传送协议SMTP(Simple Mail Transfer Protocol)</em><code>[RFC 5321]</code> 和 <em>互联网文本报文格式</em><code>[RFC 5322]</code></p>
</li>
<li><p>邮件读取协议POP3和IMAP(Internet Message Access Protocol).</p>
</li>
<li><p>不要把POP3或IMAP与SMTP弄混,发件人的用户代理向发送方邮件服务器发送邮件,以及发送方邮件服务器向接收方邮件服务器发送邮件,都使用SMTP协议. 而POP3或IMAP是用户代理从接收方邮件服务器上读取邮件时所使用的协议.</p>
</li>
</ul>
<h3 id="动态主机配置协议DHCP"><a href="#动态主机配置协议DHCP" class="headerlink" title="动态主机配置协议DHCP"></a>动态主机配置协议DHCP</h3><ul>
<li>DHCP提供了一种机制,即 <em>即插即用网(plug-and-play networking)</em>.这种机制允许一台计算机接入新的网络和获取IP地址而不用手工参与.</li>
</ul>
<h3 id="简单网络管理协议SNMP"><a href="#简单网络管理协议SNMP" class="headerlink" title="简单网络管理协议SNMP"></a>简单网络管理协议SNMP</h3><ul>
<li>SNMP(Simple Network Management Protocol)的网络管理由三个部分组成: 即SNMP本身,<em>管理信息结构SMI(Structure of Management Information)</em> 和 <em>管理信息库MIB(Management Information Base)</em></li>
</ul>
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑Blog</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 对象部分 </title>
    <url>/2019/01/15/JS-learning-03/</url>
    <content><![CDATA[<h2 id="window对象"><a href="#window对象" class="headerlink" title="window对象"></a>window对象</h2><hr>
<ul>
<li><p><img src="/images/JavaScript/model.png"></p>
</li>
<li><p>window对象处于对象链的顶端,因为它是Web浏览器中查看所有内容的主要容器. <strong>只要打开浏览器,即使窗口没有加载文档,window对象也在内存的当前模型中定义好了</strong></p>
</li>
<li><p>除了文档所在的窗口内容外,窗口的影响范围还包括窗口的尺寸和包围内容区域的所有”元素”.滚动条,工具栏和菜单栏所在的区域叫做窗口的窗框.不是每个浏览器都可以完全控制浏览器主窗口的窗框,但可以便捷地创建其他窗口,随意地设置其大小,指定要显示的窗框元素.</p>
</li>
<li><p>每个框架都可以看做一个window对象,因为每个框架都可以存放不同的文档.脚本在其中一个文档上运行时,它把拥有这个文档的框架作为对象层次结构图中的window对象.</p>
</li>
</ul>
<h3 id="访问窗口的属性和方法"><a href="#访问窗口的属性和方法" class="headerlink" title="访问窗口的属性和方法"></a>访问窗口的属性和方法</h3><ul>
<li><pre><code>
</code></pre>
</li>
</ul>
<p>window.propertyName<br>window.methodName([Parameters])</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在脚本引用指向包含文档的窗口时,window对象有一个同义词self,以上的引用也等价于:</span><br></pre></td></tr></table></figure>
<p>self.propertyName<br>self.methodName([Parameters])</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">### 创建窗口</span><br><span class="line"></span><br><span class="line">- 脚本并不创建主浏览器窗口,用户在创建主窗口的时候需要启动浏览器.但是在主窗口打开后,脚本就可以生成大量子窗口.</span><br><span class="line"></span><br><span class="line">- 生成新窗口的方法是:`window.open()`.这个方法最多包含三个参数,来定义窗口属性.</span><br><span class="line"></span><br><span class="line">- 例子:`var subWindow = window.open(&quot;define.html&quot;,&quot;def&quot;,&quot;height=200,width=300&quot;);`</span><br><span class="line">这个语句将新建的窗口赋值给了变量subWindow.(**这个语句打开了一个具有指定大小的新窗口,这个窗口包含的html文档为define.html,并且与当前页面处于同一个服务器目录下**)</span><br><span class="line"></span><br><span class="line">- 如果要关闭此子窗口,则调用`subWindow.close()`即可.而调用`window.close()/self.close()或者close()`就会关闭主窗口.</span><br><span class="line"></span><br><span class="line">### window对象的属性和方法</span><br><span class="line"></span><br><span class="line">#### window.alert()方法</span><br><span class="line"></span><br><span class="line">- 可以直接使用`alert()`来调用这个方法(省略前面的window).这个window方法会生产一个对话框,显示作为参数传送的文本.OK按钮允许用户关闭这个对话框.</span><br><span class="line"></span><br><span class="line">#### window.confirm()方法</span><br><span class="line"></span><br><span class="line">- 这个方法也是弹出一个对话框,但是有两个按钮(大多数平台上为Ok和Cancel),称为确认对话框.**更重要的是,这个方法有返回值:单击OK返回值为true,单击Cancel返回值为false**. 可以将这个方法的计算值作为if或if..else结构中的条件语句.</span><br><span class="line"></span><br><span class="line">- 例子:</span><br></pre></td></tr></table></figure>
<p>if (confirm(“Are you sure to start over?”)){<br>  location.href &#x3D; “index.html”;<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">#### window.prompt()方法</span><br><span class="line"></span><br><span class="line">- window对象的最后一种对话框是提示对话框,它显示了预置的信息,并提供一个文本域供用户输入响应.这个对话框有两个按钮:OK和Cancel.</span><br><span class="line"></span><br><span class="line">- window.prompt()方法有两个参数,第一个参数是呈现给用户的提示信息;第二个参数是一个字符串,它在文本域中显示为默认的答案.如果不希望显示默认答案就传入一个空字符串(空双引号).</span><br><span class="line"></span><br><span class="line">- 用户单击按钮时,这个方法就会返回一个值.不管用户在文本域中输入什么,单击Cancel按钮都会返回null值,单击OK则返回输入的字符串值.这个信息也可以用在判断中,`null值等同于false,空字符串也看做false`.</span><br><span class="line"></span><br><span class="line">#### load事件</span><br><span class="line"></span><br><span class="line">- window对象会响应几个系统和用户事件,最常见的是页面加载完毕时触发的事件.**这个事件等待图像, Java applet和插件程序的数据文件完全下载到浏览器中**.使用load事件调用函数的优点在于,它确保所有document对象都存在于浏览器的DOM中.</span><br><span class="line"></span><br><span class="line">- 可以通过如下方式调用onload方法:</span><br></pre></td></tr></table></figure>
<p>window.addEventListener(‘load’, functionName, false);<br>window.attachEvent(‘onload’, functionName);<br>&#x2F;&#x2F;其中functionName是页面下载完成后要运行的函数的名称.可以多次调用addEventListener和attachEvent,把多个函数添加到页面加载完毕要执行的列表中.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- 还可以把该事件直接应用于元素:</span><br></pre></td></tr></table></figure>
<p>window[‘onload’] &#x3D; functionName;<br>window.onload &#x3D; functionName;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">但是这种用法表示,页面加载完毕后,只执行一个函数,并替代已赋予window对象的其他事件处理程序.</span><br><span class="line"></span><br><span class="line">## location对象</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- 这个对象表示载入窗口的URL.它和document对象不同:文档是页面的内容,而位置是页面的URL.</span><br><span class="line"></span><br><span class="line">- 脚本大多数只使用该对象的一个属性:`href`,它定义了完整的URL.</span><br><span class="line"></span><br><span class="line">- 如果要加载的页面在另一个窗口或框架中,就必须在语句中包含窗口的引用.例如:用脚本打开一个新窗口,并将它的引用赋值给变量newWindow,将页面载入子窗口,如下:</span><br><span class="line">`newWindow.loaction.href = &quot;http://www.example.com&quot;`</span><br><span class="line"></span><br><span class="line">## navigator对象</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- navigator.userAgent属性返回一个包含浏览器和操作系统的诸多细节的字符串.</span><br><span class="line"></span><br><span class="line">## document对象</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">### document.getElementById()方法</span><br><span class="line"></span><br><span class="line">- 此方法的唯一参数是带引号的字符串,该字符串包含要引用的元素的ID.</span><br><span class="line"></span><br><span class="line">- 例子:`var oneTable = document.getElementById(&#x27;salesResults&#x27;);`.在赋值语句后,变量就表示对象,可用于获取和设置该对象的属性,或者调用此类对象的方法.</span><br><span class="line"></span><br><span class="line">### document.getElementByTagName()方法</span><br><span class="line"></span><br><span class="line">- 可以使用getElementByTagName()方法方便地收集一组共享同一个标记名称的页面元素.例如,要获得页面上所有的图像:`var aImages = document.getElementByTagName(&#x27;img&#x27;);`</span><br><span class="line"></span><br><span class="line">- 然后可以通过数组的方法来获取数组的长度:`aImages.length`,然后每个对象就可以使用数组的偏移量来访问.</span><br><span class="line"></span><br><span class="line">### document.forms[]属性</span><br><span class="line"></span><br><span class="line">- 比较下面两个表达式:</span><br></pre></td></tr></table></figure>
<p>var aForms &#x3D; document.forms;<br>var aForms &#x3D; document.getElementByTagName(‘form’);</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">**一个重要的区别是,document.forms集合允许按名称直接引用某个表单,而不仅仅是按照数组的偏移量来访问.**</span><br><span class="line"></span><br><span class="line">- 因为有些时候按照下标号引用表单未必可行.动态网页可能根据上下文包含数量可变的表单,所以表单在页面上的位置可能随上下文而变化.</span><br><span class="line"></span><br><span class="line">- 支持脚本编程的浏览器允许通过表单名或ID(即分配给`&lt;form&gt;标记的name或者id属性`)更直接地引用表单:</span><br></pre></td></tr></table></figure>
<form id='formId' name='fromName' ...>

<p>&#x2F;&#x2F;第一种方法是使用getElementById方法:<br>document.getElementById(“formId”)</p>
<p>&#x2F;&#x2F;第二种方法是使用数组语法,将表单名称或者ID作为数组的字符串下标:<br>document.forms[“formId”]<br>document.forms[“formName”]</p>
<p>&#x2F;&#x2F;第三种按名称引用form对象的简短方式是把表单的名称附加为document对象的属性:<br>document.fornName</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">### document.images[]属性</span><br><span class="line"></span><br><span class="line">- 文档利用特殊的数组属性来跟踪表单,同样,document对象也通过`&lt;img&gt;`标记保存插入文档的图像集合(数组).通过document.images数组引用的图像可以用img元素名称中的数字或字符串下标来得到.</span><br><span class="line"></span><br><span class="line">### document.createElement()和document.createTextNode()方法</span><br><span class="line"></span><br><span class="line">- 在HTML文档中添加新元素至少需要两个步骤:</span><br><span class="line">1. 为文档创建新元素.</span><br><span class="line">2. 在页面的树形结构中,将其插入到需要的位置.</span><br><span class="line"></span><br><span class="line">- document.createElement()方法可以在浏览器的内存中创建一个全新的元素对象.指定要创建的元素时,应将元素的标记名作为该方法的字符串参数:</span><br><span class="line">`var newEle, = document.createElement(&quot;p&quot;);`</span><br><span class="line"></span><br><span class="line">- 还可以给元素添加一些属性值,为此应在元素成为文档的一部分之前,向新建对象赋值:`newElem.setAttribute(&quot;class&quot;,&quot;intro&quot;);`</span><br><span class="line"></span><br><span class="line">- 可将这个元素插入文档中的任何位置,例如末尾:</span><br><span class="line">`document.body.appendChild(newElem);`</span><br><span class="line"></span><br><span class="line">### document.write()方法</span><br><span class="line"></span><br><span class="line">- document.write()方法是给文档写入内容的另一种方式.他的优点是可以临时应急,缺点如下:</span><br><span class="line">1. 只有页面第一次加载到浏览器中时,document.write()才能给一个网页添加内容.以后调用该方法都会替代整个页面.</span><br><span class="line">2. document.write()会将旧文本块插入文档,编写出草率的程序和错误的标记,将标记与脚本内容混合在一起,不利于开发的分层.</span><br><span class="line">3. document.write()不能用于XHTML,XHTML这种文档类型不允许第一次解析期间修改其内容.</span><br><span class="line"></span><br><span class="line">&gt; 注:XHTML---可扩展超文本标记语言（英语：eXtensible HyperText Markup Language，XHTML），是一种标记语言，表现方式与超文本标记语言（HTML）类似，不过语法上更加严格。从继承关系上讲，HTML是一种基于标准通用标记语言（SGML）的应用，是一种非常灵活的置标语言，而XHTML则基于可扩展标记语言（XML），XML是SGML的一个子集。XHTML 1.0在2000年1月26日成为W3C的推荐标准。</span><br><span class="line"></span><br><span class="line">## 表单和表单元素</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">### form对象</span><br><span class="line"></span><br><span class="line">- 表单以及其中的输入控件是DOM对象,其独特的属性是文档中其他对象所没有的.例如,表单对象的action属性告诉浏览器,在提价表单时,将输入的值发送到什么地方.select控件(下拉列表)的selectedIndex属性指出用户选择了哪个选项.</span><br><span class="line"></span><br><span class="line">#### 访问表单属性</span><br><span class="line"></span><br><span class="line">-  在创建表单时,可以使用HTML页面中的标准标记,也可以使用JavaScript中的DOM方法.无论采用哪种方法,都可以设置name,target,action,method和enctype特性.这些都是form对象的属性.</span><br><span class="line"></span><br><span class="line">- 用法的例子:</span><br></pre></td></tr></table></figure>
<p>var sURL &#x3D; document.getElementById(‘formName’).action;<br>document.forms[0].action &#x3D; “<a href="http://www.example.com/cgi/login.pl">http://www.example.com/cgi/login.pl</a>“;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">#### from.elements[]属性</span><br><span class="line"></span><br><span class="line">- elements[]属性是表单中所有输入控件的集合.这个数组中各项的顺序根据HTML标记在源代码中的顺序而定.使用ID直接引用单个元素通常更有效,但有时候需要遍历所有元素时会需要这样访问.</span><br><span class="line"></span><br><span class="line">- 下面的代码段查看表单中所有的控件元素,并将文本域的内容设置为空字符串.</span><br></pre></td></tr></table></figure>
<p>var oForm &#x3D; document.getElementById(‘registration-form’);<br>  if(!oForm) return false;<br>for(var i &#x3D; 0; i &lt; oForm.elements.length; i++){<br>  if(oForm.elements[i].type &#x3D;&#x3D; “text”){<br>    oForm.elements[i].values &#x3D; “”;<br>  }<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">### 将表单控件作为对象</span><br><span class="line"></span><br><span class="line">- 在所有浏览器的DOM中,嵌套在`&lt;form&gt;`标记中的HTML元素有三种是脚本对象.多数对象都在页面源代码的`&lt;input&gt;`标记中,只有赋给`&lt;input&gt;`标记的type特性的值能确定元素是文本框,密码输入域,隐藏域,按钮,复选框还是单选框.其他两种表单控件textarea和select有各自的标记.</span><br><span class="line"></span><br><span class="line">- 要将某个表单控件作为对象进行引用,可以使用其id或tagName直接引用,或者使用DOM level 0语法,将引用构造为一个以document开头,后跟form和控件的层次结构.</span><br></pre></td></tr></table></figure>
<p>document.getElementById(controlName)<br>或者<br>document.formName.controlName</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- 例如有个简单的表单:</span><br></pre></td></tr></table></figure>
<form id="searchForm" action="cgi-bin/search.pl">
  <p>
    <input type="text" id="entry" name="entry">
    <input type="submit" id="sender" name="sender" value="Search">
  </p>
</form>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">对于文本输入控件,以下引用实例都是有效的:</span><br></pre></td></tr></table></figure>
document.getElementById("entry")
document.searchForm.entry
document.searchForm.elements[0]qitak
document.forms["searchForm"].elements["entry"]
document.forms["searchForm"].entry
```
]]></content>
      <categories>
        <category>Computer Software</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑JS</tag>
      </tags>
  </entry>
  <entry>
    <title>Java学习笔记——方法</title>
    <url>/2018/04/18/Java-function/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><hr>
<ul>
<li><p><strong>Java中的函数没有默认参数，通过函数重载间接实现这个功能。</strong></p>
</li>
<li><p>java没有在类外面定义的函数，所以一般都是方法。</p>
</li>
</ul>
<h3 id="工具类函数的使用"><a href="#工具类函数的使用" class="headerlink" title="工具类函数的使用"></a>工具类函数的使用</h3><ul>
<li>如果要自己写一些可以复用的工具函数，可以新建一个class：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class myUtils&#123;</span><br><span class="line">  static void func(...)&#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>在这个类中将要用的工具函数用static来修饰，然后将函数的参数弄好之后，就可以在不用新建对象的情况下使用这个方法了。</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.6.6</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java学习笔记——类与继承</title>
    <url>/2018/04/18/Java-class/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="Java的类，继承与接口"><a href="#Java的类，继承与接口" class="headerlink" title="Java的类，继承与接口"></a>Java的类，继承与接口</h2><hr>
<h3 id="类的定义"><a href="#类的定义" class="headerlink" title="类的定义"></a>类的定义</h3><ul>
<li><p>类的修饰符有两种：public 和 缺省。public表示共有的访问级别，即任何类都可以访问它。缺省只允许同一个包中的类来访问它。</p>
</li>
<li><p>类的类型修饰符：缺省、abstract及final三种。abstract表示这是一个抽象类，<strong>有些方法</strong> 还没有实现。final表示这是一个最终类，不能被其他的类继承。</p>
</li>
<li><p><em>类是对象的模板，对象是类的实例化</em></p>
</li>
</ul>
<h4 id="一个类可以包含以下类型变量"><a href="#一个类可以包含以下类型变量" class="headerlink" title="一个类可以包含以下类型变量"></a>一个类可以包含以下类型变量</h4><ul>
<li><p>局部变量：在方法、构造方法或者语句块中定义的变量被称为局部变量。变量声明和初始化都是在方法中，方法结束后，变量就会自动销毁。</p>
</li>
<li><p>成员变量：成员变量是定义在类中，方法体之外的变量。这种变量在创建对象的时候实例化。成员变量可以被类中方法、构造方法和特定类的语句块访问。</p>
</li>
<li><p><strong>类变量</strong>：类变量也声明在类中，方法体之外，但必须声明为static类型。（<strong>同C++中的静态变量</strong>）</p>
</li>
<li><p>最终变量：即<strong>常量</strong>，用final关键字修饰。一般与static关键字连用，例如定义PI、E等。</p>
</li>
<li><p>默认构造函数：java的类与C++一样可以写默认构造参数，格式也与C++一样，没有返回值，函数名与类名相同。有参数的构造函数也一样。</p>
</li>
</ul>
<h3 id="源文件声明规则"><a href="#源文件声明规则" class="headerlink" title="源文件声明规则"></a>源文件声明规则</h3><ul>
<li><p>一个源文件中只能有一个public类</p>
</li>
<li><p>一个源文件可以有多个非public类</p>
</li>
<li><p>源文件的名称应该和public类的类名保持一致。例如：源文件中public类的类名是Employee，那么源文件应该命名为Employee.java。</p>
</li>
<li><p>如果一个类定义在某个包中，那么package语句应该在源文件的首行。</p>
</li>
<li><p>如果源文件包含import语句，那么应该放在package语句和类定义之间。如果没有package语句，那么import语句应该在源文件中最前面。<br>import语句和package语句对源文件中定义的所有类都有效。在同一源文件中，不能给不同的类不同的包声明。</p>
</li>
<li><p>类有若干种访问级别，并且类也分不同的类型：抽象类和final类等。这些将在访问控制章节介绍。</p>
</li>
<li><p>除了上面提到的几种类型，Java还有一些特殊的类，如：内部类、匿名类。</p>
</li>
</ul>
<h2 id="对象"><a href="#对象" class="headerlink" title="对象"></a>对象</h2><h3 id="对象的引用"><a href="#对象的引用" class="headerlink" title="对象的引用"></a>对象的引用</h3><ul>
<li><p>创建对象后，可以将对象的引用赋值给其他对象变量。</p>
</li>
<li><p>例如下面的程序</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class test&#123;</span><br><span class="line">	public static void main(String[] args)&#123;</span><br><span class="line">		StringBuffer s;</span><br><span class="line">		s= new StringBuffer(&quot;AAAAA&quot;);</span><br><span class="line">		StringBuffer s1 = s; //此句会使得s1和s指向同一个对象</span><br><span class="line">		s1.append(&quot;BBBB&quot;);</span><br><span class="line">		Syetem.out.println(&quot;s = &quot; + s.toString());</span><br><span class="line">		Syetem.out.println(&quot;s1 = &quot; + s1.toString());</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>输出结果为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">s = AAAAABBBB</span><br><span class="line">s1 = AAAAABBBB</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="方法参数传递"><a href="#方法参数传递" class="headerlink" title="方法参数传递"></a>方法参数传递</h3><h4 id="按值传递-一般的函数参数"><a href="#按值传递-一般的函数参数" class="headerlink" title="按值传递(一般的函数参数)"></a>按值传递(一般的函数参数)</h4><ul>
<li>传入方法中的参数是一份拷贝，对其的修改不会影响其本身<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Printer&#123;</span><br><span class="line">	public void printinfo(String name, int age)&#123;</span><br><span class="line">		age ++;</span><br><span class="line">		System.out.println(&quot;Hello! My name is&quot; + name + &quot;and I&#x27;m &quot; + age);</span><br><span class="line">	&#125;</span><br><span class="line">	public static void main(String args[])&#123;</span><br><span class="line">		Printer p = new Printer();</span><br><span class="line">		int age = 20;</span><br><span class="line">		p.printinfo(&quot;Mike&quot;,age);</span><br><span class="line">		System.out.println(&quot;Now the initial age = &quot; + age);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="按引用传递"><a href="#按引用传递" class="headerlink" title="按引用传递"></a>按引用传递</h4><ul>
<li>调用方法时，<strong>如果传递的是一个对象、接口或数组时</strong>，实际上传递的是对象的、接口或者数组的引用。因此如果方法中对参数进行了修改，那么原来的数据也会受到影响。</li>
</ul>
<h2 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h2><ul>
<li>类的继承格式：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class 父类&#123;</span><br><span class="line">	...</span><br><span class="line">&#125;</span><br><span class="line">class 子类 extends 父类&#123;</span><br><span class="line">	...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="继承的特性"><a href="#继承的特性" class="headerlink" title="继承的特性"></a>继承的特性</h3><ul>
<li><p>子类拥有父类非private的属性，方法。</p>
</li>
<li><p>子类可以拥有自己的属性和方法，即子类可以对父类进行扩展。</p>
</li>
<li><p>子类可以用自己的方式实现父类的方法。</p>
</li>
<li><p>Java的继承是单继承，但是可以多重继承，java的多继承通过接口来实现。</p>
</li>
<li><p><strong>关键字super和this在java的类中分别可以指代父类和类本身。</strong></p>
</li>
</ul>
<h2 id="多继承的实现"><a href="#多继承的实现" class="headerlink" title="多继承的实现"></a>多继承的实现</h2><hr>
<h3 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h3><ul>
<li><p>抽象类的定义：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[访问修饰符] &lt;abstract&gt; class &lt;类名&gt;&#123;</span><br><span class="line">	[成员变量声明]</span><br><span class="line">	[方法定义]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>抽象方法的定义：抽象方法是指只有声明而没有实现方法，用abstract修饰，一般格式如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[访问修饰符] &lt;abstract&gt; 返回类型 方法名(参数列表);</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意抽象类不能定义为final类，因为抽象类不能实例化。抽象方法也不能声明为final方法，因为要在子类中实现该抽象方法。</p>
</blockquote>
</li>
<li><p><strong>抽象类不能被实例化，实例化的工作应该交由它的子类来完成</strong>，它只需要有一个引用即可。</p>
</li>
<li><p>抽象方法必须由子类来进行重写。</p>
</li>
<li><p><strong>只要包含了抽象方法的类，该类必须要定义成抽象类，不管是否还包含有其他已经实现了的方法</strong>。</p>
</li>
<li><p>抽象类中可以包含具体的方法，<strong>当然也可以不包含抽象方法</strong>。</p>
</li>
<li><p>子类中的抽象方法不能与父类的抽象方法同名。</p>
</li>
<li><p>abstract 不能与private、static、final或native并列修饰同一个方法。</p>
</li>
</ul>
<h3 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h3><ul>
<li><p>接口用interface来定义</p>
</li>
<li><p><strong>抽象类是对类抽象，而接口是对行为的抽象</strong>。</p>
</li>
<li><p>接口中可以定义“成员变量”，或者说是不可变的常量，因为接口中的“成员变量”会自动变为为public static final。可以通过类命名直接访问：ImplementClass.name</p>
</li>
<li><p>接口实质也是一个抽象类，但与抽象类不同的是<strong>接口中只能包含常量的定义和方法的声明</strong>。接口只关心功能，而不关心功能的具体实现。</p>
</li>
<li><p><strong>接口中的常量在声明时必须对它赋初始值</strong>。</p>
</li>
<li><p>一般的继承使用extends作为关键字，只能单继承。接口使用implements作为关键字。可以接受多继承。</p>
</li>
<li><p>接口是抽象类的延伸，java了保证数据安全是不能多重继承的，也就是说继承只能存在一个父类，但是接口不同，一个类可以同时实现多个接口，不管这些接口之间有没有关系，所以接口弥补了抽象类不能多重继承的缺陷。</p>
</li>
<li><p>接口是用来建立类与类之间的协议，它所提供的只是一种形式，而没有具体的实现。同时实现该接口的实现类必须要实现该接口的所有方法，通过使用implements关键字，他表示该类在遵循某个或某组特定的接口。</p>
</li>
<li><p>接口的使用格式：<br> <code>[访问修饰符][类型修饰符] class 类名 implements 接口名1,接口名2,......</code></p>
</li>
<li><p>一个类如果继承了某个接口，那么它就要实现这个接口的所有方法，否则这个类要定义为抽象类。</p>
<blockquote>
<p>需要注意的是，接口中的方法默认是public，所以在类中实现接口时，必须使用public来修饰方法。如果接口中某个方法返回为void，则在类中实现该方法时，方法体可以为空，即只有两个大括号。</p>
</blockquote>
</li>
<li><p>接口使用示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">interface Computable&#123;</span><br><span class="line">	final int x = 10;</span><br><span class="line">	int add();</span><br><span class="line">	void point();</span><br><span class="line">&#125;</span><br><span class="line">class TESt implements Computable&#123;</span><br><span class="line">	private int y;</span><br><span class="line">	public TEST(int y)&#123;</span><br><span class="line">		this.y = y;</span><br><span class="line">	&#125;</span><br><span class="line">	public int add()&#123; //实现抽象方法时添加public 和 return 语句</span><br><span class="line">		return x + y;</span><br><span class="line">	&#125;</span><br><span class="line">	public void print()&#123;&#125; //返回类型为void方法体可以留空</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>接口之间也可以继承，用关键字extends，但是一个接口可以同时extends多个接口，之间以逗号间隔。</p>
<blockquote>
<p>如果一个接口继承了别的几个接口，那么实现这个接口的类同时也要实现被这个接口继承的几个接口的所有方法。</p>
</blockquote>
</li>
</ul>
<h2 id="Java中预定义的注释"><a href="#Java中预定义的注释" class="headerlink" title="Java中预定义的注释"></a>Java中预定义的注释</h2><hr>
<ul>
<li><p>形如 <code>@Override</code>等等在类方法之前的修饰就是预定义注释</p>
</li>
<li><p>预定义注释不仅仅是给人看的，编译器也会读取，会有相应的约束</p>
</li>
<li><p>预定义注释一共分为三种：<code>Override、Deprecated和SuppressWarnings </code></p>
</li>
<li><p>我们平常一般 <strong>只会用到第一个</strong>，后两个对于我们没有太多的用处。</p>
</li>
</ul>
<h3 id="Override"><a href="#Override" class="headerlink" title="Override"></a>Override</h3><ul>
<li><p>这个注释的作用是标识某一个方法是否覆盖了它的父类的方法。</p>
</li>
<li><p>如果加了这个标识，那么这个类方法则必须要覆盖父类的同名方法，即被Override注释的方法必须在父类中存在同样的方法程序才能编译通过。</p>
</li>
<li><p>这样做的目的是，防止在子类中覆写父类的方法时，写错方法名导致最后子类的实例对象调用的还是父类的方法，这样就可能产生bug。</p>
</li>
</ul>
<h3 id="Deprecated"><a href="#Deprecated" class="headerlink" title="Deprecated"></a>Deprecated</h3><ul>
<li><p>这个对类方法的注释主要是开发者用的。</p>
</li>
<li><p>这个注释不会产生什么实际的影响，但是被这个注释过的类方法如果被使用到的话，在编译时就会留下一些warning，警告内容是：这个方法现在还能用，但是下面版本的java可能会将它删除，也就是不推荐继续使用这个方法。</p>
</li>
<li><p>出现如下警告：</p>
</li>
</ul>
<blockquote>
<p>   注意：test.java 使用或覆盖了已过时的 API。</p>
</blockquote>
<blockquote>
<p>   注意：要了解详细信息，请使用 -Xlint:deprecation 重新编译</p>
</blockquote>
<blockquote>
<p>   使用-Xlint:deprecation显示更详细的警告信息： …..</p>
</blockquote>
<h3 id="SuppressWarnings"><a href="#SuppressWarnings" class="headerlink" title="SuppressWarnings"></a>SuppressWarnings</h3><ul>
<li><p>这个世界的事物总是成对出现。即然有使编译器产生警告信息的，那么就有抑制编译器产生警告信息的。 SuppressWarnings注释就是为了这样一个目的而存在的。</p>
</li>
<li><p>这个注释可以抑制编译器产生一些警告信息。</p>
</li>
</ul>
<h3 id="用户自定义注释"><a href="#用户自定义注释" class="headerlink" title="用户自定义注释"></a>用户自定义注释</h3><ul>
<li>详细了解——<a href="https://blog.csdn.net/softwave/article/details/6991913">博客</a></li>
</ul>
<br>
> 最后更新于2018.6.15
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu下使用jekyll预览博客</title>
    <url>/2018/07/08/Jekyll-usage/</url>
    <content><![CDATA[<h2 id="环境安装"><a href="#环境安装" class="headerlink" title="环境安装"></a>环境安装</h2><hr>
<p>确保下面环境的齐全：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install ruby</span><br><span class="line">//</span><br><span class="line">gem install jekyll</span><br><span class="line">//</span><br><span class="line">gem install bundler</span><br><span class="line">//</span><br><span class="line">gem install jekyll-paginate</span><br><span class="line">//</span><br><span class="line">gem install jekyll-gist</span><br></pre></td></tr></table></figure>

<ul>
<li><p>如果执行命令的过程中有提示什么什么未安装，按照提示的命令来安装即可。</p>
</li>
<li><p>然后将终端切换到你博客所在的根目录，执行：<code>jekyll serve --watch</code>即可</p>
</li>
<li><p>终端中会告诉你博客使用了本地4000端口，在网页中输入：<code>http://127.0.0.1:4000/ </code>即可预览博客的效果</p>
</li>
<li><p>更新本地内容保存后更新网页即可看到效果</p>
</li>
</ul>
<br>
> 最后更新于2018.7.14
]]></content>
      <categories>
        <category>Personal Blog</category>
      </categories>
      <tags>
        <tag>๑Blog</tag>
      </tags>
  </entry>
  <entry>
    <title>Java学习笔记——基础</title>
    <url>/2018/04/06/Java-learning/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="Java编译环境"><a href="#Java编译环境" class="headerlink" title="Java编译环境"></a>Java编译环境</h2><hr>
<p>编译环境首选JetBrian的<a href="https://www.jetbrains.com/idea/">Intellij IDEA</a>，虽然几乎所有的教材都清一色的使用eclipse，但是jetBrian的社区版Intellij IDEA已经满足了我们的需求，其无与伦比的代码补全功能，清爽的界面会让人眼前一亮。专业版只有公司里才可能会用到其中的一些功能，所以我们也不用担心。</p>
<ul>
<li><p>Ultimate版本免费获取详见——<a href="https://fleschier.github.io/2018/05/jetbrains/">Intellij IDEA Ultimate版本使用</a></p>
</li>
<li><p>使用intellij IDEA 之前需要配置一系列设置。</p>
<ol>
<li>在打开项目之前的那个主界面，在设置里，systemsettings里把reopen previous project勾掉</li>
<li>在project structure里把lib里的东西添加一下(添加需要用到的编程语言的library)，以及设置默认的sdk&#x2F;jdk。(在打开的项目里设置的仅仅对本项目有效)</li>
</ol>
<ul>
<li>ctrl + alt + s 快速打开设置界面。</li>
</ul>
</li>
</ul>
<h2 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h2><hr>
<blockquote>
<p>java与C++一样，需要用分号来分割句子</p>
</blockquote>
<h3 id="第一个JAVA程序：Helloworld"><a href="#第一个JAVA程序：Helloworld" class="headerlink" title="第一个JAVA程序：Helloworld"></a>第一个JAVA程序：Helloworld</h3><ul>
<li>示例<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class HelloWorld &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        System.out.println(&quot;Hello World&quot;); // 打印 Hello World</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="编写Java程序时注意点："><a href="#编写Java程序时注意点：" class="headerlink" title="编写Java程序时注意点："></a>编写Java程序时注意点：</h3><ul>
<li><p>大小写敏感：Java是大小写敏感的，这就意味着标识符Hello与hello是不同的。</p>
</li>
<li><p>类名：对于所有的类来说，类名的首字母应该大写。如果类名由若干单词组成，那么每个单词的首字母应该大写，例如 MyFirstJavaClass 。</p>
</li>
<li><p>方法名：所有的方法名都应该以小写字母开头。如果方法名含有若干单词，则后面的每个单词首字母大写。<br>源文件名：源文件名必须和类名相同。当保存文件的时候，你应该使用类名作为文件名保存（切记Java是大小写敏感的），文件名的后缀为.java。（如果文件名和类名不相同则会导致编译错误）。</p>
</li>
<li><p>主方法入口：所有的Java 程序由<code>public static void main(String[] args)</code>方法开始执行。</p>
</li>
<li><p>java的通配符与C++一样，都是<code>*</code></p>
</li>
<li><p><strong>Java与C++不同的地方是：Java中的整数，浮点数不能与布尔值相互转换，当然也不能用0或非0的的整数代替true或者false。</strong></p>
</li>
</ul>
<h3 id="Java数据默认值"><a href="#Java数据默认值" class="headerlink" title="Java数据默认值"></a>Java数据默认值</h3><ul>
<li>八大基本数据类型的默认值：</li>
</ul>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>默认值</th>
</tr>
</thead>
<tbody><tr>
<td>boolean</td>
<td>false</td>
</tr>
<tr>
<td>char</td>
<td>‘&#x2F;uoooo’(null)</td>
</tr>
<tr>
<td>byte</td>
<td>(byte)0</td>
</tr>
<tr>
<td>short</td>
<td>(short)0</td>
</tr>
<tr>
<td>int</td>
<td>0</td>
</tr>
<tr>
<td>long</td>
<td>0L</td>
</tr>
<tr>
<td>float</td>
<td>0.0f</td>
</tr>
<tr>
<td>double</td>
<td>0.0d</td>
</tr>
</tbody></table>
<h3 id="Java修饰符"><a href="#Java修饰符" class="headerlink" title="Java修饰符"></a>Java修饰符</h3><ul>
<li><p>像其他语言一样，Java可以使用修饰符来修饰类中方法和属性。主要有两类修饰符：</p>
</li>
<li><p>访问控制修饰符 : default, public , protected, private</p>
</li>
<li><p>非访问控制修饰符 : final, abstract, strictfp</p>
</li>
</ul>
<h3 id="Java枚举"><a href="#Java枚举" class="headerlink" title="Java枚举"></a>Java枚举</h3><ul>
<li>枚举限制变量只能是预先设定好的值,使用枚举可以减少代码中的bug<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class ENUM_class&#123;</span><br><span class="line">	enum Test&#123;Small, Medium, Large&#125;</span><br><span class="line">   Test size;</span><br><span class="line">&#125;</span><br><span class="line">public class ENUM_class_Test&#123;</span><br><span class="line">	public static void main(String[] args)&#123;</span><br><span class="line">		ENUM_class tst = new ENUM_class();</span><br><span class="line">		tst.size = ENUM_class.Test.Small;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="Java的三目运算符"><a href="#Java的三目运算符" class="headerlink" title="Java的三目运算符"></a>Java的三目运算符</h3><ul>
<li><p>格式：逻辑表达式 ? 值1 : 值2</p>
</li>
<li><p>执行过程：若逻辑值表达式为true，就取值1，否则取值2</p>
</li>
<li><p>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int x =1,y =2;</span><br><span class="line">x&lt;y ? System.out.println(&quot;x&lt;y&quot;):System.out.println(&quot;x&gt;y&quot;);</span><br></pre></td></tr></table></figure></li>
<li><p>注意，此三目运算符不是一个完整的式子，一般是用作赋值。</p>
</li>
</ul>
<h3 id="Java注释"><a href="#Java注释" class="headerlink" title="Java注释"></a>Java注释</h3><ul>
<li>Java注释与C++一致</li>
</ul>
<h2 id="Java的输入"><a href="#Java的输入" class="headerlink" title="Java的输入"></a>Java的输入</h2><blockquote>
<p>Java的输入比起C++和Python要麻烦很多</p>
</blockquote>
<hr>
<h3 id="方法一："><a href="#方法一：" class="headerlink" title="方法一："></a>方法一：</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import java.io.*   //引入java.io包的类</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">InputStreamReader in = new InputStreamReader(System.in);</span><br><span class="line">BufferedReader br = new BufferedReader(in);</span><br><span class="line">System.out.print(&quot;Please enter a num:&quot;);</span><br><span class="line">String s = br.readLine(); //从键盘读取一行字符到s中</span><br><span class="line">float num;</span><br><span class="line">num = Float.parseFloat(s); //将字符型转换成float型</span><br><span class="line">System.out.println();</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<h3 id="方法二："><a href="#方法二：" class="headerlink" title="方法二："></a>方法二：</h3><h4 id="通过Scannner类"><a href="#通过Scannner类" class="headerlink" title="通过Scannner类"></a>通过Scannner类</h4><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import java.util.*</span><br><span class="line">...</span><br><span class="line">float score;</span><br><span class="line">Scanner sn = new Scanner(System.in);</span><br><span class="line">System.out.println(&quot;Please enter a num:&quot;);</span><br><span class="line">score = sn.nextFloat();</span><br><span class="line">System.out.println();</span><br></pre></td></tr></table></figure>
</li>
<li><p><em>scan在读取多行数据时会遇到的问题：</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Scanner scan = new Scanner(System.in)</span><br><span class="line">int num  = scan.nextInt();  //这句会正确执行</span><br><span class="line">Double d = scan.nextDouble(); //这句也会正确执行，即使输入在不同的行上</span><br><span class="line">String s = scan.nextline();  //这句这样写便会读到一个空串×××××××</span><br></pre></td></tr></table></figure>
</li>
<li><p>原因分析：首先，Scanner是一个扫描器，它扫描数据都是去内存中一块缓冲区中进行扫描并读入数据的，而我们在控制台中输入的数据也都是被先存入缓冲区中等待扫描器的扫描读取。这个扫描器在扫描过程中判断停止的依据就是“空白符”，空格啊，回车啊什么的都算做是空白符。</p>
</li>
<li><p>nextInt()方法在扫描到空白符的时候会将前面的数据读取走，但会丢下空白符“\r”在缓冲区中，但是，nextLine()方法在扫描的时候会将扫描到的空白符一同清理掉。</p>
</li>
<li><p>了解了这两个方法特性和区别，就知道了上边的代码究竟是怎么回事，以及知道了解决的方法。像是上边的代码nextInt()方法之后在缓冲区中留下了“\r”，然后nextLine()方法再去缓冲区找数据的时候首先看到了“\r”，然后就把这个“\r”扫描接收进来，并在缓冲区内清除掉。其实，nextLine()方法是执行过的，并没有不执行。</p>
</li>
<li><p>解决方案：<br> 在调用nextline()之前先调用一次nextline()将那个换行符读取掉</p>
</li>
</ul>
<p> 在读取Int的时候就调用nextline()方法，再用parseInt()转化为Int</p>
<h3 id="方法三"><a href="#方法三" class="headerlink" title="方法三"></a>方法三</h3><ul>
<li>这里需要import java.io.IOException; &#x2F;&#x2F;否则会报错</li>
</ul>
<h4 id="读取下一个字符："><a href="#读取下一个字符：" class="headerlink" title="读取下一个字符："></a>读取下一个字符：</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import java.io.IOException</span><br><span class="line">class test&#123;</span><br><span class="line">    public static void main(String[] args) throws IOException&#123;</span><br><span class="line">      char c = (char) System.in.read();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>注意这里需要类型转换，默认为int类型，即读到的是字符的ASCII码。</strong></p>
<h4 id="将一串字符读入byte数组"><a href="#将一串字符读入byte数组" class="headerlink" title="将一串字符读入byte数组"></a>将一串字符读入byte数组</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import java.io.IOException;</span><br><span class="line">class test &#123;</span><br><span class="line">     public static void main(String[] args) throws IOException&#123;</span><br><span class="line">         byte[] input = new byte[5];  //创建一个byte数组来读取一串输入</span><br><span class="line">         System.in.read(input);  //将输入存入byte数组中</span><br><span class="line">         System.out.println(input[1]); //这里输出的仍然是int类型，即ASCII码</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h3 id="图形界面输入方式"><a href="#图形界面输入方式" class="headerlink" title="图形界面输入方式"></a>图形界面输入方式</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import javax.swing.* //导入javax,swing包中的类</span><br><span class="line">...</span><br><span class="line">String str;</span><br><span class="line">float score;</span><br><span class="line">str = JOptionPane.showInputDialog(&quot;please enter a num:&quot;);</span><br><span class="line">score = Float.parseFloat(str);</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<h2 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h2><hr>
<ul>
<li><p>标准输出：<code>System.out.println();</code></p>
</li>
<li><p>格式化输出：<code>System.out.printf();</code> &#x2F;&#x2F;格式化输出允许对输出进行规改</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//&quot;%&quot;表示进行格式化输出，&quot;%&quot;之后的内容为格式的定义。</span><br><span class="line">System.out.printf(&quot;%f&quot;,d);//&quot;f&quot;表示格式化输出浮点数。</span><br><span class="line">System.out.printf(&quot;%9.2f&quot;,d);//&quot;9.2&quot;中的9表示输出的长度，2表示小数点后的位数。</span><br><span class="line">System.out.printf(&quot;%+9.2f&quot;,d);//&quot;+&quot;表示输出的数带正负号。</span><br><span class="line">System.out.printf(&quot;%-9.4f&quot;,d);//&quot;-&quot;表示输出的数左对齐（默认为右对齐）。</span><br><span class="line">System.out.printf(&quot;%+-9.3f&quot;,d);//&quot;+-&quot;表示输出的数带正负号且左对齐。</span><br><span class="line">System.out.printf(&quot;%d&quot;,i);//&quot;d&quot;表示输出十进制整数。</span><br><span class="line">System.out.printf(&quot;%o&quot;,i);//&quot;o&quot;表示输出八进制整数。</span><br><span class="line">System.out.printf(&quot;%x&quot;,i);//&quot;d&quot;表示输出十六进制整数。</span><br><span class="line">System.out.printf(&quot;%#x&quot;,i);//&quot;d&quot;表示输出带有十六进制标志的整数。</span><br><span class="line">System.out.printf(&quot;%s&quot;,s);//&quot;d&quot;表示输出字符串。</span><br><span class="line">System.out.printf(&quot;输出一个浮点数：%f，一个整数：%d，一个字符串：%s&quot;,d,i,s);</span><br><span class="line">//可以输出多个变量，注意顺序。</span><br><span class="line">System.out.printf(&quot;字符串：%2$s，%1$d的十六进制数：%1$#x&quot;,i,s);</span><br><span class="line">//&quot;X$&quot;表示第几个变量。</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="图形界面输出"><a href="#图形界面输出" class="headerlink" title="图形界面输出"></a>图形界面输出</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JOptionPane.showMessageDialog(....)  //具体参数见类库</span><br></pre></td></tr></table></figure>

<h2 id="一些注意点"><a href="#一些注意点" class="headerlink" title="一些注意点"></a>一些注意点</h2><hr>
<h3 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h3><ul>
<li><p>数组的声明格式： 数据类型[] 数组名 或者 数据类型 数组名[]</p>
</li>
<li><p>Java的数组声明只是指明了一个对象引用，并没有为数组元素分配内存空间。<strong>因此声明不能指定数组元素个数</strong>。例如：<code>int a[10];</code>是错误的。**如果要声明并创建数组应该采用如下格式 ： 数据类型 数组名[] &#x3D; new 数据类型[数组长度]**。</p>
</li>
<li><p>数组的初始化与C++相同。例：<code>int a[] = &#123;1,6,8&#125;;</code>  这句相当于<code>int a = new int[3]; a[0]=1,a[1]=6,a[2]=8;</code></p>
</li>
<li><p>只进行了声明的数组，它的元素是不能被访问的，只有经过初始化后，才能访问数组的元素。</p>
</li>
<li><p>数组的长度的获取：<code>a.length</code> 就表示数组a的长度（即a的元素个数）<strong>注意这里的.length是数组的属性（相当于类的尘成员变量）而不是方法，所以a.length()的写法是错误的，他把length当做一个类的方法来调用了</strong></p>
</li>
</ul>
<h3 id="随机数"><a href="#随机数" class="headerlink" title="随机数"></a>随机数</h3><h4 id="Math-random"><a href="#Math-random" class="headerlink" title="Math.random()"></a>Math.random()</h4><ul>
<li><code>int num =(int)(Math.random() * 11);</code>  生成一个范围在[0,1)的随机数。</li>
</ul>
<h4 id="java-util-Random"><a href="#java-util-Random" class="headerlink" title="java.util.Random"></a>java.util.Random</h4><ul>
<li>例：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import Java.util.Random;</span><br><span class="line">Random rand = new Random([数值]);</span><br><span class="line">int randomNum = rand.nextInt() //Random支持的随机值类型包括：boolean, byte, int, long, float, double</span><br><span class="line">int randomNUM2 = rand.nextInt(100); //表示获取[0,100)范围的随机数</span><br><span class="line">...</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="String类"><a href="#String类" class="headerlink" title="String类"></a>String类</h3><ul>
<li>String是常量，对一个String的任何修改都会使java创建一个新的String，而原来的会被当做垃圾回收掉</li>
<li>例如：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String a = &quot;avc&quot;;</span><br><span class="line">a = &quot;bfd&quot;;    //则会创建一个新的string，然后将a链向这个string，原先的string会被抛弃掉</span><br></pre></td></tr></table></figure></li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.4.9</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven构建项目的pom.xml文件详解</title>
    <url>/2018/11/07/Maven-learning-02/</url>
    <content><![CDATA[<h2 id="Maven仓库"><a href="#Maven仓库" class="headerlink" title="Maven仓库"></a>Maven仓库</h2><hr>
<ul>
<li><p>Maven仓库分为本地仓库,中央仓库和远程仓库.</p>
</li>
<li><p>中央仓库可以查询和获取大量常用的库—-<a href="https://search.maven.org/#browse">地址</a></p>
</li>
<li><p>远程仓库是当我们在中央仓库也获取不到需要的库时,才会用到的. 一般也不会用到远程仓库.</p>
</li>
</ul>
<h3 id="仓库搜索顺序"><a href="#仓库搜索顺序" class="headerlink" title="仓库搜索顺序"></a>仓库搜索顺序</h3><ul>
<li>首先,maven会在本地仓库中搜索依赖的库,如果没有就去中央仓库,如果中央仓库也找不到,但是pom文件里设置了远程仓库,则去远程仓库中获取,否则就会报错,找不到依赖的文件.</li>
</ul>
<h2 id="Maven项目的创建"><a href="#Maven项目的创建" class="headerlink" title="Maven项目的创建"></a>Maven项目的创建</h2><hr>
<ul>
<li><p>我们一般使用intellij IDEA来创建maven项目</p>
</li>
<li><p>其中的gourpId一般的命名格式为: 组织名，公司网址的反写 + 项目名称</p>
</li>
</ul>
<h3 id="项目模板"><a href="#项目模板" class="headerlink" title="项目模板"></a>项目模板</h3><ul>
<li><p>Maven 使用 <strong>archetype(原型)</strong> 来创建自定义的项目结构，形成 Maven 项目模板。</p>
</li>
<li><p>我们在intellij IDEA中新建maven项目时,可以选择从创建一个archetype,这样maven会自动为pom.xml文件添加一些dependency</p>
</li>
</ul>
<h2 id="Maven快照-SNAPSHOT"><a href="#Maven快照-SNAPSHOT" class="headerlink" title="Maven快照(SNAPSHOT)"></a>Maven快照(SNAPSHOT)</h2><hr>
<ul>
<li><p>快照是一种特殊的版本，指定了某个当前的开发进度的副本。不同于常规的版本，Maven 每次构建都会在远程仓库中检查新的快照。</p>
<blockquote>
<p>对于版本，如果 Maven 以前下载过指定的版本文件，比如说 data-service:1.0，Maven 将不会再从仓库下载新的可用的 1.0 文件。若要下载更新的代码，data-service 的版本需要升到1.1。<br>快照的情况下，每次快照的更新，Maven 将自动获取最新的快照(data-service:1.0-SNAPSHOT)。</p>
</blockquote>
</li>
<li><p>快照和版本是用于团队开发时的,快照是为了避免频繁地手动更新pom文件,也避免了版本号的滥用.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Maven</tag>
        <tag>๑Apache</tag>
      </tags>
  </entry>
  <entry>
    <title>前端学习——JavaScript</title>
    <url>/2018/07/07/JavaScript/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h2><hr>
<blockquote>
<p>建议把javascript脚本放在 <code>&lt;body&gt; </code>部分的底部。<br>这会提高网页加载速度，因为 HTML 加载不受制于脚本加载。</p>
</blockquote>
<ul>
<li><a href="https://blog.csdn.net/w405722907/article/details/78517757">Js是放在head还是body解析</a></li>
</ul>
<h3 id="嵌入html的javascript写法"><a href="#嵌入html的javascript写法" class="headerlink" title="嵌入html的javascript写法"></a>嵌入html的javascript写法</h3><ul>
<li>JavaScript代码写在<code>&lt;script&gt;</code>和<code>&lt;/script&gt;</code>标签之前。<br><img src="/images/JavaScript/001.png"></li>
<li>其中<code>&lt;script type =&quot;text/javascript&quot;&gt;</code>表示在<code>&lt;script&gt;&lt;/script&gt;</code>之间的是文本类型<code>(text)</code>,javascript是为了告诉浏览器里面的文本是属于JavaScript语言。</li>
</ul>
<h3 id="单独写成文件"><a href="#单独写成文件" class="headerlink" title="单独写成文件"></a>单独写成文件</h3><ul>
<li><p>JavaScript代码只能写在HTML文件中吗?当然不是，我们可以把HTML文件和JS代码分开,并单独创建一个JavaScript文件(简称JS文件),其文件后缀通常为.js，然后将JS代码直接写在JS文件中。</p>
</li>
<li><p><strong>注意:在JS文件中，不需要<code>&lt;script&gt;</code>标签,直接编写JavaScript代码就可以了。</strong></p>
</li>
<li><p>单独写成文件的javascript文件不能单独运行，比如一个叫“script.js”的文件，我们需在HTML中添加如下代码，就可将JS文件嵌入HTML文件中：<code>&lt;script src=&quot;script.js&quot;&gt;&lt;/script&gt;</code></p>
</li>
</ul>
<h4 id="位置"><a href="#位置" class="headerlink" title="位置"></a>位置</h4><ul>
<li>我们可以将JavaScript代码放在html文件中任何位置，但是我们一般放在网页的head或者body部分。</li>
</ul>
<h5 id="放在部分"><a href="#放在部分" class="headerlink" title="放在&lt;head&gt;部分"></a>放在<code>&lt;head&gt;</code>部分</h5><ul>
<li>最常用的方式是在页面中head部分放置<code>&lt;script&gt;</code>元素，浏览器解析head部分就会执行这个代码，然后才解析页面的其余部分。</li>
</ul>
<h5 id="放在部分-1"><a href="#放在部分-1" class="headerlink" title="放在&lt;body&gt;部分"></a>放在<code>&lt;body&gt;</code>部分</h5><ul>
<li><p>JavaScript代码在网页读取到该语句的时候就会执行。</p>
</li>
<li><p><strong>注意: javascript作为一种脚本语言可以放在html页面中任何位置，但是浏览器解释html时是按先后顺序的，所以前面的script就先被执行。比如进行页面显示初始化的js必须放在head里面，因为初始化都要求提前进行（如给页面body设置css等）；而如果是通过事件调用执行的function那么对位置没什么要求的。</strong></p>
</li>
</ul>
<h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><hr>
<ul>
<li><p>一个JavaScript语句以<code>;</code>结尾（虽然也可以不写）</p>
</li>
<li><p>注释：单行注释：<code>//</code>，多行注释<code>/*...*/</code></p>
</li>
</ul>
<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><ul>
<li><p>定义变量使用关键字<code>var 变量名</code></p>
</li>
<li><p>注意JS中变量无需声明类型</p>
</li>
</ul>
<h3 id="判断语句"><a href="#判断语句" class="headerlink" title="判断语句"></a>判断语句</h3><ul>
<li><p>语法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if(条件)</span><br><span class="line">&#123; 条件成立时执行的代码 &#125;</span><br><span class="line">else</span><br><span class="line">&#123; 条件不成立时执行的代码 &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>例子：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script type=&quot;text/javascript&quot;&gt;</span><br><span class="line">   var myage = 18;</span><br><span class="line">   if(myage&gt;=18)  //myage&gt;=18是判断条件</span><br><span class="line">   &#123; document.write(&quot;你是成年人。&quot;);&#125;</span><br><span class="line">   else  //否则年龄小于18</span><br><span class="line">   &#123; document.write(&quot;未满18岁，你不是成年人。&quot;);&#125;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="判断函数"><a href="#判断函数" class="headerlink" title="判断函数"></a>判断函数</h3><ul>
<li><p>语法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function 函数名()</span><br><span class="line">&#123;</span><br><span class="line">     函数代码;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>例子：<br><img src="/images/JavaScript/002.png"></p>
</li>
</ul>
<h4 id="输出内容-document-write"><a href="#输出内容-document-write" class="headerlink" title="输出内容(document.write)"></a>输出内容(document.write)</h4><ul>
<li><p>输出内容可以是双引号括起来的，也可以是变量，多个输出项之间以<code>+</code>连接</p>
</li>
<li><p>例子：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script type=&quot;text/javascript&quot;&gt;</span><br><span class="line">  var mystr=&quot;hello&quot;;</span><br><span class="line">  document.write(mystr+&quot;I love JavaScript&quot;); //多项内容之间用+号连接</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<h5 id="javascript警告-消息对话框"><a href="#javascript警告-消息对话框" class="headerlink" title="javascript警告(消息对话框)"></a>javascript警告(消息对话框)</h5><ul>
<li>语法：<code>alert(字符串或变量)</code></li>
</ul>
<blockquote>
<p>注意：alert弹出一个消息对话框，只有一个确定按钮，多个对话框之间按顺序弹出。</p>
</blockquote>
<h5 id="确认-confirm对话框"><a href="#确认-confirm对话框" class="headerlink" title="确认(confirm对话框)"></a>确认(confirm对话框)</h5><ul>
<li><p>语法：<code>confirm(str);</code></p>
</li>
<li><p>参数说明：</p>
</li>
</ul>
<ol>
<li>str：在消息对话框中要显示的文本</li>
<li>返回值: Boolean值</li>
</ol>
<ul>
<li>返回值：<br>当用户点击”确定”按钮时，返回true ；当用户点击”取消”按钮时，返回false</li>
</ul>
<h5 id="打开新窗口-window-open"><a href="#打开新窗口-window-open" class="headerlink" title="打开新窗口(window.open)"></a>打开新窗口(window.open)</h5><ul>
<li><p>语法：<code>window.open([URL], [窗口名称], [参数字符串])</code></p>
</li>
<li><p>参数说明：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">URL：可选参数，在窗口中要显示网页的网址或路径。如果省略这个参数，或者它的值是空字符串，那么窗口就不显示任何文档。</span><br><span class="line">窗口名称：可选参数，被打开窗口的名称。</span><br><span class="line">    1.该名称由字母、数字和下划线字符组成。</span><br><span class="line">    2.&quot;_top&quot;、&quot;_blank&quot;、&quot;_self&quot;具有特殊意义的名称。</span><br><span class="line">       _blank：在新窗口显示目标网页</span><br><span class="line">       _self：在当前窗口显示目标网页</span><br><span class="line">       _top：框架网页中在上部窗口中显示目标网页</span><br><span class="line">    3.相同 name 的窗口只能创建一个，要想创建多个窗口则 name 不能相同。</span><br><span class="line">   4.name 不能包含有空格。</span><br><span class="line">参数字符串：可选参数，设置窗口参数，各参数用逗号隔开。</span><br></pre></td></tr></table></figure>
<p><img src="/images/JavaScript/003.png"></p>
</li>
</ul>
<blockquote>
<p>注意：所有的参数都要写在一个参数字符串中，中间以逗号间隔，例如 ‘height&#x3D;100.width&#x3D;100’ 这样。</p>
</blockquote>
<ul>
<li><p>可以将窗口对象存储在变量test中：<code>var test = window.open(...);</code></p>
</li>
<li><p>关闭窗口：</p>
</li>
</ul>
<ol>
<li><p><code>window.close();</code>:关闭本窗口</p>
</li>
<li><p><code>&lt;窗口对象&gt;.close();</code>:关闭指定窗口</p>
</li>
</ol>
<h2 id="控制权-DOM操作"><a href="#控制权-DOM操作" class="headerlink" title="控制权(DOM操作)"></a>控制权(DOM操作)</h2><hr>
<ul>
<li><p>文档对象模型DOM（Document Object Model）定义访问和处理HTML文档的标准方法。DOM 将HTML文档呈现为带有元素、属性和文本的树结构（节点树）。</p>
</li>
<li><p>将HTML代码分解为DOM节点层次图:<br><img src="/images/JavaScript/004.png"></p>
</li>
</ul>
<h3 id="HTML文档可以说由节点构成的集合，三种常见的DOM节点"><a href="#HTML文档可以说由节点构成的集合，三种常见的DOM节点" class="headerlink" title="HTML文档可以说由节点构成的集合，三种常见的DOM节点:"></a>HTML文档可以说由节点构成的集合，三种常见的DOM节点:</h3><ul>
<li><p>元素节点：上图中<code>&lt;html&gt;</code>、<code>&lt;body&gt;</code>、<code>&lt;p&gt;</code>等都是元素节点，即标签。</p>
</li>
<li><p>文本节点:向用户展示的内容，如<code>&lt;li&gt;...&lt;/li&gt;</code>中的JavaScript、DOM、CSS等文本。</p>
</li>
<li><p>属性节点:元素属性，如<code>&lt;a&gt;</code>标签的链接属性<code>href=&quot;http://www.imooc.com&quot;</code>。</p>
</li>
<li><p>例子：<code>&lt;a href=&quot;http://www.imooc.com&quot;&gt;JavaScript DOM&lt;/a&gt;</code><br><img src="/images/JavaScript/005.png"></p>
</li>
</ul>
<h3 id="通过ID获取元素"><a href="#通过ID获取元素" class="headerlink" title="通过ID获取元素"></a>通过ID获取元素</h3><ul>
<li><p>网页由标签将信息组织起来，而标签的id属性值是唯一的，就像是每人有一个身份证号一样，只要通过身份证号就可以找到相对应的人。那么在网页中，我们通过id先找到标签，然后进行操作。</p>
</li>
<li><p>语法：<code>document.getElementById(“id”) </code></p>
</li>
<li><p>例子：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE HTML&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;meta http-wquiv=&quot;Content-Type&quot; content=&quot;text/html; charest=gb2312&quot; /&gt;</span><br><span class="line">&lt;title&gt;获取元素&lt;/title&gt;</span><br><span class="line">  &lt;script type=&quot;text/javascript&quot;&gt;</span><br><span class="line">    var mye = document.getElementById(&quot;con&quot;);    //获取元素变量存储在变量mye中</span><br><span class="line">    document.write(mye);                    //输出变量</span><br><span class="line">  &lt;/script&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;h3&gt;Hello&lt;/h3&gt;</span><br><span class="line">    &lt;p id=&quot;con&quot;&gt;I love JavaScript&lt;/p&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<h3 id="innerHTML属性"><a href="#innerHTML属性" class="headerlink" title="innerHTML属性"></a>innerHTML属性</h3><ul>
<li><p>innerHTML 属性用于获取或替换 HTML 元素的内容。</p>
</li>
<li><p>语法：<code>Object.innerHTML</code></p>
</li>
<li><p>例子：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE HTML&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;title&gt;innerHTML&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;p id=&quot;con&quot;&gt;Hello World!&lt;/p&gt;</span><br><span class="line">  &lt;script&gt;</span><br><span class="line">    var mycon = document.getElementById(&quot;con&quot;);</span><br><span class="line">    document.write(&quot;P标签原始内容：&quot; + mycon.innerHTML + “&lt;br&gt;”);</span><br><span class="line">    //输入想要修改的元素内容</span><br><span class="line">    mycon.innerHTML = &quot;New test!&quot;;          //修改P元素内容</span><br><span class="line">    document.write(&quot;P标签修改后的内容：&quot; + mycon.innerHTML);</span><br><span class="line">  &lt;/script&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<h3 id="改变HTML样式"><a href="#改变HTML样式" class="headerlink" title="改变HTML样式"></a>改变HTML样式</h3><ul>
<li><p>HTML DOM 允许 JavaScript 改变 HTML 元素的样式。</p>
</li>
<li><p>语法：<code>Object.style.property=new style;</code></p>
</li>
<li><p>注意:Object是获取的元素对象，如通过<code>document.getElementById(&quot;id&quot;)</code>获取的元素。</p>
</li>
<li><p>基本属性表：</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>backgroundColor</td>
<td>设置元素背景颜色</td>
</tr>
<tr>
<td>height</td>
<td>设置元素高度</td>
</tr>
<tr>
<td>width</td>
<td>设置元素宽度</td>
</tr>
<tr>
<td>color</td>
<td>设置文本颜色</td>
</tr>
<tr>
<td>font</td>
<td>在一行设置所有的字体属性</td>
</tr>
<tr>
<td>fontFamily</td>
<td>设置元素的字体系列</td>
</tr>
<tr>
<td>fontSize</td>
<td>设置元素的字体大小</td>
</tr>
<tr>
<td>…</td>
<td>…</td>
</tr>
</tbody></table>
<ul>
<li><p>CSS的其他的一些样式也可以通过该方法修改</p>
</li>
<li><p>例子：改变<code>&lt;p&gt;</code>元素的样式，将颜色改为红色，字号改为20,背景颜色改为蓝：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;p id=&quot;pcon&quot;&gt;Hello World!&lt;/p&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">   var mychar = document.getElementById(&quot;pcon&quot;);</span><br><span class="line">   mychar.style.color=&quot;red&quot;;</span><br><span class="line">   mychar.style.fontSize=&quot;20&quot;;</span><br><span class="line">   mychar.style.backgroundColor =&quot;blue&quot;;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="显示和隐藏"><a href="#显示和隐藏" class="headerlink" title="显示和隐藏"></a>显示和隐藏</h3><ul>
<li><p>网页中经常会看到显示和隐藏的效果，可通过display属性来设置。</p>
</li>
<li><p>语法：<code>Object.style.display = value</code></p>
</li>
</ul>
<blockquote>
<p>注意这里的Object也是元素对象，可通过document.getElementById(“id”)来获取</p>
</blockquote>
<ul>
<li>value取值：</li>
</ul>
<ol>
<li>none：此元素讲不会被显示(即隐藏)</li>
<li>block：此元素将显示为块级元素</li>
</ol>
<p>例子：<br><img src="/images/JavaScript/006.png"></p>
<h3 id="控制类名"><a href="#控制类名" class="headerlink" title="控制类名"></a>控制类名</h3><ul>
<li><p>className 属性设置或返回元素的class 属性。</p>
</li>
<li><p>语法：<code>object.className = classname</code></p>
</li>
<li><p>作用：</p>
</li>
</ul>
<ol>
<li>获取元素的class属性</li>
<li>为网页内某个元素指定一个CSS样式来更改该元素的外观</li>
</ol>
<ul>
<li>例子：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE HTML&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;meta http-equiv=&quot;Content-Type&quot; content=&quot;text/html;charset=gb2312&quot;&gt;</span><br><span class="line">&lt;title&gt;className属性&lt;/title&gt;</span><br><span class="line">&lt;style type=&quot;text/css&quot;&gt;</span><br><span class="line">  input&#123;</span><br><span class="line">    font-size:10px;</span><br><span class="line">  &#125;</span><br><span class="line">  .one&#123;</span><br><span class="line">    width:200px;</span><br><span class="line">    background-color:#CCC; //灰色</span><br><span class="line">  &#125;</span><br><span class="line">  .two&#123;</span><br><span class="line">    font-size:18px;</span><br><span class="line">    color:#F00</span><br><span class="line">  &#125;</span><br><span class="line">&lt;/style&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;p id=&quot;con&quot; class=&quot;one&quot;&gt;JavaScript&lt;/p&gt;</span><br><span class="line">  &lt;form&gt;</span><br><span class="line">    &lt;input type=&quot;button&quot; value=&quot;点击更改&quot; onclick=&quot;modifyclass()&quot;/&gt;</span><br><span class="line">  &lt;/form&gt;</span><br><span class="line">  &lt;script type=&quot;text/javascript&quot;&gt;</span><br><span class="line">    var mychar=document.getElementById(&quot;con&quot;);</span><br><span class="line">    document.write(&quot;p元素的Class值为：&quot; + mychar.className + &quot;&lt;br&gt;&quot;); //输出P元素的class属性</span><br><span class="line">    function modifyclass()&#123;</span><br><span class="line">      mychar.className=&quot;two&quot;; //改变className,即更换用来修饰的class</span><br><span class="line">    &#125;</span><br><span class="line">    &lt;/script&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<ul>
<li>结果：</li>
</ul>
<p><img src="/images/JavaScript/007.png"></p>
<h2 id="进阶"><a href="#进阶" class="headerlink" title="进阶"></a>进阶</h2><hr>
<ul>
<li>循环、分支、判断等语法与C++一致</li>
</ul>
<h3 id="事件响应"><a href="#事件响应" class="headerlink" title="事件响应"></a>事件响应</h3><ul>
<li>JavaScript 创建动态页面。事件是可以被 JavaScript 侦测到的行为。 网页中的每个元素都可以产生某些可以触发 JavaScript 函数或程序的事件。</li>
</ul>
<table>
<thead>
<tr>
<th>事件</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>onclick</td>
<td>鼠标单击事件</td>
</tr>
<tr>
<td>onmouseover</td>
<td>鼠标经过事件</td>
</tr>
<tr>
<td>onmouseout</td>
<td>鼠标移开事件</td>
</tr>
<tr>
<td>onchange</td>
<td>文本框内容改变事件</td>
</tr>
<tr>
<td>onselect</td>
<td>文本框内容被选中事件</td>
</tr>
<tr>
<td>onfocus</td>
<td>光标聚集</td>
</tr>
<tr>
<td>onblur</td>
<td>光标移开</td>
</tr>
<tr>
<td>onload</td>
<td>网页导入</td>
</tr>
<tr>
<td>onunload</td>
<td>关闭网页</td>
</tr>
</tbody></table>
<h4 id="鼠标单击事件"><a href="#鼠标单击事件" class="headerlink" title="鼠标单击事件"></a>鼠标单击事件</h4><ul>
<li><p>onclick是鼠标单击事件，当在网页上单击鼠标时，就会发生该事件。同时onclick事件调用的程序块就会被执行，通常与按钮一起使用。</p>
</li>
<li><p>例如：单击按钮时出发onclick事件，调用add2()函数</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;form&gt;</span><br><span class="line">     &lt;input name=&quot;button&quot; type=&quot;button&quot; value=&quot;点击提交&quot; onclick=&quot;add2()&quot; /&gt;  //注意最后的斜杠</span><br><span class="line">  &lt;/form&gt;</span><br></pre></td></tr></table></figure>

<h4 id="鼠标经过事件"><a href="#鼠标经过事件" class="headerlink" title="鼠标经过事件"></a>鼠标经过事件</h4><ul>
<li>与鼠标单击事件调用方法一致</li>
</ul>
<h4 id="其他的事件"><a href="#其他的事件" class="headerlink" title="其他的事件"></a>其他的事件</h4><ul>
<li>除了onload与onunload的使用有一点区别，别的都一致。</li>
</ul>
<h3 id="对象"><a href="#对象" class="headerlink" title="对象"></a>对象</h3><ul>
<li><p>JavaScript 中的所有事物都是对象，如:字符串、数值、数组、函数等，每个对象带有属性和方法。</p>
</li>
<li><p>创建对象使用<code>new</code>关键字</p>
</li>
</ul>
<h4 id="日期对象"><a href="#日期对象" class="headerlink" title="日期对象"></a>日期对象</h4><ul>
<li><p>日期对象可以储存任意一个日期，并且可以精确到毫秒数（1&#x2F;1000 秒）</p>
</li>
<li><p>定义一个时间对象：<code>var Udate = new Date();</code></p>
<blockquote>
<p>这是默认定义，初始值为当前电脑时间</p>
</blockquote>
</li>
<li><p>如果要自定义值，使用以下方法：<code>var DATE = new Date(2018,10,4); //2018年10月4日</code></p>
</li>
</ul>
<h4 id="浏览器对象"><a href="#浏览器对象" class="headerlink" title="浏览器对象"></a>浏览器对象</h4><h4 id="window对象"><a href="#window对象" class="headerlink" title="window对象"></a>window对象</h4><ul>
<li><p>window对象是BOM的核心，window对象指当前的浏览器窗口。</p>
</li>
<li><p>window对象方法：</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>alert()</td>
<td>显示带有一段消息和一个确认按钮的警告框</td>
</tr>
<tr>
<td>prompt()</td>
<td>显示可提示用户输入的对话框</td>
</tr>
<tr>
<td>confirm()</td>
<td>显示带有一段消息以及确认按钮和取消按钮的对话框</td>
</tr>
<tr>
<td>open()</td>
<td>打开一个新的浏览器窗口或者查找一个已经命名的窗口</td>
</tr>
<tr>
<td>close()</td>
<td>关闭浏览器窗口</td>
</tr>
<tr>
<td>print()</td>
<td>打印当前窗口的内容</td>
</tr>
<tr>
<td>focus()</td>
<td>把键盘焦点给予一个窗口</td>
</tr>
<tr>
<td>blur()</td>
<td>把键盘焦点从顶层窗口移开</td>
</tr>
<tr>
<td>moveBy()</td>
<td>可相对窗口的当前坐标把它移动指定的像素</td>
</tr>
<tr>
<td>moveTo()</td>
<td>把窗口的左上角移动到一个指定的坐标</td>
</tr>
<tr>
<td>resizeBy()</td>
<td>按照</td>
</tr>
<tr>
<td>resizeTo()</td>
<td>把窗口的大小调整到指定的宽度和高度</td>
</tr>
<tr>
<td>scrollBy()</td>
<td>按照指定的像素值来滚动内容</td>
</tr>
<tr>
<td>scrollTo()</td>
<td>把内容滚动到指定的坐标</td>
</tr>
<tr>
<td>setInterval()</td>
<td>每隔指定的时间执行代码</td>
</tr>
<tr>
<td>setTimeout()</td>
<td>在指定的延迟时间后来执行代码</td>
</tr>
<tr>
<td>clearInterval()</td>
<td>取消setInterval()的设置</td>
</tr>
<tr>
<td>clearTimeout()</td>
<td>取消setTimeout()的设置</td>
</tr>
</tbody></table>
<ul>
<li>调用这些方法格式：<code>window.方法名(参数名);</code></li>
</ul>
<p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function myFunction()</span><br><span class="line">&#123;</span><br><span class="line">    alert(&quot;欢迎&quot;)</span><br><span class="line">    window.open(&#x27;http://www.imooc.com&#x27;,&#x27;_blank&#x27;,&#x27;width=600 height=400&#x27;)</span><br><span class="line">    //第一个参数表示要打开的网页，&#x27;_blank&#x27;参数表示打开一个新网页，后面的参数设置新窗口的初始大小</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="JavaScript计时器"><a href="#JavaScript计时器" class="headerlink" title="JavaScript计时器"></a>JavaScript计时器</h4><ul>
<li><p>在JavaScript中，我们可以在设定的时间间隔之后来执行代码，而不是在函数被调用后立即执行。</p>
</li>
<li><p>计时器类型：</p>
<ol>
<li><p>一次性计时器：仅在指定的延迟时间之后触发一次。(setTimeout())</p>
</li>
<li><p>间隔性触发计时器：每隔一定的时间间隔就触发一次。(setInterval())</p>
</li>
</ol>
</li>
<li><p>setInterval()方法格式：<code>setInterval(代码，交互时间)</code></p>
<blockquote>
<p>参数中，代码就是要执行的函数挥着代码串，交互时间就是执行的时间间隔，以毫秒为单位</p>
</blockquote>
</li>
<li><p>setInterval的返回值可以传递给clearInterval从而取消对代码的周期性执行。</p>
</li>
<li><p>假设有一个clock()函数，则调用格式为：</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">setInterval(&quot;clock()&quot;,1000)</span><br><span class="line">或者</span><br><span class="line">setInterval(clock,1000)</span><br></pre></td></tr></table></figure>

<h2 id="DOM对象"><a href="#DOM对象" class="headerlink" title="DOM对象"></a>DOM对象</h2><hr>
<h3 id="getElementsByName方法"><a href="#getElementsByName方法" class="headerlink" title="getElementsByName方法"></a>getElementsByName方法</h3><ul>
<li><p>返回带有指定名称的节点对象的集合</p>
</li>
<li><p>语法：<code>document.getElementsByName(name)</code></p>
<blockquote>
<p>其中name参数是标签上设置的name属性，和设置id属性格式一样，但是不同的标签可以设置同一个name属性，而id属性必须唯一</p>
</blockquote>
</li>
<li><p>与getElementById()不同的是，通过元素的name属性查询元素而不是通过id属性</p>
</li>
<li><p>注意：因为文档中的name属性可能不唯一，所有的getElementByName()方法的返回值是元素的数组，而不是一个元素</p>
</li>
<li><p>和数组类似的也有length属性，可以和访问数组一样的方法来访问，从0开始</p>
</li>
</ul>
<h2 id="一些报错以及解决"><a href="#一些报错以及解决" class="headerlink" title="一些报错以及解决"></a>一些报错以及解决</h2><hr>
<h3 id="jQuery报错：Uncaught-ReferenceError-is-not-defined"><a href="#jQuery报错：Uncaught-ReferenceError-is-not-defined" class="headerlink" title="jQuery报错：Uncaught ReferenceError: $ is not defined"></a>jQuery报错：Uncaught ReferenceError: $ is not defined</h3><ul>
<li>出现这个报错的原因：</li>
</ul>
<ol>
<li><p>jQuery库文件的路径不对，检查文件路径是否正确一般就能解决该错误。</p>
</li>
<li><p>如果库文件的路径是正确的，那么可能在html中加载jQuery库文件顺序有误，如果将jQuery库文件加载放到最开始位置，即可以解决该错误</p>
</li>
<li><p>检查传值错误，或者某些值不存在。</p>
</li>
</ol>
<br>
> 最后更新于2018.7.18
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑JavaScrpt</tag>
      </tags>
  </entry>
  <entry>
    <title>Node.js学习笔记</title>
    <url>/2018/05/21/NodeJS/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<ul>
<li><strong>javascript每个语句结尾不要忘了分号</strong></li>
</ul>
<h2 id="Ubuntu-下的node-js环境搭建"><a href="#Ubuntu-下的node-js环境搭建" class="headerlink" title="Ubuntu 下的node.js环境搭建"></a>Ubuntu 下的node.js环境搭建</h2><hr>
<ul>
<li><p>首先去nodejs的官网下载最新的版本，解压缩。然后移动到 <code>/opt/</code>目录下 (纯粹个人习惯)</p>
</li>
<li><p>然后编辑路径：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo su</span><br><span class="line">vim /etc/profile</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后在最后插入:(具体路径以实际为准)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export NODEJS_HOME=/opt/node-v8.11.2-linux-x64</span><br><span class="line">export PATH=$NODEJS_HOME/bin:$PATH</span><br></pre></td></tr></table></figure>
</li>
<li><p>最后执行<code>source /etc/profile</code> 或者重启电脑即可使之生效。</p>
</li>
<li><p>终端输入：<code>node -v</code>查看，如果能正确现实版本号，则说明配置成功。</p>
</li>
</ul>
<h2 id="Hello-world"><a href="#Hello-world" class="headerlink" title="Hello world"></a>Hello world</h2><hr>
<ul>
<li><p>在常用目录下新建一个<code>HelloWorld.js</code>纯文本文件，在其中写入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;use strict&#x27;;</span><br><span class="line">console.log(&#x27;Hello, world.&#x27;);</span><br></pre></td></tr></table></figure>
</li>
<li><p>第一行总是写上 <code>&#39;use strict&#39;;</code> 是因为我们总是以严格模式运行JavaScript代码，避免各种潜在陷阱。</p>
</li>
<li><p>保存退出，执行 <code>node HellowWorld.js</code>即可得到结果。(终端输出“Hello,world.”)</p>
</li>
<li><p>在交互模式下，单行命令一般不用加分号(有时候加分号还会报错)</p>
</li>
</ul>
<h2 id="交互模式"><a href="#交互模式" class="headerlink" title="交互模式"></a>交互模式</h2><hr>
<ul>
<li><p>在终端输入 <code>node</code> 即可进入 node.js的交互环境，我们可以直接输入javascript代码并直接执行。</p>
</li>
<li><p>进入交互模式实际上是进入了node的解释器，交互模式下node会直接打印每一个语句的结果，而写在js文件里的代码想要输出结果，必须自己用console.log()打印出来。(这与python，scala的命令行解释器特性一致)</p>
</li>
</ul>
<h2 id="使用严格模式"><a href="#使用严格模式" class="headerlink" title="使用严格模式"></a>使用严格模式</h2><hr>
<ul>
<li>如果在JavaScript文件开头写上<code>&#39;use strict&#39;</code>;，那么Node在执行该JavaScript时将使用严格模式。但是，在服务器环境下，如果有很多JavaScript文件，每个文件都写上<code>&#39;use strict&#39;</code>;很麻烦。我们可以给Nodejs传递一个参数，让Node直接为所有js文件开启严格模式：<code>node --use_strict XXX.js</code></li>
</ul>
<h2 id="IDE开发环境"><a href="#IDE开发环境" class="headerlink" title="IDE开发环境"></a>IDE开发环境</h2><hr>
<h3 id="在集成开发环境intellij-IDEA下使用nodejs"><a href="#在集成开发环境intellij-IDEA下使用nodejs" class="headerlink" title="在集成开发环境intellij IDEA下使用nodejs"></a>在集成开发环境intellij IDEA下使用nodejs</h3><ul>
<li><p>我们可以尝试获取免费的注册码。</p>
</li>
<li><p>上述注册码的<a href="http://idea.lanyus.com/">获取地址</a></p>
</li>
</ul>
<h4 id="hosts-文件路径"><a href="#hosts-文件路径" class="headerlink" title="hosts 文件路径"></a>hosts 文件路径</h4><ul>
<li><p>在 iOS 系统中中，hosts文件的位置为：<code>~/private/etc</code></p>
</li>
<li><p>在 Windows 系统中，hosts文件的位置为：<code>C:\Windows\System32\drivers\etc</code></p>
</li>
<li><p>在 Ubuntu 系统中，hosts文件的位置为：<code>/etc</code></p>
</li>
</ul>
<h2 id="jetbrains学生免费账号申请"><a href="#jetbrains学生免费账号申请" class="headerlink" title="jetbrains学生免费账号申请"></a>jetbrains学生免费账号申请</h2><hr>
<ul>
<li><p><a href="https://sales.jetbrains.com/hc/zh-cn/articles/207154369-%E5%AD%A6%E7%94%9F%E6%8E%88%E6%9D%83%E7%94%B3%E8%AF%B7%E6%96%B9%E5%BC%8F">Jetbrains学生免费账号申请</a></p>
</li>
<li><p>申请流程其实很简单，你需要一个你们学校的邮箱即可。然后验证通过之后会发送验证邮件，依次填写并且最终注册完账号即可激活。<strong>有效期至毕业。</strong></p>
</li>
<li><p>成功激活：</p>
</li>
</ul>
<p><img src="/images/node.js/info.png"></p>
<h2 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h2><hr>
<ul>
<li><p>在Node环境中，一个.js文件就称之为一个模块（module）。</p>
</li>
<li><p>分模块的好处是编写代码不必从零开始。当一个模块编写完毕，就可以被其他地方引用。我们在编写程序的时候，也经常引用其他模块，包括Node内置的模块和来自第三方的模块。</p>
</li>
<li><p>例如，我们在一个js文件(hello.js)中写一个函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;use strict&#x27;;</span><br><span class="line">var s = &#x27;Hello&#x27;;</span><br><span class="line">function greet(name) &#123;</span><br><span class="line">    console.log(s + &#x27;, &#x27; + name + &#x27;!&#x27;);</span><br><span class="line">&#125;</span><br><span class="line">module.exports = greet;</span><br></pre></td></tr></table></figure></li>
<li><p>函数greet()是我们在hello模块中定义的，你可能注意到最后一行是一个奇怪的赋值语句，它的意思是，把函数greet作为模块的输出暴露出去，这样其他模块就可以使用greet函数了。</p>
</li>
<li><p>其他模块对本模块的调用,此处写一个main.js：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;use strict&#x27;;</span><br><span class="line">// 引入hello模块:</span><br><span class="line">var greet = require(&#x27;./hello&#x27;);</span><br><span class="line">var s = &#x27;Michael&#x27;;</span><br><span class="line">greet(s); // Hello, Michael!</span><br></pre></td></tr></table></figure>
</li>
<li><p>引入hello模块用Node提供的require函数,引入的模块作为变量保存在greet变量中。<code>./hello</code>表明hello.js和main.js是在同一个目录下，这里需要写 <strong>相对目录</strong>。</p>
</li>
</ul>
<h3 id="CommonJS规范"><a href="#CommonJS规范" class="headerlink" title="CommonJS规范"></a>CommonJS规范</h3><ul>
<li><p>这个规范保证了各个模块之间的同名变量和函数不会冲突</p>
</li>
<li><p>一个模块想要对外暴露变量（函数也是变量），可以用<code>module.exports = variable;</code>，一个模块要引用其他模块暴露的变量，用<code>var ref = require(&#39;module_name&#39;);</code>就拿到了引用模块的变量。</p>
</li>
<li><p>输出的变量可以是任意对象、函数、数组等等</p>
</li>
<li><p>引入的对象具体是什么，取决于引入模块输出的对象</p>
</li>
<li><p>javascript本身并不支持同名函数或者变量冲突的处理，而node.js实现这一功能的原理是，JavaScript是一个函数式编程语言，node.js将每个模块包装为一个函数，这样这个模块里的变量就都成为了局部变量，这样就避免了冲突。——<a href="https://www.liaoxuefeng.com/wiki/001434446689867b27157e896e74d51a89c25cc8b43bdb3000/001434502419592fd80bbb0613a42118ccab9435af408fd000">详细教程</a></p>
</li>
</ul>
<br>
> 最后更新于2018.5.24
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑JS</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven构建项目的pom.xml主要标签解释</title>
    <url>/2018/11/07/Maven-learning/</url>
    <content><![CDATA[<h2 id="Maven-pom-xml文件标签详解"><a href="#Maven-pom-xml文件标签详解" class="headerlink" title="Maven pom.xml文件标签详解"></a>Maven pom.xml文件标签详解</h2><hr>
<blockquote>
<p><a href="http://www.runoob.com/maven/maven-pom.html">菜鸟教程-maven标签详解</a>,为了方便记录到自己的博客下.</p>
</blockquote>
<h2 id="记录"><a href="#记录" class="headerlink" title="记录"></a>记录</h2><hr>
<table>
<thead>
<tr>
<th>标签</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>project</td>
<td>工程的根标签</td>
</tr>
<tr>
<td>modelVersion</td>
<td>模型版本,一般设为4.0</td>
</tr>
<tr>
<td>groupId</td>
<td>工程组的标志,一个项目或者一个组织一般是唯一的</td>
</tr>
<tr>
<td>artifaciId</td>
<td>工程标识,是工程的名称</td>
</tr>
<tr>
<td>version</td>
<td>工程版本号,在artifact的仓库中用来区分不同的版本</td>
</tr>
<tr>
<td>build</td>
<td>该元素设置了项目脚本的源码目录,当构建项目的时候,构建系统会编译目录里的源码,该路径相对于pom.xml是相对路径</td>
</tr>
<tr>
<td>dependencies</td>
<td>该元素描述了项目相关的所有依赖。 这些依赖组成了项目构建过程中的一个个环节。它们自动从项目定义的仓库中下载。</td>
</tr>
<tr>
<td>dependency</td>
<td>上个标签的子目录,一个dependency包含一个依赖</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Maven</tag>
        <tag>๑Apache</tag>
      </tags>
  </entry>
  <entry>
    <title>Python学习笔记——进阶</title>
    <url>/2018/04/29/Python-superior/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="格式化输出方法"><a href="#格式化输出方法" class="headerlink" title="格式化输出方法"></a>格式化输出方法</h2><hr>
<ul>
<li><p>格式：<code>格式字符串.format(输出1, 输出2, ...输出n)</code></p>
</li>
<li><p>格式说明符使用大括号括起来，一般格式如下：<code>&#123;[序号或键]:格式说明符&#125;</code></p>
<blockquote>
<p>其中可选序号对应于后面的输出项，从0开始</p>
</blockquote>
</li>
<li><p>例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; &quot;&#123;0:.2f&#125;,&#123;1&#125;&quot;.format(3.145,500)  .2f表示输出的是浮点数，小数部分占两位，整数部分默认(.2f或者0.2f)</span><br><span class="line">&gt;&gt;&gt; &quot;3.15,500&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>注意前面格式字符串的引号不能少，<strong>如果省略序号，则Python会按顺序匹配后面的输出项</strong>，同时 <strong>格式字符串里除了大括号里面的内容是换成后面的对应的输出项，其余的内容会原样输出</strong>，比如<code>&quot;I\&#39;m &#123;&#125;，and &#123;&#125;&quot;.format(&quot;Jake&quot;,&quot;Hello!&quot;)</code> 会输出 <code>I&#39;m jake, and Hello!</code></p>
</li>
</ul>
<h3 id="一些例子"><a href="#一些例子" class="headerlink" title="一些例子"></a>一些例子</h3><ul>
<li><p><code>print(&quot;&#123;0:&lt;15&#125;&quot;).format(12345) </code>：左对齐  </p>
</li>
<li><p><code>print(&quot;&#123;0:&gt;15&#125;&quot;.format(12345))</code>：右对齐</p>
</li>
<li><p><code>print(&quot;&#123;0:*^15&#125;&quot;.format(12345))</code>：中间对齐，并且用 <code>*</code> 补齐空格</p>
</li>
<li><p><code>print(&quot;&#123;0:10b&#125;&quot;.format(65))</code>：以二进制输出</p>
</li>
<li><p><code>print(&quot;&#123;0:10o&#125;&quot;.format(65))</code>：以八进制输出</p>
</li>
<li><p><code>print(&quot;&#123;0:10x&#125;&quot;.format(65))</code>：以十六进制输出</p>
</li>
</ul>
<h2 id="常用模块"><a href="#常用模块" class="headerlink" title="常用模块"></a>常用模块</h2><hr>
<ul>
<li><p>使用方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import 模块名  #引入整个模块</span><br><span class="line">from 模块名 import 函数名  //引入某个函数</span><br><span class="line">from 模块名 import *   //引入所有函数</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果引入多个模块，在使用模块的函数时最好使用 <code>模块名.函数</code> 的格式，防止各模块同名函数之间的冲突。</p>
</li>
</ul>
<h3 id="math模块常用函数"><a href="#math模块常用函数" class="headerlink" title="math模块常用函数"></a>math模块常用函数</h3><ul>
<li><p><code>e</code> ：返回常数e(自然对数的底)</p>
</li>
<li><p><code>pi</code>： 返回圆周率π</p>
</li>
<li><p><code>fabs(x)</code>： 返回x的绝对值，返回值为浮点数   #注：<code>abs(x)</code>为Python内置函数，返回值与参数一致，且abs()可以用来求解复数</p>
</li>
<li><p><code>sqrt(x)</code>：返回X的平方根(x &gt; 0)</p>
</li>
<li><p><code>pow(x,y)</code>：返回x的y次方，返回值为浮点型</p>
</li>
<li><p><code>log(x,[base])</code>：返回x以base为底的对数，默认底为e</p>
</li>
<li><p><code>ceil(x)</code>：对x向上取整，例 <code>ceil(4.1)</code> 返回5</p>
</li>
<li><p><code>floor(x)</code>：对x向下取整。</p>
</li>
</ul>
<h3 id="cmath模块"><a href="#cmath模块" class="headerlink" title="cmath模块"></a>cmath模块</h3><blockquote>
<p>cmath模块与math模块函数类似，但是cmath提供对复数运算的支持</p>
</blockquote>
<ul>
<li><code>complex(x,y)</code>：返回复数 x + yi</li>
</ul>
<h3 id="random模块"><a href="#random模块" class="headerlink" title="random模块"></a>random模块</h3><ul>
<li><p><code>seed([x])</code>：在生成随机数之前必须调用，否则每次生成的随机数都一样。seed()为随机数的生成产生一个种子(实际上就是Python的随机数从这个随机数开始计算)，可以直接无参调用，这样Python会自动设置种子。</p>
</li>
<li><p><code>uniform(a,b)</code>：随机生成一个[a,b]范围内的实数</p>
</li>
<li><p><code>randint(a,b)</code>：随机生成一个[a,b]范围内的整数</p>
</li>
</ul>
<h2 id="一些内置模块的函数"><a href="#一些内置模块的函数" class="headerlink" title="一些内置模块的函数"></a>一些内置模块的函数</h2><hr>
<h3 id="字符相关"><a href="#字符相关" class="headerlink" title="字符相关"></a>字符相关</h3><ul>
<li><p><code>ord(x)</code>：返回x的ASCII码</p>
</li>
<li><p><code>chr(x)</code>：这里x是ASCII码，返回其所对应的字符</p>
</li>
<li><p><code>lower(x)</code>：将字符串中所有的大写变为小写</p>
<blockquote>
<p>#这里的lower()和upper()为方法，使用格式为x.lower()</p>
</blockquote>
</li>
<li><p><code>upper(x)</code>：将字符串中所有小写变为大写</p>
</li>
</ul>
<h3 id="数值运算相关"><a href="#数值运算相关" class="headerlink" title="数值运算相关"></a>数值运算相关</h3><ul>
<li><p><code>pow(x,y[,z])</code>：返回x的y次方，返回值保持x或y的类型。如果加上参数z，则结果再对z求余数，即x^y&#x2F;z</p>
</li>
<li><p><code>abs(x)</code>:见math模块的与fabs()的区别</p>
</li>
</ul>
<h3 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h3><ul>
<li><p><code>int(x)</code>：将x转化为整型 <strong>存为一个拷贝，x本身的值没有变</strong></p>
</li>
<li><p><code>float(x)</code>：将x转化为浮点数</p>
</li>
</ul>
<br>
> 最后更新于2018.4.29
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Python学习笔记——基础</title>
    <url>/2018/04/06/Python-learning/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<ul>
<li><p><strong>Python语句不需要分号分割，但是Python各语句严格按照缩进对其方式运行</strong></p>
</li>
<li><p>与C++一样，可以将一些非空，非零的值作为bool值的真，其余的为假</p>
</li>
<li><p>Python中以 <code>#</code>作为注释的开始，多行注释可以用三引号对。</p>
</li>
</ul>
<h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><hr>
<ul>
<li><p>Python推荐使用jetbrain公司的pycharm</p>
</li>
<li><p>Python可以直接使用自带的IDE逐句执行，方便测试</p>
</li>
</ul>
<h2 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h2><hr>
<ul>
<li><p><strong>Python中不需要声明变量类型，编译器会根据赋值自动判别，但是作为程序的编写者，我们需要清楚某个变量究竟是什么类型的</strong></p>
</li>
<li><p>Python中单独的下划线是特殊的变量，表示上一次运算的结果。</p>
</li>
</ul>
<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><ul>
<li><p>标准输出格式： <code>print([输出1，输出2,....,输出n][,sep = 分隔符][,end = 结束符])</code></p>
<blockquote>
<p>默认以空格分割，换行符结尾</p>
</blockquote>
</li>
<li><p>各个输出之间以逗号间隔</p>
</li>
<li><p><code>print()</code>函数不写参数即默认输出一个空行。在一批数据输出结束后，好的编程习惯是在末尾加上一个<code>print()</code>，使得输出美观。</p>
</li>
<li><p>Python的输出可以有运算在里面，例如：<code>print(5 * &#39;*&#39;)</code> 则会打印出 <code>*****</code></p>
</li>
<li><p>在输出字符串时，可以在字符串的引号前加一个 <code>r</code> 使得转义字符 <code>\</code> 失效。</p>
</li>
</ul>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><ul>
<li><p>相比较于java，Python的输入可以说是非常灵活而且简便了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a = input()   #若调用此函数，就算输入的是数字，a的数据类型仍然为str，Python默认将命令行读入的数据作为str类型，因为str是万能的~</span><br></pre></td></tr></table></figure>
</li>
<li><p>但是，对于如下的情况：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a =eval(input())    #eval() 函数用来执行一个字符串表达式，并返回表达式的值.例如 eval(&#x27;2 * 3&#x27;)返回 6</span><br><span class="line">#此时只能输入数字，且a是整型还是浮点型由输入数据的特征决定</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果想要同时为多个变量赋值，则只能使用<code>eval()</code>函数了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">x,y = eval(input())  #输入时以逗号间隔</span><br></pre></td></tr></table></figure></li>
<li><p>对于eval()函数的使用，一定要确保参数去掉引号之后的值是有意义的</p>
</li>
</ul>
<h2 id="一些数据类型"><a href="#一些数据类型" class="headerlink" title="一些数据类型"></a>一些数据类型</h2><h3 id="综述一些注意点"><a href="#综述一些注意点" class="headerlink" title="综述一些注意点"></a>综述一些注意点</h3><ul>
<li><p>序列的概念：序列是一个排序的项目 <strong>容器</strong>，按 <strong>非负整数索引</strong>。Python提供的内置序列包包括字符串，元组和列表。(序列就是可以通过索引或者切片访问的容器)</p>
</li>
<li><p>所有的序列都是可以迭代的，即可以作为循环的迭代器。</p>
</li>
<li><p>内置函数<code>len()</code>可以将任意一个容器作为参数，并返回该容器中元素的数量</p>
</li>
<li><p>内置函数<code>min()</code>和<code>max()</code>以一个可以比较的非空容器作为参数，返回其最小最大值。(如果给的参数不止一个，则返回最大或最小的 <strong>参数</strong>)</p>
</li>
<li><p>对于字典使用<code>min()</code>和<code>max()</code>则会返回字典最大或最小的 <strong>键</strong>。</p>
</li>
<li><p>关键字 <code>in</code> 可以检查某个元素是否在一个容器中。例如<code>k in D</code>(D是一个字典)则检查k是不是D当中的一个关键字。</p>
</li>
<li><p><code>reversed(seq)</code>函数： seq代表一个可迭代对象，比如列表</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; a = [1,2,3,4,5]</span><br><span class="line">&gt;&gt;&gt; b = reversed(a)  #此时b为一个迭代器</span><br><span class="line">&gt;&gt;&gt; b1 = list(b)    #将这个迭代器转化为列表</span><br><span class="line">&gt;&gt;&gt; b1</span><br><span class="line">&gt;&gt;&gt; [5,4,3,2,1]</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>sum()</code>函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sum(seq, start = m)   #sum()有两个参数，第二个为可选参数</span><br><span class="line"># 返回可迭代对象中的所有项目的和再加上start的值</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="一些基础数据类型的注意点"><a href="#一些基础数据类型的注意点" class="headerlink" title="一些基础数据类型的注意点"></a>一些基础数据类型的注意点</h3><ul>
<li><p>输入一个数，获取其各个位的数值的方法：<strong>注意Python的整除符号与别的编程语言不同</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">假设 x = 123</span><br><span class="line">个位  = x % 10</span><br><span class="line">十位  = x // 10 % 10   (注意，Python与其他编程语言的整除符号不同，比如C++就是 &#x27;/&#x27; 即表示整除，而Python为 &#x27;//&#x27;)</span><br><span class="line">百位 = x //100 % 10</span><br></pre></td></tr></table></figure>
</li>
<li><p>复数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a = complex(2,3)</span><br><span class="line">b = 1 + 2j</span><br><span class="line">c = 3 - 5j</span><br><span class="line">#a,b,c都是复数</span><br><span class="line">#要获取复数的实部和虚部，可使用例如：a.real(a的实部), a.imag(a的虚部)，结果都是浮点型</span><br></pre></td></tr></table></figure></li>
<li><p><code>45e15</code>表示45*10^15，这是科学计数法的写法</p>
</li>
<li><p>要判断两个浮点数是否相等，如果不确定精度的情况下，最好不要使用<code>==</code>来判断。因为可能存在看起来一样的数实际上因为精度不同而被判定为不等。解决的方法是<code>abs(x - y) &lt; 1.0e-15</code>，即是用两数之差的绝对值小于一个很小的数，即可判断两数近似相等。</p>
</li>
<li><p>关于Python中的bool类型，与C++类似，非零数或非空容器为真，0，None和空容器为假</p>
</li>
</ul>
<h3 id="List"><a href="#List" class="headerlink" title="List"></a>List</h3><blockquote>
<p>Python中list里的元素允许是不同的数据类型，这一点完全打破了C和C++体系的传统，所以平常编写程序不推荐这样做。list中也允许某个元素是list或者元组什么的。</p>
</blockquote>
<ul>
<li><p>例： <code>classmates = [&quot;Mike&quot;,&quot;Davaid&quot;]</code></p>
</li>
<li><p><code>len()</code>函数可以获得list中的元素个数</p>
</li>
<li><p>与其他编程语言不同的是，Python的list下标允许负数的index，例如： <code>classmates[-1]</code>就表示这个list的最后一个元素，以此类推。</p>
</li>
<li><p><code>append()</code>方法：在list末尾末尾添加元素：<code>classmates.append(&quot;Alex&quot;)</code></p>
</li>
<li><p>把元素插入到指定的位置，比如索引号为1的位置：<code>classmates.insert(1, &#39;Jack&#39;)</code> 这样Jack的位置索引即为1，原来索引为1的元素及后面的元素依次后移。</p>
</li>
<li><p>删除list末尾的元素，使用<code>.pop()</code>方法，要删除指定位置的元素，使用<code>.pop(i)</code>的方法，i为元素的索引。</p>
</li>
</ul>
<h3 id="tuple"><a href="#tuple" class="headerlink" title="tuple"></a>tuple</h3><ul>
<li><p>元组与；list非常类似，但是tuple一经初始化其内容便不允许修改，不允许插入和删除等操作。</p>
</li>
<li><p>元组一般用来保存那些不允许修改的数据以保障安全。</p>
</li>
<li><p>但是如果tuple中某个元素是list时，这个list中的元素还是可以修改的。</p>
</li>
</ul>
<h3 id="disk"><a href="#disk" class="headerlink" title="disk"></a>disk</h3><blockquote>
<p>disk（字典）是一种键-值对类型的数据结构，在其他语言中会被称为map。dict的key必须为不可变的量</p>
</blockquote>
<blockquote>
<p>注意，Python中的dick类型是以hash方式存储的，读取速度非常快。</p>
</blockquote>
<ul>
<li><p>将数据添加进dick的方式，除了初始化时设定之外，还可以直接给一个键赋值的方式添加，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">d = &#123;&#x27;A&#x27;: 95, &#x27;B&#x27;: 75, &#x27;C&#x27;: 85&#125; #初始化添加元素</span><br><span class="line">d[&#x27;E&#x27;] = 100   #直接赋值添加元素</span><br></pre></td></tr></table></figure>
</li>
<li><p>判断一个key是否存在的方法：</p>
</li>
</ul>
<ol>
<li><code>&quot;Thomas&quot; in d</code>		#若不存在会返回false</li>
<li><code>d.get(&quot;Thomas&quot;)</code>  #若不存在返回null</li>
<li><code>d.get(&quot;Thomas&quot;,-1)</code>  #自己指定返回的值</li>
</ol>
<h3 id="set"><a href="#set" class="headerlink" title="set"></a>set</h3><blockquote>
<p>set和dict类似，也是一组key的集合，但不存储value。由于key不能重复，所以，在set中，没有重复的key。</p>
</blockquote>
<ul>
<li><p>通过<code>.add(key)</code>的方法添加元素。</p>
</li>
<li><p>通过<code>.remove(key)</code>的方法删除元素。</p>
</li>
<li><p>符号 <code>&amp;</code> 和 <code>|</code> 分别表示集合的交和并</p>
</li>
<li><p>list的简单的去重：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; a = [1,2,2,3,4,5,5,6,1]</span><br><span class="line">&gt;&gt;&gt; b= set(a)</span><br><span class="line">&gt;&gt;&gt; b</span><br><span class="line">&gt;&gt;&gt;&#123;1,2,3,4,5,6&#125;</span><br><span class="line">&gt;&gt;&gt;a =list(b)</span><br><span class="line">&gt;&gt;&gt;a</span><br><span class="line">&gt;&gt;&gt;[1,2,3,4,5,6]</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="条件判断"><a href="#条件判断" class="headerlink" title="条件判断"></a>条件判断</h2><ul>
<li><p>Python的if判断语句的判断条件部分不需要用括号括起来，这与其他的大多数语言均不同</p>
</li>
<li><p>格式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if &lt;条件判断1&gt;:</span><br><span class="line">    &lt;执行1&gt;</span><br><span class="line">elif &lt;条件判断2&gt;:</span><br><span class="line">    &lt;执行2&gt;</span><br><span class="line">elif &lt;条件判断3&gt;:</span><br><span class="line">    &lt;执行3&gt;</span><br><span class="line">else:</span><br><span class="line">    &lt;执行4&gt;</span><br></pre></td></tr></table></figure></li>
<li><p>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">age = 20</span><br><span class="line">if age &gt;= 6 and age &lt;= 30:  #多条件判断用 and 链接</span><br><span class="line">    print(&#x27;teenager&#x27;)</span><br><span class="line">elif not (age &gt;= 18):     #not相当于 非，后面的内容用括号括起来</span><br><span class="line">    print(&#x27;adult&#x27;)</span><br><span class="line">else:</span><br><span class="line">    print(&#x27;kid&#x27;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>与input结合（涉及数据类型转换）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">s = input(&#x27;birth: &#x27;)</span><br><span class="line">birth = int(s) 		#如果没有这句转换会报错，因为input进来的是str，不能直接和int比较，因此要先转换为int再比较。</span><br><span class="line">#但是如果此处的输入不是一个数值类型，int函数无法强制转化便会报错。</span><br><span class="line">if birth &lt; 2000:</span><br><span class="line">    print(&#x27;00前&#x27;)</span><br><span class="line">else:</span><br><span class="line">    print(&#x27;00后&#x27;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>利用运算符优先级： z &#x3D; x if x &gt; y else y</p>
</li>
</ul>
<h3 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h3><ul>
<li><p>例（求和）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for x in [1,2,3,4,5,6]:</span><br><span class="line">	sum = sum + x</span><br><span class="line">print(sum)</span><br></pre></td></tr></table></figure></li>
<li><p><code>range()</code>函数： 生成从0 - <strong>输入数值-1</strong> 的一个迭代器，可用作循环条件。例<code>list(range(5))</code>的结果为 <code>[0,1,2,3,4]</code></p>
</li>
<li><p>例求 1- 100 的和：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sum = 0</span><br><span class="line">for x in range(101):</span><br><span class="line">    sum = sum + x</span><br><span class="line">print(sum)</span><br></pre></td></tr></table></figure>
</li>
<li><p>while循环示例：(素数的判断)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import math</span><br><span class="line">m = int(input(&quot;Please enter a num: &quot;))  #待判断的数 m</span><br><span class="line">i,j = 2, int(math.sqrt(m))       #只需要计算 2 到 根号m 范围内是否有m的因数即可，后面的计算均为重复计算</span><br><span class="line">flag = 1                        #素数标志</span><br><span class="line">while i &lt; j and flag == 1:</span><br><span class="line">  if m % 1 == 0</span><br><span class="line">    flag = 0                    #不是素数的标志</span><br><span class="line">  i += 1</span><br><span class="line">if flag and m &gt; 1:              #素数必须大于1</span><br><span class="line">  print(m, &quot;是素数&quot;)</span><br><span class="line">else:</span><br><span class="line">  print(m, &quot;不是素数&quot;)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>while和for语句使用拖尾的else子句</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for ... :</span><br><span class="line">  ...</span><br><span class="line">  ...</span><br><span class="line">else ...   #注意这个else是与for对齐的</span><br></pre></td></tr></table></figure>
<p>这种写法的else子句块会在循环 <strong>正常终止时执行</strong>。意思就是，如果循环中的break语句生效而导致的循环终止，是不会触发这个else子句的执行的。因此这种写法可以 <strong>优雅地判断一个循环是正常结束还是break掉了</strong>。</p>
</li>
<li><p>在循环中使用print()输出时，注意Print之后是否正确地结束循环或者进入下一个输出，否则可能出现重复输出大量重复数据的可能。</p>
</li>
<li><p>求两个数的最大公约数(辗转相除法):</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a,b = eval(input(&quot;请输入两个整数: &quot;))</span><br><span class="line">if a &gt; b : a,b = b,a   #Python中可以简便地交换两个变量的值</span><br><span class="line">r = a % b</span><br><span class="line">while r != 0</span><br><span class="line">  a,b = b,r</span><br><span class="line">  r = a % b</span><br><span class="line">print(&quot;最大公约数是：&quot;,b)</span><br><span class="line">print()</span><br></pre></td></tr></table></figure>
</li>
<li><p>输出[100,1000]以内的所有素数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import math</span><br><span class="line">n = 0</span><br><span class="line">for m in range(101,1000,2)    #直接去掉100和1000，这两个肯定不是素数</span><br><span class="line">  i,j = 2, int(math.sqrt(m))</span><br><span class="line">  while i &lt;= j:</span><br><span class="line">    if not(m % j):      #如果 m % j == 0 就是找到因数了，直接退出这一次循环，进入下一个循环</span><br><span class="line">      break</span><br><span class="line">    else:</span><br><span class="line">      i = i + 1</span><br><span class="line">  else:</span><br><span class="line">    print(m, end = &quot; &quot;)</span><br><span class="line">    n += 1</span><br><span class="line">    if n % 10 == 0: print(&quot;\n&quot;)    #每输出10个数就换行</span><br><span class="line">print()</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><hr>
<h3 id="函数定义的格式"><a href="#函数定义的格式" class="headerlink" title="函数定义的格式"></a>函数定义的格式</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def 函数名([参数1,参数2,参数3...]):   #参数没有参数类型，如果要设置参数的默认值则要满足从右向左的原则</span><br><span class="line">	[函数体]</span><br><span class="line">	[return [返回值]] 		#没有写return或者直接写return都是返回None</span><br></pre></td></tr></table></figure>

<ul>
<li>Python中可以让函数返回一个有多个元素的tuple来实现返回多个值的要求。</li>
</ul>
<h2 id="一些注意点"><a href="#一些注意点" class="headerlink" title="一些注意点"></a>一些注意点</h2><hr>
<ul>
<li><p><code>abs()</code> 函数的返回值与参数一致，而<code>fabs()</code> 的返回值均为浮点型</p>
</li>
<li><p>Python支持连续的不等式，例如<code>a &gt; b &gt; c</code> 等价于 <code>a &gt; b &amp;&amp; b &gt; c</code></p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.4.28</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Python</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库SQL语句</title>
    <url>/2018/04/07/SQL-quary/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<ul>
<li><strong>数据库的命令语句均不区分大小写</strong></li>
</ul>
<h2 id="创建语句"><a href="#创建语句" class="headerlink" title="创建语句"></a>创建语句</h2><hr>
<h3 id="基本创建"><a href="#基本创建" class="headerlink" title="基本创建"></a>基本创建</h3><ul>
<li><p>创建数据库 ：<code>create database XXX</code></p>
</li>
<li><p>带详细参数的数据库创建：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create database School on(</span><br><span class="line">	name = &#x27;School_data&#x27;,</span><br><span class="line">	filename = &#x27;c:\data\School_data.mdf&#x27;,</span><br><span class="line">	size = 5mb,</span><br><span class="line">	filegrowth = 1mb --每次增长的大小</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建表： <code>create table XXX</code></p>
</li>
<li><p>带属性的创建：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create table student(</span><br><span class="line">	Sno char(6) primary key,</span><br><span class="line">	Sname varchar(8),</span><br><span class="line">	Ssex char(2),</span><br><span class="line">	Sage smallint,</span><br><span class="line">	Sdept varchar(15)</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
</li>
<li><p>更加复杂的创建：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create table student(</span><br><span class="line">	Sno char(6) primary key,  --主键</span><br><span class="line">	Sname varchar(8) not null constraint ck_1 unique, --创建时加入约束条件</span><br><span class="line">	Ssex char(2) not null constraint ck_2 check(Ssex in(&#x27;男&#x27;,&#x27;女&#x27;)),</span><br><span class="line">	Sage smallint constraint ck_3 check(sage&gt;16),</span><br><span class="line">	Sdept varchar(15) default &#x27;JSJ&#x27;  --默认值</span><br><span class="line">)</span><br><span class="line">create table course(</span><br><span class="line">	Cno char(6) primary key,</span><br><span class="line">	Cname varchar(20),</span><br><span class="line">	Cpno char(4),</span><br><span class="line">	Ccredit tinyint</span><br><span class="line">)</span><br><span class="line">create table SC(</span><br><span class="line">	Sno char(6) not null, --要在表外添加主键的约束，则这个属性不能为空</span><br><span class="line">	Cno char(6) not null,</span><br><span class="line">	Grade decimal(12,1) constraint ck_6 check(Grade &lt;= 100 and Grade &gt;=0)</span><br><span class="line">	constraint fk_1 foreign key (Sno) references student(sno)</span><br><span class="line">)</span><br><span class="line">alter table SC add constraint PK_SC primary key(Sno,Cno)</span><br></pre></td></tr></table></figure>
<p>check语句是一种约束条件写法，与constraint功能类似</p>
</li>
<li><p>在创建时设置foreign key：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create table sc(</span><br><span class="line">	Sno char(6),</span><br><span class="line">	Cno char(4),</span><br><span class="line">	Grade decimal(12,2),</span><br><span class="line">	primary key(Sno,Cno),</span><br><span class="line">	foreign key(Sno) references student(Sno),</span><br><span class="line">	foreign key(Cno) references course(Cno)</span><br><span class="line">)</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="创建索引"><a href="#创建索引" class="headerlink" title="创建索引"></a>创建索引</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create index Ix_student_sname on student(Sname)</span><br><span class="line">create index Ix_student_sdept on student(Sdept)</span><br><span class="line">create index Ix_sc_cno on SC(Cno)</span><br><span class="line">create index Ix_course_cname on Course(Cname)</span><br><span class="line">sp_help student  --查看索引等信息</span><br><span class="line">drop index student.ix_student_sname    --删除索引</span><br></pre></td></tr></table></figure>

<h3 id="插入数据："><a href="#插入数据：" class="headerlink" title="插入数据："></a>插入数据：</h3><blockquote>
<p>数据顺序和类型要与创建时的定义一致</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">insert into student values(&#x27;5001&#x27;,&#x27;赵强&#x27;,&#x27;男&#x27;,&#x27;20&#x27;,&#x27;SX&#x27;)</span><br><span class="line">insert into student values(&#x27;5002&#x27;,&#x27;李丽华&#x27;,&#x27;女&#x27;,&#x27;21&#x27;,&#x27;JSJ&#x27;)</span><br><span class="line">insert into student values(&#x27;5001&#x27;,&#x27;李静&#x27;,&#x27;女&#x27;,&#x27;20&#x27;,&#x27;SX&#x27;)</span><br></pre></td></tr></table></figure>
<h3 id="将数据从一个表插入另一个表"><a href="#将数据从一个表插入另一个表" class="headerlink" title="将数据从一个表插入另一个表"></a>将数据从一个表插入另一个表</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--批量插入数据(从student 到 sc_name)</span><br><span class="line">insert into sc_name (Sno,Sname,Ssex)</span><br><span class="line">select Sno,Sname,Ssex from student where Sdept = &#x27;SX&#x27;</span><br></pre></td></tr></table></figure>


<h2 id="在表外的更改操作"><a href="#在表外的更改操作" class="headerlink" title="在表外的更改操作"></a>在表外的更改操作</h2><hr>
<h3 id="在表外更改表结构"><a href="#在表外更改表结构" class="headerlink" title="在表外更改表结构"></a>在表外更改表结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create table student(</span><br><span class="line">	Sno char(6),</span><br><span class="line">	Sname varchar(8),</span><br><span class="line">	Ssex char(2),</span><br><span class="line">	Sage smallint,</span><br><span class="line">	Sdept varchar(15)</span><br><span class="line">)</span><br><span class="line">alter table student add address varchar(60) --添加列</span><br><span class="line">alter table student add inDate datetime --添加列，数据类型为datetime</span><br><span class="line">alter table student alter column adress varchar(50)</span><br><span class="line">alter table student drop column inDate</span><br></pre></td></tr></table></figure>

<h3 id="用户自定义完整性约束"><a href="#用户自定义完整性约束" class="headerlink" title="用户自定义完整性约束"></a>用户自定义完整性约束</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">alter table student add constraint cs_1 check(Ssex in (&#x27;男&#x27;,&#x27;女&#x27;))</span><br><span class="line">alter table student add constraint cs_2 check(Sage &gt;16)</span><br><span class="line">alter table Course add constraint cs_3 check(Ccredit in (0,1,2,3,4,5))</span><br><span class="line">alter table Course add constraint cs_4 check(Cno != Cpno)</span><br></pre></td></tr></table></figure>

<ul>
<li>删除约束： <code>alter table student drop constraint cs_1</code></li>
</ul>
<h3 id="更新表数据"><a href="#更新表数据" class="headerlink" title="更新表数据"></a>更新表数据</h3><ul>
<li>使用update和查找条件： <code>update student set Sno = 4018 where Sno = 4003</code></li>
</ul>
<h2 id="获取详细信息"><a href="#获取详细信息" class="headerlink" title="获取详细信息"></a>获取详细信息</h2><hr>
<ul>
<li><p><code>sp_help 表名</code></p>
</li>
<li><p><code>sp_helpdb 数据库名</code></p>
</li>
</ul>
<h2 id="查询语句"><a href="#查询语句" class="headerlink" title="查询语句"></a>查询语句</h2><hr>
<h3 id="基本查询操作"><a href="#基本查询操作" class="headerlink" title="基本查询操作"></a>基本查询操作</h3><ul>
<li><code>select [列名（逗号间隔）或者*] from 表名</code></li>
</ul>
<h3 id="进阶-单表"><a href="#进阶-单表" class="headerlink" title="进阶(单表)"></a>进阶(单表)</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Sname,Sage from student</span><br><span class="line">where Sage &gt;= 19 and Sage &lt;= 21 and Ssex = &#x27;女&#x27; order by Sage desc</span><br><span class="line">-- desc表示降序，asc表示升序</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Ssex from student where Sname like &#x27;_明%&#x27;  </span><br><span class="line">--&#x27;_&#x27;表示一个字符，%表示任意多个字符,这里筛选出名字第二个字为‘明’的人</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Cno from SC where Grade is null</span><br><span class="line">-- 如果用&#x27;= null&#x27;则查询结果为空</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno,Cno,cast((Grade/10) as int)from SC</span><br><span class="line">-- cast(a as int)转换数据类型</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select distinct Sdept from student</span><br><span class="line">-- distinct写在最前面</span><br></pre></td></tr></table></figure>

<h3 id="进阶（统计）"><a href="#进阶（统计）" class="headerlink" title="进阶（统计）"></a>进阶（统计）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select count(*) as number from student</span><br><span class="line">where Sname like &#x27;%明%&#x27;</span><br><span class="line">-- 统计名字有‘明’的人数</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Cno,sum(Grade) as total_grade,avg(Grade) as avg_grade,max(Grade) as max_grade,min(Grade) as min_grade from SC  </span><br><span class="line">group by Cno</span><br><span class="line">order by avg_grade desc</span><br><span class="line">-- sum()求和</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Cno from SC</span><br><span class="line">where Grade &gt;= 85</span><br><span class="line">group by Cno</span><br><span class="line">having count(Sno)&gt;10</span><br><span class="line">-- where 筛选必须在group 之前，group之后可以用having筛选，两者可以并用</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select Sno from SC</span><br><span class="line">where Grade &lt;60  </span><br><span class="line">group by Sno</span><br><span class="line">having count(Cno) &gt; 2</span><br></pre></td></tr></table></figure>
<h3 id="进阶（连接）"><a href="#进阶（连接）" class="headerlink" title="进阶（连接）"></a>进阶（连接）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select distinct Cno from student,SC</span><br><span class="line">where student.Sno = SC.Sno and Sdept = &#x27;JSJ&#x27;</span><br><span class="line">-- 连接要写出连接条件</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 三种写法</span><br><span class="line">select Sname from student,SC</span><br><span class="line">where student.Sno = SC.Sno and Cno = 1002</span><br><span class="line">XXXXXX</span><br><span class="line">select Sname from student</span><br><span class="line">join SC on student.Sno = Sc.Sno</span><br><span class="line">where Cno = 1002             -- JOIN..ON..语句</span><br><span class="line">XXXXXX</span><br><span class="line">select Sname from student</span><br><span class="line">where Sno in(                --嵌套执行</span><br><span class="line">	select Sno from SC where Cno = 1002</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select student.Sno, Grade from student,course,SC</span><br><span class="line">where student.Sno = SC.Sno and course.Cno = SC.Cno and Cname = &#x27;数据库原理&#x27; and Grade &lt;60</span><br><span class="line">--用表名.列名来获取具有相同列名的几个表中某个表的某一列</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 两种写法</span><br><span class="line">select student.Sname from student,SC</span><br><span class="line">where student.Sno = SC.Sno and Grade &gt;= 80 and Cno = 1004</span><br><span class="line">XXXXXX</span><br><span class="line">select Sname from student</span><br><span class="line">where Sno in(                --嵌套写法</span><br><span class="line">	select Sno from SC where Grade &gt; 80 and Cno in(</span><br><span class="line">		select Cno from course where Cname = &#x27;数据库原理&#x27;</span><br><span class="line">	)</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<h3 id=""><a href="#" class="headerlink" title=""></a></h3><h3 id="进阶（嵌套）"><a href="#进阶（嵌套）" class="headerlink" title="进阶（嵌套）"></a>进阶（嵌套）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 找出平均成绩最大的一个（两种写法）</span><br><span class="line">select  top 1 Sno,avg(Grade) as avg_grade from SC</span><br><span class="line">group by Sno</span><br><span class="line">order by avg(Grade) desc</span><br><span class="line">XXXXXX</span><br><span class="line">select Sno,avg(Grade) as avg_grade from SC</span><br><span class="line">group by Sno</span><br><span class="line">having avg(Grade) &gt;= all(select avg(Grade) from SC group by Sno)</span><br></pre></td></tr></table></figure>

<h2 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h2><hr>
<ul>
<li><p>删除表： <code>drop table XXX</code></p>
</li>
<li><p>删除表中的内容： <code>delete from XXX</code></p>
</li>
<li><p>删除数据库： <code>drop database XXX</code></p>
</li>
<li><p>删除依赖： <code>drop constraint XXX</code></p>
</li>
</ul>
<h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><hr>
<h3 id="循环生成数据"><a href="#循环生成数据" class="headerlink" title="循环生成数据"></a>循环生成数据</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--生成大量数据</span><br><span class="line">create procedure usp_makedata as</span><br><span class="line">declare @nCnt int, @sNo char(6), @sname varchar(8)</span><br><span class="line">set @nCnt = 12000 --counter</span><br><span class="line">while @nCnt &lt;999999</span><br><span class="line">begin</span><br><span class="line">	set @nCnt = @nCnt +1</span><br><span class="line">	set @sNo = convert(varchar(6),@nCnt)</span><br><span class="line">	set @sName = &#x27;张&#x27; +@sno</span><br><span class="line">	insert into student (sno,sname,ssex,sage) values (@sno,@sname,&#x27;男&#x27;,20)</span><br><span class="line">end</span><br><span class="line">return</span><br><span class="line">exec usp_makedata	--执行生成过程</span><br></pre></td></tr></table></figure>
<h3 id="测试生成的数据"><a href="#测试生成的数据" class="headerlink" title="测试生成的数据"></a>测试生成的数据</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">--生成测试</span><br><span class="line">create procedure usp_test as</span><br><span class="line">declare @nCount int ,@data int</span><br><span class="line">set @nCount=0</span><br><span class="line">while @nCount &lt;100</span><br><span class="line">begin</span><br><span class="line">	select @data = count(*) from student</span><br><span class="line">	where sname &lt;&#x27;张3800&#x27; or sname &gt;&#x27;张8800&#x27;</span><br><span class="line">	set @nCount =@nCount + 1</span><br><span class="line">end</span><br><span class="line">exec usp_test --执行测试</span><br></pre></td></tr></table></figure>
<h3 id="善后"><a href="#善后" class="headerlink" title="善后"></a>善后</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">drop procedure usp_makedata</span><br><span class="line">drop procedure usp_test</span><br></pre></td></tr></table></figure>
<h2 id="E-R图的画法"><a href="#E-R图的画法" class="headerlink" title="E-R图的画法"></a>E-R图的画法</h2><hr>
<ul>
<li><p>实体型(Entity)：用矩形表示，矩形框内写明实体名</p>
</li>
<li><p>属性(Attribute)：用椭圆形表示，并用无向边将其与相应的实体连接起来</p>
</li>
<li><p>联系(Relationship)：用菱形表示，菱形框内写明联系名，并用无向边分别与有关实体连接起来，同时在无向边旁标上联系的类型(1 : 1，1 : n或m : n)。 (一对一，一对多还是多对多)</p>
</li>
<li><p>图例：<br>![](&#x2F;images&#x2F;SQL&#x2F;E-R graphe.png)</p>
</li>
<li><p>示例：<br><img src="/images/SQL/TIM%E5%9B%BE%E7%89%8720180503210911.jpg"><br>画图时先画实体型和联系，最后添加属性即可。对于那些有公共属性的两个实体型，属性应当连到联系上(菱形上的属性)</p>
</li>
</ul>
<h3 id="从-E-R图转为关系模式："><a href="#从-E-R图转为关系模式：" class="headerlink" title="从 E-R图转为关系模式："></a>从 E-R图转为关系模式：</h3><ul>
<li><p>首先是看有几个实体，每个实体单独存放为一个关系，即表。<strong>每个表的字段包含了这个实体所有的属性，即括号内的内容</strong></p>
</li>
<li><p>然后分析实体间的联系。实体间联系有的要单独存一个表，有的不要。</p>
</li>
<li><p><strong>如果实体和实体间是多对一或者一对一的关系则不需要单独创建一个表，而是通过在参与联系的多方实体中，增加一个参与联系的1方的实体的主键作为一列来表示联系</strong>。例如，车队和车辆是一对多的关系，这里 <strong>多</strong> 是车辆这个实体，1是车队这个实体。车队的实体的主键是车队号，所以，我们在多方(车辆)这个表中，增加1方(车队)的主键(车队号)作为一列来表示联系，即将  车队号  加到 车辆 这个实体的属性列表中。这里的 车队号 显然是 <strong>外键</strong>。</p>
</li>
<li><p><strong>如果两个实体之间的联系是多对多的，联系要单独存放为一个表</strong>。这个表的主键，是参与联系的两个实体的主键的组合，而联系的属性作为其他列。例如：车辆和司机之间是多对多的关系，使用 <code>联系名(车牌号，司机编码，使用日期，公里数)</code> 这里的车牌号是 车辆 这一实体的主键，司机编码是 司机 这一实体的主键，而后面两个是联系的属性。外键是： 车牌号，司机编码。</p>
</li>
<li><p>每个关系的主键都要标出来，外键写在关系的最后，属性括号的外面。</p>
</li>
<li><p><a href="https://blog.csdn.net/sunzhenhua0608/article/details/8822871">简明教学</a></p>
</li>
<li><p><a href="https://blog.csdn.net/zxq1138634642/article/details/9121363">详细教学</a></p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.5.3</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑SQL</tag>
      </tags>
  </entry>
  <entry>
    <title>《Scala编程》阅读记录——入门部分</title>
    <url>/2018/07/12/Scala-learning-01/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="入门"><a href="#入门" class="headerlink" title="入门"></a>入门</h2><hr>
<ul>
<li><p>scala是一门函数式编程语言,同时也是面向对象的</p>
</li>
<li><p>函数值就是对象</p>
</li>
<li><p>scala对面向对象的实现是纯的：每个值都是对象，每个操作都是方法调用</p>
</li>
<li><p>例如，两个Int类型的相加（注意scala里所有的数据类型首字母都是大写，这说明了他们都是类），+实际上是调用了一个名为+的方法</p>
</li>
</ul>
<h3 id="函数式编程以两大核心概念为指导："><a href="#函数式编程以两大核心概念为指导：" class="headerlink" title="函数式编程以两大核心概念为指导："></a>函数式编程以两大核心概念为指导：</h3><ul>
<li><p>(1)函数是一等的值：函数值可以作为参数传递给其他函数，可以作为返回值，可以保存在一个变量里。还可以在一个函数里定义另一个函数，就像在函数里定义一个整数一样。也可以在定义函数时不指定名字。</p>
</li>
<li><p>(2)程序中的操作应该将输入值映射成输出值，而不是当场修改数据。例如Java对String的修改就是生成了一个新的String对象而原先的String不变。这个理念的另一种表述就是函数不应该有副作用(sideeffect)。方法只能通过接受入参数和返回结果这两种方式与外部通信，即参数本身不会被修改，这样的方法被称为是“指称透明的”。<br>函数式编程鼓励不可变的数据结构和指称透明的方法。</p>
</li>
<li><p>Scala可以选择使用指令式编程的方法，即使用可变数据和副作用编程。但是推荐使用函数式编程。</p>
</li>
<li><p>scala定义类的简便的写法： <code>class MyClass(index: Int, name:String)</code>这样scala编译器就会产生带有两个 <strong>私有</strong> 实例变量和一个接受这两个变量初始值的参数的构造方法类</p>
</li>
<li><p>假设name是一个String 类型的变量<br><code>val IfHasUpperCase = name.exists(_.isUpper)</code></p>
</li>
<li><p>这段代码实现了判断name中是否存在大写字母，并赋值给IfHasUpperCase这个Boolean类型的变量(scala赋值时如果不明确写出变量类型，则编译器会自动判断)</p>
</li>
<li><p>scala代码将字符串当做更高级的字符序列(sequence) 来处理，用 前提(predicate) 来处理</p>
</li>
<li><p>这里的 前提 <code>_.isUpper</code> 是scala的函数字面量(当函数字面量的结果类型是Boolean时，可以被称作前提)。它描述了一个接受 <strong>字符</strong> 作为 **入参(以下划线表示)**，判断该字符是否为大写字母的函数。</p>
</li>
<li><p>布尔值不能和整数相加</p>
</li>
<li><p>scala几乎所有语法结构都支持嵌套</p>
</li>
<li><p>scala输出： <code>println(&quot;....&quot;)</code></p>
</li>
<li><p>注意编译器不会推断函数参数的类型，因此函数中参数的数据类型要显式地写出</p>
</li>
<li><p>scala函数形式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def FuncName(x:Int, y:Int): Int = &#123;</span><br><span class="line">	if(...)&#123;..&#125;</span><br><span class="line">	else&#123;..&#125;</span><br><span class="line">	x</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>注意函数结果类型前面的冒号不能省（这里函数的格式实际山是与变量保持一致的，即类型跟在冒号后面）</p>
</li>
<li><p>scala的函数会将函数体里的最后一条语句的值作为函数的返回值（例子里的函数将x的值返回）</p>
</li>
<li><p>大多数时候函数的结果类型不需要显式地给出。但是递归函数必须显式地给出结果类型</p>
</li>
<li><p>scala表达式<code>if(x&gt;y) x else y</code>的行为，与Java里的<code>(x&gt;y)? x:y</code>的含义一致</p>
</li>
<li><p>编译scala脚本：<code>scala hello.scala</code></p>
</li>
<li><p>命令行参数可以通过名为args的scala数组来获取：<code>println(&quot;Hello &quot; + args(0) + &quot;!&quot;)</code></p>
</li>
<li><p>然后执行： <code>$ scala hello.scala planet  //其中planet被当做参数传入</code></p>
</li>
<li><p>结果： <code>Hello planet!</code></p>
</li>
<li><p>尽量少用for 和 while 循环</p>
</li>
</ul>
<h3 id="foreach遍历"><a href="#foreach遍历" class="headerlink" title="foreach遍历"></a>foreach遍历</h3><ul>
<li><p>打印每一个命令行参数的方式：args.foreach(arg &#x3D;&gt; println(arg))<br>这段代码中，对args执行foreach方法，传入一个函数。本例中传入的是一个函数字面量(function literal)</p>
</li>
<li><p>这个(匿名)函数接受一个名为arg的参数(arg仅仅是参数名没有别的特殊含义)<br>上面也可以指明类型名(这样需要将参数的部分包在圆括号里，这是函数字面量的常规语法)：<code>args.foreach((arg:String) =&gt; println(arg))</code></p>
</li>
<li><p>scala函数字面量语法： <code>(x:Int, y: Int) =&gt; x + y</code><br>其中从左到右，括号中的是参数部分，然后是右箭头，最右边是函数体</p>
</li>
<li><p>scala对函数字面量还有一个特殊的简写规则：如果函数字面量只是一个接收单个参数的语句，可以不必给出参数名和参数本身。<br>因此上例可以写为：<code>args.foreach(println)</code></p>
</li>
<li><p>scala只支持指令式的for语句的函数式亲戚。<em>for表达式</em></p>
</li>
<li><p>for表达式一个简单的例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for(arg &lt;- args)</span><br><span class="line">    println(arg)</span><br></pre></td></tr></table></figure>
</li>
<li><p>其中，位于 <code>&lt;-</code> 符号右边的，是args数组，在 <code>&lt;-</code> 左边的是一个 val变量的名字，为arg。<br>注意左边的这个变量一定是 val类型 的(即它不能在for表达式循环体里被重新赋值)，并且只能写为 <code>arg</code> 而不能写成 <code>val arg</code></p>
</li>
</ul>
<h3 id="用类型参数化数组"><a href="#用类型参数化数组" class="headerlink" title="用类型参数化数组"></a>用类型参数化数组</h3><ul>
<li><p>在scala中，可以用new来实例化对象或类的实例，当实例化对象时，可以用值和类型来对其进行参数化(parameterize)</p>
</li>
<li><p>参数化的意思是：在创建实例的时候对实例做”配置”，可以用值来参数化一个实例，做法是在构造方法的括号中传入对象参数。</p>
</li>
<li><p>参数化创建数组：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val myArr = new Array[String](3)</span><br><span class="line">myArr(0) = ...</span><br><span class="line">myArr(1) = ...</span><br><span class="line">myArr(2) = ...</span><br></pre></td></tr></table></figure>
</li>
<li><p>另一种创建方法： <code>val numArr = Array(&quot;zero&quot;,&quot;one&quot;,&quot;two&quot;)</code></p>
</li>
<li><p>这实际上是调用了一个名为apply的工厂方法，这个方法创建并返回了新的数组。这个apply方法接受一个变长的参数列表。</p>
</li>
<li><p>scala数组使用 <strong>圆括号</strong> 来访问元素而不是方括号，原因(原文)：</p>
<blockquote>
<p>数组不过是类的实例，这一点跟其他scala的实例没有本质区别。当你用一组圆括号讲一个或多个值包起来，并将其应用(apply)到某个对象时，scala会将这段代码转换成对这个对象的一个名为apply的方法调用。因此myArray(i)实际上就是myArray.apply(i)。因此，在scala中访问一个数组的元素就是一个简单的方法调用。</p>
</blockquote>
</li>
<li><p>scala将从数组到表达式的一切都当做带有方法的对象来处理，由此来实现概念是的简单化。不需要记住各种特例。同时，这种统一并不会带来显著的性能开销。因为scala在编译代码时，会尽可能使用Java数组、基本类型和原生的算术指令。</p>
</li>
</ul>
<h3 id="使用列表"><a href="#使用列表" class="headerlink" title="使用列表"></a>使用列表</h3><ul>
<li><p>scala的列表(list)是不可变的(数组虽然不能更改长度，但是元素是可以改变的，因此是可变的)<br>因此list的修改有点像Java的字符串，每次对其中元素的修改都会返回一个新的list对象。</p>
</li>
<li><p>不需要写 <code>new List</code>，因为scala.List的伴生对象上定义了一个工厂方法<code>List.apply()</code></p>
</li>
<li><p>列表上用的最多的操作是 <code>::</code>，读作”cons”。它是在一个已有的列表最前面添加一个新元素，并返回这个新的列表。<br>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val twoThree = List(2,3)</span><br><span class="line">val oneTwoThree = 1 :: twoThree</span><br><span class="line">println(onTwoThree)</span><br></pre></td></tr></table></figure>
<p>结果： <code>List(1,2,3)</code></p>
</li>
<li><p>注意：在表达式<code>1::twoThree</code>中，<code>::</code>是它右操作元的方法。（一般用在操作符表示法的方法都是左结合的，例如 <code>a * b</code>相当于 <code>a.*(b)</code> ) 除非方法名是以 <code>:</code> 结尾的，那么这个方法的调用就会发生在他的右操作元上。 因此上例相当于： <code>twoThree.::(1)</code>，1是作为参数传入的。</p>
</li>
<li><p>表示空列表的快捷方式是<code>Nil</code>，初始化一个新的列表的另一种方式是用<code>::</code>将元素串接起来，并将Nil作为最后一个元素。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val oneTwoThree = 1 :: 2 :: 3 :: Nil</span><br><span class="line">println(oneTwoThree)</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>:::</code>连接两个List：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val a = List(1)</span><br><span class="line">val b = List(2,3)</span><br><span class="line">val c = a ::: b   //c: List(1,2,3)</span><br></pre></td></tr></table></figure>
</li>
<li><p>列表也有从末尾增加元素的方法，但是应当尽力避免这种写法，因为末尾追加元素的操作所需时间随列表的大小线性增加，而使用<code>::</code>只需要常量级的时间。</p>
</li>
<li><p>可以通过在头部添加元素的方法，最后调用reverse方法来实现末尾添加的效果</p>
</li>
<li><p>也可以使用ListBuffer，这是个可变列表，支持追加操作，最后使用toList方法即可转换为List</p>
</li>
</ul>
<h3 id="使用元祖"><a href="#使用元祖" class="headerlink" title="使用元祖"></a>使用元祖</h3><ul>
<li><p>元祖也是不可变的，但是元祖可以容纳不同类型的元素。</p>
</li>
<li><p>当需要从方法返回多个对象时，元祖就非常有用</p>
</li>
<li><p>实例化一个元祖非常简单，只要将对象放在圆括号当中，用逗号隔开即可</p>
</li>
<li><p>一旦实例化好一个元祖，就可以用英文句点、下划线和 <strong>从1开始</strong> 的序号来访问每一个元素。</p>
</li>
<li><p>例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val pair = (99,&quot;test&quot;)</span><br><span class="line">pritln(pair._1)		//访问第一个元素</span><br><span class="line">println(pair._2)	//访问第二个元素</span><br></pre></td></tr></table></figure>
</li>
<li><p>元祖的实际类型取决于它的元素。例如：<code>(99,&quot;test&quot;)</code>的类型为<code>Tuple2[Int,String]</code>,而元祖<code>(&#39;u&#39;,&#39;r&#39;,&quot;the&quot;,1,4,&quot;me&quot;)</code>的类型是<code>Tuple6[Char,Char,String,Int,Int,String]</code></p>
</li>
<li><p>元祖为什么不能像数组一样用圆括号来访问元素(原文)：</p>
<blockquote>
<p>你也许正好奇问什么不能像访问列表元素，也就是“pair(0)”那样访问元祖元素。背后的原因是列表的apply方法永远<strong>只返回同一种类型</strong>，但元祖里的元素可以是不同类型的：<code>_1</code>可能是一种类型，<code>_2</code>可能是另一种</p>
</blockquote>
</li>
</ul>
<h3 id="使用集和映射"><a href="#使用集和映射" class="headerlink" title="使用集和映射"></a>使用集和映射</h3><ul>
<li><p>scala的API包含了一个基础的特质(trait)来表示集，这里的特质跟Java的接口定义类似</p>
</li>
<li><p>scala提供了两个子特质：一个表示可变集，一个表示不可变集。(可变集与不可变集位于不同的包)</p>
</li>
<li><p>创建一个集合的例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var jetSet = Set(&quot;Boeing&quot;,&quot;Airbus&quot;)</span><br><span class="line">jetSet += &quot;Lear&quot;</span><br><span class="line">println(jetSet.contains(&quot;Cessna&quot;))</span><br></pre></td></tr></table></figure></li>
<li><p>第一行代码表示，可以像创建列表和数组那样创建集：通过调用Set伴生对象的apply工厂方法。</p>
</li>
<li><p>在这个例子中，实际上调用了scala.collection.immutable.Set的伴生对象的apply方法，返回一个默认的、<strong>不可变</strong> 的Set对象，类型为<code>Set[String]</code></p>
</li>
<li><p>向集添加元素需要调用<code>+</code>方法。无论是可变集还是不可变集，<code>+</code>方法都会创建并返回一个新的集</p>
</li>
<li><p>而本例中的<code>+=</code>方法，实际上不可变集并不提供这个方法，其本质上是<code>jetSet = jetSet + ”Lear&quot;</code>的简写。因此，<strong>其本质上是将jetSet这个var重新赋值成了一个包含三个元素的新的集</strong></p>
</li>
<li><p>最后一行是打印集中是否包含”Cessna”这个元素，结果应当是<code>false</code></p>
</li>
</ul>
<h3 id="可变集"><a href="#可变集" class="headerlink" title="可变集"></a>可变集</h3><ul>
<li><p>使用可变集需要一次引入，例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import scala.collection.mutable</span><br><span class="line">val movieSet = mutable.Set(&quot;Hitch&quot;,&quot;Peter&quot;)</span><br><span class="line">movieSet += &quot;Shrek&quot;</span><br><span class="line">println(movieSet)</span><br></pre></td></tr></table></figure></li>
<li><p>通过import语句，可以使用简短的<code>mutable.Set()</code>而不是<code>scala.colection.mutable.Set()</code>这样完整的句子</p>
</li>
<li><p>这里的<code>+=</code>方法在可变集里是有定义的，因此这个可变集声明时用的是 <code>val</code>而不是 <code>var</code>(上面也提到了不可变集实际上是被重新赋值了，如果使用<code>val</code>会编译报错)</p>
</li>
<li><p><code>HashSet</code>的用法与<code>Set</code>一致，只是<code>HashSet</code>的存储方式不同，因此一般在需要考虑性能的地方会用到<code>HashSet</code></p>
</li>
</ul>
<h3 id="映射-Map"><a href="#映射-Map" class="headerlink" title="映射(Map)"></a>映射(Map)</h3><ul>
<li><p>与Set类似，Map也有可变与不可变的版本。</p>
</li>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import scala.collection.mutable</span><br><span class="line">val treasureMap = mutable.Map[Int,String]()</span><br><span class="line">treasureMap += (1 -&gt; &quot;Go to island&quot;)</span><br><span class="line">treasureMap += (2 -&gt; &quot;Find big X on ground&quot;)</span><br><span class="line">treasureMap += (3 -&gt; &quot;Dig&quot;)</span><br><span class="line">println(treasureMap(2))</span><br></pre></td></tr></table></figure>
</li>
<li><p>上面的例子引入了可变Map(默认为不可变映射)，这里与上面可变集一样，定义为val类型。接下来，使用<code>-&gt;</code>来定义键值对。这里scala会将二元操作，比如<code>1 -&gt; &quot;Go to island&quot;</code>转换为标准的方法调用，即<code>(1).-&gt;(&quot;Go to island&quot;)</code>，这里<code>&quot;Go to island&quot;</code>是方法<code>-&gt;</code>的参数。</p>
</li>
<li><p>可以对任何对象调用<code>-&gt;</code>方法，它将返回包含键和值两个元素的元组。</p>
</li>
<li><p>若要获取Map里的某个键的值，如上面代码的最后一行<code>treasureMap(2)</code>将返回<code>2</code>这个键对应的值。</p>
</li>
</ul>
<h3 id="函数式编程风格"><a href="#函数式编程风格" class="headerlink" title="函数式编程风格"></a>函数式编程风格</h3><ul>
<li><p>如果一段代码避免了使用<code>var</code>(也就是全是<code>val</code>类型的变量)，那么它很可能是函数式的。因此，向函数式风格的转化应该尽可能地避免使用<code>var</code>类型的变量。</p>
</li>
<li><p>指令式编程的例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def printArgs(args: Array[String]): Unit = &#123;</span><br><span class="line">	var i = 0</span><br><span class="line">	while (i &lt; args.length)&#123;</span><br><span class="line">		println(args(i))</span><br><span class="line">		i += 1</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>可以将这段代码转化为函数式的：</p>
</li>
</ul>
<ol>
<li><p>第一种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def printArgs(args: Array[String]): Unit = &#123;</span><br><span class="line">	for (arg &lt;- args)  //for表达式形式</span><br><span class="line">		println(arg)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def printArgs(args: Array[String]): Unit = &#123;</span><br><span class="line">	args.foreach(println)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<ul>
<li><p>另一方面，尽量避免使用带有副作用的函数。就比如上面修改之后的函数，它依旧不是“纯”的函数式代码，因为他有副作用(<em>本处的副作用是向标准输出流打印</em>)。带有副作用的函数的标志特征就是结果类型为<code>Unit</code>。(因为一个一个结果类型为<code>Unit</code>的函数，存在世上的唯一意义就是产生副作用)</p>
</li>
<li><p>对于这一问题，函数式编程的做法就是定义一个将传入的<code>args</code>作为格式化(用于打印)的方法，但是只返回这个格式化的字符串。例如上例可以改成：<code>def formatArgs(args: Array[String]) = args.mkString(&quot;\n&quot;)</code><br>这个函数就满足了既没有<code>var</code>变量也没有副作用。</p>
</li>
<li><p>这时我们就可以使用<code>println(formatArgs(args))</code>来实现与上面相同的打印的效果。</p>
</li>
</ul>
<h3 id="从文件读取文本行"><a href="#从文件读取文本行" class="headerlink" title="从文件读取文本行"></a>从文件读取文本行</h3><ul>
<li><p>例子：从文本读取文本行，并将它们打印出来，在每一行前面带上当前的字符数。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import scala.io.Source</span><br><span class="line">if(args.length &gt;0)&#123;</span><br><span class="line">	for (line &lt;- Source.fromFile(args(0)).getLines())</span><br><span class="line">		println(line.length + &quot; &quot; + filename)</span><br><span class="line">	else</span><br><span class="line">		Console.err.println(&quot;Please enter filename&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>这段脚本首先引入了scala.io名为Srouce的类，然后检查命令行是不是至少给出了一个参数。表达式<code>Source.fromFile(args(0))</code>尝试打开指定的文件并且返回一个Source对象。在这个对象上，继续调用<code>getLines</code>方法。</p>
</li>
<li><p><code>getLines</code>方法返回一个<code>Iterator[String]</code>，每次迭代都给出一行内容，去掉了最后的换行符。for表达式遍历这些文本行。</p>
</li>
<li><p>可以将文本行赋值给一个变量：<code>val lines = Source.fromFile(args(0)).getLines().toList</code></p>
</li>
<li><p>其中，最后的<code>toList</code>是必须的。因为<code>getLines</code>方法返回的是一个迭代器，一旦完成遍历，迭代器就会被消耗掉，通过<code>toList</code>方法将它转化为列表，就可以随便遍历这些文本行，多少次都可以(迭代器只能遍历一次)。但是代价就是要在内存中同时存储所有的行。因此<code>lines</code>指向了一个字符串列表。</p>
</li>
<li><p><strong>scala也可以使用<code>*</code>来重复一个字符串</strong>，例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; var s = &quot;a&quot; * 5</span><br><span class="line">s: String = aaaaa</span><br></pre></td></tr></table></figure></li>
</ul>
<br>
> 最后更新于2018.7.16
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala学习笔记——基础</title>
    <url>/2018/04/06/Scala-learning/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="编译环境"><a href="#编译环境" class="headerlink" title="编译环境"></a>编译环境</h2><hr>
<ul>
<li><p>scala在终端中就可以调用解释器，与python类似，可以逐语句执行。使用命令<code>scala</code>即可进入终端解释器，使用命令<code>:q</code>或者<code>:quit</code>退出解释器。</p>
</li>
<li><p>编译首选jetbrain的intellij IDEA，下载一个Scala plugin，然后选择创建maven工程，main函数应当写在object文件中，object通过创建Scala class文件选项创建。</p>
</li>
<li><p>如果要写分布式处理程序的话，需要自己重写配置文件</p>
</li>
<li><p>Scala注释与C++类似</p>
</li>
</ul>
<h3 id="让intellij默认不打开上个项目的方法"><a href="#让intellij默认不打开上个项目的方法" class="headerlink" title="让intellij默认不打开上个项目的方法"></a>让intellij默认不打开上个项目的方法</h3><ul>
<li>settings<code>&gt;</code>Apperance&amp;Behavior<code>&gt;</code>System Settings 将<code>Reopen last project on startup</code> 勾去掉即可.</li>
</ul>
<h2 id="在intellij-IDEA下创建scala项目的步骤"><a href="#在intellij-IDEA下创建scala项目的步骤" class="headerlink" title="在intellij IDEA下创建scala项目的步骤"></a>在intellij IDEA下创建scala项目的步骤</h2><hr>
<ul>
<li><p>首先，要确保已经安装了scala的插件(这个有很大概率会安装失败，如果失败次数太多可以选择离线安装，或者跳过scala sdk的选择，在进入一个scala项目之后选择补全)</p>
</li>
<li><p>然后，在欢迎页面的右下角点击Configure，然后在Project Defaults的下拉菜单中选择Project Structure，在打开的页面左侧选择Global Libraries，然后在中间一栏中有一个绿色的加号标志 +，点击后在下拉菜单中选择 Scala SDK</p>
</li>
<li><p>然后在打开的对话框中选择系统本身所安装的Scala（即System对应的版本），点击OK确定，这时候会在中间一栏位置处出现Scala的SDK，在其上右键点击后选择Copy to Project Libraries…，这个操作是为了将Scala SDK添加到项目的默认Library中去。</p>
</li>
<li><p>在欢迎界面点击Create New Project，在打开的页面左侧边栏中，选择Maven，然后在右侧的Project SDK一项中，查看是否是正确的JDK配置项正常来说这一栏会自动填充的，因为我们之前在1.3中已经配置过了全局的Project JDK了，如果这里没有正常显示JDK的话，可以点击右侧的New…按钮，然后指定JDK安装路径的根目录即可）。<strong>注意不要选择create from archetype</strong></p>
</li>
</ul>
<p><img src="/images/Scala/create_1.png"></p>
<ul>
<li>然后点击Next，来到Maven项目最重要三个参数的设置页面，这三个参数分别为：GroupId, ArtifactId和Version.</li>
</ul>
<p><img src="/images/Scala/create_2.png"></p>
<h3 id="创建Hello-world"><a href="#创建Hello-world" class="headerlink" title="创建Hello world"></a>创建Hello world</h3><p><img src="/images/Scala/first_program.png"></p>
<ul>
<li><p>为了让体验Scala更清爽一些，将一些暂时无关的文件和文件夹都删除掉，主要有 main&#x2F;java, main&#x2F;resources 和 test 这三个文件夹</p>
</li>
<li><p>将Scala的框架添加到这个项目中，方法是在左侧栏中的 <strong>项目名称</strong> 上右键菜单中点击 Add Framework Support…，然后在打开的对话框左侧边栏中，勾选Scala前面的复选框，然后点击确定即可（前提是上文中所述步骤都已正确走通，否则你很有可能看不到Scala这个选项的）；</p>
</li>
<li><p>在main文件夹中建立一个名为 scala 的文件夹，并右键点击 scala 文件夹，选择 Make Directory as，然后选择Sources Root ，<strong>这里主要意思是将 scala 文件夹标记为一个源文件的根目录，然后在其内的所有代码中的 package ，其路径就从这个根目录下开始算起</strong>。</p>
</li>
<li><p>在已经标记好为源文件根目录的 scala 文件夹 上，右键选择 New，然后选择 Scala Class，随后设置好程序的名称，并且记得将其设置为一个 Object(类似于Java中含有静态成员的静态类)，正常的话，将会打开这个 Object 代码界面，并且可以看到IntelliJ IDEA自动添加了一些最基本的信息；</p>
</li>
</ul>
<h3 id="示例程序"><a href="#示例程序" class="headerlink" title="示例程序"></a>示例程序</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">object HelloWorld &#123;</span><br><span class="line">   def main(args: Array[String]) &#123;</span><br><span class="line">      println(&quot;Hello, world!&quot;) // 输出 Hello World</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="执行scala程序"><a href="#执行scala程序" class="headerlink" title="执行scala程序"></a>执行scala程序</h3><ul>
<li><p>脚本执行：我们可以将 Scala 表达式写在一个文件里，比如 Hello.scala。在命令行中直接输入 scala Hello.scala</p>
</li>
<li><p>作为应用程序执行：作为应用程序执行时，我们需要在一个单例对象中定义入口函数 main，经过编译后就可以执行该应用程序了。</p>
</li>
</ul>
<h2 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h2><hr>
<blockquote>
<p>scala的语句分号规则与C++相类似(一般都不用加，加了一般也不会错)</p>
</blockquote>
<blockquote>
<p>scala中的通配符为<code>_</code>，而C++中为&#96;*&#96;&#96;</p>
</blockquote>
<blockquote>
<p>scala与spark很契合，所以写spark程序推荐使用scala语言</p>
</blockquote>
<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><ul>
<li><p>Scala中变量分为可变变量和不可变变量。可变变量用关键字<code>var</code>修饰，不可变变量用<code>val</code>修饰。</p>
<blockquote>
<p>注意scala中变量定义时必须赋初始值</p>
</blockquote>
</li>
<li><p>scala大多数数据结构中的变量默认为val类型</p>
</li>
<li><p>特例：若用占位符<code>_</code>作为值进行初始化，则String类会被初始化为null,Float,Int,Double等被初始化为0,Char类型会被初始化为”?”.</p>
</li>
<li><p>如果用lazy关键字修饰变量，则他只有在真正被使用时才会被赋值。</p>
<blockquote>
<p>注意lazy关键字只能修饰val类型变量，不能修饰var类型</p>
</blockquote>
</li>
</ul>
<h2 id="输入输出"><a href="#输入输出" class="headerlink" title="输入输出"></a>输入输出</h2><h3 id="输出：print-或者-println"><a href="#输出：print-或者-println" class="headerlink" title="输出：print(&quot;...&quot;) 或者 println(&quot;...&quot;)"></a>输出：<code>print(&quot;...&quot;)</code> 或者 <code>println(&quot;...&quot;)</code></h3><h3 id="输入："><a href="#输入：" class="headerlink" title="输入："></a>输入：</h3><blockquote>
<p>注意，控制台的输入均为string类型，需要在后面加上.toInt  .toFloat等转换操作</p>
</blockquote>
<ul>
<li><p>控制台输入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val scan = scala.io.StdIn  //设置scan</span><br><span class="line">val n = scan.readLine.trim.toInt  //输入一个int</span><br><span class="line">val arr = scan.readLine.split(&quot; &quot;).map(_.trim.toInt)  //输入一串数字，之间以空格间隔</span><br><span class="line">// .trim方法是去掉输入字符串两旁的空格</span><br></pre></td></tr></table></figure>
</li>
<li><p>scala还有一系列专门的用来读下 <strong>一个</strong> 输入并且可以自动转换的方法，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val scan = scala.io.StdIn</span><br><span class="line">val myint = scan.readInt()</span><br><span class="line">val myfloat = scan.readFloat()</span><br><span class="line">val myboolean = scan.readBoolean()</span><br><span class="line">...</span><br><span class="line">//这些方法均可以读取下一个输入并且自动转换类型</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><ul>
<li>例如：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val a = new Array[Int](3)</span><br><span class="line">a(0)  =1</span><br><span class="line">a(1) = 2</span><br><span class="line">a(2) = 0</span><br><span class="line">a.foreach(print)  //结果: 120</span><br><span class="line">val sorted_a = a.sortWith((x,y) =&gt; x &gt; y)  //从大到小排序（sortWith的参数为一个lambda函数，返回一个布尔值用于比较）</span><br><span class="line">a.foreach(print) //结果： 210</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="一些注意点"><a href="#一些注意点" class="headerlink" title="一些注意点"></a>一些注意点</h3><ul>
<li><p>scala中没有 <code>++</code> 操作，可以用 <code>+= 1</code> 代替 <code>++</code>（x +&#x3D; y ，y可以是一个数字，也可以是一个数字变量，但不能是数组或是其他一些类似结构的某个元素）</p>
</li>
<li><p>Scala所有数据类型首字母都要大写，因为Scala中所有值类型都是对象，<strong>其中布尔类型应写为“Boolean”</strong></p>
</li>
<li><p>Scala中有 <code>&lt;=</code> 和 <code>&gt;=</code> 以及逻辑与 <code>&amp;&amp;</code> 和逻辑或 <code>||</code> 运算符，与C++相同。</p>
</li>
<li><p>Scala中String类型就是Java中的String.lang.String类型，因此可以调用Java中String的所有方法（例：<code>str.reverse</code>表示字符逆序）</p>
</li>
<li><p><strong>Scala中有一类特殊的运算符 “::”(list构造)，”:::”(list拼接)，他们是右操作。例如：a::b::Nill执行顺序为(a::(b::Nill))</strong></p>
</li>
</ul>
<h3 id="一些数据类型"><a href="#一些数据类型" class="headerlink" title="一些数据类型"></a>一些数据类型</h3><h4 id="整型和浮点型"><a href="#整型和浮点型" class="headerlink" title="整型和浮点型"></a>整型和浮点型</h4><ul>
<li><p>整型字面量用于 Int 类型，如果表示 Long，可以在数字后面添加 L 或者小写 l 作为后缀。</p>
</li>
<li><p>如果浮点数后面有f或者F后缀时，表示这是一个Float类型，否则就是一个Double类型的。</p>
</li>
</ul>
<h4 id="符号字面量"><a href="#符号字面量" class="headerlink" title="符号字面量"></a>符号字面量</h4><ul>
<li><p>也就是符号类型</p>
</li>
<li><p>符号字面量被写成： ‘&lt;标识符&gt; ，这里 &lt;标识符&gt; 可以是任何字母或数字的标识（注意：不能以数字开头）。这种字面量被映射成预定义类scala.Symbol的实例。</p>
</li>
</ul>
<h4 id="string类"><a href="#string类" class="headerlink" title="string类"></a>string类</h4><ul>
<li><p>一个Array[String]类型的数据使用 .toString方法后得到的是一个java.lang.string类型的数据，这个数据直接print是这个Array的地址（一堆符号）</p>
</li>
<li><p>string类的substring方法，有两个参数，第一个参数为起始位置，第二个参数为结束位置，可以不写，默认为末尾</p>
</li>
<li><p>通常，toString 方法会返回一个“以文本方式表示”此对象的字符串。结果应是一个简明但易于读懂的信息表达式。建议所有子类都重写此方法。</p>
</li>
<li><p>Object 类的 toString 方法返回一个字符串，该字符串由类名（对象是该类的一个实例）、at 标记符“@”和此对象哈希码的无符号十六进制表示组成。换句话说，该方法返回一个字符串，它的值等于：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">getClass().getName() + &#x27;@&#x27; + Integer.toHexString(hashCode())</span><br></pre></td></tr></table></figure>
</li>
<li><p>将java.lang.string以可读的形式打印出来的方法：</p>
</li>
</ul>
<h4 id="元组类型（类似于Python中的元组）"><a href="#元组类型（类似于Python中的元组）" class="headerlink" title="元组类型（类似于Python中的元组）"></a>元组类型（类似于Python中的元组）</h4><ul>
<li><p>如果想要访问元组的内容，可以通过 <code>变量名._N</code>的方式进行。其中N表示元组中元素的索引号。 例：<code>tuple._1</code> 表示tuple中的第一个元素。</p>
</li>
<li><p><strong>注意：元组的访问元素的索引是从1开始，而且是不可改变的。</strong></p>
</li>
<li><p>元组用 <code>==</code> 进行比较时，是进行内容的比较，这与String类型一致。</p>
</li>
</ul>
<h4 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h4><blockquote>
<p>数组的索引是从0开始的</p>
</blockquote>
<ul>
<li><p>声明格式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val a: Array[String] = new Array[String](3)</span><br><span class="line">或者： val a = new Array[String](3)</span><br></pre></td></tr></table></figure></li>
<li><p>赋值： <code>a(0) = &quot;abc&quot;</code>  &#x2F;&#x2F;注意即使是声明为val变量，数组a的元素的值也是可以改变的。 <strong>同时，使用索引时数组名后面跟的是圆括号而不是方括号，这点与C++不同，切记</strong></p>
</li>
<li><p>可以使用foreach()方法实现遍历。<br><img src="/images/Scala/Scala-test.jpg"></p>
</li>
</ul>
<h4 id="List"><a href="#List" class="headerlink" title="List"></a>List</h4><ul>
<li>创建List:<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val a = List.apply(&quot;1&quot;,&quot;2&quot;,&quot;3&quot;)</span><br><span class="line">val a: List[String] = List(&quot;1&quot;,&quot;2&quot;,&quot;3&quot;)</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="流程控制结构"><a href="#流程控制结构" class="headerlink" title="流程控制结构"></a>流程控制结构</h2><hr>
<h3 id="判断语句"><a href="#判断语句" class="headerlink" title="判断语句"></a>判断语句</h3><ul>
<li><p>if语句格式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if(条件判断)&#123;</span><br><span class="line">	[...] //条件判断为真时执行</span><br><span class="line">&#125;</span><br><span class="line">else&#123;</span><br><span class="line">	[...] //条件判断为假时执行</span><br><span class="line">&#125;  //也可以写成 if()&#123;...&#125; else if()&#123;...&#125; 同C++</span><br></pre></td></tr></table></figure>
</li>
<li><p>与Java，C++不同的是，Scala中if语句可以作为表达式使用。例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; val x = if(&quot;hello&quot; == &quot;hell&quot;) 1 else 0</span><br><span class="line">x: Int = 0</span><br></pre></td></tr></table></figure>
<p>从这个例子可以看出，if是个表达式，其返回值可以给变量赋值。<strong>Scala会将if语句的最后一条执行语句作为返回值</strong></p>
</li>
</ul>
<h3 id="循环语句"><a href="#循环语句" class="headerlink" title="循环语句"></a>循环语句</h3><ul>
<li><p>while循环,do..while循环同C++</p>
</li>
<li><p>for循环格式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for(i &lt;- 表达式)&#123;</span><br><span class="line">	[...]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for(i &lt;- 1 to 3)&#123;</span><br><span class="line">	println(&quot;i = &quot; + i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>结果： i &#x3D; 1  i &#x3D; 2  <strong>i &#x3D; 3</strong> <strong>(1 to 3 的集合是包含3的)</strong></p>
</li>
<li><p>其中 ‘&lt;-‘被称为生成器（generator），for循环实际上是通过集合的遍历来达到循环的目的的。</p>
</li>
<li><p>如果希望集合是右开的，可以使用<code>1 until 5</code>来实现，范围是[1,5)</p>
</li>
<li><p>还可以设置for 循环的步长。例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for(i &lt;- 1 until (10，2))</span><br><span class="line">	println(&quot;i = &quot; + i);</span><br><span class="line">结果： i =1 i = 3 .... i = 9</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>不过，由于Scala是函数式编程，应当尽量使用迭代来代替循环</strong></p>
</li>
<li><p>例如：使用 <code>标识符.foreach()</code>方法来实现对元素的遍历</p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.5.27</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala学习笔记——进阶</title>
    <url>/2018/04/06/Scala-superior/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<h2 id="case-class与模式匹配"><a href="#case-class与模式匹配" class="headerlink" title="case class与模式匹配"></a>case class与模式匹配</h2><hr>
<ul>
<li><p>case class一般被翻译为样例类，它是一种特殊的类，能够被优化以用于模式匹配。</p>
</li>
<li></li>
</ul>
<h2 id="scala性能测试"><a href="#scala性能测试" class="headerlink" title="scala性能测试"></a>scala性能测试</h2><hr>
<ul>
<li><p>测试程序的运行时间的函数：<code>System.currentTimeMillis()</code></p>
</li>
<li><p>用法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[...]</span><br><span class="line">  val time = System.currentTimeMillis()</span><br><span class="line">  [要测试运行时间的代码段]</span><br><span class="line">  println(&quot; 耗时: &quot; + (System.currentTimeMillis() - time))  //以时间差来看运行时间</span><br><span class="line">[...]</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="函数式循环"><a href="#函数式循环" class="headerlink" title="函数式循环"></a>函数式循环</h2><hr>
<ul>
<li><p>函数式编程的循环都是用迭代来实现的</p>
</li>
<li><p>例1：对于一个Int数组a,对里面每个元素执行 +1的操作</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a.map&#123;x =&gt;</span><br><span class="line">  val y = x + 1  //因为Int是不可变类型，所以必须要新建一个值来存储改变后的值</span><br><span class="line">  y  //返回值是y</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>例2：对于一个Int数组，每找出一个大于5的数count就加1</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var count:Int = 0</span><br><span class="line">a.foreach&#123;x =&gt;      //与map遍历方式不同，foreach不会对里面的内容修改，所以匿名函数也不需要返回值</span><br><span class="line">  if(x &gt;  5)</span><br><span class="line">    count += 1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="一些数据结构的使用"><a href="#一些数据结构的使用" class="headerlink" title="一些数据结构的使用"></a>一些数据结构的使用</h2><hr>
<h3 id="ListBuffer"><a href="#ListBuffer" class="headerlink" title="ListBuffer:"></a>ListBuffer:</h3><ul>
<li><p>首先需要引入包 <code>import scala.collection.mutable.ListBuffer</code></p>
</li>
<li><p>在使用之前，先声明一个ListBuffer变量，通过以下代码：<br><code>val buf = new ListBuffer[Int]()    //类型自定义，这里采用的是Int</code></p>
</li>
<li><p>在末尾进行追加，使用append()方法：<br><code>buf.append(x)</code></p>
</li>
<li><p>将ListBuffer转化为List:<br><code>val lst = buf.toList</code></p>
</li>
</ul>
<h2 id="Scala类的实例"><a href="#Scala类的实例" class="headerlink" title="Scala类的实例"></a>Scala类的实例</h2><hr>
<ul>
<li><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Point(xc: Int, yc: Int) &#123;</span><br><span class="line">   var x: Int = xc</span><br><span class="line">   var y: Int = yc</span><br><span class="line">   def move(dx: Int, dy: Int) &#123;</span><br><span class="line">      x = x + dx</span><br><span class="line">      y = y + dy</span><br><span class="line">      println (&quot;x 的坐标点: &quot; + x);</span><br><span class="line">      println (&quot;y 的坐标点: &quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>Scala中的类不声明为public，一个Scala源文件中可以有多个类。以上实例的类定义了两个变量 x 和 y ，一个方法：move，方法没有返回值。Scala 的类定义可以有参数，称为类参数，如上面的 xc, yc，类参数在整个类中都可以访问。</p>
</li>
<li><p>类的字段要初始化，不然会报错(错误：类必须申明为抽象类才行)</p>
</li>
</ul>
<h2 id="scala的一些笔记综合"><a href="#scala的一些笔记综合" class="headerlink" title="scala的一些笔记综合"></a>scala的一些笔记综合</h2><hr>
<h3 id="this关键字与构造函数"><a href="#this关键字与构造函数" class="headerlink" title="this关键字与构造函数"></a>this关键字与构造函数</h3><h4 id="主构造函数"><a href="#主构造函数" class="headerlink" title="主构造函数"></a>主构造函数</h4><ul>
<li><p>Scala提供了一个类的主构造函数的概念。如果代码只有一个构造函数，则可以不需要定义明确的构造函数。它有助于优化代码，可以创建具有零个或多个参数的主构造函数。</p>
</li>
<li><p>scala主构造函数示例：(即在类中以参数的形式体现)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Student(id:Int, name:String)&#123;  </span><br><span class="line">    def showDetails()&#123;  </span><br><span class="line">        println(id+&quot; &quot;+name);  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;  </span><br><span class="line">object Demo&#123;  </span><br><span class="line">    def main(args:Array[String])&#123;  </span><br><span class="line">        var s = new Student(1010,&quot;Maxsu&quot;);  </span><br><span class="line">        s.showDetails()  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="次要-辅助-构造器"><a href="#次要-辅助-构造器" class="headerlink" title="次要(辅助)构造器"></a>次要(辅助)构造器</h4><ul>
<li><p>可以在类中创建任意数量的辅助构造函数，必须要从辅助构造函数内部调用主构造函数。<strong>this关键字用于从其他构造函数调用构造函数</strong>。当调用其他构造函数时，<strong>确保要将其放在构造函数中的第一行</strong>。</p>
</li>
<li><p>Scala二次构造函数示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Student(id:Int, name:String)&#123;  </span><br><span class="line">    var age:Int = 0  </span><br><span class="line">    def showDetails()&#123;  </span><br><span class="line">        println(id+&quot; &quot;+name+&quot; &quot;+age)  </span><br><span class="line">    &#125;  </span><br><span class="line">    def this(id:Int, name:String,age:Int)&#123;  </span><br><span class="line">        this(id,name)       // Calling primary constructor, and it is first line  </span><br><span class="line">        this.age = age  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;  </span><br><span class="line">object Demo&#123;  </span><br><span class="line">    def main(args:Array[String])&#123;  </span><br><span class="line">        var s = new Student(1010,&quot;Maxsu&quot;, 25);  </span><br><span class="line">        s.showDetails()  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>并且在this二次构造函数的第一个语句，调用主构造函数this()的参数与主构造函数一致(如上例中的id和name两个参数)。</p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.5.27</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala方法查询记录</title>
    <url>/2018/07/12/Scala-infos/</url>
    <content><![CDATA[<blockquote>
<p>仅供查询，禁止转载</p>
</blockquote>
<ul>
<li>一般的Int类型的都可以通过<code>.max</code>和<code>.min</code>获取最大最小元素</li>
</ul>
<h2 id="List的一些方法和用途"><a href="#List的一些方法和用途" class="headerlink" title="List的一些方法和用途"></a>List的一些方法和用途</h2><table>
<thead>
<tr>
<th>方法</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>List()或者Nil</td>
<td>表示空列表</td>
</tr>
<tr>
<td>List(“Cool”,”Tools”)</td>
<td>创建一个新的List[String],包含两个值:”Cool”,”Tools”</td>
</tr>
<tr>
<td>val thrill &#x3D; “Will”::”Fill”::Nil</td>
<td>创建一个新的List[String]包含两个值：”Will”和”Fill”</td>
</tr>
<tr>
<td>List(“a”,”b”):::List(“c”,”d”)</td>
<td>将两个列表拼接起来(返回一个新的列表List(“a”,”b”,”c”,”d”))</td>
</tr>
<tr>
<td>thrill(2)</td>
<td>返回列表thrill下标为2(从0开始计数)</td>
</tr>
<tr>
<td>thrill.count(s &#x3D;&gt; s.length &#x3D;&#x3D; 4)</td>
<td>对thrill中长度为4的字符串进行计数</td>
</tr>
<tr>
<td>thrill.drop(2)</td>
<td>返回去掉了thrill的头两个元素的列表</td>
</tr>
<tr>
<td>thrill.dropRight(2)</td>
<td>返回去掉了thrill后两个元素的列表</td>
</tr>
<tr>
<td>thrill.exists(s &#x3D;&gt; s&#x3D;&#x3D;”until”)</td>
<td>判断thrill中是否有字符串元素的值为”until”，返回值为Boolean</td>
</tr>
<tr>
<td>thrill.filter(s &#x3D;&gt; s.length &#x3D;&#x3D; 4)</td>
<td>按顺序返回列表thrill中所有长度为4的元素列表</td>
</tr>
<tr>
<td>thrill.forall(s &#x3D;&gt; s.endsWith(“1”))</td>
<td>表示列表中是否所有元素都以字母”1”结尾</td>
</tr>
<tr>
<td>thrill.foreach(s &#x3D;&gt; println(s)) &#x2F; thrill.foreach(println)</td>
<td>遍历列表执行打印</td>
</tr>
<tr>
<td>thrill.head</td>
<td>返回列表的首个元素</td>
</tr>
<tr>
<td>thrill.init</td>
<td>返回列表thrill除最后一个元素之外其他元素组成的列表</td>
</tr>
<tr>
<td>thrill.isEmpty</td>
<td>判断列表是否为空</td>
</tr>
<tr>
<td>thrill.last</td>
<td>返回thrill的最后一个元素</td>
</tr>
<tr>
<td>thrill.length</td>
<td>返回列表的元素个数</td>
</tr>
<tr>
<td>thrill.map(s &#x3D;&gt; s + “y”)</td>
<td>返回一个对列表thrill所有字符串元素末尾添加”y”的新字符串列表</td>
</tr>
<tr>
<td>thrill.makeString”, “</td>
<td>返回用列表thrill的所有元素组合成的字符串，以”, “连接各个元素</td>
</tr>
<tr>
<td>thrill.filterNot(s &#x3D;&gt; s.length &#x3D;&#x3D; 4)</td>
<td>按顺序返回列表中所有长度不为4的元素列表</td>
</tr>
<tr>
<td>thrill.reverse</td>
<td>返回包含列表thrill的逆序列表</td>
</tr>
<tr>
<td>thrill.sort( (s,t) &#x3D;&gt; s.charAt(0).toLower &lt; t.charAt(0).toLower)</td>
<td>返回包含thrill的所有元素，按照首字母小写顺序排序的列表</td>
</tr>
<tr>
<td>thrill.tail</td>
<td>返回列表thrill除首个元素之外其他元素组成的列表</td>
</tr>
</tbody></table>
<h2 id="常用的集操作"><a href="#常用的集操作" class="headerlink" title="常用的集操作"></a>常用的集操作</h2><table>
<thead>
<tr>
<th>操作</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>val nums &#x3D; Set(1,2,3)</td>
<td>创建一个不可变集(nums.toString返回Set(1,2,3))</td>
</tr>
<tr>
<td>nums + 5</td>
<td>添加一个元素(返回Set(1,2,3,5))</td>
</tr>
<tr>
<td>nums - 3</td>
<td>移除一个元素(返回Set(1,2))</td>
</tr>
<tr>
<td>nums ++ List(5,6)</td>
<td>添加多个元素(返回Set(1,2,3,5,6))</td>
</tr>
<tr>
<td>nums – List(1,2)</td>
<td>移除多个元素(返回Set(3))</td>
</tr>
<tr>
<td>nums &amp; Set(1,3,5,7)</td>
<td>获取两个集的交集(返回Set(1,3))</td>
</tr>
<tr>
<td>nums.size</td>
<td>返回集的大小(返回3)</td>
</tr>
<tr>
<td>nums.contains(3)</td>
<td>检查是否包含(此处返回true)</td>
</tr>
<tr>
<td>import scala.collection.mutable</td>
<td>让可变集易于访问</td>
</tr>
<tr>
<td>val words &#x3D; mutable.Set.empty[String]</td>
<td>创建一个空的可变集(words.toString将返回Set())</td>
</tr>
<tr>
<td>words +&#x3D; “the”</td>
<td>添加一个元素(words.toString将返回Set(the))</td>
</tr>
<tr>
<td>words -&#x3D; “the”</td>
<td>移除一个元素，如果这个元素存在(words.toString将返回Set())</td>
</tr>
<tr>
<td>words ++&#x3D; List(“do”,”re”,”mi”)</td>
<td>添加多个元素(words.toString将返回Set(do,re,mi))</td>
</tr>
<tr>
<td>words –&#x3D; List(“do”,”re”)</td>
<td>移除多个元素(words.toString将返回Set(mi))</td>
</tr>
<tr>
<td>words.clear</td>
<td>移除所有元素(words.toString将返回Set())</td>
</tr>
</tbody></table>
<ul>
<li><a href="https://docs.scala-lang.org/zh-cn/overviews/collections/sets.html">更详细的链接</a></li>
</ul>
<h2 id="常用的映射操作"><a href="#常用的映射操作" class="headerlink" title="常用的映射操作"></a>常用的映射操作</h2><table>
<thead>
<tr>
<th>操作</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>val nums &#x3D; Map(“i” -&gt; 1, “ii” -&gt; 2)</td>
<td>创建一个不可变映射(nums.toString返回Map(i -&gt; 1, ii -&gt; 2))</td>
</tr>
<tr>
<td>nums + (“vi” -&gt; 6)</td>
<td>添加一个条目(返回Map(i -&gt; 1, ii -&gt; 2, vi -&gt; 6))</td>
</tr>
<tr>
<td>nums - “ii”</td>
<td>移除一个条目(返回Map(i -&gt; 1))</td>
</tr>
<tr>
<td>nums ++ List(“iii” -&gt; 3, “v” -&gt; 5)</td>
<td>添加多个条目(返回Map(i -&gt; 1, ii -&gt; 2, iii -&gt; 3, v -&gt; 5))</td>
</tr>
<tr>
<td>nums – List(“i”,”ii”)</td>
<td>移除多个条目(返回Map())</td>
</tr>
<tr>
<td>nums.size</td>
<td>返回映射的大小(返回2)</td>
</tr>
<tr>
<td>nums.contains(“ii”)</td>
<td>检查是否包含(返回true)</td>
</tr>
<tr>
<td>nums(“ii”)</td>
<td>获取指定键的值(返回2)</td>
</tr>
<tr>
<td>nums.keys</td>
<td>返回所有键(返回字符串”i”和”ii”的Iterable)</td>
</tr>
<tr>
<td>nums.keySet</td>
<td>以集的形式返回所有的键(返回Set(i,ii))</td>
</tr>
<tr>
<td>nums.values</td>
<td>返回所有的值(返回整数1和2的Iterable)</td>
</tr>
<tr>
<td>nums.isEmpty</td>
<td>表示映射是否为空</td>
</tr>
<tr>
<td>import scala.collection.mutable</td>
<td>让可变集合易于访问</td>
</tr>
<tr>
<td>val words &#x3D; mutable.Map.empty[String,Int]</td>
<td>创建一个空的可变映射</td>
</tr>
<tr>
<td>words +&#x3D; (“one” -&gt; 1, “two” -&gt; 2, “three” -&gt; 3)</td>
<td>添加一个从”one”到1的映射条目</td>
</tr>
<tr>
<td>words -&#x3D; “one”</td>
<td>移除一个映射条目，如果存在</td>
</tr>
<tr>
<td>words ++&#x3D; List(“one” -&gt; 1, “two” -&gt; 2, “three” -&gt; 3)</td>
<td>添加多个条目</td>
</tr>
<tr>
<td>words –&#x3D; List(“one”,”two”)</td>
<td>移除多个条目</td>
</tr>
</tbody></table>
<ul>
<li><a href="https://docs.scala-lang.org/zh-cn/overviews/collections/maps.html">更详细的链接</a></li>
</ul>
<h2 id="序列"><a href="#序列" class="headerlink" title="序列"></a>序列</h2><table>
<thead>
<tr>
<th>操作</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>索引和长度的操作 apply、isDefinedAt、length、indices，及lengthCompare</td>
<td>序列的apply操作用于索引访问；因此，Seq[T]类型的序列也是一个以单个Int（索引下标）为参数、返回值类型为T的偏函数。换言之，Seq[T]继承自Partial Function[Int, T]。序列各元素的索引下标从0开始计数，最大索引下标为序列长度减一。序列的length方法是collection的size方法的别名。lengthCompare方法可以比较两个序列的长度，即便其中一个序列长度无限也可以处理。</td>
</tr>
<tr>
<td>索引检索操作（indexOf、lastIndexOf、indexofSlice、lastIndexOfSlice、indexWhere、lastIndexWhere、segmentLength、prefixLength）</td>
<td>用于返回等于给定值或满足某个谓词的元素的索引。</td>
</tr>
<tr>
<td>加法运算（+:，:+，padTo）</td>
<td>用于在序列的前面或者后面添加一个元素并作为新序列返回。</td>
</tr>
<tr>
<td>更新操作（updated，patch）</td>
<td>用于替换原序列的某些元素并作为一个新序列返回。</td>
</tr>
<tr>
<td>排序操作（sorted, sortWith, sortBy）</td>
<td>根据不同的条件对序列元素进行排序。</td>
</tr>
<tr>
<td>反转操作（reverse, reverseIterator, reverseMap）</td>
<td>用于将序列中的元素以相反的顺序排列。</td>
</tr>
<tr>
<td>比较（startsWith, endsWith, contains, containsSlice, corresponds）</td>
<td>用于对两个序列进行比较，或者在序列中查找某个元素。</td>
</tr>
<tr>
<td>多集操作（intersect, diff, union, distinct）</td>
<td>用于对两个序列中的元素进行类似集合的操作，或者删除重复元素。</td>
</tr>
</tbody></table>
<h2 id="Seq类的操作"><a href="#Seq类的操作" class="headerlink" title="Seq类的操作"></a>Seq类的操作</h2><table>
<thead>
<tr>
<th>WHAT IT IS</th>
<th>WHAT IT DOES</th>
</tr>
</thead>
<tbody><tr>
<td>索引和长度</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs(i)</td>
<td>(或者写作xs apply i)。xs的第i个元素</td>
</tr>
<tr>
<td>xs isDefinedAt i</td>
<td>测试xs.indices中是否包含i。</td>
</tr>
<tr>
<td>xs.length</td>
<td>序列的长度（同size）。</td>
</tr>
<tr>
<td>xs.lengthCompare ys</td>
<td>如果xs的长度小于ys的长度，则返回-1。如果xs的长度大于ys的长度，则返回+1，如果它们长度相等，则返回0。即使其中一个序列是无限的，也可以使用此方法。</td>
</tr>
<tr>
<td>xs.indices</td>
<td>xs的索引范围，从0到xs.length - 1。</td>
</tr>
<tr>
<td>索引搜索</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs indexOf x</td>
<td>返回序列xs中等于x的第一个元素的索引（存在多种变体）。</td>
</tr>
<tr>
<td>xs lastIndexOf x</td>
<td>返回序列xs中等于x的最后一个元素的索引（存在多种变体）。</td>
</tr>
<tr>
<td>xs indexOfSlice ys</td>
<td>查找子序列ys，返回xs中匹配的第一个索引。</td>
</tr>
<tr>
<td>xs indexOfSlice ys</td>
<td>查找子序列ys，返回xs中匹配的倒数一个索引。</td>
</tr>
<tr>
<td>xs indexWhere p</td>
<td>xs序列中满足p的第一个元素。（有多种形式）</td>
</tr>
<tr>
<td>xs segmentLength (p, i)</td>
<td>xs中，从xs(i)开始并满足条件p的元素的最长连续片段的长度。</td>
</tr>
<tr>
<td>xs prefixLength p</td>
<td>xs序列中满足p条件的先头元素的最大个数。</td>
</tr>
<tr>
<td>加法</td>
<td>———————————–</td>
</tr>
<tr>
<td>x +: xs</td>
<td>由序列xs的前方添加x所得的新序列。</td>
</tr>
<tr>
<td>xs :+ x</td>
<td>由序列xs的后方追加x所得的新序列。</td>
</tr>
<tr>
<td>xs padTo (len, x)</td>
<td>在xs后方追加x，直到长度达到len后得到的序列。</td>
</tr>
<tr>
<td>更新</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs patch (i, ys, r)</td>
<td>将xs中第i个元素开始的r个元素，替换为ys所得的序列。</td>
</tr>
<tr>
<td>xs updated (i, x)</td>
<td>将xs中第i个元素替换为x后所得的xs的副本。</td>
</tr>
<tr>
<td>xs(i) &#x3D; x</td>
<td>（或写作 xs.update(i, x)，仅适用于可变序列）将xs序列中第i个元素修改为x。</td>
</tr>
<tr>
<td>排序</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs.sorted</td>
<td>通过使用xs中元素类型的标准顺序，将xs元素进行排序后得到的新序列。</td>
</tr>
<tr>
<td>xs sortWith lt</td>
<td>将lt作为比较操作，并以此将xs中的元素进行排序后得到的新序列。</td>
</tr>
<tr>
<td>xs sortBy f</td>
<td>将序列xs的元素进行排序后得到的新序列。参与比较的两个元素各自经f函数映射后得到一个结果，通过比较它们的结果来进行排序。</td>
</tr>
<tr>
<td>反转</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs.reverse</td>
<td>与xs序列元素顺序相反的一个新序列。</td>
</tr>
<tr>
<td>xs.reverseIterator</td>
<td>产生序列xs中元素的反序迭代器。</td>
</tr>
<tr>
<td>xs reverseMap f</td>
<td>以xs的相反顺序，通过f映射xs序列中的元素得到的新序列。</td>
</tr>
<tr>
<td>比较</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs startsWith ys</td>
<td>测试序列xs是否以序列ys开头（存在多种形式）。</td>
</tr>
<tr>
<td>xs endsWith ys</td>
<td>测试序列xs是否以序列ys结束（存在多种形式）。</td>
</tr>
<tr>
<td>xs contains x</td>
<td>测试xs序列中是否存在一个与x相等的元素。</td>
</tr>
<tr>
<td>xs containsSlice ys</td>
<td>测试xs序列中是否存在一个与ys相同的连续子序列。</td>
</tr>
<tr>
<td>(xs corresponds ys)(p)</td>
<td>测试序列xs与序列ys中对应的元素是否满足二元的判断式p。</td>
</tr>
<tr>
<td>多集操作</td>
<td>———————————–</td>
</tr>
<tr>
<td>xs intersect ys</td>
<td>序列xs和ys的交集，并保留序列xs中的顺序。</td>
</tr>
<tr>
<td>xs diff ys</td>
<td>序列xs和ys的差集，并保留序列xs中的顺序。</td>
</tr>
<tr>
<td>xs union ys</td>
<td>并集；同xs ++ ys。</td>
</tr>
<tr>
<td>xs.distinct</td>
<td>不含重复元素的xs的子序列。</td>
</tr>
</tbody></table>
<h2 id="Buffer类的操作"><a href="#Buffer类的操作" class="headerlink" title="Buffer类的操作"></a>Buffer类的操作</h2><table>
<thead>
<tr>
<th>WHAT IT IS</th>
<th>WHAT IT DOES</th>
</tr>
</thead>
<tbody><tr>
<td>加法：</td>
<td>———————————–</td>
</tr>
<tr>
<td>buf +&#x3D; x</td>
<td>将元素x追加到buffer，并将buf自身作为结果返回。</td>
</tr>
<tr>
<td>buf +&#x3D; (x, y, z)</td>
<td>将给定的元素追加到buffer。</td>
</tr>
<tr>
<td>buf ++&#x3D; xs</td>
<td>将xs中的所有元素追加到buffer。</td>
</tr>
<tr>
<td>x +&#x3D;: buf</td>
<td>将元素x添加到buffer的前方。</td>
</tr>
<tr>
<td>xs ++&#x3D;: buf</td>
<td>将xs中的所有元素都添加到buffer的前方。</td>
</tr>
<tr>
<td>buf insert (i, x)</td>
<td>将元素x插入到buffer中索引为i的位置。</td>
</tr>
<tr>
<td>buf insertAll (i, xs)</td>
<td>将xs的所有元素都插入到buffer中索引为i的位置。</td>
</tr>
<tr>
<td>移除：</td>
<td>———————————–</td>
</tr>
<tr>
<td>buf -&#x3D; x</td>
<td>将元素x从buffer中移除。</td>
</tr>
<tr>
<td>buf remove i</td>
<td>将buffer中索引为i的元素移除。</td>
</tr>
<tr>
<td>buf remove (i, n)</td>
<td>将buffer中从索引i开始的n个元素移除。</td>
</tr>
<tr>
<td>buf trimStart n</td>
<td>移除buffer中的前n个元素。</td>
</tr>
<tr>
<td>buf trimEnd n</td>
<td>移除buffer中的后n个元素。</td>
</tr>
<tr>
<td>buf.clear()</td>
<td>移除buffer中的所有元素。</td>
</tr>
<tr>
<td>克隆：</td>
<td>———————————–</td>
</tr>
<tr>
<td>buf.clone</td>
<td>与buf具有相同元素的新buffer。</td>
</tr>
</tbody></table>
<h2 id="Traversable对象的操作"><a href="#Traversable对象的操作" class="headerlink" title="Traversable对象的操作"></a>Traversable对象的操作</h2><table>
<thead>
<tr>
<th>WHAT IT IS</th>
<th>WHAT IT DOES</th>
</tr>
</thead>
<tbody><tr>
<td>抽象方法：</td>
<td></td>
</tr>
<tr>
<td>xs foreach f</td>
<td>对xs中的每一个元素执行函数f</td>
</tr>
<tr>
<td>加运算（Addition）：</td>
<td></td>
</tr>
<tr>
<td>xs ++ ys</td>
<td>生成一个由xs和ys中的元素组成容器。ys是一个TraversableOnce容器，即Taversable类型或迭代器。</td>
</tr>
<tr>
<td>Maps:</td>
<td></td>
</tr>
<tr>
<td>xs map f</td>
<td>通过函数xs中的每一个元素调用函数f来生成一个容器。</td>
</tr>
<tr>
<td>xs flatMap f</td>
<td>通过对容器xs中的每一个元素调用作为容器的值函数f，在把所得的结果连接起来作为一个新的容器。</td>
</tr>
<tr>
<td>xs collect f</td>
<td>通过对每个xs中的符合定义的元素调用偏函数f，并把结果收集起来生成一个集合。</td>
</tr>
<tr>
<td>转换（Conversions）：</td>
<td></td>
</tr>
<tr>
<td>xs.toArray</td>
<td>把容器转换为一个数组</td>
</tr>
<tr>
<td>xs.toList</td>
<td>把容器转换为一个list</td>
</tr>
<tr>
<td>xs.toIterable</td>
<td>把容器转换为一个迭代器。</td>
</tr>
<tr>
<td>xs.toSeq</td>
<td>把容器转换为一个序列</td>
</tr>
<tr>
<td>xs.toIndexedSeq</td>
<td>把容器转换为一个索引序列</td>
</tr>
<tr>
<td>xs.toStream</td>
<td>把容器转换为一个延迟计算的流。</td>
</tr>
<tr>
<td>xs.toSet</td>
<td>把容器转换为一个集合（Set）。</td>
</tr>
<tr>
<td>xs.toMap</td>
<td>把由键&#x2F;值对组成的容器转换为一个映射表（map）。如果该容器并不是以键&#x2F;值对作为元素的，那么调用这个操作将会导致一个静态类型的错误。</td>
</tr>
<tr>
<td>拷贝（Copying）：</td>
<td></td>
</tr>
<tr>
<td>xs copyToBuffer buf</td>
<td>把容器的所有元素拷贝到buf缓冲区。</td>
</tr>
<tr>
<td>xs copyToArray(arr, s, n)</td>
<td>拷贝最多n个元素到数组arr的坐标s处。参数s，n是可选项。</td>
</tr>
<tr>
<td>大小判断（Size info）：</td>
<td></td>
</tr>
<tr>
<td>xs.isEmpty</td>
<td>测试容器是否为空。</td>
</tr>
<tr>
<td>xs.nonEmpty</td>
<td>测试容器是否包含元素。</td>
</tr>
<tr>
<td>xs.size</td>
<td>计算容器内元素的个数。</td>
</tr>
<tr>
<td>xs.hasDefiniteSize</td>
<td>如果xs的大小是有限的，则为true。</td>
</tr>
<tr>
<td>元素检索（Element Retrieval）：</td>
<td></td>
</tr>
<tr>
<td>xs.head</td>
<td>返回容器内第一个元素（或其他元素，若当前的容器无序）。</td>
</tr>
<tr>
<td>xs.headOption</td>
<td>xs选项值中的第一个元素，若xs为空则为None。</td>
</tr>
<tr>
<td>xs.last</td>
<td>返回容器的最后一个元素（或某个元素，如果当前的容器无序的话）。</td>
</tr>
<tr>
<td>xs.lastOption</td>
<td>xs选项值中的最后一个元素，如果xs为空则为None。</td>
</tr>
<tr>
<td>xs find p</td>
<td>查找xs中满足p条件的元素，若存在则返回第一个元素；若不存在，则为空。</td>
</tr>
<tr>
<td>子容器（Subcollection）：</td>
<td></td>
</tr>
<tr>
<td>xs.tail</td>
<td>返回由除了xs.head外的其余部分。</td>
</tr>
<tr>
<td>xs.init</td>
<td>返回除xs.last外的其余部分。</td>
</tr>
<tr>
<td>xs slice (from, to)</td>
<td>返回由xs的一个片段索引中的元素组成的容器（从from到to，但不包括to）。</td>
</tr>
<tr>
<td>xs take n</td>
<td>由xs的第一个到第n个元素（或当xs无序时任意的n个元素）组成的容器。</td>
</tr>
<tr>
<td>xs drop n</td>
<td>由除了xs take n以外的元素组成的容器。</td>
</tr>
<tr>
<td>xs takeWhile p</td>
<td>容器xs中最长能够满足断言p的前缀。</td>
</tr>
<tr>
<td>xs dropWhile p</td>
<td>容器xs中除了xs takeWhile p以外的全部元素。</td>
</tr>
<tr>
<td>xs filter p</td>
<td>由xs中满足条件p的元素组成的容器。</td>
</tr>
<tr>
<td>xs withFilter p</td>
<td>这个容器是一个不太严格的过滤器。子容器调用map，flatMap，foreach和withFilter只适用于xs中那些的满足条件p的元素。</td>
</tr>
<tr>
<td>xs filterNot p</td>
<td>由xs中不满足条件p的元素组成的容器。</td>
</tr>
<tr>
<td>拆分（Subdivision）：</td>
<td></td>
</tr>
<tr>
<td>xs splitAt n</td>
<td>把xs从指定位置的拆分成两个容器（xs take n和xs drop n）。</td>
</tr>
<tr>
<td>xs span p</td>
<td>根据一个断言p将xs拆分为两个容器（xs takeWhile p, xs.dropWhile p）。</td>
</tr>
<tr>
<td>xs partition p</td>
<td>把xs分割为两个容器，符合断言p的元素赋给一个容器，其余的赋给另一个(xs filter p, xs.filterNot p)。</td>
</tr>
<tr>
<td>xs groupBy f</td>
<td>根据判别函数f把xs拆分一个到容器（collection）的map中。</td>
</tr>
<tr>
<td>条件元素（Element Conditions）：</td>
<td></td>
</tr>
<tr>
<td>xs forall p</td>
<td>返回一个布尔值表示用于表示断言p是否适用xs中的所有元素。</td>
</tr>
<tr>
<td>xs exists p</td>
<td>返回一个布尔值判断xs中是否有部分元素满足断言p。</td>
</tr>
<tr>
<td>xs count p</td>
<td>返回xs中符合断言p条件的元素个数。</td>
</tr>
<tr>
<td>折叠（Fold）：</td>
<td></td>
</tr>
<tr>
<td>(z &#x2F;: xs)(op)</td>
<td>在xs中，对由z开始从左到右的连续元素应用二进制运算op。</td>
</tr>
<tr>
<td>(xs :\ z)(op)</td>
<td>在xs中，对由z开始从右到左的连续元素应用二进制运算op</td>
</tr>
<tr>
<td>xs.foldLeft(z)(op)</td>
<td>与(z &#x2F;: xs)(op)相同。</td>
</tr>
<tr>
<td>xs.foldRight(z)(op)</td>
<td>与 (xs :\ z)(op)相同。</td>
</tr>
<tr>
<td>xs reduceLeft op</td>
<td>非空容器xs中的连续元素从左至右调用二进制运算op。</td>
</tr>
<tr>
<td>xs reduceRight op</td>
<td>非空容器xs中的连续元素从右至左调用二进制运算op。</td>
</tr>
<tr>
<td>特殊折叠（Specific Fold）：</td>
<td></td>
</tr>
<tr>
<td>xs.sum</td>
<td>返回容器xs中数字元素的和。</td>
</tr>
<tr>
<td>xs.product</td>
<td>xs返回容器xs中数字元素的积。</td>
</tr>
<tr>
<td>xs.min</td>
<td>容器xs中有序元素值中的最小值。</td>
</tr>
<tr>
<td>xs.max</td>
<td>容器xs中有序元素值中的最大值。</td>
</tr>
<tr>
<td>字符串（String）：</td>
<td></td>
</tr>
<tr>
<td>xs addString (b, start, sep, end)</td>
<td>把一个字符串加到StringBuilder对象b中，该字符串显示为将xs中所有元素用分隔符sep连接起来并封装在start和end之间。其中start，end和sep都是可选的。</td>
</tr>
<tr>
<td>xs mkString (start, sep, end)</td>
<td>把容器xs转换为一个字符串，该字符串显示为将xs中所有元素用分隔符sep连接起来并封装在start和end之间。其中start，end和sep都是可选的。</td>
</tr>
<tr>
<td>xs.stringPrefix</td>
<td>返回一个字符串，该字符串是以容器名开头的xs.toString。</td>
</tr>
<tr>
<td>视图（View）：</td>
<td></td>
</tr>
<tr>
<td>xs.view</td>
<td>通过容器xs生成一个视图。</td>
</tr>
<tr>
<td>xs view (from, to)</td>
<td>生成一个表示在指定索引范围内的xs元素的视图。</td>
</tr>
</tbody></table>
<h2 id="Trait-Iterable操作"><a href="#Trait-Iterable操作" class="headerlink" title="Trait Iterable操作"></a>Trait Iterable操作</h2><table>
<thead>
<tr>
<th>WHAT IT IS</th>
<th>WHAT IT DOES</th>
</tr>
</thead>
<tbody><tr>
<td>抽象方法：</td>
<td></td>
</tr>
<tr>
<td>xs.iterator	xs</td>
<td>迭代器生成的每一个元素，以相同的顺序就像foreach一样遍历元素。</td>
</tr>
<tr>
<td>其他迭代器：</td>
<td></td>
</tr>
<tr>
<td>xs grouped size</td>
<td>一个迭代器生成一个固定大小的容器（collection）块。</td>
</tr>
<tr>
<td>xs sliding size</td>
<td>一个迭代器生成一个固定大小的滑动窗口作为容器（collection）的元素。</td>
</tr>
<tr>
<td>子容器（Subcollection）：</td>
<td></td>
</tr>
<tr>
<td>xs takeRight n</td>
<td>一个容器（collection）由xs的最后n个元素组成（或，若定义的元素是无序，则由任意的n个元素组成）。</td>
</tr>
<tr>
<td>xs dropRight n</td>
<td>一个容器（collection）由除了xs 被取走的（执行过takeRight （）方法）n个元素外的其余元素组成。</td>
</tr>
<tr>
<td>拉链方法（Zippers）：</td>
<td></td>
</tr>
<tr>
<td>xs zip ys</td>
<td>把一对容器 xs和ys的包含的元素合成到一个iterabale。</td>
</tr>
<tr>
<td>xs zipAll (ys, x, y)</td>
<td>一对容器 xs 和ys的相应的元素合并到一个iterable ，实现方式是通过附加的元素x或y，把短的序列被延展到相对更长的一个上。</td>
</tr>
<tr>
<td>xs.zip WithIndex</td>
<td>把一对容器xs和它的序列，所包含的元素组成一个iterable 。</td>
</tr>
<tr>
<td>比对：</td>
<td></td>
</tr>
<tr>
<td>xs sameElements ys</td>
<td>测试 xs 和 ys 是否以相同的顺序包含相同的元素。</td>
</tr>
</tbody></table>
<br>
> 最后更新于2018.7.26
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Shadowsocks</title>
    <url>/2018/05/28/Shaow-socks/</url>
    <content><![CDATA[<blockquote>
<p>科学上网从我做起  </p>
</blockquote>
<h2 id="重大更新"><a href="#重大更新" class="headerlink" title="重大更新"></a>重大更新</h2><hr>
<ul>
<li>现在大多数的ssr或者ss图形界面的在ubuntu18上已经歇菜了,但是这里分享一个命令行下的ss和一个图形界面的ssr(推荐)。</li>
</ul>
<h3 id="命令行方式的ss"><a href="#命令行方式的ss" class="headerlink" title="命令行方式的ss"></a>命令行方式的ss</h3><hr>
<ul>
<li><pre><code>
</code></pre>
</li>
</ul>
<p>sudo apt-get install python-pip<br>sudo pip install shadowsocks</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- 通过配置文件指定配置信息</span><br></pre></td></tr></table></figure>
<p>mkdir ~&#x2F;.shadowsocks<br>cd .shadowsocks<br>touch ss.json<br>&#x2F;&#x2F;添加内容如下<br>{<br>    “server”:”1.1.1.1”,<br>    “server_port”:8388,<br>    “local_address”: “127.0.0.1”,<br>    “local_port”:1080,<br>    “password”:”your passwd”,<br>    “timeout”:300,<br>    “method”:”aes-256-cfb”<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- server填你的服务器ip，sever_port填远程端口号，local_address本地ip，local_part本地端口，password填密码，timeout是延迟时间，method是加密方式，按照实际情况填写并保存</span><br><span class="line"></span><br><span class="line">- 终端运行：`sslocal -c ~/.shadowsocks/ss.json`</span><br><span class="line"></span><br><span class="line">- 后台运行(不推荐)：`sslocal -c ~/.shadowsocks/ss.json -d start`</span><br><span class="line"></span><br><span class="line">- 现在运行的话会有报错，修改方法：</span><br></pre></td></tr></table></figure>
<p>vim &#x2F;usr&#x2F;local&#x2F;lib&#x2F;python2.7&#x2F;dist-packages&#x2F;shadowsocks&#x2F;crypto&#x2F;openssl.py<br>&#x2F;&#x2F;跳转到52行（shadowsocks2.8.2版本，其他版本搜索一下cleanup<br>&#x2F;&#x2F;<br>将第52行libcrypto.EVP_CIPHER_CTX_cleanup.argtypes &#x3D; (c_void_p,)<br>改为libcrypto.EVP_CIPHER_CTX_reset.argtypes &#x3D; (c_void_p,)<br>&#x2F;&#x2F;<br>再次搜索cleanup（全文件共2处，此处位于111行），将libcrypto.EVP_CIPHER_CTX_cleanup(self._ctx)<br>改为libcrypto.EVP_CIPHER_CTX_reset(self._ctx)<br>&#x2F;&#x2F;然后就可以正常运行了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- 详细见[博客](https://blog.csdn.net/blackfrog_unique/article/details/60320737)</span><br><span class="line"></span><br><span class="line">### 图形界面的ssr</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- 首先说一句，图形界面不仅美观好用，而且可以支持在线订阅，及时更新，总之，**我们又可以优雅地翻墙了**，美滋滋～</span><br><span class="line"></span><br><span class="line">- 这里我fork了一份相关github的代码，[链接](https://github.com/erguotou520/electron-ssr/releases)</span><br><span class="line"></span><br><span class="line">- 配置方法非常简单，可以导入json文件，或者从剪贴板导入等等。</span><br><span class="line"></span><br><span class="line">&gt; [参考文档](https://blog.csdn.net/u010658816/article/details/79344970)</span><br><span class="line"></span><br><span class="line">## 重点</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">- 上面两种方式而选一即可，但是下面还有最关键的一步，配置浏览器代理。</span><br><span class="line"></span><br><span class="line">- 首先，想要实现全局模式请按如下方法设置：</span><br><span class="line">系统设置 &gt;&gt; 网络 &gt;&gt; 网络代理 &gt;&gt; 方法 &gt;&gt; 手动</span><br><span class="line"></span><br><span class="line">- 再将Socks主机的ip和端口填好，如图。</span><br><span class="line">![](/images/ssr/systemset.png)</span><br><span class="line"></span><br><span class="line">- 然后，需要在google商店下载SwitchyOmega插件，但是需要翻墙～这里可以进入如下[网址](http://googlehelper.net/),先下载一个googlehelper插件(打开chrome所有插件，把下载的插件直接拉进去即可添加完成)，用邮箱注册一个免费的体验账号，先翻了墙再说。</span><br><span class="line"></span><br><span class="line">- 然后去chrome网上应用商店下载SwitchyOmega，接下来便可以配置了。</span><br><span class="line"></span><br><span class="line">- 在SwitchyOmega的配置页面  </span><br><span class="line">先新建一个情景模式，选择代理服务器，名称为gfwlist(这个随意)  </span><br><span class="line">  配置时,代理服务器和代理端口分别是ssr config中的   </span><br><span class="line">    &quot;local_address&quot;:&quot;127.0.0.1&quot;  </span><br><span class="line">    &quot;local_port&quot;:1080  </span><br><span class="line">确认后无误后应用选项  </span><br><span class="line">![](/images/ssr/gfwlist.png)</span><br><span class="line"></span><br><span class="line">- 接下来点击情景模式中的自动切换，输入参数如下：</span><br></pre></td></tr></table></figure>
<p>条件设置：raw.githubusercontent.com<br>规则列表网址：<a href="https://raw.githubusercontent.com/gfwlist/gfwlist/master/gfwlist.txt">https://raw.githubusercontent.com/gfwlist/gfwlist/master/gfwlist.txt</a><br>点击立即更新情景模式<br>结束,在SwitchyOmega插件(chorme浏览器右上角处)那换成自动切换,大功告成.</p>
<pre><code>![](/images/ssr/auto_switch_01.png)

![](/images/ssr/auto_switch_02.png)

![](/images/ssr/example.png)

- 最后把多余的情景模式删除,看起来清爽一点。

- 这样就相当于在chorme中启用的是pcd模式,在能穿墙的同时,既保证了访问国内网站速度不慢,又节省了ssr的流量。

- 接下来就可以优雅地体验翻墙上网了～

&lt;br&gt;
&gt; 最后更新于2018.5.28
</code></pre>
]]></content>
      <categories>
        <category>ScientificSurfing</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
        <tag>๑ScientificSurfing</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark学习笔记——基础</title>
    <url>/2018/04/11/Spark-learning/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记  </p>
</blockquote>
<blockquote>
<p>有问题经常去 <strong><a href="https://stackoverflow.com/">stackoverflow.com</a></strong> 上找找</p>
</blockquote>
<h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><hr>
<ul>
<li><p>首先确保电脑安装了JAVA Development Kit JDK(使用java -version查看版本)</p>
</li>
<li><p>安装hadoop（比如我在安装了hadoop-2.7.5），后面下载spark的版本时也要选择与hadoop版本一致的spark版本。然后添加hadoop路径到PATH（修改&#x2F;etc&#x2F;profile文件）</p>
</li>
<li><p>hadoop单机伪分布式环境搭建详细见我的另一篇博客——<a href="https://fleschier.github.io/2018/05/Hadoop-usage/">hadoop单机伪分布式搭建</a></p>
</li>
<li><p>下载spark，解压，设置路径。 ———<a href="https://www.cnblogs.com/earendil/p/5564492.html">详细设置</a></p>
</li>
<li><p>调整日志级别控制输出的信息量：在spark安装文件目录的conf目录下将log4j.properties.template 复制为  log4j.properties, 然后找到 log4j.rootCategory &#x3D; INFO, console。将其中的INFO改为WARN即可。</p>
</li>
<li><p>我的spark的安装目录是<code>/usr/local/Apache/</code>，可以使用命令<code>echo $SPARK_HOME</code>来查看spark的安装路径。</p>
</li>
<li><p>将spark的安装路径加到系统PATH当中(修改<code>/etc/profile文件</code>)，然后重启生效之后可在终端通过<code>spark-shell</code>命令进入scala的解释器环境，如图：<br><img src="/images/spark/spark-shell.png"></p>
</li>
<li><p><a href="https://www.scala-sbt.org/download.html">sbt安装网址</a></p>
</li>
</ul>
<h2 id="编程环境"><a href="#编程环境" class="headerlink" title="编程环境"></a>编程环境</h2><hr>
<ul>
<li>spark支持多种语言的编写，包括Python，Java，scala以及R语言，<strong>本篇中均采用scala作为编程语言</strong>。</li>
<li>使用的编译器为intellij IDEA，工程为maven项目</li>
</ul>
<h3 id="编译器的一些使用技巧"><a href="#编译器的一些使用技巧" class="headerlink" title="编译器的一些使用技巧"></a>编译器的一些使用技巧</h3><ul>
<li><p>ctrl + Q 在intellij中可以快速地查看变量的类型</p>
</li>
<li><p>Maven 项目使用maven自带的打jar包的即可，不推荐在build当中打jar包</p>
</li>
<li><p>新建的Maven项目很可能缺失很多spark的库，需要修改pom文件来使得编译器引入这些库</p>
</li>
</ul>
<h2 id="以maven项目创建scala工程"><a href="#以maven项目创建scala工程" class="headerlink" title="以maven项目创建scala工程"></a>以maven项目创建scala工程</h2><ul>
<li><p>详见 <a href="https://blog.csdn.net/u012373815/article/details/53266301">spark开发环境搭建</a></p>
</li>
<li><p>其中包括修改pom配置文件。</p>
</li>
</ul>
<h2 id="spark下的-wordcount"><a href="#spark下的-wordcount" class="headerlink" title="spark下的 wordcount"></a>spark下的 wordcount</h2><hr>
<blockquote>
<p>摘自官方文档</p>
</blockquote>
<ul>
<li>完整版：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">text_file = sc.textFile(&quot;hdfs://...&quot;) //文件需要放在hdfs上</span><br><span class="line">counts = text_file.flatMap(lambda line: line.split(&quot; &quot;)) \</span><br><span class="line">             .map(lambda word: (word, 1)) \</span><br><span class="line">             .reduceByKey(lambda a, b: a + b)</span><br><span class="line">counts.saveAsTextFile(&quot;hdfs://...&quot;)</span><br></pre></td></tr></table></figure></li>
<li>精简版(在spark-shell中执行就这一条语句即可)：  (足见scala的优美与强大)<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val counts = sc.textFile(&quot;hdfs://...(文件路径，单机或者hdfs文件都可以)&quot;).flatMap(_.split(&quot; &quot;)).map(x =&gt; (x,1)).reduceByKey(_ + _).collect().foreach(println) //打印结果</span><br></pre></td></tr></table></figure></li>
<li>注意：上面的.collect()不能少，因为这是一个action操作，一个语句如果没有action操作则spark什么也不会做。同时，<strong>读取文件的路径一定要用引号括起来！</strong></li>
</ul>
<h2 id="关于jar包以及提交集群运行"><a href="#关于jar包以及提交集群运行" class="headerlink" title="关于jar包以及提交集群运行"></a>关于jar包以及提交集群运行</h2><hr>
<h3 id="spark-submit命令"><a href="#spark-submit命令" class="headerlink" title="spark-submit命令"></a><code>spark-submit</code>命令</h3><ul>
<li>spark-submit脚本用于在集群上启动应用程序,它位于Spark的bin目录中。</li>
</ul>
<h4 id="命令行提交"><a href="#命令行提交" class="headerlink" title="命令行提交"></a>命令行提交</h4><ul>
<li>命令行下切换到spark的安装目录，执行</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">./bin/spark-submit \  </span><br><span class="line"> --class &lt;main-class&gt; \</span><br><span class="line"> --master &lt;master-url&gt; \</span><br><span class="line"> --deploy-mode &lt;deploy-mode&gt; \</span><br><span class="line"> --conf &lt;key&gt;=&lt;value&gt; \  </span><br><span class="line">     ... # other options  </span><br><span class="line"> &lt;application-jar&gt; \  </span><br><span class="line"> [application-arguments]</span><br></pre></td></tr></table></figure>

<h4 id="脚本方式：-创建并编辑-sh脚本文件，赋予执行权限即可"><a href="#脚本方式：-创建并编辑-sh脚本文件，赋予执行权限即可" class="headerlink" title="脚本方式：(创建并编辑.sh脚本文件，赋予执行权限即可)"></a>脚本方式：(创建并编辑.sh脚本文件，赋予执行权限即可)</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">input=/data/</span><br><span class="line">output=/AR_res/</span><br><span class="line">    hdfs dfs -rm -r $output</span><br><span class="line">    hdfs dfs -rm -r /user_res</span><br><span class="line">    spark-submit \</span><br><span class="line">        -v \</span><br><span class="line">        --name &quot;ARsMiner&quot; \</span><br><span class="line">        --class &quot;com.hazzacheng.AR.Main&quot; \</span><br><span class="line">        --executor-cores 4 \</span><br><span class="line">        --executor-memory 10G \</span><br><span class="line">        --driver-memory 20G \</span><br><span class="line">        AR.jar \</span><br><span class="line">        $input \</span><br><span class="line">        $output \</span><br><span class="line">        2&gt;&amp;1 | tee log/AR_log</span><br><span class="line">    rm -rf AR_res/</span><br><span class="line">    hdfs dfs -get $output</span><br><span class="line">    cat /root$output/part* &gt; /root/res</span><br><span class="line">    #sort /root$output/part* &gt; /root/res</span><br></pre></td></tr></table></figure>

<h4 id="常用的参数讲解"><a href="#常用的参数讲解" class="headerlink" title="常用的参数讲解"></a>常用的参数讲解</h4><ul>
<li><p>–class：应用程序的入口点,main函数所在的类（例如org.apache.spark.examples.SparkPi）</p>
</li>
<li><p>–master：群集的主网址（例如spark：&#x2F;&#x2F;23.195.26.187：7077）</p>
</li>
<li><p>–deploy-mode：是否将驱动程序部署在工作节点（cluster）上，或作为外部客户机（client）本地部署（默认值：client）</p>
</li>
<li><p>–conf：Key &#x3D; value格式的任意Spark配置属性。对于包含空格的值，用引号括起“key &#x3D; value”（参见示例）。</p>
</li>
<li><p>application-jar：包含应用程序和所有依赖关系的捆绑jar的路径。该URL必须在集群内全局可见，例如hdfs：&#x2F;&#x2F;路径或所有节点上存在的file：&#x2F;&#x2F;路径。</p>
</li>
<li><p>application-arguments：参数传递给主类的main方法（如果有的话）</p>
</li>
<li><p>常见的部署策略是从与您的工作机器物理上位于的网关机器提交应用程序（例如，独立的EC2集群中的主节点）。在此设置中，client模式是适当的。在client模式下，驱动程序直接在spark-submit过程中启动，该过程充当集群的客户端。应用程序的输入和输出连接到控制台。因此，该模式特别适用于涉及REPL（例如Spark shell）的应用。</p>
</li>
<li><p>或者，如果您的应用程序从远离工作机器（例如本地在笔记本电脑上）的机器提交，通常使用cluster模式来最大限度地减少驱动程序和执行程序之间的网络延迟。</p>
</li>
</ul>
<h4 id="其他的一些示例"><a href="#其他的一些示例" class="headerlink" title="其他的一些示例"></a>其他的一些示例</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Run application locally on 8 cores</span><br><span class="line">./bin/spark-submit \</span><br><span class="line">  --class org.apache.spark.examples.SparkPi \</span><br><span class="line">  --master local[8] \</span><br><span class="line">  /path/to/examples.jar \</span><br><span class="line">  100</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Run on a Spark standalone cluster in client deploy mode</span><br><span class="line">./bin/spark-submit \</span><br><span class="line">  --class org.apache.spark.examples.SparkPi \</span><br><span class="line">  --master spark://207.184.161.138:7077 \</span><br><span class="line">  --executor-memory 20G \</span><br><span class="line">  --total-executor-cores 100 \</span><br><span class="line">  /path/to/examples.jar \</span><br><span class="line">  1000</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Run on a Spark standalone cluster in cluster deploy mode with supervise</span><br><span class="line">./bin/spark-submit \</span><br><span class="line">  --class org.apache.spark.examples.SparkPi \</span><br><span class="line">  --master spark://207.184.161.138:7077 \</span><br><span class="line">  --deploy-mode cluster \</span><br><span class="line">  --supervise \</span><br><span class="line">  --executor-memory 20G \</span><br><span class="line">  --total-executor-cores 100 \</span><br><span class="line">  /path/to/examples.jar \</span><br><span class="line">  1000</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Run on a YARN cluster</span><br><span class="line">export HADOOP_CONF_DIR=XXX</span><br><span class="line">./bin/spark-submit \</span><br><span class="line">  --class org.apache.spark.examples.SparkPi \</span><br><span class="line">  --master yarn \</span><br><span class="line">  --deploy-mode cluster \  # can be client for client mode</span><br><span class="line">  --executor-memory 20G \</span><br><span class="line">  --num-executors 50 \</span><br><span class="line">  /path/to/examples.jar \</span><br><span class="line">  1000</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Run a Python application on a Spark standalone cluster</span><br><span class="line">./bin/spark-submit \</span><br><span class="line">  --master spark://207.184.161.138:7077 \</span><br><span class="line">  examples/src/main/python/pi.py \</span><br><span class="line">  1000</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Run on a Mesos cluster in cluster deploy mode with supervise</span><br><span class="line">./bin/spark-submit \</span><br><span class="line">  --class org.apache.spark.examples.SparkPi \</span><br><span class="line">  --master mesos://207.184.161.138:7077 \</span><br><span class="line">  --deploy-mode cluster \</span><br><span class="line">  --supervise \</span><br><span class="line">  --executor-memory 20G \</span><br><span class="line">  --total-executor-cores 100 \</span><br><span class="line">  http://path/to/examples.jar \</span><br><span class="line">  1000</span><br></pre></td></tr></table></figure>

<h2 id="spark程序入口"><a href="#spark程序入口" class="headerlink" title="spark程序入口"></a>spark程序入口</h2><ul>
<li>Spark SQL中所有功能的入口点是 SparkSession 类. 要创建一个 SparkSession, 仅使用 SparkSession.builder()就可以了：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import org.apache.spark.sql.SparkSession</span><br><span class="line">val sparkses = SparkSession</span><br><span class="line">  .builder()	//必须有</span><br><span class="line">  [.appName(&quot;Spark SQL basic example&quot;) ]</span><br><span class="line">  [.config(&quot;spark.some.config.option&quot;, &quot;some-value&quot;)]</span><br><span class="line">  [.master(&quot;local&quot;)]</span><br><span class="line">  .getOrCreate() //必须有</span><br><span class="line">// For implicit conversions like converting RDDs to DataFrames    //即需要用到类型转换时，要引入这个以你的变量名字开头的隐式转换包</span><br><span class="line">import sparkses.implicits._</span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>未来Dataset可能会取代RDD作为主要使用的API，而RDD会成为最底层的供用户使用的API</p>
</blockquote>
<ul>
<li>spark一般的入口是 sparkcontext类：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import org.apache.spark.&#123;SparkConf, SparkContext&#125;</span><br><span class="line">...</span><br><span class="line"> val conf = new SparkConf()[.set(...)...]</span><br><span class="line"> val sc = new SparkContext(conf)</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="一些问题以及解决方法"><a href="#一些问题以及解决方法" class="headerlink" title="一些问题以及解决方法"></a>一些问题以及解决方法</h2><hr>
<blockquote>
<p>如要学习spark请看官方文档以及源码</p>
</blockquote>
<blockquote>
<p>有问题经常去 <strong><a href="https://stackoverflow.com/">stackoverflow.com</a></strong> 上找找</p>
</blockquote>
<ul>
<li><strong>这里推荐一个<a href="http://spark.apachecn.org/">官方文档的中文版</a>供英语薄弱的朋友阅读。</strong></li>
</ul>
<h3 id="试验了FP-Growth算法，打包到集群运行时报错"><a href="#试验了FP-Growth算法，打包到集群运行时报错" class="headerlink" title="试验了FP-Growth算法，打包到集群运行时报错:"></a>试验了FP-Growth算法，打包到集群运行时报错:</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Items in a transaction must be unique but got WrappedArrayMaven</span><br></pre></td></tr></table></figure>
<p>出现此问题的主要原因是fpg算法要求输入的数据是<code>RDD(Array[String])</code>类型，但是Array类型中是不允许出现重复的内容的，所以才会出现这个错误，只需要对输入的数据做个distinct即可</p>
<h3 id="spark-shell进不去，提示Insufficient-space-for-shared-memory-file"><a href="#spark-shell进不去，提示Insufficient-space-for-shared-memory-file" class="headerlink" title="spark-shell进不去，提示Insufficient space for shared memory file"></a>spark-shell进不去，提示Insufficient space for shared memory file</h3><ul>
<li><p>出现这个问题的原因可能是某个文件分区的空间满了，一般可能是日志文件太多。</p>
</li>
<li><p>使用<code>df</code>命令查看文件系统：<br><img src="/images/spark/%E9%9B%86%E7%BE%A4%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%9A%84%E9%97%AE%E9%A2%98.png"><br>可以看到挂载点 <code>/</code> 的使用率已经是100%了，所以spark-shell会打不开，删掉一些文件即可。</p>
</li>
</ul>
<h2 id="一些小细节"><a href="#一些小细节" class="headerlink" title="一些小细节"></a>一些小细节</h2><hr>
<ul>
<li><p>spark中出现一长细条的信息才是报错</p>
</li>
<li><p>List 的 <code>distinct</code> 方法会对列表去重</p>
</li>
<li><p>item::list 将新生成一个列表，item会成为新列表的有元素</p>
</li>
<li><p>Array 不能被<code>flatMap</code>,必须为<code>list.(?)</code></p>
</li>
<li><p>scala中的<code>.sortBy()</code>方法如果在括号里最前面加上一个减号表示按逆序排序。</p>
</li>
</ul>
<br>
> 最后更新于2018.5.27
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑BigData</tag>
        <tag>๑Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark实际操作过程中的一些调优笔记</title>
    <url>/2018/09/13/Spark-notes/</url>
    <content><![CDATA[<h2 id="Spark调优的一些参数设置"><a href="#Spark调优的一些参数设置" class="headerlink" title="Spark调优的一些参数设置"></a>Spark调优的一些参数设置</h2><hr>
<h3 id="SparkConf的一些参数设置"><a href="#SparkConf的一些参数设置" class="headerlink" title="SparkConf的一些参数设置"></a>SparkConf的一些参数设置</h3><ul>
<li>见博客<a href="https://www.iteblog.com/archives/1659.html">Spark性能优化：资源调优篇</a></li>
</ul>
<h3 id="较为全面的一个Spark性能参数讲解"><a href="#较为全面的一个Spark性能参数讲解" class="headerlink" title="较为全面的一个Spark性能参数讲解"></a>较为全面的一个Spark性能参数讲解</h3><ul>
<li><a href="https://spark-config.readthedocs.io/en/latest/index.html">Spark性能相关参数配置</a></li>
</ul>
<h3 id="spark分区器"><a href="#spark分区器" class="headerlink" title="spark分区器"></a>spark分区器</h3><ul>
<li><p>主要有HashPartitioner和RangePartitioner两种。</p>
</li>
<li><p>HashPatitioner按照给定key计算出的HashCode通过模运算来分区，缺点是可能造成分区出现数据倾斜。</p>
</li>
<li><p>RangePartioner尽量保证每个分区中数据量的均匀，而且分区与分区之间是有序的(分区内部无序)。在分区计算时可能需要消耗部分资源，因此选择那种分区方法应看具体情况。</p>
</li>
<li><p><a href="https://www.iteblog.com/archives/1522.html">Spark分区器HashPartitioner和RangePartitioner代码详解</a></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑BigData</tag>
        <tag>๑Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark学习笔记——进阶</title>
    <url>/2018/04/11/Spark-superior/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<blockquote>
<p><strong>有问题经常去<a href="https://stackoverflow.com/">stackoverflow.com</a>上找找</strong><br>或者直接去看官方的API原版或者翻译版:<a href="http://spark.apachecn.org/">官方文档的中文版</a></p>
</blockquote>
<h2 id="DataSet-与-DataFrame"><a href="#DataSet-与-DataFrame" class="headerlink" title="DataSet 与 DataFrame"></a>DataSet 与 DataFrame</h2><hr>
<ul>
<li><p>Dataset是特定域对象中的强类型集合，它可以使用函数或者相关操作并行地进行转换等操作。每个Dataset都有一个称为DataFrame的非类型化的视图，这个视图是行的数据集。</p>
</li>
<li><p>DataSet和RDD主要的区别是：DataSet是特定域的对象集合；然而RDD是任何对象的集合。DataSet的API总是强类型的；而且可以利用这些模式进行优化，然而RDD却不行。</p>
</li>
<li><p>Dataset的定义中还提到了DataFrame，DataFrame是特殊的Dataset，它在编译时不会对模式进行检测。在未来版本的Spark，Dataset将会替代RDD成为我们开发编程使用的API。</p>
</li>
</ul>
<h3 id="创建DataFrame"><a href="#创建DataFrame" class="headerlink" title="创建DataFrame"></a>创建DataFrame</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import org.apache.spark.sql.SparkSession</span><br><span class="line">...</span><br><span class="line">val sparkses = SparkSession</span><br><span class="line">				.builder()</span><br><span class="line">                .getOrCreate() //这两个为必须有的</span><br><span class="line">val df = sparkses.read.csv(&quot;...filename.csv&quot;)</span><br><span class="line">//这里read后面的方法名称与具体要读的文件类型有关</span><br><span class="line">df.show()  //打印内容</span><br></pre></td></tr></table></figure>

<h3 id="创建DataSet"><a href="#创建DataSet" class="headerlink" title="创建DataSet"></a>创建DataSet</h3><ul>
<li><p>方法一：创建case class （对于复杂的数据没有成功）</p>
</li>
<li><p>例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val sparkses = SparkSession</span><br><span class="line">				.builder()</span><br><span class="line">                .getOrCreate()</span><br><span class="line">case class KeyValue(key: Int, value: String)</span><br><span class="line">import sparkses.implicit._   //引入隐式转换库</span><br><span class="line">val df = Seq((1,&quot;asdf&quot;),(2,&quot;34234&quot;)).toDF(&quot;key&quot;, &quot;value&quot;)</span><br><span class="line">val ds = df.as[KeyValue]</span><br><span class="line">// org.apache.spark.sql.Dataset[KeyValue] = [key: int, value: string]</span><br></pre></td></tr></table></figure>
</li>
<li><p>case class的参数上限为22</p>
</li>
<li><p>方法二：以元组隐式转化</p>
</li>
<li><p>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val tupDs = df.as[(Int,String)]</span><br><span class="line">// org.apache.spark.sql.Dataset[(Int, String)] = [_1: int, _2: string]</span><br></pre></td></tr></table></figure></li>
</ul>
<br>
> 最后更新于2018.4.21
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑BigData</tag>
        <tag>๑Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu进阶记录 &amp; Ubuntu-18</title>
    <url>/2018/05/10/Ubuntu-18/</url>
    <content><![CDATA[<h2 id="Ubuntu18-使用体验"><a href="#Ubuntu18-使用体验" class="headerlink" title="Ubuntu18 使用体验"></a>Ubuntu18 使用体验</h2><hr>
<h2 id="一些装机软件的安装"><a href="#一些装机软件的安装" class="headerlink" title="一些装机软件的安装"></a>一些装机软件的安装</h2><hr>
<ul>
<li><p>由于Ubuntu 18 自带了 gnome3.0桌面,所以我们只需要下载一个tweak-tool据可以自定义桌面了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install gnome-tweak-tool</span><br></pre></td></tr></table></figure>
</li>
<li><p>这次Ubuntu把标题栏改到了窗口的右侧，如果要改到左侧的话，打开tweak-tool转到窗口里面更改即可。</p>
</li>
<li><p>在更改用户主题之前，我们需要添加一个拓展插件才能使用自定义的主题</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install gnome-shell-extensions</span><br></pre></td></tr></table></figure>
<p>或者在gnome拓展网站上搜索 shell extensions ，找到这个插件并且安装最后重启tweak-tool即可。</p>
</li>
</ul>
<h3 id="Albert"><a href="#Albert" class="headerlink" title="Albert"></a>Albert</h3><ul>
<li><p>一款很强大的快捷交互命令行。</p>
</li>
<li><p>勾选extention后可以执行强大的命令包括文件，程序，终端，计算器等等。</p>
</li>
<li><p>安装步骤：</p>
</li>
</ul>
<ol>
<li><pre><code>
</code></pre>
</li>
</ol>
<p>wget -nv -O Release.key <br>  <a href="https://build.opensuse.org/projects/home:manuelschneid3r/public_key">https://build.opensuse.org/projects/home:manuelschneid3r/public_key</a><br>sudo apt-key add - &lt; Release.key<br>sudo apt-get update</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">2. ```</span><br><span class="line">sudo sh -c &quot;echo &#x27;deb http://download.opensuse.org/repositories/home:/manuelschneid3r/xUbuntu_18.04/ /&#x27; &gt; /etc/apt/sources.list.d/home:manuelschneid3r.list&quot;</span><br><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install albert</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="https://albertlauncher.github.io/docs/installing/">官网</a></li>
</ul>
<h2 id="主题修改"><a href="#主题修改" class="headerlink" title="主题修改"></a>主题修改</h2><hr>
<ul>
<li><p>在网上找好主题以及美化的图标之后，分别下载下来，在home下创建 <code>.themes</code> 和 <code>.icons</code>两个文件夹，把主题和图标分别放到两个文件夹下即可使用。</p>
</li>
<li><p><a href="https://imcn.me/html/y2017/29004.html">一个mac风格的主题</a></p>
</li>
</ul>
<h2 id="gnome-extensions-recommend"><a href="#gnome-extensions-recommend" class="headerlink" title="gnome extensions recommend"></a>gnome extensions recommend</h2><hr>
<h3 id="Ubuntu强大就强大在其可自定义的系统插件-这里罗列一下我自己使用的插件"><a href="#Ubuntu强大就强大在其可自定义的系统插件-这里罗列一下我自己使用的插件" class="headerlink" title="Ubuntu强大就强大在其可自定义的系统插件,这里罗列一下我自己使用的插件."></a>Ubuntu强大就强大在其可自定义的系统插件,这里罗列一下我自己使用的插件.</h3><ul>
<li><p>Desktop Scroller: 可以在左右上下(用户自定义)屏幕边滑动鼠标滚轮来实现工作区的切换,十分便捷</p>
</li>
<li><p>Drop Down Terminal: 从顶端展开的一个终端,可以跨工作区.</p>
</li>
<li><p>Keep awake!: 手动激活按钮使电脑不会进入待机状态</p>
</li>
<li><p>NetSpeed: 显示当前网速</p>
</li>
<li><p>Place Status Indicator: 可以快速获取当前位置和切换位置</p>
</li>
<li><p>Removable Drive Menu: 显示当前是否有挂载的设备,并且可以一键解除挂载</p>
</li>
<li><p>Screenshot Tool: 屏幕截图工具</p>
</li>
<li><p>User Themes: 可以自定义用户主题的拓展,必备.</p>
</li>
<li><p>Blyr：使Overview有模糊背景效果</p>
</li>
<li><p>Coverflow(Alt-Tab)：应用程序切换效果，类似Mac OS X</p>
</li>
</ul>
<h2 id="使用ppa源时可能遇到的问题以及解决方案"><a href="#使用ppa源时可能遇到的问题以及解决方案" class="headerlink" title="使用ppa源时可能遇到的问题以及解决方案"></a>使用ppa源时可能遇到的问题以及解决方案</h2><hr>
<ul>
<li><p>遇到 <code>sudo apt-get update</code> 时提示 <em>无法安全地用该源进行更新，所以默认禁用该源</em>,使用<code>sudo apt -o Acquire::AllowInsecureRepositories=true update</code>来强制更新不安全的源。</p>
</li>
<li><p>遇到没有签名的问题时，使用 <code>sudo apt-get update --allow-unauthenticated</code></p>
</li>
</ul>
<h2 id="Ubuntu18装机的坑"><a href="#Ubuntu18装机的坑" class="headerlink" title="Ubuntu18装机的坑"></a>Ubuntu18装机的坑</h2><hr>
<ul>
<li><p>ubuntu对独立显卡的支持很有问题，导致我在安装时一进去就卡死，后来找到了解决方法。</p>
</li>
<li><p>在进入安装界面之前(选择体验还是安装的那个界面)时侯按 e 进入启动参数编辑 ，找到 quiet splash 这一句，把它删掉，改为  <code>i915.modest=0  nouveau.modeset=0  nomodeset</code> (参数之间空格间隔) 然后 ctrl+x 就可以进入安装界面了。但是此时的安装界面分辨率是900*400的，贼坑，还好我只是覆盖安装之前的Ubuntu16，所以自动安装也还凑合，如果要手动分区安装的话就悲剧了…</p>
</li>
<li><p>同样的，在安装完成之后重启进入ubuntu系统之前(在那个紫色的界面时)还要按 e 进入启动参数编辑界面，更改同上，进入系统之后，找到 系统设置&gt;软件和更新&gt;附加驱动，安装Nvidia的独显驱动然后重启即可。</p>
</li>
<li><p>按下 alt + F7 之后就可以不受限制地自由移动窗口了。</p>
</li>
<li><p>详见我知乎上的<a href="https://www.zhihu.com/question/276308597/answer/388030874">文章</a></p>
</li>
</ul>
<h3 id="终端的字体"><a href="#终端的字体" class="headerlink" title="终端的字体"></a>终端的字体</h3><ul>
<li>如果要自己更改一些字体的话，在tweak-tools 里面其他的字体都可以随便改，但是等宽字体不能随便该，否则终端的字体会出现交叠错乱的情况。终端的字体由等宽字体来控制，一定要选择以 “mono” 开头或者结尾的字体才不会出现问题。</li>
</ul>
<h2 id="相比较-Ubuntu-16-04LTS"><a href="#相比较-Ubuntu-16-04LTS" class="headerlink" title="相比较 Ubuntu 16.04LTS"></a>相比较 Ubuntu 16.04LTS</h2><hr>
<ul>
<li><p>网易云音乐比Ubuntu16更加坑了～我内心是万马奔腾的…</p>
</li>
<li><p>ss-Qt5也用不了，翻墙只能靠命令行ssr了</p>
</li>
<li><p>搜狗输入法也是各种安装失败，我也懒得搞了，但是Ubuntu 18 的内置中文输入法手感贼舒服～搜狗装不上也就算了</p>
</li>
</ul>
<h2 id="Ubuntu的问题查询"><a href="#Ubuntu的问题查询" class="headerlink" title="Ubuntu的问题查询"></a>Ubuntu的问题查询</h2><hr>
<ul>
<li>很多时候国内的一些论坛无法解决我们在使用Ubuntu时遇到的一些问题，这时候就需要去一些国外的论坛上找我们需要的解答。这里推荐一个网站——<a href="https://askubuntu.com/">askUbuntu</a></li>
</ul>
<h2 id="Ubuntu-18-下可以使用的-ssr"><a href="#Ubuntu-18-下可以使用的-ssr" class="headerlink" title="Ubuntu 18 下可以使用的 ssr"></a>Ubuntu 18 下可以使用的 ssr</h2><hr>
<ul>
<li><p><a href="https://github.com/erguotou520/electron-ssr/releases">链接</a></p>
</li>
<li><p>配置方法与windows几乎一致</p>
</li>
</ul>
<h2 id="关于网易云音乐安装之后必须要终端sudo才能打开的问题"><a href="#关于网易云音乐安装之后必须要终端sudo才能打开的问题" class="headerlink" title="关于网易云音乐安装之后必须要终端sudo才能打开的问题"></a>关于网易云音乐安装之后必须要终端sudo才能打开的问题</h2><hr>
<ul>
<li><p>首先sudo打开会有报错,但是会自动启动默认设置</p>
</li>
<li><p>根据报错我们可以进行对应的安装: <code>sudo apt-get install libcanberra-gtk-module</code></p>
</li>
<li><p>然后修改网易云音乐的desktop文件，在exec那一行 <code>%U</code> 之前 加上 <code>--no-sandbox</code>然后保存退出，重启电脑即可愉快的使用了～～(注意修改文件之前先要切换到root用户)</p>
</li>
<li><p>另一种神奇的打开方式(不需要上面的操作):启动之后，点关机按钮，弹出来确认框，随后网易乐音乐页面也弹出来了，这时取消关机，可以正常使用云音乐~~~~~~~</p>
</li>
</ul>
<hr>
<h3 id="2019-3-21更新"><a href="#2019-3-21更新" class="headerlink" title="2019.3.21更新"></a>2019.3.21更新</h3><ul>
<li><p>最近又发现了一个 <strong>完全解决问题</strong> 的方法:</p>
</li>
<li><p>把<code>%U</code>那一行改为:<code>Exec=sh -c &quot;unset SESSION_MANAGER &amp;&amp; netease-cloud-music %U&quot;</code></p>
</li>
</ul>
<h4 id="然后关于每次正常启动要重新登录的问题"><a href="#然后关于每次正常启动要重新登录的问题" class="headerlink" title="然后关于每次正常启动要重新登录的问题"></a>然后关于每次正常启动要重新登录的问题</h4><ul>
<li><p>把<code>.cache</code>文件夹下的<code>netease-cloud-music</code>全部删掉(这是缓存文件夹).然后查看<code>.config</code>文件夹下的<code>netease-cloud-music</code>的所有权是否是自己的当前用户.如果是root的话要改.</p>
</li>
<li><p>参考 – <a href="https://www.zhihu.com/question/277330447/answer/478510195">https://www.zhihu.com/question/277330447/answer/478510195</a></p>
</li>
<li><p>然后终于可以美滋滋地使用了~</p>
</li>
</ul>
<h2 id="关于ibus输入法的问题"><a href="#关于ibus输入法的问题" class="headerlink" title="关于ibus输入法的问题"></a>关于ibus输入法的问题</h2><hr>
<ul>
<li><p>最近输入法中文一直有问题，没办法选词。</p>
</li>
<li><p><a href="https://blog.csdn.net/AshinLi/article/details/72773989">博客链接</a></p>
</li>
<li><p>终端打开ibus设置命令：<code>ibus-setup</code></p>
</li>
<li><p>但是感觉上面这个办法并没有声什么卵用。。。。</p>
</li>
<li><p>所以我弃坑了，还是选择了搜狗输入法</p>
</li>
</ul>
<h3 id="Ubuntu18-的搜狗输入法的安装"><a href="#Ubuntu18-的搜狗输入法的安装" class="headerlink" title="Ubuntu18 的搜狗输入法的安装"></a>Ubuntu18 的搜狗输入法的安装</h3><ul>
<li><p>首先安装fcitx的相关依赖</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install fictx-bin</span><br><span class="line">sudo apt-get install fictx-table</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后安装下载好的搜狗输入法deb包<br><code>dpkg -i sougou...</code><br>这里应该会提示缺少依赖，这时候可就需要执行修复依赖：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get upgrade -f</span><br></pre></td></tr></table></figure>
<p>系统会自动修复缺少的依赖包</p>
</li>
<li><p>然后我们就可以安装完搜狗输入法了。这时候在系统语言设置(右上角)里更改键盘输入法系统为fictx，然后把不用的输入法去掉。</p>
</li>
<li><p>最后重启电脑，打开fictx配置下搜狗输入法，把他调到第一个即可。</p>
</li>
<li><p>搜狗输入法支持登录一键同步，还是很好用的～</p>
</li>
</ul>
<h3 id="搜狗輸入法的新坑"><a href="#搜狗輸入法的新坑" class="headerlink" title="搜狗輸入法的新坑"></a>搜狗輸入法的新坑</h3><ul>
<li><p>Ubuntu18 自从安装以来一直问题不断。</p>
</li>
<li><p>最近启动电脑发现搜狗输入法的候选词栏全变成了乱码。。。坑爹啊</p>
</li>
<li><p>后来百度了解决方法：</p>
</li>
<li><p>首先遇到高级全都勾上以便显示出所有的可更改项<br>进入fictx设置&gt;&gt;附加组件&gt;&gt;简繁转换&gt;&gt;点击配置&gt;&gt;勾上显示高级选项&gt;&gt;将<code>sogoupinyin:false</code>改为<code>sogoupinyin:true</code></p>
</li>
<li><p>但是这个方法只管用一次…下次重启还要手动改~~垃圾</p>
</li>
<li><p>所以接下来这个方法貌似有用(亲测)</p>
</li>
<li><p>输入如下命令：(就是把你个人用户的一些配置删除掉然后重新创建)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd ~/.config</span><br><span class="line">sudo rm -rf SogouPY* sogou*</span><br></pre></td></tr></table></figure></li>
<li><p>之后注销重新登录即可。</p>
</li>
<li><p><strong>就目前个人测试的结果看来，登录个人中心是元凶，登录之后必炸…还是老老实实地用最基本的功能吧，期待早日更新吧。</strong></p>
</li>
</ul>
<h2 id="10-15更新"><a href="#10-15更新" class="headerlink" title="10.15更新"></a>10.15更新</h2><ul>
<li><p><a href="https://zhuanlan.zhihu.com/p/41708902#2-%E7%BD%91%E6%98%93%E4%BA%91%E9%9F%B3%E4%B9%90">Ubuntu18.04详细配置、美化</a></p>
</li>
<li><p><a href="https://www.gnome-look.org/">gnome主题网站</a></p>
</li>
</ul>
<br>
> 最后更新于2018.5.14
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu终端常用命令</title>
    <url>/2018/04/06/Ubuntu-order/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的备忘笔记  </p>
</blockquote>
<h2 id="命令"><a href="#命令" class="headerlink" title="命令"></a>命令</h2><hr>
<h3 id="常用基本命令"><a href="#常用基本命令" class="headerlink" title="常用基本命令"></a>常用基本命令</h3><ul>
<li><p><code>ls -l</code> 的简略写法就是 <code>ll</code>，但是这个与<code>ls -a</code>又不同，后者用来显示隐藏文件和文件夹。</p>
</li>
<li><p>使用 <code>vim</code>修改文件时，如果想要不保存退出，使用<code>:q!</code>命令。</p>
</li>
<li><p>ubuntu改文件名用 <code>mv name1 name2</code>即可</p>
</li>
<li><p><code>cp filename filepath</code> &#x2F;&#x2F;拷贝文件</p>
</li>
<li><p><code>cp -r 源目录  指定目录</code> &#x2F;&#x2F;拷贝一个文件夹到另一个目录</p>
</li>
<li><p>从hdfs上取文件到本地需要链接集群，然后 <code>hdfs dfs -get /.../filename  storepath</code></p>
</li>
<li><p>查看路径：例如用<code>which python</code>查看python安装的路径</p>
</li>
<li><p><code>sudo apt-get remove + 软件名</code> 用于删除apt方式安装的软件</p>
</li>
<li><p><code>chmod [-可选参数][&lt;权限范围&gt;+/-/=&lt;权限设置&gt;] 文件/目录</code>  &#x2F;&#x2F;改变文件权限</p>
</li>
<li><p><code>chown [-R] username filename/directoryname</code>  &#x2F;&#x2F;更改文件或者文件夹所属用户</p>
</li>
<li><p><code>touch 文件名</code>命令用来创建一个空的文件</p>
</li>
<li><p>查看系统的所有PATH环境变量： <code>echo $PATH</code></p>
</li>
<li><p><code>rm -rf</code> 强制 递归地删除某个文件或者文件夹的所有文件</p>
</li>
<li><p><code>dpkg -i filename.deb</code>  &#x2F;&#x2F;安装 .deb 文件</p>
</li>
</ul>
<h3 id="grep语句"><a href="#grep语句" class="headerlink" title="grep语句"></a>grep语句</h3><ul>
<li><p>Linux grep命令用于查找文件里符合条件的字符串。</p>
</li>
<li><p>简单命令格式： <code>grep [option] pattern file</code></p>
</li>
<li><p>完整格式： <code>grep [-abcEFGhHilLnqrsvVwxy][-A&lt;显示列数&gt;][-B&lt;显示列数&gt;][-C&lt;显示列数&gt;][-d&lt;进行动作&gt;][-e&lt;范本样式&gt;][-f&lt;范本文件&gt;][--help][范本样式][文件或目录...]</code></p>
</li>
<li><p><strong>例（最常用）</strong>：<code>cat AR_log | grep &quot;====&quot; </code>此句就是打印日志AR_log当中所有包含了”&#x3D;&#x3D;&#x3D;&#x3D;”的行</p>
</li>
<li><p>在当前目录中，查找后缀有 file 字样的文件中包含 test 字符串的文件，并打印出该字符串的行。此时，可以使用如下命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep test *file</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="apt常用命令"><a href="#apt常用命令" class="headerlink" title="apt常用命令"></a>apt常用命令</h3><ul>
<li><p><code>apt-cache search package</code> 搜索包</p>
</li>
<li><p><code>apt-cache show package</code> 获取包的相关信息，如说明、大小、版本等</p>
</li>
<li><p><code>sudo apt-get install package</code> 安装包</p>
</li>
<li><p><code>sudo apt-get install package - - reinstall</code> 重新安装包</p>
</li>
<li><p><code>sudo apt-get -f install</code> 修复安装 “-f &#x3D; ——fix-missing”</p>
</li>
<li><p><code>sudo apt autoremove</code> <strong>卸载某个软件并且删除与之相关的多余依赖</strong></p>
</li>
<li><p><code>sudo apt-get update</code> 更新源</p>
</li>
<li><p><code>sudo apt-get upgrade</code> <strong>更新已安装的包</strong></p>
</li>
<li><p><code>apt-cache depends package</code> 了解使用依赖</p>
</li>
<li><p><code>apt-cache rdepends package</code> 是查看该包被哪些包依赖</p>
</li>
<li><p><code>sudo apt-get build-dep package</code> 安装相关的编译环境</p>
</li>
<li><p><code>sudo apt-get clean &amp;&amp; sudo apt-get autoclean</code> 清理无用的包</p>
</li>
<li><p><code>sudo apt-get check</code> 检查是否有损坏的依赖</p>
</li>
</ul>
<h3 id="scp语句"><a href="#scp语句" class="headerlink" title="scp语句"></a>scp语句</h3><ul>
<li><p>语法： <code>scp [可选参数] file_source file_target </code></p>
</li>
<li><p>例:<br><code>scp /home/fleshier/My_Programes/Programes/AssociationRuleDiscovery/target/AR.jar   root@192.168.1.201:/root</code><br>此句就是吧一个本地的文件拷贝到远端的服务器上，用户是root，服务器地址是192.168.1.201，存放目录是 <code>/root</code></p>
</li>
<li><p>如果指定了用户名（如上例），则需要输入密码，如果没有指定用户名，则回车后需要输入用户名和密码。</p>
</li>
<li><p>使用参数<code>-r</code>来实现递归拷贝</p>
</li>
</ul>
<p>例：<code>scp root@192.168.1.201:/root/runAR.sh /home/fleschier/cloud\ computing/ </code>从远程服务器拷贝到本地</p>
<ul>
<li><p>远程服务器的格式为 <code>用户名@IP地址:文件路径</code></p>
</li>
<li><p>额外说明：如果远程服务器防火墙有为scp命令设置了指定的端口，我们需要使用 -P 参数来设置命令的端口号，命令格式如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#scp 命令使用端口号 4588</span><br><span class="line">scp -P 4588 remote@www.runoob.com:/usr/local/sin.sh /home/administrator</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="curl命令"><a href="#curl命令" class="headerlink" title="curl命令"></a>curl命令</h3><ul>
<li><p>curl命令是个功能强大的网络工具，支持通过http、ftp等方式下载文件、上传文件。还可以用来抓取网页、网络监控等方面的开发，解决开发过程中遇到的问题。</p>
</li>
<li><p>curl安装：<code>sudo apt-get install curl</code></p>
</li>
<li><p>get请求：<code>curl http://www.baidu.com</code> 回车之后，HTML内容打印在屏幕上；如果这里的URL指向的是一个文件或者一幅图都可以直接下载到本地。</p>
</li>
<li><p>更多详细参数——<a href="http://www.cnblogs.com/linjiqin/p/5484910.html">curl命令详解</a></p>
</li>
</ul>
<h3 id="gem命令"><a href="#gem命令" class="headerlink" title="gem命令"></a>gem命令</h3><ul>
<li><p>安装命令 <code>gem install appname</code></p>
</li>
<li><p>Gem 是一个管理 Ruby 库和程序的标准包</p>
</li>
<li><p>所有的 gem 包，会被安装到 <code>/[Ruby root]/lib/ruby/gems/[ver]/</code> 目录下，这其中包括了 <code>Cache、doc、gems、specifications</code> 4个目录，<code>cache</code> 下放置下载的原生 gem 包，<code>gems</code> 下则放置的是解压过的 gem 包。</p>
</li>
<li><p>当安装过程中遇到问题时，可以进入这些目录，手动删除有问题的 gem 包，然后重新运行 <code>gem install [gemname]</code> 命令即可。</p>
</li>
</ul>
<br>
- 参考资料：[linux命令大全-菜鸟教程](http://www.runoob.com/linux/linux-command-manual.html)以及各大博客网站大佬们的博文。

<h3 id="df命令"><a href="#df命令" class="headerlink" title="df命令"></a>df命令</h3><ul>
<li><p>df命令用来查看磁盘上的空间，默认以KB为单位</p>
</li>
<li><p>一般使用<code>df-h</code>命令来以KB以上的单位显示，可读性高</p>
</li>
<li><p><a href="http://man.linuxde.net/df">详解</a></p>
</li>
</ul>
<h3 id="find命令"><a href="#find命令" class="headerlink" title="find命令"></a>find命令</h3><ul>
<li><p>语法：<code>find path -option [ -print ] [-exec -ok command ] &#123;&#125; \;</code></p>
</li>
<li><p><a href="http://www.runoob.com/linux/linux-comm-find.html">详解</a></p>
</li>
</ul>
<h3 id="du命令"><a href="#du命令" class="headerlink" title="du命令"></a>du命令</h3><ul>
<li><p>一般使用<code>du -h [--max-depth = k] /路径</code>来查看这个目录下的各个文件占用的空间</p>
</li>
<li><p><code>--max-depth</code>用来调节深度，一般k取1</p>
</li>
<li><p><a href="https://www.jianshu.com/p/1c22dcb17a2e">详细教程</a></p>
</li>
</ul>
<h3 id="压缩命令"><a href="#压缩命令" class="headerlink" title="压缩命令"></a>压缩命令</h3><h4 id="使用tar指令"><a href="#使用tar指令" class="headerlink" title="使用tar指令"></a>使用tar指令</h4><ul>
<li><p>Tar压缩文件: <code>tar [-zxcvfpP] filename</code> </p>
</li>
<li><p><code>tar -N &#39;yyyy/mm/dd&#39; /path -zcvf target.tar.gz source</code></p>
</li>
<li><p>参数说明：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-z  ：是否同时具有 gzip 的属性？ </span><br><span class="line">-x  ：解开一个压缩档案的参数指令！ </span><br><span class="line">-t  ：查看 tarfile 里面的档案！</span><br><span class="line">-c  ：建立一个压缩档案的参数指令 </span><br><span class="line">-v  ：压缩的过程中显示档案！ </span><br><span class="line">-f  ：使用档名，请留意，在 f 之后要立即接档名,不要再加参数</span><br><span class="line">-p  ：使用原档案的原来属性（属性不会依据使用者而变） </span><br><span class="line">-P  ：可以使用绝对路径 </span><br><span class="line">-N  ：比后面接的日期(yyyy/mm/dd)还要新的才会被打包进新建的档案中！</span><br><span class="line">--exclude FILE：在压缩的过程中，不要将 FILE 打包！ </span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h5><ul>
<li><p><code>tar -cvf directory.tar directory</code><br>&#x2F;&#x2F;只将目录整合打包成一个档案 </p>
</li>
<li><p><code>tar -zcvf directory.tar.gz directory</code> <br>&#x2F;&#x2F;除了将目录打包外，同时以 gzip 压缩 </p>
</li>
<li><p><code>tar -zcvf filename.tar.gz  /home/test/*</code><br>&#x2F;&#x2F;将<code>/home/test/</code> 这个目录下的档案全部打包并压缩成为一个 filename.tar.gz 的档案</p>
</li>
<li><p><code>tar -jcvf /tmp/etc.tar.bz2 /etc</code> &#x2F;&#x2F;打包后，以 bzip2 压缩</p>
</li>
<li><p><code>tar -xvf  directory.tar</code> <br>&#x2F;&#x2F;解 tar 的封包，请注意，由于没有 gzip (.tar 而非 .tar.gz) 的作用，所以只要使用<code>–xvf</code> 即可！不需要加上 z ，否则会显示有问题</p>
</li>
<li><p><code>tar -zxvf directory.tar.gz</code> <br>&#x2F;&#x2F;解压</p>
</li>
<li><p><code>tar –ztvf directory.tar.gz</code><br>&#x2F;&#x2F;查看 tar 里面的档案信息</p>
</li>
<li><p><code>tar -zcvPf home.tar.gz /home</code><br>&#x2F;&#x2F;则建立起来的压缩档内档案为绝对路径 <br>&#x2F;&#x2F;请注意，使用这个 P 的参数时，不要将 P 加在 f 后面，因为 f 之后要立即接档名才行！</p>
</li>
</ul>
<h3 id="HDFS相关命令"><a href="#HDFS相关命令" class="headerlink" title="HDFS相关命令"></a>HDFS相关命令</h3><ul>
<li><p>基本所有与HDFS相关的命令都以<code>hdfs dfs</code>开头</p>
</li>
<li><p><code>-mkdir /dirname</code> 创建hdfs目录</p>
</li>
<li><p><code>-ls /dirname[/...]</code> 列出某个目录下的所有文件</p>
</li>
<li><p><code>-rm -r /dirname</code> 删除某个目录及其所有子文件</p>
</li>
<li><p><code>-get /hdfs路径  /本地路径</code> 从hdfs拿文件到本地(例如<code>hdfs dfs -get /data/test.txt /home/xxx/</code>)</p>
</li>
<li><p><code>-put /本地文件路径 /hdfs路径</code>与拿文件相反,将本地一个文件传到hdfs上</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>在VScode使用Anacoda自带的tensorflow插件问题记录</title>
    <url>/2019/04/16/anacoda-tensorflow-pep8/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><ul>
<li>首先需要明确是否安装了tensorflow.<strong>虽然Anacoda说是集成了Tensorflow,但是下载的部分是不包括Tensorflow的,需要自己安装才行.</strong></li>
</ul>
<hr>
<ul>
<li><strong>最新更新:pylint的问题现在已经不存在了,要是检测不到tensorflow,主要原因可能就是tensorflow未安装或安装不正确.</strong></li>
</ul>
<h2 id="问题记录"><a href="#问题记录" class="headerlink" title="问题记录"></a>问题记录</h2><hr>
<ul>
<li><p>最近在写项目使用tensorflow做深度学习,使用了Anacoda并结合VScode来进行开发.</p>
</li>
<li><p>但是遇到一个问题,就是VScode python默认使用pylint,而pylint识别不了tensorflow框架,在import的时候一直报错.</p>
</li>
</ul>
<h2 id="StackOverflow的解释"><a href="#StackOverflow的解释" class="headerlink" title="StackOverflow的解释"></a>StackOverflow的解释</h2><hr>
<ul>
<li><p><a href="https://stackoverflow.com/questions/49601292/python-not-finding-tensorflow-module-under-anaconda">原问题及回答</a></p>
</li>
<li><p>有用的一个回答:</p>
</li>
</ul>
<blockquote>
<p>are you using Visual Studio Code (VSC) or just pylint in general? I found out the reason why this is happening.<br>For VSC, the python extension, use pylint for intellisense of python. Pylint seems to have a bug with the sub-module. For me, the error only shows in VSC and not in prompt.<br>I solved this problem by doing the following steps:<br>Click “Code” -&gt; Click “Preferences” -&gt; Click “Settings”<br>Now in the settings, you have a search bar on top, search:<br>python.linting.pylintEnable and set it to false<br>Now there is alternative for linting, I am using the pep8 as a example here since it come with the anaconda, search this<br>python.linting.pep8Enabled and set it to true<br>Now pylint is not the default linter anymore, we are now using the pep8. Just to make sure, quit VSC and reopen it. there should not be any error anymore.<br>I am fairly sure this is a problem with pylint, instead of the TF you installed. By default, the Microsoft python extension in the VSC is using pylint as the linting tool. By changing it to pep8 or other we can avoid the error.<br>大致意思就是,pylint在解析库的时候不会遍历深层的子目录导致找不到tensorflow.<strong>解决办法是不使用pylint,改为pep8</strong></p>
</blockquote>
<ul>
<li>方法: 在VScode的设置中搜索<code>python.linting</code>,将<code>pylint.Enabling</code>勾掉,将<code>pep8.Enabling</code>勾上即可.同时我们还会发现还有许多其他的选项可以采用,这里不一一尝试了.</li>
</ul>
<h2 id="后续问题"><a href="#后续问题" class="headerlink" title="后续问题"></a>后续问题</h2><hr>
<ul>
<li><p>采用pep8之后,果然问题解决了,但是又多了一个新的毛病: 很多报错.</p>
</li>
<li><p>仔细一看,诸如什么”一行过长”,”逗号之后空格没加”等等小问题,到了这里都变成了Error,就让人看起来很难受.</p>
</li>
<li><p>搜索了半天,也没找到合适的解决方法(国内论坛真是垃圾,还得自己想办法).</p>
</li>
<li><p>最终找到了一篇日本的,虽然看不懂日语,但是代码命令还是一目了然的,试了一下既简单又好用(这™就是差距啊~)</p>
</li>
<li><p><a href="https://qiita.com/zaki-yama/items/d05adce9d23d67144fbf">原文链接(需要翻墙)</a></p>
</li>
<li><p>解决方法如下:</p>
</li>
</ul>
<ol>
<li><p>在家目录的.config文件夹下新建一个叫pep8的文件:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vim ~ .config/pep8</span><br></pre></td></tr></table></figure>
</li>
<li><p>在其中写入:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[pep8]</span><br><span class="line">ignore = E226,E302,E41,E265,E231,E303</span><br><span class="line">max-line-length = 200</span><br></pre></td></tr></table></figure>
<p>其中,<code>ignore</code>后面的是需要忽略的编号,你可以查阅官方文档或者直接看终端的报错信息,在报错的最后会有编号,把想要忽略的加进来即可.</p>
</li>
<li><p>保存文件,重启VScode即可.</p>
</li>
</ol>
]]></content>
      <categories>
        <category>Python</category>
        <category>Tensorflow</category>
      </categories>
      <tags>
        <tag>๑Tensorflow</tag>
        <tag>๑VScode</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker使用</title>
    <url>/2018/11/08/docker-learning-02/</url>
    <content><![CDATA[<blockquote>
<p>需要的docker image可以从Docker Hub获取</p>
</blockquote>
<h2 id="Docker-images"><a href="#Docker-images" class="headerlink" title="Docker images"></a>Docker images</h2><hr>
<ul>
<li><p>Docker images文件: Docker 把应用程序及其依赖，打包在 image 文件里面。只有通过这个文件，才能生成 Docker 容器。image 文件可以看作是容器的模板。Docker 根据 image 文件生成容器的实例。同一个 image 文件，可以生成多个同时运行的容器实例。</p>
</li>
<li><p>查看本机的image文件:<code>docker image ls</code></p>
</li>
<li><p>删除某个image:<code>docker image rm [imageName]</code></p>
</li>
</ul>
<h3 id="获取image"><a href="#获取image" class="headerlink" title="获取image"></a>获取image</h3><ul>
<li><code>docker image pull 仓库路径/文件名</code></li>
</ul>
<h3 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h3><ul>
<li><p><code>docker container run containID</code></p>
</li>
<li><p>可以先用<code>docker image ls</code>查看所有的镜像文件. 同时<code>docker container run</code>当本地仓库不存在相对应的image时,会自动从远程仓库获取,所以上面pull的一步不是必须的.</p>
</li>
</ul>
<h3 id="终止"><a href="#终止" class="headerlink" title="终止"></a>终止</h3><ul>
<li>有些容器需要手动终止,使用命令:<code>docker container kill containID</code></li>
</ul>
<h2 id="Docker-container"><a href="#Docker-container" class="headerlink" title="Docker container"></a>Docker container</h2><hr>
<ul>
<li><p>image 文件生成的容器实例，本身也是一个文件，称为容器文件。也就是说，一旦容器生成，就会同时存在两个文件： image 文件和容器文件。而且关闭容器并不会删除容器文件，只是容器停止运行。</p>
</li>
<li><p>列出正在运行的容器:<code>docker container ls</code></p>
</li>
<li><p>列出所有的容器: <code>docker container ls --all</code></p>
</li>
</ul>
<h3 id="删除容器"><a href="#删除容器" class="headerlink" title="删除容器"></a>删除容器</h3><ul>
<li><p><code>docker container rm containerID</code></p>
</li>
<li><p>删除之后的容器不会再出现在容器列表里</p>
</li>
</ul>
<h2 id="创建自己的docker容器"><a href="#创建自己的docker容器" class="headerlink" title="创建自己的docker容器"></a>创建自己的docker容器</h2><hr>
<ul>
<li>待更新</li>
</ul>
<h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><hr>
<ul>
<li><p><strong>注意</strong>,<code>docker container run</code>命令是创建一个container,每运行一次就会多一个容器,如果要重复使用一个容器需要用:<code>docker container start</code></p>
</li>
<li><p>查看容器的输出,即容器里面shell的标准输出:<code>docker container logs containerID</code>. 如果<code>docker run</code>命令运行容器的时候，没有使用<code>-it</code>参数，就要用这个命令查看输出。</p>
</li>
<li><p>进入一个正在运行的容器:<code>docker container exec -it containerID /bin/bash</code>.如果<code>docker run</code>命令运行容器的时候，没有使用<code>-it</code>参数，就要用这个命令进入容器。一旦进入了容器，就可以在容器的Shell执行命令了。</p>
</li>
<li><p>从容器中拷贝文件到本地:<code>$ docker container cp containID:[/path/to/file] [/dest/path/]</code>. 如果要拷贝到当前目录,则可以写为:<code>$ docker container cp [containID]:[/path/to/file] .</code></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu随笔</title>
    <url>/2018/04/06/Ubuntu-operation/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的备忘笔记</p>
</blockquote>
<h2 id="Ubuntu目录结构"><a href="#Ubuntu目录结构" class="headerlink" title="Ubuntu目录结构"></a>Ubuntu目录结构</h2><ul>
<li><p><code>/</code>:这就是根目录，一台电脑有且只有一个根目录，所有的文件都是从这里开始的。举个例子：当你在终端里输入“&#x2F;home”，你其实是在告诉电脑，先从&#x2F;（根目录）开始，再进入到home目录。</p>
</li>
<li><p><code>/root</code>:系统管理员（root user）的目录。至于系统管理员的权限有多大我这里就不在废话了。因此，请小心使用root帐号。</p>
</li>
<li><p><code>/boot</code>: 系统启动文件，所有与系统启动有关的文件都保存在这里 。</p>
</li>
<li><p><code>/bin</code>: 这里是存放系统的程序。</p>
</li>
<li><p><code>/etc</code>:主要存放了系统配置方面的文件。</p>
</li>
<li><p><code>/dev</code>:这里主要存放与设备（包括外设）有关的文件（unix和linux系统均把设备当成文件）。想连线打印机吗？系统就是从这个目录开始工作的。另外还有一些包括磁盘驱动、USB驱动等都放在这个目录。</p>
</li>
<li><p><code>/home</code>:这里主要存放你的个人数据。具体每个用户的设置文件，用户的桌面文件夹，还有用户的数据都放在这里。每个用户都有自己的用户目录，位置为：&#x2F;home&#x2F;用户名。当然，root用户除外。</p>
</li>
<li><p><code>/tmp</code>:这是临时目录。对于某些程序来说，有些文件被用了一次两次之后，就不会再被用到，像这样的文件就放在这里。因此，千万不要把重要的数据放在这里哦。</p>
</li>
<li><p><code>/usr</code>:在这个目录下，你可以找到那些不适合放在&#x2F;bin或&#x2F;etc目录下的额外的工具。比如像游戏阿，一些打印工具等等。&#x2F;usr目录包含了许多子目录： &#x2F;usr&#x2F;bin目录用于存放程序；&#x2F;usr&#x2F;share用于存放一些共享的数据，比如音乐文件或者图标等等；&#x2F;usr&#x2F;lib目录用于存放那些不能直接 运行的，但却是许多程序运行所必需的一些函数库文件。你的软件包管理器会自动帮你管理好&#x2F;usr目录的。</p>
</li>
<li><p><code>/opt</code>:这里主要存放一些可选的程序。如你想尝试最新的firefox测试版吗？那就装到&#x2F;opt目录下吧，这样，当你尝试完，想删掉firefox的时候，你就 可以直接删除它，而不影响系统其他任何设置。安装到&#x2F;opt目录下的程序，它所有的数据、库文件等等都是放在同个目录下面。</p>
</li>
<li><p><code>usr/local</code>:这里主要存放那些手动安装的软件，即不是通过“新立得”或apt-get安装的软件。它和&#x2F;usr目录具有相类似的目录结构。让软件包管理器来管理&#x2F;usr目录，而把自定义的脚本（scripts）放到&#x2F;usr&#x2F;local目录下面。</p>
</li>
<li><p><code>/media</code>:这个目录是用来挂载那些usb接口的移动硬盘（包括U盘）、CD&#x2F;DVD驱动器等等。</p>
</li>
</ul>
<h2 id="一些推荐的软件"><a href="#一些推荐的软件" class="headerlink" title="一些推荐的软件"></a>一些推荐的软件</h2><hr>
<blockquote>
<p> 一旦出现某个软件没有安装，Linux系统大多数情况下都会提示你怎样安装这个软件，最常用的就是 sudo apt-get install XXX 命令</p>
</blockquote>
<ul>
<li><p>fish! fish! fish!： fish大法好啊，功能强大，自动补全，Ubuntu最好用的软件之一。</p>
</li>
<li><p>vim和gedit：这个不用我多说大家都知道，Linux非常好用的编辑软件</p>
</li>
<li><p><a href="https://atom.io/">atom</a>  &#x2F;&#x2F;集成许多功能于一体的编辑器</p>
</li>
<li><p><a href="https://zhuanlan.zhihu.com/p/24802058">SpaceVim</a>模块化Vim-IDE。<a href="https://spacevim.org/cn/documentation/">使用手册</a></p>
</li>
<li><p>gnome 桌面： 装了gnome桌面和它的一系列插件之后我才真正感受到了Ubuntu的魅力！（教程以及主题百度很多）</p>
</li>
<li><p>docky: 一款mac风格的dock,删除图标直接拉出去(sudo apt-get install docky)</p>
</li>
<li><p>tweak-tool: 与gnome桌面对应的是tweak-tool(sudo apt install unity-tweak-tool)</p>
</li>
<li><p>yakuake : 可以从上方一键滑出和滑入的终端（滑出时强制窗口在最前端）（gnome中也有类似功能的插件，两个选一个即可）</p>
</li>
<li><p>Moeditor: Ubuntu下界面简洁美观，功能强大的markdown格式文件编辑器，已经在github上开源</p>
</li>
<li><p>processon： 一个网页上的设计工具，可以很简单地完成诸如流程图的设计和生成图片文件。</p>
</li>
</ul>
<h2 id="一些注意点"><a href="#一些注意点" class="headerlink" title="一些注意点"></a>一些注意点</h2><hr>
<ul>
<li><p><strong>F11键可以进入全屏模式</strong></p>
</li>
<li><p>触摸板双击之后再移动就相当于长按鼠标左键的效果了</p>
</li>
<li><p>在Ubuntu终端窗口中，复制粘贴的快捷键需要加上 shift，即粘贴是 ctrl+shift+v。</p>
</li>
<li><p>ssh <a href="mailto:&#99;&#121;&#120;&#x40;&#49;&#x39;&#50;&#46;&#49;&#54;&#56;&#46;&#x31;&#46;&#50;&#48;&#x31;">&#99;&#121;&#120;&#x40;&#49;&#x39;&#50;&#46;&#49;&#54;&#56;&#46;&#x31;&#46;&#50;&#48;&#x31;</a> cyx  高性能服务器  WIFI:sklcc_slave</p>
</li>
<li><p>192.168.1.201:18080</p>
</li>
<li><p>192.168.1.201:18088 进程</p>
</li>
<li><p>192.168.1.201:50070 hdfs</p>
</li>
</ul>
<h3 id="关于添加Ubuntu的本地ssh-key与github相关联"><a href="#关于添加Ubuntu的本地ssh-key与github相关联" class="headerlink" title="关于添加Ubuntu的本地ssh-key与github相关联"></a>关于添加Ubuntu的本地ssh-key与github相关联</h3><ul>
<li><p>首先检测本地有没有安装ssh  <code>终端输入ssh</code></p>
</li>
<li><p>然后终端执行<code>ssh-kengen -t rsa</code>，连续三次默认回车就行。接着 ~&#x2F;.ssh目录下就会多出来两个文件：<code>id_rsa</code>和<code>id_rsa.pub</code></p>
</li>
<li><p>接着只要在github上添加<code>id_rsa.pub</code>这个key就行了。在github的setting中，找到sshkey一栏，选择new key，将<code>id_rsa.pub</code>的文件内容复制到里面，随便起个title即可。</p>
</li>
</ul>
<h3 id="关于挂载本地的卷到Ubuntu"><a href="#关于挂载本地的卷到Ubuntu" class="headerlink" title="关于挂载本地的卷到Ubuntu"></a>关于挂载本地的卷到Ubuntu</h3><ul>
<li><p>使用<code>mount /dev/卷名  /挂载点</code>   &#x2F;&#x2F;将某个卷挂载到某个文件目录下，该卷就可以在这个文件目录下访问了。</p>
</li>
<li><p>Ubuntu中，所有的硬件设备都是作为文件存储的，这一点与Windows不同。</p>
</li>
</ul>
<h3 id="关于桌面快捷方式的创建"><a href="#关于桌面快捷方式的创建" class="headerlink" title="关于桌面快捷方式的创建"></a>关于桌面快捷方式的创建</h3><ul>
<li><p>方法一：进入 &#x2F;usr&#x2F;share&#x2F;applications&#x2F; 目录，里面会有<strong>绝大多数</strong>你安装的软件的 .desktop文件，只需要把你需要的软件的 .desktop文件复制到home目录下的 桌面文件夹里即可。</p>
</li>
<li><p>方法二： 有点蠢但是很简单的一个方法。对于那些没有安装直接可以使用的软件，我们在上个方法中提到的文件夹里是找不到 .desktop文件的。怎么办呢？我们只需要进入我们那个软件的目录，找到启动文件，创建一个它的链接文件（右键还是终端随意），然后将这个链接文件复制到桌面，改下名字即可。虽然和简单，但是缺点是没有图标….身为一个外观党还是有点难以接受的…</p>
</li>
<li><p>方法三：就是自己写一个.desktop文件，这里举例我写的Moeditor的桌面文件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//首先终端切到桌面</span><br><span class="line">vim Moeditor.desktop</span><br><span class="line">//然后输入如下内容</span><br><span class="line">[Desktop Entry]</span><br><span class="line">Version=1.0</span><br><span class="line">Type=Application</span><br><span class="line">Name=Moeditor</span><br><span class="line">GenericName=Moeditor</span><br><span class="line">Comment=editor for markdown</span><br><span class="line">Exec=/home/fleshier/Programe\ Files/Moeditor-linux-x64/Moeditor %F  //文件启动器的路径</span><br><span class="line">Terminal=false</span><br><span class="line">Icon=/home/fleshier/Programe\ Files/Images/icons/g10.png  //图标的路径</span><br><span class="line">StartupNotify=true</span><br><span class="line">Actions=Window;</span><br><span class="line">//空行</span><br><span class="line">[Desktop Action Window]</span><br><span class="line">Name=New Window</span><br><span class="line">Exec=/home/fleshier/Programe\ Files/Moeditor-linux-x64/Moeditor -n</span><br><span class="line">OnlyShowIn=Unity;</span><br></pre></td></tr></table></figure></li>
<li><p>保存之后执行 <code>chmod a+x filename</code> 改变执行权限即可运行</p>
</li>
<li><p>实际写的时候要把注释删除掉，不然会出错。</p>
</li>
</ul>
<h3 id="关于shell脚本的创建"><a href="#关于shell脚本的创建" class="headerlink" title="关于shell脚本的创建"></a>关于shell脚本的创建</h3><ul>
<li><p>首先可以touch一个文件再进行内容的修改。或者直接<code>vim filename</code>新建一个文件并进行内容的编辑</p>
</li>
<li><p>所有shell脚本本质上都是文本文件，命名格式为<code>filename.sh</code>,内容必须以<code>#!/bin/bash</code>为第一行，当中没有空格。</p>
</li>
<li><p>事实上，这个#!字符序列是一种特殊的结构叫做 shebang。 这个 shebang 被用来告诉操作系统将执行此脚本所用的解释器的名字。每个 shell 脚本都应该把这一文本行作为它的第一行。</p>
</li>
<li><p>示例写法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">#This is a test script!    #本句是一个注释，&#x27;#&#x27;号之后的字符都会被忽略</span><br><span class="line">echo &#x27;Hello World!&#x27;  #本句会在终端中打印 &quot;Hello World!&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后在终端中执行： <code>chmod +x filename.sh</code> 为这个脚本文件添加可执行权限</p>
</li>
<li><p>或者使用另外一个更改权限的写法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[me@linuxbox ~]$ ll hello_world</span><br><span class="line">-rw-r--r-- 1  me    me      63  2009-03-07 10:10 hello_world</span><br><span class="line">[me@linuxbox ~]$ chmod 755 hello_world</span><br><span class="line">[me@linuxbox ~]$ ll hello_world</span><br><span class="line">-rwxr-xr-x 1  me    me      63  2009-03-07 10:10 hello_world</span><br></pre></td></tr></table></figure>
<p>对于脚本文件，有两个常见的权限设置：权限为755的脚本，则每个人都能执行，权限为700的 脚本，只有文件所有者能够执行。注意为了能够执行脚本，脚本必须是可读的。</p>
</li>
<li><p>最后是运行脚本，终端进入脚本所在文件夹，输入： <code>./filename.sh</code></p>
</li>
</ul>
<h2 id="Linux中添加环境变量的方法"><a href="#Linux中添加环境变量的方法" class="headerlink" title="Linux中添加环境变量的方法"></a>Linux中添加环境变量的方法</h2><blockquote>
<p>$PATH：决定了shell将到哪些目录中寻找命令或程序，PATH的值是一系列目录，当您运行一个程序时，Linux在这些目录下进行搜寻编译链接。</p>
</blockquote>
<ul>
<li><p>为什么要添加环境变量呢？如果你在Linux下载了一个免安装的软件，你要启动它的话，要么创建一个桌面快捷方式（方法见上文），要么就是每次进入它的目录点开。这时候如果我们把它添加进了PATH环境变量，那么我们只需要在终端中输入这个软件的名字，回车，就可以启动它了。</p>
</li>
<li><p>方法一：修改profile文件（<strong>注意：一定要切换到root用户，一定要切换到root用户，一定要切换到root用户(sudo su)! ! ! !</strong> 否则最后保存文件会一直被拒绝又退不出去只能强退终端…可能还会有其他一些麻烦事…总之有的爽呢…） &#x2F;&#x2F;对所有用户永久生效</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(root模式下执行) vim /etc/profile</span><br><span class="line">在文档最后加入一行:</span><br><span class="line">export PATH=$PATH: 软件的启动脚本文件所在的**绝对路径**&quot;</span><br><span class="line">(一般都是 /.../bin目录)</span><br><span class="line">退出修改后在root模式下执行： source /etc/profile</span><br><span class="line">或者重启</span><br></pre></td></tr></table></figure>
</li>
<li><p>spark-shell的路径写法例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export SPARK_HOME=/usr/lib/spark/spark-2.2.0-bin-hadoop2.7</span><br><span class="line">export PATH=$&#123;SPARK_HOME&#125;/bin:$PATH</span><br></pre></td></tr></table></figure>
</li>
<li><p><em>使用source命令时要关闭fish，否则会报错</em></p>
</li>
<li><p>方法二：修改.bashrc文件（注意点同上）&#x2F;&#x2F;仅对当前用户永久生效</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(root模式下执行)vi /root/.bashrc</span><br><span class="line">在文档最后加入一行：</span><br><span class="line">export PATH=$PATH: 软件的启动脚本文件所在的**绝对路径**&quot;</span><br><span class="line">退出修改后在root模式下执行： source /root/.bashrc</span><br></pre></td></tr></table></figure>
</li>
<li><p>这两种方法最后的source语句就是让系统重新读取文件使之生效，没有写最后一句source的话，会在下次启动系统后生效</p>
</li>
</ul>
<h2 id="分布式文件系统-HDFS"><a href="#分布式文件系统-HDFS" class="headerlink" title="分布式文件系统(HDFS)"></a>分布式文件系统(HDFS)</h2><ul>
<li><p><strong>注意，分布式文件系统中没有切换到某个目录下的概念，因为其文件全是分散在各个节点上的</strong></p>
</li>
<li><p>分布式文件系统的文件路径完整格式为 <code>hdfs://namenode|master:编号/root/...</code></p>
</li>
<li><p>例如：<code>hdfs://master:8020/dataset/evaluation/bots_10m_10.csv</code></p>
</li>
<li><p><code>hdfs dfs -ls  /...</code>显示分布式文件系统中某个目录（实际并不存在，只是逻辑上的）下的文件</p>
</li>
<li><p><code>hdfs dfs -rm -r file|directory</code> 删除某个文件</p>
</li>
<li><p>附<a href="https://github.com/wsdjeg/vim-galore-zh_cn">vim教程</a></p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.4.19</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu18 安装Docker</title>
    <url>/2018/11/08/docker-learning-01/</url>
    <content><![CDATA[<h2 id="为什么用Docker"><a href="#为什么用Docker" class="headerlink" title="为什么用Docker"></a>为什么用Docker</h2><hr>
<ul>
<li><p>环境搭建一直是运维一件非常头疼的事情, 如果系统崩溃而重新安装系统,则所有的环境都需要重新搭建一遍,非常耗时而且恼人.</p>
</li>
<li><p>我使用的Ubuntu18下配置好了大量的环境,如果这时候要我重新安装一遍的话,就算我做了详细的记录,也至少是一下午的工作量.</p>
</li>
<li><p>而Docker相当于一个容器,与虚拟机不同,虚拟机模拟了一个完整的操作系统,非常消耗系统资源,而且就算不做任何操作,内存也必须被占用.而Docker是容器,基于Linux容器的概念,封装而成.</p>
</li>
<li><p>Docker将运行在其中的软件隔离开来, 不占用额外的系统资源.</p>
</li>
<li><p>Docker 将应用程序与该程序的依赖，打包在一个文件里面。运行这个文件，就会生成一个虚拟容器。程序在这个虚拟容器里运行，就好像在真实的物理机上运行一样。有了 Docker，就不用担心环境问题。</p>
</li>
</ul>
<h2 id="Docker-Hub"><a href="#Docker-Hub" class="headerlink" title="Docker Hub"></a>Docker Hub</h2><hr>
<ul>
<li>Docker Hub是一个平台,注册之后我们可以下载使用别人搭建好的image,非常方便.</li>
</ul>
<h2 id="Ubuntu18安装Docker"><a href="#Ubuntu18安装Docker" class="headerlink" title="Ubuntu18安装Docker"></a>Ubuntu18安装Docker</h2><hr>
<ul>
<li><p>由于Docker被墙得厉害,导致经常性地连官网也进不去.</p>
</li>
<li><p>我们为了安装过程的顺利,首先切换到中国科技大的源:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak</span><br><span class="line">sudo sed -i &#x27;s/archive.ubuntu.com/mirrors.ustc.edu.cn/g&#x27; /etc/apt/sources.list</span><br><span class="line">sudo apt update</span><br></pre></td></tr></table></figure>
</li>
<li><p>安装需要的包:<code>sudo apt install apt-transport-https ca-certificates software-properties-common curl</code></p>
</li>
<li><p>添加 GPG 密钥，并添加 Docker-ce 软件源:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://mirrors.ustc.edu.cn/docker-ce/linux/ubuntu/gpg | sudo apt-key add -</span><br><span class="line">sudo add-apt-repository &quot;deb [arch=amd64] https://mirrors.ustc.edu.cn/docker-ce/linux/ubuntu \</span><br><span class="line">$(lsb_release -cs) stable&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>更新缓存:<code>sudo apt update</code></p>
</li>
<li><p>安装Docker-ce:<code>sudo apt install docker-ce</code></p>
</li>
<li><p>测试Helloworld:<code>sudo docker run hello-world</code></p>
</li>
<li><p>结果:<br><img src="/images/Docker/docker-01.png"></p>
</li>
<li><p>添加当前用户到 docker 用户组，可以不用 sudo 运行 docker:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo groupadd docker</span><br><span class="line">sudo usermod -aG docker $USER</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="docker-pull之后文件的存放目录"><a href="#docker-pull之后文件的存放目录" class="headerlink" title="docker pull之后文件的存放目录"></a>docker pull之后文件的存放目录</h2><hr>
<ul>
<li><p>使用<code>docker pull XXX</code>下载的文件存放在<code>/var/lib/docker/</code>中</p>
</li>
<li><p>其中目录结构如下:<br><img src="/images/Docker/docker-02.png"></p>
</li>
<li><p><code>containers</code>中每个序列号都是一个镜像</p>
</li>
<li><p>Docker是使用repositories JSON文件来记述镜像信息的，此JSON文件包含了仓库名、标签、以及标签对应的镜像ID。</p>
</li>
</ul>
<h2 id="Uninstall-Docker-ce"><a href="#Uninstall-Docker-ce" class="headerlink" title="Uninstall Docker-ce"></a>Uninstall Docker-ce</h2><hr>
<blockquote>
<p>摘自官方文档</p>
</blockquote>
<ul>
<li><p>Uninstall the Docker CE package:<code>sudo apt-get purge docker-ce</code></p>
</li>
<li><p>Images, containers, volumes, or customized configuration files on your host are not automatically removed. To delete all images, containers, and volumes:<code>sudo rm -rf /var/lib/docker</code></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>hadoop集群使用中的一些问题记录</title>
    <url>/2018/07/20/cluster-usage/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h4 id="无法运行spark-shell"><a href="#无法运行spark-shell" class="headerlink" title="无法运行spark-shell"></a>无法运行spark-shell</h4><ul>
<li><p>一般出现这种情况是集群的内存满了</p>
</li>
<li><p>使用<code>df -h</code>命令来查看存储情况</p>
</li>
<li><p>空间几乎占满的状况：<br><img src="/images/Hadoop/cluster_storage.png"></p>
</li>
</ul>
<h4 id="Linux下-proc目录简介"><a href="#Linux下-proc目录简介" class="headerlink" title="Linux下&#x2F;proc目录简介"></a>Linux下&#x2F;proc目录简介</h4><ul>
<li><a href="https://blog.csdn.net/zdwzzu2006/article/details/7747977">linux下proc目录的作用</a></li>
</ul>
<h5 id="删除文件夹"><a href="#删除文件夹" class="headerlink" title="删除文件夹"></a>删除文件夹</h5><ul>
<li>尽量使用<code>rmdir</code>来删除一个空目录，如果目录下存在多个文件，要一次性删除所有的使用<code>rm -rf</code>来实现。<code>-r</code>表示递归删除，<code>-f</code>表示强制执行。不过在删除之前一定要仔细确认，一旦删除无法撤销</li>
</ul>
]]></content>
      <categories>
        <category>Hadoop CLuster</category>
      </categories>
      <tags>
        <tag>๑Linux</tag>
        <tag>๑Hadoop</tag>
        <tag>๑CLuster</tag>
      </tags>
  </entry>
  <entry>
    <title>gitignore的使用及写法</title>
    <url>/2018/10/27/gitignore-learning/</url>
    <content><![CDATA[<h2 id="gitignore的作用"><a href="#gitignore的作用" class="headerlink" title="gitignore的作用"></a>gitignore的作用</h2><hr>
<ul>
<li><p>对于git同步到github来说，估计没有人会 <code>git add 文件名</code>这样的方式来添加到工作区的，都是<code>git add .</code>这样一次添加吧</p>
</li>
<li><p>但是，有时候我们直接add所有的文件会将一些我们不需要的生成的临时文件也加入进去，导致文件冗余。同时，更有可能的是我们在push的时候失败，由于一些不支持的文件类型或者文件大小过大。</p>
</li>
<li><p>这时候我们就需要gitignore脚本来过滤我们需要上传的文件。</p>
</li>
</ul>
<h2 id="命名格式以及位置"><a href="#命名格式以及位置" class="headerlink" title="命名格式以及位置"></a>命名格式以及位置</h2><hr>
<ul>
<li><p>命名统一为<code>.gitignore</code></p>
</li>
<li><p>windows下由于不能文件名为空，则可以先命名为<code>.gitignore.</code>，回车确定之后windows会自动将最后一个点去掉。</p>
</li>
<li><p><code>.gitignore</code>文件放在根目录下即可。</p>
</li>
</ul>
<h2 id="内容写法"><a href="#内容写法" class="headerlink" title="内容写法"></a>内容写法</h2><hr>
<ul>
<li>gitignore本质上是一个按正则表达式匹配文件名&#x2F;文件夹名的文件。</li>
</ul>
<h3 id="语法：-与正则表达式几乎一致"><a href="#语法：-与正则表达式几乎一致" class="headerlink" title="语法：(与正则表达式几乎一致)"></a>语法：(与正则表达式几乎一致)</h3><ul>
<li><p>空行或是以<code>#</code>开头的行即注释行将被忽略</p>
</li>
<li><p>以斜杠 <code>/</code>结尾表示目录</p>
</li>
<li><p>以星号<code>*</code>通配多个字符</p>
</li>
<li><p>以问号 <code>?</code> 通配单个字符</p>
</li>
<li><p>以方括号 <code>[]</code> 包含单个字符的匹配列表</p>
</li>
<li><p>以叹号 <code>!</code> 表示不忽略(跟踪)匹配到的文件或目录</p>
</li>
<li><p><a href="https://www.cnblogs.com/jingtyu/p/6831772.html">参考</a></p>
</li>
</ul>
<h2 id="添加github仓库"><a href="#添加github仓库" class="headerlink" title="添加github仓库"></a>添加github仓库</h2><hr>
<ul>
<li>github上有一个为各种环境和编程语言提供gitignore模板的项目——<a href="https://github.com/github/gitignore">https://github.com/github/gitignore</a></li>
</ul>
<blockquote>
<p>他的项目介绍是：This is GitHub’s collection of .gitignore file templates. We use this list to populate the .gitignore template choosers available in the GitHub.com interface when creating new repositories and files.(意思大概就是可以用它来实现在创建repository的时候可以选择初始化一个.gitignore文件，并且可以选择一个模板)</p>
</blockquote>
<ul>
<li>我们在创建github仓库的时候尽量先预先创建好.gitignore文件，因为一旦push之后，添加了新的.gitignore貌似也是不起作用的，非常麻烦，所以要养成写.gitignore的好习惯。</li>
</ul>
]]></content>
      <categories>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>๑Git</tag>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Welcome to Fleschier Blog</title>
    <url>/2018/04/05/hello-2018/</url>
    <content><![CDATA[<blockquote>
<p>“It’s just a begining.”</p>
</blockquote>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><ul>
<li><p>渐寒 的 博客终于开通了。</p>
</li>
<li><p>几经辗转，在github上发现了自定义的博客这一功能，终于可以自己写一些东西了。然而对于我这样一个对前端没啥了解的人来说，只能借用别人造好的轮子来自定义博客的风格。这一点等以后啥时候有兴趣学前端了再自己DIY吧。</p>
</li>
</ul>
<hr>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><ul>
<li><p>第一次自己写博客还是很有新鲜感的。</p>
</li>
<li><p>搭建自己在github上的博客需要创建一个名为<em>yourusername.github.io</em>的repository，然后你需要选择一款Jekyll主题来使你的博客更加个性化。<em>（当然像我一样的小白就先借用别人写好的框架来吧）</em>。在创建好repository之后最好先clone到本地，这样便于后面的更改直接在本地进行然后通过git来提交就好了。在github上选择一个别人写好的，你比较中意的主题，然后clone下来，将其中你需要的文件加到你自己的库当中。<em>（其中可能会包含别人写的文章啊，一些图片什么的，这些都可以删掉或者替换掉）</em>。这里附上一个相对比较详细的教程——<a href="https://blog.csdn.net/walkerhau/article/details/77394659?utm_source=debugrun&utm_medium=referral">如何快速搭建自己的github.io博客</a>。<br>当然这里不可能把所有的细节都写清楚，有很多有趣的东西会在你尝试的过程中不断涌现出来，大胆的去尝试吧。</p>
</li>
<li><p>博文都是需要通过markdown编辑器来编写的。当然这也很简单，推荐使用的软件是<a href="http://markdownpad.com/">MarkdownPad2</a>。如果需要激活秘钥什么的直接百度吧（滑稽）。这里需要补充一下的是，MarkdownPad2 这款软件使用时会产生bug，官方的说法是从 Win 8 开始就有这个问题了(我用的是win10)，解决办法就是安装 <a href="http://markdownpad.com/download/awesomium_v1.6.6_sdk_win.exe">Awesomium 1.6.6 SDK</a>。如果还是不行就再安装 <a href="http://www.microsoft.com/en-us/download/details.aspx?id=8109">Microsoft’s DirectX End-User Runtimes (June 2010)</a>。下面是官方的原文：</p>
<blockquote>
</blockquote>
<p>LivePreview is not working - it displays an error message stating This view has crashed!</p>
<blockquote>
<p>This issue has been specifically observed in Windows 8. You may see an error message as shown here, and no HTML will be rendered when you type in the Markdown Editor pane.</p>
</blockquote>
<p>To fix this issue, please try installing the Awesomium 1.6.6 SDK.</p>
<blockquote>
</blockquote>
<p>If you continue to experience issues, please install Microsoft’s DirectX End-User Runtimes (June 2010).</p>
</li>
<li><p>对于提交的 XXX.markdown格式的文章，必须要放在<code>_posts</code>文件夹下，命名的格式必须为<code>YEAR-MONTH-DAY-title.markdown</code><br>并且title也必须用-来连接而<em>不要用下划线，不要用下划线，不要用下划线…</em></p>
</li>
<li><p>最后附上markdown的简单教程，很简单，一个小时之内上手完全没问题。<a href="https://www.jianshu.com/p/q81RER">献给写作者的 Markdown 新手指南</a>。</p>
</li>
</ul>
<h2 id="关于多个标签"><a href="#关于多个标签" class="headerlink" title="关于多个标签"></a>关于多个标签</h2><hr>
<ul>
<li>如果一篇博客归属于多个标签，则在tags一栏以 <strong>空格</strong> 分割各个标签。</li>
</ul>
<h2 id="使用jekyll本地预览网页"><a href="#使用jekyll本地预览网页" class="headerlink" title="使用jekyll本地预览网页"></a>使用jekyll本地预览网页</h2><hr>
<ul>
<li><p>参考<a href="https://fleschier.github.io/2018/07/08/Jekyll-usage/">我的另一个博客——Ubuntu下使用jekyll本地预览博客效果</a></p>
</li>
<li><p><a href="https://github.com/uolcano/blog/issues/11">更详细的一个博客</a></p>
</li>
</ul>
<h2 id="关于表格对齐的问题"><a href="#关于表格对齐的问题" class="headerlink" title="关于表格对齐的问题"></a>关于表格对齐的问题</h2><hr>
<ul>
<li>在表格的第二行：<code>|---|---|...|</code>这样的格式里，如果某个单元格写成<code>|---|</code>，则表示居中。如果写成<code>|:---|</code>则表示左对齐。</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.7.12</p>
</blockquote>
]]></content>
      <categories>
        <category>Personal Blog</category>
      </categories>
      <tags>
        <tag>๑Blog</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo插件live2d删除以及遇到的一些问题</title>
    <url>/2019/02/17/hexo-live2d-delete/</url>
    <content><![CDATA[<h2 id="删除live2d插件"><a href="#删除live2d插件" class="headerlink" title="删除live2d插件"></a>删除live2d插件</h2><hr>
<ul>
<li><p>之前由于兴趣以及探索欲安装了live2d插件,但是在使用过程中不知道如何替换模型,也不是很懂怎么自己DIY模型,于是便放弃了这一插件.</p>
</li>
<li><p>首先删除<code>\themes\next\layout</code>下<code>_layout.swig</code>文件内的与live2d有关的代码</p>
</li>
<li><p>然后使用<code>npm uninstall hexo-helper-live2d</code>来删除这一momdule.</p>
</li>
</ul>
<h2 id="然而事情并没有结束"><a href="#然而事情并没有结束" class="headerlink" title="然而事情并没有结束"></a>然而事情并没有结束</h2><hr>
<ul>
<li><p>本以为这个事情就这样搞定了,直到最近我又<code>npm install</code>了一个插件,然后<code>npm audit fix</code>了一下,结果这个module就像幽灵一样又出现了!WTF?!</p>
</li>
<li><p>本着研究学习的想法捣鼓了半天也没用,只能按照原先删除的步骤再删除一遍</p>
</li>
<li><p>不过或许是<code>npm audit fix</code>这条命令的问题,查了一下:</p>
<blockquote>
<p>npm audit ： <a href="mailto:&#x6e;&#112;&#109;&#x40;&#x35;&#x2e;&#x31;&#48;&#x2e;&#48;">&#x6e;&#112;&#109;&#x40;&#x35;&#x2e;&#x31;&#48;&#x2e;&#48;</a> &amp; npm@6，允许开发人员分析复杂的代码，并查明特定的漏洞和缺陷。<br>npm audit fix ：<a href="mailto:&#110;&#112;&#x6d;&#x40;&#54;&#46;&#49;&#x2e;&#48;">&#110;&#112;&#x6d;&#x40;&#54;&#46;&#49;&#x2e;&#48;</a>,  检测项目依赖中的漏洞并自动安装需要更新的有漏洞的依赖，而不必再自己进行跟踪和修复。</p>
</blockquote>
</li>
<li><p>官方还提供了其他的一些命令,比如:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(1)运行audit fix，但是只更新pkglock， 不更新node_modules：</span><br><span class="line">$ npm audit fix --package-lock-only</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(2)只更新dependencies中安装的包，跳过devDependencies中的包：</span><br><span class="line">$ npm audit fix --only=prod</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">运行命令，得到audit fix将会更新的内容，并且输出json格式的安装信息，但是并不真的安装更新：</span><br><span class="line">$ npm audit fix --dry-run --json</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">得到json格式的详细检测报告</span><br><span class="line">$ npm audit --json</span><br></pre></td></tr></table></figure>
</li>
<li><p>特此记录.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Personal Blog</category>
      </categories>
      <tags>
        <tag>๑Blog</tag>
      </tags>
  </entry>
  <entry>
    <title>Intellij IDEA Ultimate版本使用</title>
    <url>/2018/05/25/jetbrains/</url>
    <content><![CDATA[<h2 id="JetBrains-Toolbox"><a href="#JetBrains-Toolbox" class="headerlink" title="JetBrains Toolbox"></a>JetBrains Toolbox</h2><hr>
<ul>
<li><p>如果你决定使用jetbrains的开发环境的话，那么去官网上获取一个jetbrains toolbox会是个不错的主意～</p>
</li>
<li><p>这个工具可以很方便地管理你的所有jetbrains的产品，避免了太多快捷方式的麻烦。</p>
</li>
</ul>
<h2 id="免费获取Ultimate版本"><a href="#免费获取Ultimate版本" class="headerlink" title="免费获取Ultimate版本"></a>免费获取Ultimate版本</h2><hr>
<ul>
<li><p>jetbrains给学生的优惠还是非常大的，我们可以用我们学校的邮箱免费获取JetBrains全家桶</p>
</li>
<li><p>这个在我上一篇文章里也提到了，这里再作一次系统的记录。</p>
</li>
<li><p><a href="https://sales.jetbrains.com/hc/zh-cn/articles/207154369-%E5%AD%A6%E7%94%9F%E6%8E%88%E6%9D%83%E7%94%B3%E8%AF%B7%E6%96%B9%E5%BC%8F">Jetbrains学生免费账号申请</a></p>
</li>
<li><p>申请流程其实很简单，你需要一个你们学校的邮箱即可。然后验证通过之后会发送验证邮件，依次填写并且最终注册完账号即可激活。注册的时候一定要确定邮箱的地址填写正确，不出于意外一分钟之内就能收到确认邮件。</p>
</li>
<li><p>如果填错了也没关系，你可以一遍一遍地写邮箱然后申请，jetbrains好像对此没有什么限制。</p>
</li>
<li><p>成功激活：</p>
</li>
</ul>
<p><img src="/images/node.js/info.png"></p>
<h2 id="初始配置"><a href="#初始配置" class="headerlink" title="初始配置"></a>初始配置</h2><hr>
<ul>
<li><p>装好Ultimate版本之后我们就可以开始配置一些东西了。</p>
</li>
<li><p>首先打开intellij IDEA，不用进入某个项目，在初始界面设置(这样这些设置便是对全局生效)</p>
</li>
<li><p>在plugins里面添加一些我们需要的插件。比如 scala , nodejs, junitGenerator2.0等，这些插件比community版本要丰富很多。</p>
</li>
<li><p>在 system settings 里面把 reopen previous project勾掉，这样每次打开我们就可以手动选择要打开的项目了。</p>
</li>
<li><p>然后在global lib里面点击绿色的加号，添加我们用到的编成语言的lib，在弹出的选择框里找到我们安装的目录。比如添加scala的lib，要找到scala sdk的安装目录即可，intellij 会自己扫描目录然后添加lib，最后保存确定即可。</p>
</li>
</ul>
<h2 id="添加已有工程时遇到的坑"><a href="#添加已有工程时遇到的坑" class="headerlink" title="添加已有工程时遇到的坑"></a>添加已有工程时遇到的坑</h2><hr>
<ul>
<li><p>最近老师布置了个jdbc的作业，是个eclipse的工程。但是在intellij打开的话，test部分始终有问题，不能访问到src里的代码。。</p>
</li>
<li><p>然后换了一种导入工程的方式，但是无论是eclipse方式导入还是maven方式导入，都会出现没有工程可以导入的错误???(No projects are found to import 这个问题在用eclipse来导入工程的时候也可能遇到) 上网查阅之后发现，可能是缺少<code>.classpath</code>文件和<code>.project</code>文件(或者有可能是这两个文件有问题)。</p>
</li>
<li><p>解决方法就是，用eclipse重新建立一个同名的工程，然后把这个工程里面的这两个文件复制到要导入的工程里再试着导入。</p>
</li>
<li><p>或者如果代码不太多的话，直接新建一个工程，把代码复制进去就好了。</p>
</li>
</ul>
<br>
> 最后更新于2018.6.4
]]></content>
      <categories>
        <category>Computer Software</category>
      </categories>
      <tags>
        <tag>๑JS</tag>
        <tag>๑JetBrains</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu18 Pycharm无法识别安装的Python库的解决方案</title>
    <url>/2018/10/16/pycharm-problem-solving/</url>
    <content><![CDATA[<blockquote>
<p>凌晨更~</p>
</blockquote>
<h2 id="pycahrm无法识别"><a href="#pycahrm无法识别" class="headerlink" title="pycahrm无法识别"></a>pycahrm无法识别</h2><hr>
<ul>
<li><p>我们有时候在需要使用一些第三方python库的时候，需要<code>pip install XXX</code>，然而愉快地安装完了之后，在pycharm中新建项目<code>import</code>却失败，提示找不到。</p>
</li>
<li><p>这个问题坑了好久，在终端里使用完全没有问题然而pycahrm里却不行。今天终于下定决心搞定这个问题。</p>
</li>
</ul>
<h2 id="首先从默认python版本下手"><a href="#首先从默认python版本下手" class="headerlink" title="首先从默认python版本下手"></a>首先从默认python版本下手</h2><hr>
<ul>
<li><p>由于我使用的是ubuntu18.04，其默认的python版本是python2.7，而我在IDE里使用的是后来安装的python3.6版本，因此pip install的module不会被识别(<em>因为终端里<code>pip install</code> 命令使用的解释器版本是2.7，安装的module也只能被2.7版本的识别</em>)</p>
</li>
<li><p>首先执行<code>python --version</code>查看当前默认版本</p>
</li>
<li><p>然后查看当前已安装的所有版本<code>ls /usr/bin/python*</code></p>
</li>
<li><p>接着修改系统信息：(<strong>需要管理员权限</strong>)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">root@fleschier-GE5S:/home/fleschier# update-alternatives --install /usr/bin/python python /usr/bin/python2.7 1</span><br><span class="line">root@fleschier-GE5S:/home/fleschier# update-alternatives --install /usr/bin/python python /usr/bin/python3.6 2</span><br><span class="line">update-alternatives: 使用 /usr/bin/python3.6 来在自动模式中提供 /usr/bin/python (python)</span><br></pre></td></tr></table></figure>
<p><code>--install</code> 选项使用了多个参数用于创建符号链接。最后一个参数指定了此选项的优先级，如果我们没有手动来设置替代选项，那么具有最高优先级的选项就会被选中。这个例子中，我们为 <code>/usr/bin/python3.6</code> 设置的优先级为2，所以<code>update-alternatives</code> 命令会自动将它设置为默认 Python 版本。</p>
</li>
<li><p>参考<a href="https://blog.csdn.net/fang_chuan/article/details/60958329">更改Ubuntu默认python版本的两种方法</a></p>
</li>
</ul>
<h3 id="一个坑接着一个坑"><a href="#一个坑接着一个坑" class="headerlink" title="一个坑接着一个坑"></a>一个坑接着一个坑</h3><ul>
<li><p>默认python版本改完了，下面就是<code>pip</code>了</p>
</li>
<li><p>改完之后会发现，执行<code>pip</code>命令会提示没有这个module。。。好吧又是坑</p>
</li>
<li><p>解决方案：<code>sudo apt-get install python3-pip</code></p>
</li>
<li><p>安装完成然后就可以使用pip的一系列命令了</p>
</li>
</ul>
<h3 id="更新pip版本"><a href="#更新pip版本" class="headerlink" title="更新pip版本"></a>更新pip版本</h3><ul>
<li><p><code>python -m pip install --upgrade pip</code></p>
</li>
<li><p>参考<a href="https://stackoverflow.com/questions/18363022/importerror-no-module-named-pip">stackoverflow的一个问题</a></p>
</li>
</ul>
<h2 id="然后就是重新安装我们需要的库了"><a href="#然后就是重新安装我们需要的库了" class="headerlink" title="然后就是重新安装我们需要的库了"></a>然后就是重新安装我们需要的库了</h2><hr>
<ul>
<li><p>我这里要搞一个python绘图，需要matplotlib库</p>
</li>
<li><p>按照其官方的安装方法(<em>仍然需要管理员权限否则最后一步会失败提示权限不够</em>):<code>python -m pip install -U matplotlib</code></p>
</li>
<li><p>或者是一般的安装<code>pip3 install moduleName</code></p>
</li>
</ul>
<h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><hr>
<ul>
<li>最后就是网上烂大街的解决方法，指定pycharm的编译器为python3.x，即可～</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows10使用navicat链接mySQL错误</title>
    <url>/2018/11/09/mySQL-download-windows/</url>
    <content><![CDATA[<h2 id="错误代码"><a href="#错误代码" class="headerlink" title="错误代码"></a>错误代码</h2><hr>
<ul>
<li><p>安装mysql8.0服务器端，连接Navicat会提示报错：<br><code>1251 Client does not support authentication protocol requested by server; consider upgrading MySQL client</code></p>
</li>
<li><p>网上一堆方法,但是很麻烦</p>
</li>
</ul>
<h2 id="根本原因"><a href="#根本原因" class="headerlink" title="根本原因"></a>根本原因</h2><hr>
<ul>
<li><p>最后终于发现了发生这种情况的根本原因,就是加密方式的更新.</p>
</li>
<li><p>由于最新版的MySQL安装的时候,推荐使用了一种最新的加密方式,就是在安装过程中,Authentication Method这一配置过程，给了两个基于不同的加密方式. 第一个是推荐的,当时我也没多想就选了第一个,所以才导致了后面的状况.</p>
</li>
</ul>
<h2 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h2><hr>
<ul>
<li>再次打开mysql的安装程序,<em>这里不需要卸载重装</em>,选择更改设置, 将Authentication Method的加密方式改为第二个兼容加密方式就可以在navicat正确链接了.</li>
</ul>
]]></content>
      <categories>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>๑Windows</tag>
        <tag>๑mySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 面向对象编程</title>
    <url>/2018/11/25/python-class/</url>
    <content><![CDATA[<h2 id="模块与包"><a href="#模块与包" class="headerlink" title="模块与包"></a>模块与包</h2><hr>
<ul>
<li><p>简单举例就是:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mypackage</span><br><span class="line">├─ __init__.py</span><br><span class="line">├─ abcd.py</span><br><span class="line">└─ efgh.py</span><br></pre></td></tr></table></figure>
</li>
<li><p>注意每个包下需要一个<code>__init__.py</code>文件</p>
</li>
<li><p>如果另一个py文件需要引用abcd.py,则写为<code>mypackage.abcd</code></p>
</li>
</ul>
<h2 id="python类"><a href="#python类" class="headerlink" title="python类"></a>python类</h2><hr>
<ul>
<li><p>类不需要显式地申明类成员变量(与C++和Java不同)</p>
</li>
<li><p>类中所有的方法的第一个参数都是<code>self</code>, self代表类的实例而非类,并且在调用类方法时,self参数不必传入.</p>
</li>
<li><p><code>__init__()</code>方法是一种特殊的方法，被称为类的构造函数或初始化方法，当创建了这个类的实例时就会调用该方法</p>
</li>
</ul>
<h3 id="python类内置属性"><a href="#python类内置属性" class="headerlink" title="python类内置属性"></a>python类内置属性</h3><ul>
<li><p><code>__dict__</code> : 类的属性（包含一个字典，由类的数据属性组成）</p>
</li>
<li><p><code>__doc__</code> :类的文档字符串</p>
</li>
<li><p><code>__name__</code>: 类名</p>
</li>
<li><p><code>__module__</code>: 类定义所在的模块（类的全名是’<strong>main</strong>.className’，如果类位于一个导入模块mymod中，那么className.<strong>module</strong> 等于 mymod）</p>
</li>
<li><p><code>__bases__</code> : 类的所有父类构成元素（包含了一个由所有父类组成的元组）</p>
</li>
</ul>
<h4 id="判断一个对象是否是一个类的实例"><a href="#判断一个对象是否是一个类的实例" class="headerlink" title="判断一个对象是否是一个类的实例"></a>判断一个对象是否是一个类的实例</h4><ul>
<li>目前我所知的只有两个方法可行(python3):<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a = myclass()</span><br><span class="line">b = myclass()</span><br><span class="line">type(a) == type(b) #a和b都是类实例</span><br><span class="line">isinstance(a, myclass.__bases__)    #虽然__bases__返回的是父类元组,但是却能成功判断</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala IO 记录</title>
    <url>/2018/09/06/scala-io/</url>
    <content><![CDATA[<h2 id="控制台IO"><a href="#控制台IO" class="headerlink" title="控制台IO"></a>控制台IO</h2><hr>
<h3 id="调用java的IO库"><a href="#调用java的IO库" class="headerlink" title="调用java的IO库"></a>调用java的IO库</h3><ul>
<li><p>scala使用java的io类来读取</p>
</li>
<li><p>需要引入java的io类：<code>import java.io._</code></p>
</li>
<li><p>使用Java的<code>BufferedReader</code>和<code>InputStreamReader</code>方法来实现</p>
</li>
<li><p>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val br = new BufferedReader(new InputStreamReader(System.in))</span><br><span class="line">val x = br.readLine().trim  //注意readLine的L是大写</span><br></pre></td></tr></table></figure>
</li>
<li><p>所有的读入的内容都是String类型，可以使用<code>toInt</code>，<code>toDouble</code>等方法来将输入转化成各种需要的格式</p>
</li>
</ul>
<h3 id="调用scala的IO库"><a href="#调用scala的IO库" class="headerlink" title="调用scala的IO库"></a>调用scala的IO库</h3><ul>
<li><p>例:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import scala.io._</span><br><span class="line">object Test &#123;</span><br><span class="line">   def main(args: Array[String]) &#123;</span><br><span class="line">      val line = StdIn.readLine()</span><br><span class="line">      ...</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>scala的IO库更为简洁，可以调用<code>readInt()</code>,<code>readBoolean</code>等一系列方法，直接进行转换。</p>
</li>
</ul>
<blockquote>
<p>Scala2.11 后的版本 Console.readLine 已废弃，使用 scala.io.StdIn.readLine() 方法代替。</p>
</blockquote>
<h2 id="文件IO"><a href="#文件IO" class="headerlink" title="文件IO"></a>文件IO</h2><hr>
<ul>
<li><p>需要引入<code>scala.io</code>库</p>
</li>
<li><p>例子(读取test.txt文件的内容)：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import scala.io.Source</span><br><span class="line"></span><br><span class="line">object Test &#123;</span><br><span class="line">   def main(args: Array[String]) &#123;</span><br><span class="line">      println(&quot;文件内容为:&quot; )</span><br><span class="line">      Source.fromFile(&quot;test.txt&quot; ).foreach&#123;</span><br><span class="line">         println</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>《Scala编程》阅读记录——列表进阶</title>
    <url>/2018/07/24/scala-learning-04/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="第十六章-使用列表"><a href="#第十六章-使用列表" class="headerlink" title="第十六章 使用列表"></a>第十六章 使用列表</h2><hr>
<ul>
<li><p><a href="https://fleschier.github.io/2018/07/Scala-infos/">列表的使用方法表格见另一篇博客</a></p>
</li>
<li><p>列表跟数组非常像，但是有两个重要的区别：</p>
</li>
</ul>
<ol>
<li>列表是不可变的，也就是说列表的元素不能通过赋值改变</li>
<li>列表是递归的(即链表)，而数组是平的</li>
</ol>
<ul>
<li><p><code>init</code>和<code>last</code>要消耗的时间与列表的长度成正比，而<code>head</code>和<code>tail</code>消耗常量级别的时间</p>
</li>
<li><p><code>indices</code>方法返回包含了指定列表所有有效的下标，所以一般用在for表达式遍历列表时：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val lst = new List(&quot;af&quot;,&quot;g3g&quot;,&quot;g53v&quot;)</span><br><span class="line">for( i &lt;- lst.indices)&#123; //这里i即是从lst的第一个有效下标开始直到最后一个下标</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="flatten方法：接受一个列表的列表并返回单个列表"><a href="#flatten方法：接受一个列表的列表并返回单个列表" class="headerlink" title="flatten方法：接受一个列表的列表并返回单个列表"></a><code>flatten</code>方法：接受一个列表的列表并返回单个列表</h3><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; List(List(1,2),List(3),List(),List(4,5)).flatten</span><br><span class="line">res0: List[Int] = List(1,2,3,4,5)</span><br></pre></td></tr></table></figure>
</li>
<li><p>例子：<br><img src="/images/Scala/flatten_test.jpg"></p>
</li>
</ul>
<h3 id="zip和unzip"><a href="#zip和unzip" class="headerlink" title="zip和unzip"></a><code>zip</code>和<code>unzip</code></h3><ul>
<li><p>zip操作接受两个列表，返回一个有对偶组成的列表</p>
</li>
<li><p>如果两个列表的元素个数不同，那么任何没有配对上的元素将被丢弃</p>
</li>
</ul>
<h4 id="zipWithIndex方法"><a href="#zipWithIndex方法" class="headerlink" title="zipWithIndex方法"></a><code>zipWithIndex</code>方法</h4><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; val abcde = List(&#x27;a&#x27;,&#x27;b&#x27;,&#x27;c&#x27;,&#x27;d&#x27;,&#x27;e&#x27;)</span><br><span class="line">abcde: List[Char] = List(a,b,c,d,e)</span><br><span class="line">scala&gt; abcde.zipWithIndex</span><br><span class="line">res0: List[(Char,Int)] = List((a,0),(b,1),(c,2),(d,3),(e,4))</span><br></pre></td></tr></table></figure>
</li>
<li><p>任何元组的列表都可以通过unzip方法转换回由列表组成的元组</p>
</li>
</ul>
<h3 id="显示列表"><a href="#显示列表" class="headerlink" title="显示列表"></a>显示列表</h3><ul>
<li>下面一些例子：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; abcde mkString(&quot;[&quot;, &quot;,&quot;, &quot;]&quot;)</span><br><span class="line">res0: String = [a,b,c,d,e]</span><br><span class="line">scala&gt; abcde mkString &quot;&quot;</span><br><span class="line">res1: String = abcde</span><br><span class="line">scala&gt; abcde.mkString</span><br><span class="line">res2: String = abcde</span><br><span class="line">scala&gt; abcde mkString (&quot;List(&quot;, &quot;, &quot;, &quot;)&quot;)</span><br><span class="line">res3: String = List(a,b,c,d,e)</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="过滤列表"><a href="#过滤列表" class="headerlink" title="过滤列表"></a>过滤列表</h3><ul>
<li><p><code>filter</code>，<code>partition</code>，<code>find</code>，<code>takeWhile</code>，<code>dropWhile</code>和<code>span</code></p>
</li>
<li><p>这些过滤函数的参数都是<code>T =&gt; Boolean</code>类型的</p>
</li>
<li><p><code>x.takeWhile(p)</code>返回x列表中连续满足p的最长前缀，例：<code>nums.takeWhile(_ &gt; 0)</code></p>
</li>
<li><p><code>x.dropWhile</code>将去除列表x中连续满足p的最长前缀，例：<code>words.dropWhile(_.startsWith(&quot;t&quot;))</code></p>
</li>
</ul>
<h3 id="对列表前提条件的检查"><a href="#对列表前提条件的检查" class="headerlink" title="对列表前提条件的检查"></a>对列表前提条件的检查</h3><ul>
<li><p><code>forall</code>和<code>exists</code></p>
</li>
<li><p><code>xs.forall(p)</code>，如果列表中所有元素都满足p则返回true</p>
</li>
<li><p><code>xs.exists(p)</code>，如果列表中有至少一个元素满足p则返回true</p>
</li>
</ul>
<br>
> 最后更新于2018.7.24
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>《Scala编程》阅读记录——集合类进阶</title>
    <url>/2018/07/24/scala-learning-05/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="第十七章-使用集合类"><a href="#第十七章-使用集合类" class="headerlink" title="第十七章 使用集合类"></a>第十七章 使用集合类</h2><hr>
<h3 id="序列"><a href="#序列" class="headerlink" title="序列"></a>序列</h3><h4 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h4><ul>
<li>List类是不可变 <strong>链表</strong>, 因此支持在头部快速添加和移除条目，但是不提供按下标快速访问的功能，因为实现这个功能需要线性地遍历列表。</li>
</ul>
<h4 id="列表缓冲-ListBuffer"><a href="#列表缓冲-ListBuffer" class="headerlink" title="列表缓冲(ListBuffer)"></a>列表缓冲(ListBuffer)</h4><ul>
<li><p>List类提供对列表头部的快速访问，但是对尾部就不那么高效，因此在尾部添加元素时我们经常需要用到reverse方法，在头部添加元素最后再reverse来获得我们想要的顺序</p>
</li>
<li><p>ListBuffer是一个可变对象(包含在scala.collection.mutable中)，帮助我们在需要追加元素来构建列表时能够更加高效。ListBuffer提供了常量时间的往前和往后追加元素的操作。</p>
</li>
<li><p>可以使用<code>+=</code>来往后追加元素，使用<code>+=:</code>来往前追加元素</p>
</li>
<li><p>最后可以使用<code>toList</code>方法来转换成List类型</p>
</li>
<li><p>使用ListBuffer的另一个原因是可以防止可能的栈溢出</p>
</li>
</ul>
<h4 id="数组缓冲-ArrayBuffer"><a href="#数组缓冲-ArrayBuffer" class="headerlink" title="数组缓冲(ArrayBuffer)"></a>数组缓冲(ArrayBuffer)</h4><ul>
<li><p>创建数组缓冲的时候不需要指定长度，只要给定类型</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import scala.collection.mutable.ArrayBuffer</span><br><span class="line">val buf = new ArrayBuffer[Int]()</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用<code>+=</code>方法来追加元素</p>
</li>
<li><p>所有的数组的常规操作都是可以用的</p>
</li>
</ul>
<h4 id="字符串-通过StringOps"><a href="#字符串-通过StringOps" class="headerlink" title="字符串(通过StringOps)"></a>字符串(通过StringOps)</h4><ul>
<li><p>StringOps实现了很多序列的方法。由于Predef有一个从String到StringOps的隐式转换，可以将任何字符串当做序列来处理</p>
</li>
<li><p>例如:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def hasUpperCase(s: String) = s.exists(_.isUpper)</span><br><span class="line">hasUpperCase(&quot;Robert Frost&quot;)</span><br></pre></td></tr></table></figure>
<p>这里由于String类本身并没有任何名为<code>exists</code>的方法，但是StringOps有，通过隐式转换将s转换成StringOps来实现</p>
</li>
</ul>
<h3 id="集和映射"><a href="#集和映射" class="headerlink" title="集和映射"></a>集和映射</h3><hr>
<h4 id="使用集"><a href="#使用集" class="headerlink" title="使用集"></a>使用集</h4><ul>
<li><p>集的关键特征是它们会确保在同一时刻，以<code>==</code>为标准，集里的每个对象都最多出现一次</p>
</li>
<li><p>集的方法见博客<a href="https://fleschier.github.io/2018/07/Scala-infos/">方法查询</a></p>
</li>
</ul>
<h4 id="使用映射"><a href="#使用映射" class="headerlink" title="使用映射"></a>使用映射</h4><ul>
<li><p>映射使用键来索引对应的值</p>
</li>
<li><p>下面是一个统计每个单词在字符串中出现次数的方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def countWords(text: String) = &#123;</span><br><span class="line">  val counts = mutable.Map.empty[String,Int]</span><br><span class="line">  for (rawWord &lt;- text.split(&quot;[ ,!.]+&quot;))&#123;</span><br><span class="line">    val word = rawWord.toLowerCase // 统一变为小写字母</span><br><span class="line">    val oldCount =</span><br><span class="line">      if(counts.contains(word)) counts(word)</span><br><span class="line">      else 0</span><br><span class="line">    counts += (word -&gt; (oldCount + 1))</span><br><span class="line">  &#125;</span><br><span class="line">  counts</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>映射的方法见博客<a href="https://fleschier.github.io/2018/07/Scala-infos/">方法查询</a></p>
</li>
<li><p>可变集合和可变映射的工厂方法都采用了hash的技术。例如<code>scala.collection.mutable.Set()</code>这个工厂方法返回一个<code>scala.collection.mutable.HashSet</code>。同理， <code>scala.collection.mutable.Map()</code>这个工厂方法返回一个<code>scala.collection.mutable.HashMap</code></p>
</li>
</ul>
<h3 id="元组"><a href="#元组" class="headerlink" title="元组"></a>元组</h3><hr>
<ul>
<li>元组帮助我们省去定义那些简单的主要承载数据的类的麻烦。<strong>元组的一个常见的应用场景是从方法返回多个值</strong></li>
</ul>
<br>
> 最后更新于2018.7.24
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>《Scala编程》阅读记录——类进阶</title>
    <url>/2018/07/23/scala-learning-03/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="第十章-组合和继承-面向对象进阶"><a href="#第十章-组合和继承-面向对象进阶" class="headerlink" title="第十章 组合和继承  面向对象进阶"></a>第十章 组合和继承  面向对象进阶</h2><h3 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h3><ul>
<li>例子：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">abstract class Element&#123;</span><br><span class="line">	def contents: Array[String]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
注意，Element类中content方法没有标上abstract修饰符。一个方法只要是没有实现的(即没有等号或方法体)，那么它就是抽象的。跟Java不同，我们不能对方法加上abstract修饰符</li>
</ul>
<h3 id="定义无参方法"><a href="#定义无参方法" class="headerlink" title="定义无参方法"></a>定义无参方法</h3><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">abstract class Element &#123;</span><br><span class="line">	def contents: Array[String]</span><br><span class="line">	def height: Int = contents.length</span><br><span class="line">	def width: Int = if(height == 0) 0 else contents(0).length</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>这个无参方法的特点是，如果将<code>def</code>换成<code>val</code>，那么他就成了一个字段而不会有编译错误</p>
</li>
<li><p>在这里无参方法和字段的区别是：字段的调用会更加快，但是每次实例化对象都要分配内存空间。具体取舍要看具体的用法。</p>
</li>
<li><p>总结下来就是，scala推荐我们将那些没有参数也没有副作用的方法定义为无参方法，而对于无参数而有副作用的方法不应当省略空括号，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&quot;hello&quot;.length  //没有()，因为没有副作用</span><br><span class="line">println()	//有()，因为有副作用</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="扩展类"><a href="#扩展类" class="headerlink" title="扩展类"></a>扩展类</h3><ul>
<li><p>使用<code>extends</code>关键字，相当于继承</p>
</li>
<li><p>scala中所有类的默认父类是AnyRef</p>
</li>
<li><p>继承的意思是超类的所有成员也是子类的成员，但是有两个例外：<br>一个是超类的私有成员不会被子类继承；<br>二是如果子类实现了相同名称和参数的成员，那么该成员不会被继承(重写，override)</p>
</li>
<li><p>scala只有两个命名空间用于定义：</p>
</li>
</ul>
<ol>
<li>值(字段，方法，包和单例对象)</li>
<li>类型(类和特质名)</li>
</ol>
<ul>
<li><p>因此scala可以用val来重写一个无参方法(即将一个无参方法重写为一个字段)</p>
</li>
<li><p>如果我们要在子类中重写父类的方法，则要在方法前加上override修饰符：<code>override def funcName(...)&#123;...&#125;</code></p>
</li>
<li><p>使用final修饰符来确保某个成员不能被子类继承</p>
</li>
<li><p>scala的继承关系图<br><img src="/images/Scala/relation.jpg"></p>
</li>
</ul>
<h2 id="第十二章-特质"><a href="#第十二章-特质" class="headerlink" title="第十二章 特质"></a>第十二章 特质</h2><hr>
<ul>
<li><p>特质的定义跟类定义很像，除了关键字trait</p>
</li>
<li><p>scala可以使用<code>extends</code>或者<code>with</code>关键字来 <em>混入</em> 特质，而不是继承。</p>
</li>
<li><p>例子：<code>class Frog extends Animal with Philosophical with HasLegs&#123;..&#125;</code><br>混入多个特质用<code>with</code>连接</p>
</li>
</ul>
<h3 id="要点如下"><a href="#要点如下" class="headerlink" title="要点如下:"></a>要点如下:</h3><ul>
<li><p><strong>Scala中类只能继承一个超类, 可以扩展任意数量的特质</strong></p>
</li>
<li><p>特质可以要求实现它们的类具备特定的字段, 方法和超类</p>
</li>
<li><p>与Java接口不同, Scala特质可以提供方法和字段的实现</p>
</li>
<li><p>当将多个特质叠加使用的时候, 顺序很重要</p>
</li>
<li><p><a href="https://www.cnblogs.com/nowgood/p/scalatrait.html">scala特质详细解析</a></p>
</li>
<li><p>重写特质的抽象方法时, 不需要<code>override</code>关键字</p>
</li>
<li><p>可以为类和对象添加多个相互调用的特质时, 从最后一个开始调用. 这对于需要分阶段加工处理某个值的场景很有用.</p>
</li>
</ul>
<h2 id="第十三章-包和引入"><a href="#第十三章-包和引入" class="headerlink" title="第十三章 包和引入"></a>第十三章 包和引入</h2><hr>
<ul>
<li><p>scala的引入可以出现在任何地方，不仅仅是某个编译单元的开始</p>
</li>
<li><p>例如，假设一个Fruit类，其中有有name和color字段。有如下的一个函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def showFruit(fruit: Fruit) = &#123;</span><br><span class="line">	import Fruit._</span><br><span class="line">	println(name + &quot;s are&quot; + color)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>showFruit引入了其参数fruit(Fruit类)的所有成员。下面的name和color就相当于fruit.name和fruit.color</p>
</li>
</ul>
<h2 id="第十四章-断言和测试"><a href="#第十四章-断言和测试" class="headerlink" title="第十四章 断言和测试"></a>第十四章 断言和测试</h2><hr>
<ul>
<li><p>在scala中，断言的写法是对预定义方法assert的调用。</p>
</li>
<li><p>表达式：<code>assert(condition)</code>。如果condition不满足则抛出AssertionError</p>
</li>
<li><p>另一种：<code>assert(condition,explanation)</code>如果condition不满足则抛出指定的explanation的AssertionError</p>
</li>
</ul>
<h2 id="第十五章-样例类和模式匹配"><a href="#第十五章-样例类和模式匹配" class="headerlink" title="第十五章 样例类和模式匹配"></a>第十五章 样例类和模式匹配</h2><hr>
<ul>
<li><p>样例类使用case关键字修饰，scala编译器会自动创建工厂方法，即可以直接用<code>className(parameter1,parameter2,...)</code>而不用<code>new</code>关键字即可创建对象</p>
</li>
<li><p>模式匹配，例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">expr match&#123;</span><br><span class="line">	case BinOp(...) =&gt; println(...)</span><br><span class="line">	case _ =&gt;  //处理器默认case,这一行是必须的，否则所有非满足第一个条件的case都会返回MatchError</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通配模式(<code>_</code>)会匹配任何对象</p>
</li>
</ul>
<h2 id="第二十章-抽象成员"><a href="#第二十章-抽象成员" class="headerlink" title="第二十章 抽象成员"></a>第二十章 抽象成员</h2><hr>
<ul>
<li><p>所有四种抽象成员：<code>val</code>，<code>var</code>，方法和类型</p>
</li>
<li><p>例子：下面这个特质声明了四种抽象成员：一个抽象类型(T)，一个抽象方法(transform)，一个val(initial)和一个var(current)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">trait Anstract&#123;</span><br><span class="line">	type T</span><br><span class="line">	def transform(x: T): T</span><br><span class="line">	val initial: T</span><br><span class="line">	var current: T</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>scala的抽象类型指的是用type关键字声明为某个类或者特质的成员(但并不给出定义)的类型。类或者特质都不能够叫抽象类型，抽象类型永远是某个类或者特质的成员，比如Abstract特质中的T</p>
</li>
<li><p><code>lazy</code>关键字：如果在<code>val</code>关键字前面加上<code>lazy</code>关键字，则该变量会在第一次被使用时被赋值。</p>
</li>
<li><p>T是指一个在声明时还未知的类型。不同的子类可以提供不同的T的实现</p>
</li>
</ul>
<br>
> 最后更新于2018.7.24
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>《Scala编程》阅读记录——进阶部分</title>
    <url>/2018/07/19/scala-learning-02/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><hr>
<h3 id="序列"><a href="#序列" class="headerlink" title="序列"></a>序列</h3><ul>
<li><p>Seq trait用于表示序列。所谓序列，指的是一类具有一定长度的可迭代访问的对象，其中每个元素均带有一个从0开始计数的固定索引位置。</p>
</li>
<li><p>常用线性序列有 <code>scala.collection.immutable.List</code>和<code>scala.collection.immutable.Stream</code>。常用索引序列有 <code>scala.Array scala.collection.mutable.ArrayBuffer</code>。Vector 类提供一个在索引访问和线性访问之间有趣的折中。它同时具有高效的恒定时间的索引开销，和恒定时间的线性访问开销。正因为如此，对于混合访问模式，vector是一个很好的基础。</p>
</li>
</ul>
<h3 id="缓冲器"><a href="#缓冲器" class="headerlink" title="缓冲器"></a>缓冲器</h3><ul>
<li>Buffers是可变序列一个重要的种类。它们不仅允许更新现有的元素，而且允许元素的插入、移除和在buffer尾部高效地添加新元素。buffer 支持的主要新方法有：用于在尾部添加元素的<code>+=</code> 和 <code>++=</code>；用于在前方添加元素的<code>+=:</code> 和 <code>++=:</code> ；用于插入元素的 <code>insert</code>和<code>insertAll</code>；以及用于删除元素的 <code>remove</code> 和 <code>-=</code>。</li>
</ul>
<h2 id="第四章"><a href="#第四章" class="headerlink" title="第四章"></a>第四章</h2><hr>
<h3 id="类和对象"><a href="#类和对象" class="headerlink" title="类和对象"></a>类和对象</h3><ul>
<li><p>在类定义中，我们会填入字段(field)和方法(method)，这些统称为成员(member)</p>
</li>
<li><p>保持健壮性，我们可以将字段声明为私有(在字段前面加上private修饰符)，访问和修改都通过方法来实现。<br>scala的默认访问级别是public</p>
</li>
<li><p>scala方法参数有一个特征，就是 <strong>它们都是<code>val</code>而不是<code>var</code></strong></p>
</li>
<li><p>一个类的例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class test&#123;</span><br><span class="line">	private var sum = 0</span><br><span class="line">	def add(b: Byte): Unit = &#123;</span><br><span class="line">		sum += b</span><br><span class="line">	&#125;</span><br><span class="line">	def checksum(): Int = &#123;</span><br><span class="line">		return ~(sum &amp; 0xFF) + 1</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在方法中，没有任何显式的return语句时，<strong>scala返回的是该方法计算出的最后一个(表达式的)值</strong> ,因此上面这个例子的return语句可以省略不写</p>
</li>
<li><p>同时，另一种方法的简写方式是，当一个方法只会计算一个返回结果的表达式时，可以不写花括号。如果这个表达式很短，他甚至可以被放置在def的同一行。为了极致的精简，还可以省略掉结果类型。</p>
</li>
<li><p>更改后的写法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class test&#123;</span><br><span class="line">	private var sum = 0</span><br><span class="line">	def add(b: Byte) = sum += b</span><br><span class="line">	def checksum() = ~(sum &amp; 0xFF) + 1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在这段代码中，scala能够正确地推断出add和checksum这两个方法的结果类型。但是最好的写法是显式地给出结果类型，这样便于代码的调试和维护</p>
</li>
<li><p>本例中的add方法结果类型为Unit，执行它的目的就是产生副作用。对于那些仅仅为了其副作用而被执行的方法被称作<em>过程</em>。</p>
</li>
<li><p>一般分号是没有必要加的，但是如果一行写多个语句，则用分号间隔</p>
</li>
</ul>
<h3 id="单例对象"><a href="#单例对象" class="headerlink" title="单例对象"></a>单例对象</h3><ul>
<li><p>scala比Java更面向对象一点，是scala <strong>不允许有静态成员</strong>。<br>因此，scala提供了<em>单例对象</em>，单例对象与普通的类很像，只是将<code>class</code>关键字换成了<code>object</code></p>
</li>
<li><p>当单例对象跟某个类共用同一个名字时，它被称作这个类的 <strong>伴生对象(companion object)</strong></p>
</li>
<li><p>必须在同一个源码文件中定义类和类的伴生对象，同时，类又叫做这个单例对象的 <strong>伴生类(companion class)</strong></p>
</li>
<li><p>类和它的伴生对象可以互相访问对方的私有成员</p>
</li>
<li><p>单例对象当中的方法可以像Java中的静态方法一样直接通过类名来访问</p>
</li>
<li><p>单例对象不仅仅用来存放静态方法，他是一等的对象，可以把单例对象想象成附加在对象身上的“名字标签”</p>
</li>
<li><p>类和单例对象的区别是单例对象不能被实例化(即没法new一个单例对象，除非这个单例对象有伴生类)</p>
</li>
<li><p>单例对象在有代码首次访问时才会被初始化</p>
</li>
<li><p>没有同名的伴生类的单例对象称为 <em>孤立对象</em>(standalone object)，一般用来将工具方法归集在一起，或定义scala程序的入口。</p>
</li>
</ul>
<h3 id="scala应用程序"><a href="#scala应用程序" class="headerlink" title="scala应用程序"></a>scala应用程序</h3><ul>
<li>要运行一个scala程序，必须提供一个孤立对象，这个孤立对象需要包含一个main方法，该方法接收一个Array[String]作为参数，结果类型为Unit,例如：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">object Test&#123;</span><br><span class="line">	def main(args: Array[String]): Unit = &#123;</span><br><span class="line">		...</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>注意：scala在每一个源码文件都隐式地引入了java.lang和scala包的成员，以及名为Predef的单例对象的所有成员。例如使用的println和assert都是来自于Predef</p>
</blockquote>
<ul>
<li>注意要运行这个程序，那么文件名需要与这个单例对象的名称相同。</li>
</ul>
<h3 id="App特质"><a href="#App特质" class="headerlink" title="App特质"></a>App特质</h3><ul>
<li>scala提供了一个特质scala.App，我们可以不用编写main方法，而是将打算放在main方法里的代码直接写在单例对象的花括号里。不过首先要在单例对象名后加上<code>extends App</code>。例子：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">object AppTest extends App&#123;</span><br><span class="line">	for(i &lt;- 0 to 10)&#123;</span><br><span class="line">		println(&quot;...&quot;)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
它可以像正常的scala程序一样运行</li>
</ul>
<h2 id="第五章"><a href="#第五章" class="headerlink" title="第五章"></a>第五章</h2><hr>
<h3 id="基础类型和操作"><a href="#基础类型和操作" class="headerlink" title="基础类型和操作"></a>基础类型和操作</h3><h4 id="一些基础类型"><a href="#一些基础类型" class="headerlink" title="一些基础类型"></a>一些基础类型</h4><ul>
<li><p>Byte、Short、Int、Long和Char类型统称为 <em>整数类型</em>，整数类型加上Float和Double类型统称为 <em>数值类型</em></p>
</li>
<li><p>String是java.lang包的成员</p>
</li>
<li><p>注意这些数据类型实际上都是封装好的类，所以首字母都是大写</p>
</li>
<li><p>Long类型以l或者L结尾</p>
</li>
<li><p>字符串插值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val name=&quot;reader&quot;</span><br><span class="line">println(s&quot;Hello, $name&quot;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>这个的结果与<code>println(&quot;Hello&quot; + name)</code>是一样的</p>
</li>
<li><p>scala中raw可以屏蔽转义字符<code>/</code>，例如<code>println(raw&quot;abc\n\n&quot;)</code>其中斜杠和n将被原样输出</p>
</li>
<li><p>scala中判断任意两个对象都可以用<code>==</code>方法，甚至可以是与null值进行比较而不会报错，并且总能返回正确的比较结果。scala的<code>==</code> <strong>只比较值</strong>，而Java的引用类型的&#x3D;&#x3D;比较的是引用是否相同。</p>
</li>
<li><p>赋值操作符<code>=</code>的优先级是最低的,而像<code>*=</code>这样的操作符也被当做赋值操作符，因此其优先级不如<code>+</code>操作符</p>
</li>
</ul>
<h2 id="第六章-函数式对象"><a href="#第六章-函数式对象" class="headerlink" title="第六章 函数式对象"></a>第六章 函数式对象</h2><hr>
<ul>
<li><p>如果要在类中重新实现一个已经实现的方法，例如toString方法，要用到override关键字</p>
</li>
<li><p>例如<code>override def toString = &#123;...&#125;</code></p>
</li>
<li><p>一个例子：创建一个有理数的类，这是个不可变的类并且要求分母不为零</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Rational(n: Int, d:Int)&#123;</span><br><span class="line">	require(d!=0)</span><br><span class="line">	override def toString = n + &quot;/&quot; + d</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>require方法接受一个boolean的参数。如果传入的参数为true，require将会正常返回。否则,require将会抛出IllegalArgumentException来阻止对象的构建。</p>
</li>
</ul>
<h3 id="添加字段"><a href="#添加字段" class="headerlink" title="添加字段"></a>添加字段</h3><ul>
<li><p>例如添加一个add方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def add(that: Rational): Rational = new(</span><br><span class="line">	new Rational(n * that.d + that.n * d, d * that.d)</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>这个方法是会报错的</p>
</li>
<li><p>上面那个类如果要添加一个add方法，实现两个Rational类型的值的相加，此时add是不能直接调用this.n或者this.d的(虽然类参数n和d在add方法中是在作用域内的，但是编译器并不允许使用<code>that.n</code>和<code>that.d</code>，因为that并非指向调用add的那个对象，要访问that的d和n，需要将它做成字段(相当于成员变量))<br>我们需要添加字段才行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Rational(n: Int, d:Int)&#123;</span><br><span class="line">	require(d != 0)</span><br><span class="line">	val numer: Int = n</span><br><span class="line">	val denom: Int = d</span><br><span class="line">	override def toString = numer + &quot;/&quot; + denom</span><br><span class="line">	def add(that: Rational): Rational =</span><br><span class="line">		new Rational(</span><br><span class="line">			numer * that.denom + that.numer * denom,</span><br><span class="line">			denom * that.denom		</span><br><span class="line">		)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>scala中 字段 默认的访问权限是public</p>
</li>
</ul>
<h3 id="自引用"><a href="#自引用" class="headerlink" title="自引用"></a>自引用</h3><ul>
<li><p>使用关键字this，写法为<code>this.字段名</code>，也可以直接写字段名，用法与C++和Java一致</p>
</li>
<li><p>辅助构造方法：<br><code>def this(...)&#123;...&#125;</code></p>
</li>
<li><p>操作符也可以像方法一样定义，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def + (that: MyClass): MyClass =&#123;...&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>scala支持方法重载，用法与C++和Java相同，通过定义多个参数不同的同名函数来实现重载</p>
</li>
</ul>
<h2 id="第七章-内建的控制结构"><a href="#第七章-内建的控制结构" class="headerlink" title="第七章 内建的控制结构"></a>第七章 内建的控制结构</h2><hr>
<ul>
<li><p>scala的控制语句都有返回值，可以直接拿来用</p>
</li>
<li><p>例子：<code>println(if (!args.isEmoty) args(0) else &quot;default.txt&quot;)</code></p>
</li>
<li><p>形如 <code>a &lt;- b</code>的称为生成器语法</p>
</li>
<li><p><code>1 to 4</code> 包含上界 4</p>
</li>
<li><p><code>1 until 4</code> 不包含上界 4</p>
</li>
</ul>
<h3 id="过滤"><a href="#过滤" class="headerlink" title="过滤"></a>过滤</h3><ul>
<li>有时候遍历集合的时候不想完整地遍历集合，而是先过滤成一个子集。这时可以给for表达式添加过滤器(filter)<br>例：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val filesHere = (new java.io.File(&quot;.&quot;)).listDiles</span><br><span class="line">for(file &lt;- filesHere if file.getName.endsWith(&quot;.scala&quot;))</span><br><span class="line">	println(file)</span><br></pre></td></tr></table></figure></li>
<li>过滤器就是for表达式圆括号中的一个if子句，并且可以随意添加更多的过滤器，直接添加if子句即可</li>
</ul>
<h3 id="嵌套迭代"><a href="#嵌套迭代" class="headerlink" title="嵌套迭代"></a>嵌套迭代</h3><ul>
<li>如果添加多个<code>&lt;-</code>子句，将得到嵌套的循环<br>例：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def fileLines(file: java.io.File) =</span><br><span class="line">	scala.io.Source.fromFile(file).getLines().toList</span><br><span class="line">def grep(pattern: String) =</span><br><span class="line">	for(</span><br><span class="line">		file &lt;- filesHere</span><br><span class="line">		if file.getName.endsWith(&quot;.scala&quot;);</span><br><span class="line">		trimed = line.trim</span><br><span class="line">		line &lt;- fileLines(file)</span><br><span class="line">		if trimed.matches(pattern)</span><br><span class="line">	)</span><br><span class="line">	println(file + &quot;: &quot; + line.trimed)</span><br><span class="line">grep(&quot;.*gcd.*&quot;)</span><br></pre></td></tr></table></figure></li>
<li>其中，外部循环遍历filesHere，内部循环遍历每个以.scala结尾的file的fileLines(file)</li>
<li>其中trimed作为中途变量绑定，用来保存line.trim的值，避免line.trim被重复计算两次</li>
</ul>
<h3 id="match表达式"><a href="#match表达式" class="headerlink" title="match表达式"></a>match表达式</h3><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val firstArg = if (args.length &gt; 0) args(0) else &quot;&quot;</span><br><span class="line">val friend =</span><br><span class="line">firstArg match &#123;</span><br><span class="line">	case &quot;salt&quot; =&gt; println(&quot;pepper&quot;)</span><br><span class="line">	case &quot;chips&quot; =&gt; println(&quot;salsa&quot;)</span><br><span class="line">	case &quot;eggs&quot; =&gt; println(&quot;bacon&quot;)</span><br><span class="line">	case _ =&gt;println(&quot;huh?&quot;)</span><br><span class="line">&#125;</span><br><span class="line">println(friend)</span><br></pre></td></tr></table></figure></li>
<li><p>这个例子中，match 表达式跟java的switch相比，有一些很重要的区别。其中一个区别是任何常量、字符串等都可以用作样例。另一个区别是，每个可选项后面没有break，因为scala中break是隐含的，不会出现某个可选项执行完又执行下一个的情况</p>
</li>
<li><p>match表达式会返回值，上例可以直接将匹配的结果打印出来</p>
</li>
<li><p>一般不会用到continue和break</p>
</li>
<li><p>例如一段java代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int i = 0;</span><br><span class="line">boolean foundIt = false;</span><br><span class="line">while (i &lt; args.length)&#123;</span><br><span class="line">	if(args[i].startsWith(&quot;-&quot;))&#123;</span><br><span class="line">		i = i + 1;</span><br><span class="line">		continue;</span><br><span class="line">	&#125;</span><br><span class="line">	if(args[i].endsWith(&quot;.scala&quot;))&#123;</span><br><span class="line">		foundIt = true;</span><br><span class="line">		break;</span><br><span class="line">	&#125;</span><br><span class="line">	i = i+ 1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>scala可以写成：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var i = 0</span><br><span class="line">var foundIt : Boolean = false</span><br><span class="line">while (i &lt; args.length &amp;&amp; !foundIt)&#123;</span><br><span class="line">	if(!args(i).startsWith(&quot;-&quot;))&#123;</span><br><span class="line">		if(args(i).endsWith(&quot;.scala&quot;)</span><br><span class="line">			foundIt = true</span><br><span class="line">	&#125;</span><br><span class="line">	i = i + 1</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>或者用递归函数代替循环：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def searchFrom(i: Int): Int = &#123;</span><br><span class="line">	if(i &gt;= args.length) -1		//未找到返回-1</span><br><span class="line">	else if(args(i).startsWith(&quot;-&quot;)) searchFrom(i + 1)	//递归查找下一处</span><br><span class="line">	else if(args(i).endsWith(&quot;.scala&quot;)) i	//找到了就返回</span><br><span class="line">	else searchFrom(i + 1)</span><br><span class="line">&#125;</span><br><span class="line">val i = searchFrom(0)</span><br></pre></td></tr></table></figure></li>
<li><p>这个递归去掉了循环，每一个continue都换成了一次以i+1为入参的递归调用</p>
</li>
</ul>
<h2 id="第八章-函数和闭包"><a href="#第八章-函数和闭包" class="headerlink" title="第八章 函数和闭包"></a>第八章 函数和闭包</h2><hr>
<h3 id="局部函数"><a href="#局部函数" class="headerlink" title="局部函数"></a>局部函数</h3><ul>
<li>我们可以在某个函数内部定义函数，就像局部变量一样，这样局部函数只在包含他的代码块中可见，实现了与类中私有方法相同的效果。<br>例：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def processFile(filename: String, width: Int) = &#123;</span><br><span class="line">	def processLine(line: String) = &#123; //局部函数可以访问包含它们的函数的参数</span><br><span class="line">		if(line.length &gt; width)</span><br><span class="line">			println(filename + &quot;: &quot; + line.trim)</span><br><span class="line">	&#125;</span><br><span class="line">	val source = Source.fromFile(filename) //从文件名创建一个名为source的Source对象</span><br><span class="line">	for (line &lt;- source.getLines())&#123; //getLines()返回一个每次迭代从文件读取一行并去掉换行符的迭代器</span><br><span class="line">		processLine(filename, width, line)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="一等函数"><a href="#一等函数" class="headerlink" title="一等函数*"></a>一等函数*</h3><ul>
<li><p>函数字面量被编译成类，并在运行时实例化为函数值。因此，函数字面量和函数值的区别在于，函数字面量存在于，函数字面量存在于源码，而函数值以对象的形式存在于运行时</p>
</li>
<li><p>一个函数字面量的简单示例：<code>(x: Int) =&gt; x + 1</code></p>
</li>
<li><p><code>=&gt;</code>表示该函数将左侧的内容转换成右侧的内容，因此这是一个将任何整数x <strong>映射</strong> 成 (x + 1)的函数</p>
</li>
<li><p>函数值是对象，所以还可以将他们存放在变量中。同时他们也是函数，也可以用常规的圆括号来调用他们：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var increase = (x: Int) =&gt; x + 1 //可以将函数字面量赋值给变量</span><br><span class="line">val n = increase(10)  //可以像一般的函数一样调用</span><br><span class="line">increase = (x: Int) =&gt; x + 999 //因为increase是var类型，还可以重新赋值</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果在函数字面量中有多于一条语句，可以将函数体用花括号括起来</p>
</li>
<li><p>所有的集合类(List,Set,Array,Map)都提供了foreach方法，<strong>它接受一个函数作为入参，并对它的每个元素调用这个函数</strong></p>
</li>
<li><p>同时，集合类还有个filter方法。这个方法从集合中选出那些满足条件的元素。这个指定条件由函数表示,例如：<code>(x: Int) =&gt; x &gt; 0</code>这个方法可以被用来过滤。 <strong>这个函数将所有的正整数映射成true，所有其他的整数映射成false</strong></p>
</li>
</ul>
<h3 id="函数字面量的简写形式"><a href="#函数字面量的简写形式" class="headerlink" title="函数字面量的简写形式"></a>函数字面量的简写形式</h3><ul>
<li><p>一种更为简要的方法是省去参数类型声明：<code>someNumbers.filter((x) =&gt; x &gt; 0)</code></p>
</li>
<li><p>同时圆括号也可以去掉，因为可以省略掉自动推断类型的参数的圆括号：<code>someNumbers.filter(x =&gt; x &gt; 0)</code></p>
</li>
</ul>
<h3 id="占位符语法"><a href="#占位符语法" class="headerlink" title="占位符语法"></a>占位符语法</h3><ul>
<li><p>为了让函数字面量更加精简，还可以使用下划线作为占位符，用来表示一个或多个参数，只要满足每个参数只在函数字面量中出现一次即可。例如：<code>_ &gt; 0</code> 是一个非常短的表示法，表示一个检查某个值是否大于0的函数</p>
</li>
<li><p>例子：<code>someNumber.filter(_ &gt; 0)</code></p>
</li>
<li><p>可以将下划线当成是表达式中需要被”填”的”空”。函数每次被调用，这个空都会被一个入参给填上</p>
</li>
<li><p>如果someNumber被初始化为<code>List(11,10,9,8)</code>，那么filter方法首先把 <code>_ &gt; 0</code>中的空替换成11，即 11 &gt; 0，然后替换成10，以此类推，直到List末尾</p>
</li>
<li><p>有时候当你用下划线作为参数占位时，编译器可能没有足够的信息来推断缺失的参数类型。</p>
</li>
<li><p>例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; val f = _ + _</span><br><span class="line">&lt;console&gt; error: missing parameter type for expanded</span><br><span class="line">function ((x$1, x$2) =&gt; x$1.$plus(x$2))</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
</li>
<li><p>这种情况下，可以用冒号来给出类型，就像这样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; val f = (_: Int) + (_: Int)</span><br><span class="line">f: (Int, Int) =&gt; Int = &lt;function0&gt;</span><br><span class="line">scala&gt; f(5,10)</span><br><span class="line">res0: Int = 15</span><br></pre></td></tr></table></figure>
</li>
<li><p>注意，<code>_ + _</code>将会展开成一个接收两个参数的函数字面量。这就是为什么只有当每个参数在函数字面量中出现不多不少正好一次的时候才能使用这样的精简写法。多个下划线意味着多个参数，而不是对单个参数的重复利用。</p>
</li>
</ul>
<h3 id="部分应用的函数"><a href="#部分应用的函数" class="headerlink" title="部分应用的函数"></a>部分应用的函数</h3><ul>
<li><p>下划线还能替换掉整个参数列表，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt;def sum(a:Int, b:Int, c:Int) = a + b + c</span><br><span class="line">sum: (a:Int, b:Int, c:Int)Int</span><br><span class="line">scala&gt;val a = sum _</span><br><span class="line">a:(Int, Int, Int) =&gt; Int = &lt;function3&gt;</span><br><span class="line">scala&gt; a(1,2,3)</span><br><span class="line">res0: Int = 6</span><br></pre></td></tr></table></figure>
</li>
<li><p>后面的a 即是基于sum创建的一个部分应用函数</p>
</li>
</ul>
<h3 id="特殊的函数调用形式"><a href="#特殊的函数调用形式" class="headerlink" title="特殊的函数调用形式"></a>特殊的函数调用形式</h3><h4 id="重复参数"><a href="#重复参数" class="headerlink" title="重复参数"></a>重复参数</h4><ul>
<li><p>scala允许你标示出的参数的最后一个参数可以被重复，这样我们可以对函数传入一个可变长度的参数列表。这样一个重复参数的表示，需要在参数的类型之后加上一个<code>*</code>号：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; def echo(args: String) =</span><br><span class="line">	for (arg &lt;- args) println(arg)</span><br><span class="line">echo: (args: String*)Unit</span><br></pre></td></tr></table></figure>
<p>这样echo就可以接受任意多个String类型的参数(可以是0个)</p>
</li>
<li><p>但是如果要将一个适合类型的数组以这种重复参数的形式传入的时候，需要写成如下形式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scala&gt; var arr = Array(&quot;a&quot;,&quot;v&quot;,&quot;gf&quot;)</span><br><span class="line">arr:Array[String] = Array(a,v,gf)</span><br><span class="line">scala&gt; echo(arr: _*)</span><br><span class="line">a</span><br><span class="line">v</span><br><span class="line">gf</span><br></pre></td></tr></table></figure>
<p>这种表示法告诉编译器将arr的每个元素作为参数传给echo，而不是将所有元素放在一起作为单个实参传入</p>
</li>
</ul>
<h4 id="尾递归"><a href="#尾递归" class="headerlink" title="尾递归"></a>尾递归</h4><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def approximate(guess: Double): Double =</span><br><span class="line">	if (isGoodEnough(guess)) guess</span><br><span class="line">	else approximate(improve(guess))</span><br></pre></td></tr></table></figure>
<p>在合适的isGoodEnough和improve函数的实现下，这个函数就是一个递归函数。而这个递归调用在最后一步的函数称为尾递归(tail recursive)函数</p>
</li>
<li><p>scala编译器对尾递归函数有特殊的优化，能够检测到尾递归并将他替换为跳转到函数的最开始，并在跳转之前更新参数的值</p>
</li>
<li><p>也就是说，我们完全可以用尾递归的方法来代替循环而不用付出任何额外的开销</p>
</li>
<li><p>尾递归不会在每次调用时构建一个新的栈，所有的调用都会在同一个栈中进行</p>
</li>
<li><p>如下有个例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def boom(x: Int): Int =</span><br><span class="line">	if(x == 0) throw new Exception (&quot;boom!&quot;)</span><br><span class="line">	else boom(x - 1) + 1</span><br></pre></td></tr></table></figure>
<p>这个函数不是一个尾递归的，因为 <strong>它在递归调用之后还执行了一个递增操作</strong></p>
</li>
<li><p>但是scala中尾递归是受限的，更高级形式的尾递归实现十分困难。scala只能对那些直接尾递归的函数做优化</p>
</li>
</ul>
<h2 id="第九章-控制抽象"><a href="#第九章-控制抽象" class="headerlink" title="第九章 控制抽象"></a>第九章 控制抽象</h2><hr>
<h3 id="减少代码重复"><a href="#减少代码重复" class="headerlink" title="减少代码重复"></a>减少代码重复</h3><ul>
<li><p>例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def filesMatching(query: String,</span><br><span class="line">	matcher: (String, String) =&gt; Boolean) = &#123;</span><br><span class="line">	for (file &lt;- filesHere; if matcher(file.getName, query))</span><br><span class="line">		yield file</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>在这个方法中，if子句用matcher来检查文件名是否满足条件，这个检查具体做什么，取决于具体的matcher。matcher本身是一个函数，因此类型声明中有一个<code>=&gt;</code>符号。这个函数接受两个字符串类型的参数(分别是文件名和查询条件)，返回一个布尔值，因此这个参数的类型为<code>(String, String) =&gt; Boolean</code></p>
</li>
<li><p>下面三个方法分别使用三种不同的参数matcher来实现不同的文件查找功能:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def filesEnding(query: String) =</span><br><span class="line">	filesMatching(query, _.endsWith(_))</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def filesContaning(query: String) =</span><br><span class="line">	filesMatching(query, _.contains(_))</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def filesRegex(query: String) =</span><br><span class="line">	filesMatching(query, _.matches(_))</span><br></pre></td></tr></table></figure></li>
<li><p>其中，<code>_.endsWith(_)</code>的含义与下面的代码是一样的<code>(fileName: String, query: String) =&gt; fileName.endsWith(query)</code> 两个下划线按顺序作为传入的两个参数的占位符</p>
</li>
</ul>
<br>
> 最后更新于2018.7.23
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>UEFI双系统的那些坑</title>
    <url>/2018/04/06/ubuntu-and-windows/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的自我备忘笔记  </p>
</blockquote>
<h2 id="关于ubuntu-和-windows-双系统"><a href="#关于ubuntu-和-windows-双系统" class="headerlink" title="关于ubuntu 和 windows 双系统"></a>关于ubuntu 和 windows 双系统</h2><hr>
<ul>
<li><p>在长时间使用Windows时候，难免会想要尝试新系统，而专业又是如此，于是我选择了linux中图形界面最为友好的ubuntu系统作为自己双系统的入门系统。</p>
</li>
<li><p>虽然网上的双系统教程多说的很是简单，但是大多数已经是几年前的文章了，其中所讲的一些计算机的硬件系统已经落伍了，就比如说，网上绝大多数的双系统引导模式是建立在bios支持legacy（传统模式）模式下的，然而我自己的bios只能在UEFI模式下引导系统启动…..作为一名计算机系的学生当然不服气啊，当然各种百度什么的，期间踩过的坑实在难为外人道也。</p>
</li>
<li><p>如果你的电脑支持legacy模式的话，自行百度一些教程就行了。装好ubuntu系统之后，进入bios将启动顺序整下，将Ubuntu启动器放在Windows boot manager前面就可以了。因为Ubuntu启动器可以调用Windows boot manager 而Windows不能启动Ubuntu。</p>
</li>
<li><p>但是，UEFI作为最新的计算机启动模式，肯定要比原来的更加快速，或者如果你也像我一样，电脑不支持legacy启动，那么下面将要介绍一个非常好用的第三方启动引导程序。</p>
</li>
</ul>
<h2 id="第三方启动引导程序——refind"><a href="#第三方启动引导程序——refind" class="headerlink" title="第三方启动引导程序——refind"></a>第三方启动引导程序——refind</h2><hr>
<ul>
<li><p>refind是一款功能非常强大的开机启动引导程序，它能够自定义开机界面。你可以吧系统图标，背景什么的换成你自己喜欢的风格。这里附上一张我采用的主题开机界面<img src="/images/linux-and-windows/opening-bg.jpg">如果你也喜欢这一风格，<a href="https://github.com/EvanPurkhiser/rEFInd-minimal">这里是链接地址</a>。</p>
</li>
<li><p>refind是一款免费的软件(<a href="http://www.rodsbooks.com/refind/getting.html">官方下载地址</a>)，如果你高兴也可以给作者赞助。</p>
</li>
<li><p>如果使用UEFI引导方式启动系统的话，<strong>在安装Ubuntu时要预留一个efi分区用来存放开机引导文件</strong>，预留大概200MB-500MB即可。</p>
</li>
</ul>
<h3 id="Ubuntu下安装"><a href="#Ubuntu下安装" class="headerlink" title="Ubuntu下安装"></a>Ubuntu下安装</h3><ul>
<li>推荐在ubuntu下安装refind，简单到让人不敢相信。你只需要把软件下载下来，在终端运行里面的.shell文件，就会自动安装，无需任何额外的操作，重启系统，进入bios在启动顺序里将rEFInd调为第一个即可。</li>
</ul>
<h3 id="Windows下安装"><a href="#Windows下安装" class="headerlink" title="Windows下安装"></a>Windows下安装</h3><ul>
<li><p>如果你非要不信邪，在Windows下尝试安装的话，我也不反对。首先你需要一个能够进入系统隐藏分区的软件——<a href="http://www.diskgenius.cn/">DiskGenius</a>。推荐去官网下载正版而不要用盗版（然后去百度激活码…咳咳），毕竟牵涉到系统文件万一崩了别怪我没提醒。</p>
</li>
<li><p>第一步，找到你系统的esp分区<img src="/img/linux-and-windows/ubuntu-boot.jpg"></p>
</li>
<li><p>第二步，将下载的refind文件夹及其里面所有的东西全部复制进去。<em>（注意：虽然DiskGenius支持同时复制多个文件，但是文件夹没法复制，只能自己先手动创建）</em> <strong>同时必须要自己创建一个refind.conf文件，可以什么都不写但是不能没有。</strong> 然后原来的refind.conf-sample文件就可以删除了。<img src="/img/linux-and-windows/refind-info.jpg"></p>
</li>
<li><p>第三步，将一些不需要的文件删除，如果你在网上下载了一些主题的话，也一并把文件复制进去。</p>
</li>
<li><p>第四步，下载<a href="https://bootice.en.softonic.com/?ex=REG-60.2">bootice</a>，打开DIskGenius给你放refind文件的那个分区取个卷标（随便什么都可以，下面有用）。打开bootice——UEFI——修改启动序列<img src="/img/linux-and-windows/bootice01.jpg"><em>(也可以顺手吧下面那个下次启动直接进入bios勾上，省的下次进bios要手动)<em>。点击添加，找到你之前分配盘符的那个盘（也就是esp分区），找到里面的refind文件夹，选择对应你系统的.efi文件，点击确定。<img src="/img/linux-and-windows/bootice02.jpg">重启系统，进入bios，将rEFInd启动项调整为第一个即可。</em>（没有对比就没有伤害，Windows下简直烦死，还不一定成功…）</em></p>
</li>
<li><p>最后希望各位都能成功在UEFI模式下完成双系统的安装和引导。</p>
</li>
</ul>
<h2 id="双系统时间差的问题解决"><a href="#双系统时间差的问题解决" class="headerlink" title="双系统时间差的问题解决"></a>双系统时间差的问题解决</h2><hr>
<ul>
<li><p>这个问题出现在切换系统时，每次时间都会错误到0时区。</p>
</li>
<li><p>解决方法：Ubuntu下关闭UTC(世界协调时间):</p>
</li>
</ul>
<ol>
<li><p><code>sudo vim /etc/default/rcS</code></p>
</li>
<li><p>找到<code>UTC=yes</code>，改为<code>UTC=no</code>即可</p>
</li>
</ol>
<ul>
<li><p><a href="https://blog.csdn.net/gatieme/article/details/51883981">详细原因</a></p>
</li>
<li><p><strong>但是注意</strong>：在 Ubuntu <strong>16.04</strong> 版本以前，关闭UTC的方法是编辑<code>/etc/default/rcS</code>，将<code>UTC=yes</code>改成<code>UTC=no</code>， 但在Ubuntu 16.04使用systemd启动之后，时间改成了由timedatectl来管理，所以更改方法是:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">timedatectl set-local-rtc 1 --adjust-system-clock</span><br></pre></td></tr></table></figure></li>
<li><p>之后重启应该就不会出现问题了。</p>
</li>
</ul>
<br>

<blockquote>
<p>最后更新于2018.10.15</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Ubuntu</tag>
        <tag>๑Linux</tag>
        <tag>๑DualSystem</tag>
      </tags>
  </entry>
  <entry>
    <title>VScode python编译错误问题记录</title>
    <url>/2019/03/05/vscode-python/</url>
    <content><![CDATA[<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><hr>
<ul>
<li><p>在Vscode上装完python环境后,在按F5运行后却报错了:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">ImportError: No module named &#x27;_tkinter&#x27;</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
</li>
<li><p>python版本为3.6, pip版本为18</p>
</li>
<li><p>查阅了网上相关资料后发现是缺少包的原因,在终端中手动下载一下即可:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install python3-tk</span><br></pre></td></tr></table></figure></li>
<li><p>然后再运行python程序即可.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑Python</tag>
        <tag>๑VsCode</tag>
      </tags>
  </entry>
  <entry>
    <title>Tornado建站踩坑记录</title>
    <url>/2019/03/10/tornado-bug/</url>
    <content><![CDATA[<h2 id="直入主题"><a href="#直入主题" class="headerlink" title="直入主题"></a>直入主题</h2><hr>
<ul>
<li><p>被一个bug搞了一下午: 服务启动后localhost始终加载不出静态文件(诸如CSS,JS脚本,图片什么的).</p>
</li>
<li><p>仔细校验了文件中的路径,绝对路径和相对路径都试了,都是没用.</p>
</li>
</ul>
<h2 id="问题解决"><a href="#问题解决" class="headerlink" title="问题解决"></a>问题解决</h2><hr>
<ul>
<li><p><strong>如果有教程推荐你建的文件目录里有<code>statics</code>,相信我,你如果有机会见到它一定会砍死它</strong></p>
</li>
<li><p>错误的文件结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/.</span><br><span class="line">|</span><br><span class="line">handlers      //后端 Python 程序，主要处理来自前端的请求，并且操作数据库</span><br><span class="line">|</span><br><span class="line">templates   //存放html文件包括模板html文件</span><br><span class="line">|</span><br><span class="line">statics   //存放静态文件的目录</span><br><span class="line">|</span><br><span class="line">methods   //存放通用方法的目录,主要是供给handlers里面的程序调用</span><br><span class="line">|</span><br><span class="line">XXX.py    //python程序</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<blockquote>
<p>如上的目录文件,你就算写死了,网站也不会加载出你的静态文件的</p>
</blockquote>
</li>
<li><p><strong>正确的目录结构</strong>:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/.</span><br><span class="line">|</span><br><span class="line">handlers      //后端 Python 程序，主要处理来自前端的请求，并且操作数据库</span><br><span class="line">|</span><br><span class="line">templates   //存放html文件包括模板html文件</span><br><span class="line">|</span><br><span class="line">static      //存放静态文件的目录</span><br><span class="line">|</span><br><span class="line">methods   //存放通用方法的目录,主要是供给handlers里面的程序调用</span><br><span class="line">|</span><br><span class="line">XXX.py    //python程序</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<blockquote>
<p>咋一看是不是一样啊,再仔细看看,static是没有s的.</p>
</blockquote>
</li>
<li><p><em>只要你敢加上这个s,你就走远了…</em></p>
</li>
</ul>
<h3 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h3><hr>
<ul>
<li><p>这里感谢这位博主的文章,最终问题得以解决 <a href="https://blog.csdn.net/uestc_v/article/details/83152390">tornado 静态文件路径绑定细节</a></p>
</li>
<li><p>人生苦短, 我用python~</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑FrontEnd</tag>
        <tag>๑Python</tag>
        <tag>๑Tornado</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu18 VsCode搭建C++编译环境(已测试)</title>
    <url>/2018/11/21/vscode-cpp/</url>
    <content><![CDATA[<blockquote>
<p>坑了一早上,终于摸索出两个编译运行C++的方法,特此记录.</p>
</blockquote>
<h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><hr>
<ul>
<li>安装VsCode和g++环境这里就不赀述了</li>
</ul>
<h2 id="安装C-插件"><a href="#安装C-插件" class="headerlink" title="安装C++插件"></a>安装C++插件</h2><hr>
<ul>
<li>安装一系列C++的插件,我安装的有:<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C/C++</span><br><span class="line">C/C++ Intellisense</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="重点"><a href="#重点" class="headerlink" title="重点"></a>重点</h2><hr>
<h3 id="两种方法编译C-文件"><a href="#两种方法编译C-文件" class="headerlink" title="两种方法编译C++文件"></a>两种方法编译C++文件</h3><h4 id="第一种-安装C-C-Compile-Run插件-简单-推荐"><a href="#第一种-安装C-C-Compile-Run插件-简单-推荐" class="headerlink" title="第一种:安装C&#x2F;C++ Compile Run插件(简单,推荐)"></a>第一种:安装C&#x2F;C++ Compile Run插件(简单,推荐)</h4><ul>
<li><p>人生苦短,何必浪费时间在环境搭建上~</p>
</li>
<li><p>虽然只支持单文件(single file)的编译和运行,但是日常使用足够了啊</p>
</li>
<li><p>打开C++文件F6即可运行</p>
</li>
<li><p>官方使用手册:</p>
<blockquote>
<p>Requirements<br>If you are on linux you must install gcc<br>If you are on window you must install mingw<br>How to use<br>Make sure you have .c or .cpp file open and press “F6”, this will compile the file. If you want to register gcc&#x2F;g++ path manually you can set it under settings. You can also set to save file before compiling.</p>
</blockquote>
</li>
</ul>
<h4 id="第二种-手动添加task-json和launch-json"><a href="#第二种-手动添加task-json和launch-json" class="headerlink" title="第二种:手动添加task.json和launch.json"></a>第二种:手动添加task.json和launch.json</h4><ul>
<li><p>打开C++文件,切换到Debug下,运行一次,他会创建一个默认文件,改成如下即可:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;version&quot;: &quot;0.2.0&quot;,</span><br><span class="line">    &quot;configurations&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;name&quot;: &quot;(gdb) Launch&quot;,</span><br><span class="line">            &quot;type&quot;: &quot;cppdbg&quot;,</span><br><span class="line">            &quot;request&quot;: &quot;launch&quot;,</span><br><span class="line">            &quot;program&quot;: &quot;$&#123;workspaceRoot&#125;/$&#123;fileBasenameNoExtension&#125;.o&quot;,</span><br><span class="line">            &quot;args&quot;: [],</span><br><span class="line">            &quot;stopAtEntry&quot;: false,</span><br><span class="line">            &quot;cwd&quot;: &quot;$&#123;workspaceRoot&#125;&quot;,</span><br><span class="line">            &quot;environment&quot;: [],</span><br><span class="line">            &quot;externalConsole&quot;: true,</span><br><span class="line">            &quot;preLaunchTask&quot;: &quot;build&quot;,  </span><br><span class="line">            &quot;MIMode&quot;: &quot;gdb&quot;,</span><br><span class="line">            &quot;setupCommands&quot;: [</span><br><span class="line">                &#123;</span><br><span class="line">                    &quot;description&quot;: &quot;Enable pretty-printing for gdb&quot;,</span><br><span class="line">                    &quot;text&quot;: &quot;-enable-pretty-printing&quot;,</span><br><span class="line">                    &quot;ignoreFailures&quot;: true</span><br><span class="line">                &#125;</span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后切回C++文件,<code>Ctrl + Shift + B</code> build &gt;&gt; 选择other新建一个 &gt;&gt; 修改tasks.json如下:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;  </span><br><span class="line">    &quot;version&quot;: &quot;0.1.0&quot;,  </span><br><span class="line">    &quot;showOutput&quot;: &quot;always&quot;,  </span><br><span class="line">    &quot;tasks&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;taskName&quot;: &quot;build&quot;,</span><br><span class="line">            &quot;command&quot;: &quot;g++&quot;,</span><br><span class="line">            &quot;isShellCommand&quot;: true,</span><br><span class="line">            &quot;showOutput&quot;: &quot;always&quot;,</span><br><span class="line">            &quot;args&quot;: [</span><br><span class="line">                &quot;-g&quot;,</span><br><span class="line">                &quot;$&#123;file&#125;&quot;,</span><br><span class="line">                &quot;-o&quot;,</span><br><span class="line">                &quot;$&#123;workspaceRoot&#125;/$&#123;fileBasenameNoExtension&#125;.o&quot;</span><br><span class="line">            ],</span><br><span class="line">            &quot;problemMatcher&quot;: [</span><br><span class="line">                &quot;$g++&quot;</span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    ]  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><ul>
<li><p><code>Ctrl + Shift + B</code> build</p>
</li>
<li><p>点击debug下的绿色箭头即可运行</p>
</li>
</ul>
<blockquote>
<p>愿天下再没有配环境的坑~</p>
</blockquote>
]]></content>
      <categories>
        <category>Computer Programs</category>
      </categories>
      <tags>
        <tag>๑C++</tag>
        <tag>๑Ubuntu</tag>
        <tag>๑VsCode</tag>
      </tags>
  </entry>
  <entry>
    <title>网页状态码记录</title>
    <url>/2018/11/07/web-learning-01/</url>
    <content><![CDATA[<h2 id="HTTP状态码"><a href="#HTTP状态码" class="headerlink" title="HTTP状态码"></a>HTTP状态码</h2><hr>
<ul>
<li>五个不同的类别:<br>　　1. <code>1XX</code>临时&#x2F;信息响应<br>　　2. <code>2XX</code>成功<br>　　3. <code>3XX</code>重定向<br>　　4. <code>4XX</code>客户端&#x2F;请求错误<br>　　5. <code>5XX</code>服务器错误<blockquote>
<p>五个类别的响应状态代码的第一个数字是唯一代表。</p>
</blockquote>
</li>
</ul>
<h3 id="4XX-服务器外部错误"><a href="#4XX-服务器外部错误" class="headerlink" title="4XX 服务器外部错误"></a>4XX 服务器外部错误</h3><h4 id="400"><a href="#400" class="headerlink" title="400"></a>400</h4><ul>
<li>400 无法解析此请求。说明正在搜索的网页可能已经删除、更名或暂时不可用。</li>
</ul>
<h4 id="401-X"><a href="#401-X" class="headerlink" title="401.X"></a>401.X</h4><ul>
<li><p>401.1 未经授权：访问由于凭据无效被拒绝。说明没有权限查看该目录或网页。</p>
</li>
<li><p>401.2 未经授权: 访问由于服务器配置倾向使用替代身份验证方法而被拒绝。<br><code>由于服务器配置问题而导致登陆失败，由于服务器端脚本未能正确发送 WWW 身份验证头文件字段。如果要通过 Active Server Pages 脚本完成此项任务，可以使用&quot;Response&quot;对象的&quot;AddHeader&quot;方法来要求客户端用特定身份验证方法访问资源。</code></p>
</li>
<li><p>401.3 未经授权：访问由于 ACL 对所请求资源的设置被拒绝。</p>
</li>
<li><p>401.4 未经授权：Web 服务器上安装的筛选器授权失败。<br><code>如果Web 服务器安装了筛选器程序以检查连接到服务器的用户。该筛选器程序能够禁止通过连接到服务器的身份验证来访问资源。</code></p>
</li>
<li><p>401.5 未经授权：ISAPI&#x2F;CGI 应用程序授权失败。<br><code>由于 ISAPI/CGI 应用程序导致授权失败。如果所要访问的 Web 服务器地址上安装了 ISAPI 或 CGI 程序用于在继续执行之前检验用户证书。该程序能够禁止通过连接到服务器的身份验证证书来访问资源。</code></p>
</li>
<li><p>401.7 未经授权：由于 Web 服务器上的 URL 授权策略而拒绝访问。</p>
</li>
</ul>
<h4 id="403-X"><a href="#403-X" class="headerlink" title="403.X"></a>403.X</h4><ul>
<li><p>403 禁止访问：访问被拒绝。</p>
</li>
<li><p>403.1 禁止访问：执行访问被拒绝。<br><code>由于&quot;执行&quot;访问被禁止而造成的，若试图从目录中执行 CGI、ISAPI 或其他可执行程序，但该目录不允许执行程序时便会出现此种错误。</code></p>
</li>
<li><p>403.2 禁止访问：读取访问被拒绝。<br><code>导致此错误是由于没有可用的默认网页并且没有对目录启用目录浏览，或者要显示的 HTML 网页所驻留的目录仅标记为&quot;可执行&quot;或&quot;脚本&quot;权限。</code></p>
</li>
<li><p>403.3 禁止访问：写入访问被拒绝。<br><code>当试图将文件上载到目录或在目录中修改文件，但该目录不允许&quot;写&quot;访问时就会出现此种错误。</code></p>
</li>
<li><p>403.4 禁止访问：需要使用 SSL 查看该资源。<br><code>由于要求SSL而造成的，您必须在要查看的网页的地址中使用&quot;https&quot;。</code></p>
</li>
<li><p>403.5 禁止访问：需要使用 SSL 128 查看该资源。<br><code>由于要求使用 128 位加密算法的 Web 浏览器而造成的，如果您的浏览器不支持128位加密算法就会出现这个错误，您可以连接微软网站进行浏览器升级。</code></p>
</li>
<li><p>403.6 禁止访问：客户端的 IP 地址被拒绝。<br><code>如果服务器中有不能访问该站点的 IP 地址列表，并且您使用的 IP 地址在该列表中时您就会返回这条错误信息。</code></p>
</li>
<li><p>403.7 禁止访问：需要 SSL 客户端证书。</p>
</li>
<li><p>403.8 禁止访问：客户端的 DNS 名称被拒绝。<br><code>由于禁止站点访问而造成的，若服务器中有不能访问该站点的 DNS 名称列表，而您使用的 DNS 名称在列表中时就会返回此种信息。请注意区别403.6与403.8错误。</code></p>
</li>
<li><p>403.9 禁止访问：太多客户端试图连接到 Web 服务器。<br><code>由于连接的用户过多而造成的，由于Web 服务器很忙，因通讯量过多而无法处理请求时便会返回这条错误。</code></p>
</li>
<li><p>403.10 禁止访问：Web 服务器配置为拒绝执行访问。<br><code>由于无效配置而导致的错误，当您试图从目录中执行 CGI、ISAPI 或其他可执行程序，但该目录不允许执行程序时便会返回这条错误。</code></p>
</li>
<li><p>403.11 禁止访问：密码已更改。</p>
</li>
<li><p>403.12 禁止访问：服务器证书映射器拒绝了客户端证书访问。<br><code>由于映射器拒绝访问而造成的。若要查看的网页要求使用有效的客户证书，而您的客户证书映射没有权限访问该 Web 站点时就会返回映射器拒绝访问的错误。</code></p>
</li>
<li><p>403.13 禁止访问：客户端证书已在 Web 服务器上吊销。<br><code>由于需要查看的网页要求使用有效的客户证书而使用的客户证书已经被吊销，或者无法确定证书是否已吊销造成的。</code></p>
</li>
<li><p>403.14 禁止访问：在 Web 服务器上已拒绝目录列表。</p>
</li>
<li><p>403.15 禁止访问：Web 服务器已超过客户端访问许可证限制。</p>
</li>
<li><p>403.16 禁止访问：客户端证书格式错误或未被 Web 服务器信任。</p>
</li>
<li><p>403.17 禁止访问：客户端证书已经到期或者尚未生效。</p>
</li>
<li><p>403.18 禁止访问：无法在当前应用程序池中执行请求的 URL。</p>
</li>
<li><p>403.19 禁止访问：无法在该应用程序池中为客户端执行 CGI。</p>
</li>
<li><p>403.20 禁止访问：Passport 登录失败。</p>
</li>
</ul>
<h4 id="404-X"><a href="#404-X" class="headerlink" title="404.X"></a>404.X</h4><ul>
<li><p>404 找不到文件或目录。<br><code>由于无法找到文件而造成的，通常是由于正在搜索的网页可能已经删除、更名或暂时不可用。</code></p>
</li>
<li><p>404.1 文件或目录未找到：网站无法在所请求的端口访问。<br><code>表明所访问 Web 站点的 IP 地址不接受对端口（请求的来源端口）的请求。404.1 错误只会出现在具有多个 IP 地址的计算机上。如果在特定 IP 地址/端口组合上收到客户端请求，而且没有将 IP 地址配置为在该特定的端口上侦听，则 IIS 返回 404.1 HTTP 错误。例如，如果一台计算机有两个 IP 地址，而只将其中一个 IP 地址配置为在端口 80 上侦听，则另一个 IP 地址从端口 80 收到的任何请求都将导致 IIS 返回 404.1 错误。只应在此服务级别设置该错误，因为只有当服务器上使用多个 IP 地址时才会将它返回给客户端。</code></p>
</li>
<li><p>404.2 文件或目录无法找到：锁定策略禁止该请求。</p>
</li>
<li><p>404.3 文件或目录无法找到：MIME 映射策略禁止该请求。</p>
</li>
</ul>
<h4 id="else"><a href="#else" class="headerlink" title="else"></a>else</h4><ul>
<li><p>405 用于访问该页的 HTTP 动作未被许可。<br><code>由于资源被禁止而导致的网页地址不正确，因此要寻找的网页无法显示。</code></p>
</li>
<li><p>406 客户端浏览器不接受所请求页面的 MIME 类型。<code>由于浏览器无法打开正在寻找的资源而导致的错误。</code></p>
</li>
<li><p>407 Web 服务器需要初始的代理验证。</p>
</li>
<li><p>410 要寻找的网页已被永久删除而导致的，这意味着资源永远无法使用。</p>
</li>
<li><p>412 客户端设置的前提条件在 Web 服务器上评估时失败。<br><code>412错误是由于要查看的网页设置有先决条件，因此该请求无法完成。一般是网页中有一个或多个请求标题字段中具有先决条件，这些字段经服务器测试后被认为是&quot;FALSE&quot;。客户端为当前资源的 meta 信息（头文件字段数据）设置了先决条件，以便防止请求的方法被用于指定资源外的其他资源。</code></p>
</li>
<li><p>414 请求 URL 太大，因此在 Web 服务器上不接受该 URL。<br><code>一般的可能性有： 1）客户端错误地将 POST 请求转换为带有长查询信息的 GET 请求。 2）或者是客户端遇到重定向问题（例如，重定向 URL 的前缀指向自身的后缀）。 3）服务器遭到客户端的攻击，该客户端试图利用那些使用定长缓冲来读取或控制请求 URI 的服务器上的安全漏洞。</code></p>
</li>
</ul>
<h3 id="5XX-服务器内部错误"><a href="#5XX-服务器内部错误" class="headerlink" title="5XX 服务器内部错误"></a>5XX 服务器内部错误</h3><ul>
<li><p>500 服务器内部错误。</p>
</li>
<li><p>500.11 服务器错误：Web 服务器上的应用程序正在关闭。</p>
</li>
<li><p>500.12 服务器错误：Web 服务器上的应用程序正在重新启动。</p>
</li>
<li><p>500.13 服务器错误：Web 服务器太忙。</p>
</li>
<li><p>500.14 服务器错误：服务器上的无效应用程序配置。</p>
</li>
<li><p>500.15 服务器错误：不允许直接请求 GLOBAL.ASA。</p>
</li>
<li><p>500.16 服务器错误：UNC 授权凭据不正确。</p>
</li>
<li><p>500.17 服务器错误：URL 授权存储无法找到。</p>
</li>
<li><p>500.18 服务器错误：URL 授权存储无法打开。</p>
</li>
<li><p>500.19 服务器错误：该文件的数据在配置数据库中配置不正确。</p>
</li>
<li><p>500.20 服务器错误：URL 授权域无法找到。</p>
</li>
<li><p>500-100.asp 内部服务器错误：ASP 错误。</p>
</li>
<li><p>501 标题值指定的配置没有执行。</p>
</li>
<li><p>502 Web 服务器作为网关或代理服务器时收到无效的响应。<br><code>由于网关错误而造成的，当作为网关或代理的服务器与上层内容服务器联络时，收到无效的响应时就会出现502错误。</code></p>
</li>
<li><p>参考 <a href="https://blog.csdn.net/aaa_aa000/article/details/46997991">https://blog.csdn.net/aaa_aa000&#x2F;article&#x2F;details&#x2F;46997991</a></p>
</li>
<li><p>参考<a href="https://www.jianshu.com/p/d50bcc605a13">https://www.jianshu.com/p/d50bcc605a13</a></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Web Security</category>
      </categories>
      <tags>
        <tag>๑Web</tag>
        <tag>๑Security</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows10蓝屏巨坑--已填</title>
    <url>/2018/11/14/win10-problem-01/</url>
    <content><![CDATA[<h2 id="win10蓝屏问题"><a href="#win10蓝屏问题" class="headerlink" title="win10蓝屏问题"></a>win10蓝屏问题</h2><hr>
<ul>
<li>最近一个月也不知道干了啥,Windows有时候就是莫名其妙地蓝掉…内心无比崩溃</li>
</ul>
<blockquote>
<p>而且是我在玩Fortinite的时候无限崩…</p>
</blockquote>
<h2 id="问题规律"><a href="#问题规律" class="headerlink" title="问题规律"></a>问题规律</h2><hr>
<ul>
<li><p>最近在使用Android Studio开发, 但是莫名其妙就模拟器打不开, intel虚拟线程插件安装也报错,就很炸裂.</p>
</li>
<li><p>偶然间又打开了我的木木模拟器,结果马上蓝屏!!!</p>
</li>
<li><p>至此我发现了蓝屏的一点规律性—跟模拟器(虚拟化技术)有关!!</p>
</li>
</ul>
<h2 id="有了线索-问题便得以解决"><a href="#有了线索-问题便得以解决" class="headerlink" title="有了线索,问题便得以解决"></a>有了线索,问题便得以解决</h2><hr>
<ul>
<li><p>经过几番搜索和尝试, 最终锁定了问题的源泉—Windows服务–<strong>Hyper-V</strong></p>
</li>
<li><p>原来是Windows自己的虚拟化技术与别的虚拟化技术有冲突, 导致了蓝屏的问题.</p>
</li>
</ul>
<h2 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h2><hr>
<ul>
<li>Ctrl+R -&gt; control.exe(控制面板) -&gt; 启动或关闭Windows功能 -&gt; 取消Hyper-V服务 -&gt;重启电脑</li>
</ul>
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows10使用过程中遇到的一些坑</title>
    <url>/2018/07/23/windows-realated/</url>
    <content><![CDATA[<blockquote>
<p>不适合人类阅读的学习笔记</p>
</blockquote>
<h2 id="解决windows下atom字体模糊的问题"><a href="#解决windows下atom字体模糊的问题" class="headerlink" title="解决windows下atom字体模糊的问题"></a>解决windows下atom字体模糊的问题</h2><hr>
<ul>
<li><p>这种情况是由windows下GPU的一些优化带来的问题，主要是会导致字体模糊。</p>
</li>
<li><p>也有人说是由于抗锯齿导致的问题</p>
</li>
<li><p>更改方法如下：</p>
</li>
</ul>
<p><img src="/images/Windows/atom.jpg"></p>
<h2 id="关闭windows自动更新"><a href="#关闭windows自动更新" class="headerlink" title="关闭windows自动更新"></a>关闭windows自动更新</h2><hr>
<ul>
<li><p>首先<code>ctrl+s</code>呼出小娜，搜索<code>service</code>,出现服务这个匹配项。</p>
</li>
<li><p>在弹出的窗口中找到<code>Windows Update</code>的本地服务，右键 -&gt; 属性，在弹出的窗口中，首先点击并停止服务，然后将启动类型由<code>自动</code>改为<code>手动</code>。</p>
</li>
<li><p>重启电脑即可。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Computer System</category>
      </categories>
      <tags>
        <tag>๑Windows</tag>
      </tags>
  </entry>
</search>
